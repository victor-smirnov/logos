// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 Victor Smirnov
//
// Example: parse a Hermes document, stringify it, print to stdout.

package hermes_example;

use hermes.ctr;

extern fn strlen(s: *const u8) -> i64;

fn main() -> i32 {
    let mut doc: HermesCtr = HermesCtr::new(8192);

    let text: *const u8 = r#"
        {
            name:"widget",
            version:42,
            active:true,
            count:7_s8,
            tags:["fast","safe"]
        }
    "#;

    match doc.parse(text, unsafe { strlen(text) }) {
        Result::Ok(_)  => {}
        Result::Err(_) => { return 1; }
    }

    doc.stringify();
    doc.print();

    return 0;
}
