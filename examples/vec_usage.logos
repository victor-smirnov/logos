package vec_usage;

use std.vec;

unsafe fn main() -> i32 {
    let mut v: Vec<i32> = vec_new::<i32>();
    v.push(10);
    v.push(15);
    v.push(17);
    let total: i32 = v.get(0) + v.get(1) + v.get(2);
    vec_free(&mut v);
    return total;
}
