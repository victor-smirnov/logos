package nested_tuple;

fn inner_sum(t: (i32, (i32, i32))) -> i32 {
    return t.1.0 + t.1.1;
}

fn main() -> i32 {
    let inner: (i32, i32) = (11, 31);
    let outer: (i32, (i32, i32)) = (0, inner);
    // t.0 = 0, t.1.0 = 11, t.1.1 = 31
    return outer.0 + inner_sum(outer);
}
