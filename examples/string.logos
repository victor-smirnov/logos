package string;

extern fn printf(fmt: *const u8, ...) -> i32;

fn str_len(s: str) -> i64 {
    return s.len();
}

fn main() -> i32 {
    let s: str = "hello, logos!";
    let n: i64 = str_len(s);
    printf("len = %d\n", n as i32);
    return n as i32;
}
