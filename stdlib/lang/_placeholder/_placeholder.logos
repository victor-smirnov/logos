// Three-layer split Phase 3.6: bootstrap placeholder for the lang
// layer. Exists so the archive isn't empty and the loader's
// binary_index has a discoverable package. Removed in Phase 7 when
// the layer has real content.

#![no_implicit_prelude]

package logos.lang._placeholder;

pub fn lang_placeholder_noop() { }
