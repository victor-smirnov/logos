// SPDX-License-Identifier: Apache-2.0
// Logos standard library — std.mem
//
// Memory allocation via C FFI.

package std.mem;

extern fn malloc(size: i64) -> *mut u8;
extern fn free(ptr: *mut u8);
extern fn realloc(ptr: *mut u8, size: i64) -> *mut u8;
