{
  "unit": "mono/mono_clone/logos.part8",
  "layer": "mono",
  "source": "src/compiler/mono_clone.cpp#L3700-L4226",
  "source_hash": "67349c977318ca51",
  "rules": [
    {
      "id": "mono.dispatch.trait-qualified-mangling",
      "domain": "mono",
      "title": "Ambiguous-by-name dispatch resolves to trait-qualified symbol",
      "statement": "When a method call on a type-variable receiver is dispatch-ambiguous by name and a trait was selected (trait T), monomorphization resolves the callee base to `<recv-type>__<T>__<method>` if such a symbol exists; otherwise it falls back to the plain `<recv-type>__<method>`.",
      "evidence": ["src/compiler/mono_clone.cpp#L3716-L3737"],
      "divergence": "Logos-specific name-mangling scheme; see G156-1 baghunt for two-impl collision"
    },
    {
      "id": "mono.dispatch.ref-receiver-fallback",
      "domain": "mono",
      "title": "Reference-receiver method falls back to ref-mangled symbol",
      "statement": "If a method `<recv>__<method>` does not exist but a reference-typed variant `$ref_<recv>__<method>` (or `$mut_ref_<recv>__<method>` for a mut-ref receiver) does, the call resolves to the reference-typed symbol.",
      "evidence": ["src/compiler/mono_clone.cpp#L3700-L3715"]
    },
    {
      "id": "mono.subst.enum-receiver-spec-synthesis",
      "domain": "mono",
      "title": "Generic-trait-for-enum method instantiates per-enum spec",
      "statement": "For a method call whose receiver is an enum (or pointer/ref/mut-ref to an enum) dispatched through a generic `impl Trait for Enum<T>`, monomorphization synthesizes the concrete spec key by inserting the receiver enum's concrete type-args between the enum base name and the method suffix, mirroring eager enum-template instantiation, so the call binds to the cloned spec rather than the bare base template.",
      "evidence": ["src/compiler/mono_clone.cpp#L3761-L3803"]
    },
    {
      "id": "mono.subst.lazy-blanket-method-instantiation",
      "domain": "mono",
      "title": "Lazy blanket-impl method instantiation for generic receiver",
      "statement": "A method call on a generic struct/enum receiver (after stripping ptr/ref/mut-ref) dispatched through a blanket `impl<T: Bound> Trait for T` is instantiated lazily: the blanket method template is cloned with {T -> receiver type} and enqueued, provided the receiver concrete type satisfies the blanket's bound trait and all extra bounds; the resulting spec is `<recv>__<method>`.",
      "evidence": ["src/compiler/mono_clone.cpp#L3804-L3878"],
      "divergence": "Logos-specific blanket-impl monomorphization (G159-1)"
    },
    {
      "id": "mono.subst.blanket-bound-check",
      "domain": "mono",
      "title": "Blanket-impl instantiation gated by bound satisfaction",
      "statement": "A blanket `impl<T: Bound + Extra...> Trait for T` is instantiated for a concrete receiver type only if that type satisfies the bound trait and every extra bound; otherwise the blanket impl does not apply.",
      "evidence": ["src/compiler/mono_clone.cpp#L3854-L3864"]
    },
    {
      "id": "mono.subst.typevar-pack-expansion",
      "domain": "mono",
      "title": "Type-argument variable bound to a pack expands in place",
      "statement": "When substituting a method call's type-arguments, a type-argument that is a type-variable currently bound to a type pack is replaced by the full sequence of the pack's element types (variadic expansion); other type-arguments are substituted individually.",
      "evidence": ["src/compiler/mono_clone.cpp#L3884-L3893", "src/compiler/mono_clone.cpp#L3995-L4004"]
    },
    {
      "id": "mono.subst.tuple-receiver-elem-args",
      "domain": "mono",
      "title": "Tuple receiver supplies its element types as impl type-args",
      "statement": "For a method call whose receiver is a tuple type, the call's impl-level type-arguments are set to the tuple's element types, enabling specialization of `impl<A,B,...> Trait for (A,B,...)`; for nested tuple recursion the inner receiver's own element types override stale outer-spec args.",
      "evidence": ["src/compiler/mono_clone.cpp#L3894-L3912"],
      "divergence": "Logos-specific (SL-sl-08 / CP-cm-08b)"
    },
    {
      "id": "mono.subst.drop-args-non-generic-impl",
      "domain": "mono",
      "title": "Type-args dropped/truncated to template type-param count",
      "statement": "A method call's type-arguments are kept only up to the resolved template's declared type-parameter count: if the template has zero type-params (concrete impl) all type-args are cleared; otherwise they are truncated to the param count, EXCEPT a variadic template type-param consumes all trailing type-args (no truncation).",
      "evidence": ["src/compiler/mono_clone.cpp#L3913-L3963"],
      "divergence": "Logos-specific (T9-tr-02) plus variadic-pack handling"
    },
    {
      "id": "mono.subst.mangle-and-enqueue-method-generic",
      "domain": "mono",
      "title": "Method-generic call is mangled then enqueued for cloning",
      "statement": "When a method call retains nonzero type-arguments after truncation, its callee is mangled with those type-args and the resulting specialization is explicitly enqueued onto the monomorphization worklist so its body is cloned (otherwise the call would name a spec that is never emitted).",
      "evidence": ["src/compiler/mono_clone.cpp#L3964-L3974", "src/compiler/mono_clone.cpp#L4064-L4080", "src/compiler/mono_clone.cpp#L4126-L4138"]
    },
    {
      "id": "mono.dispatch.spec-lookup-best-match",
      "domain": "mono",
      "title": "Concrete struct/enum method resolves to best matching spec",
      "statement": "For a method call on a concrete struct/enum receiver (after stripping ptr/ref/mut-ref), the candidate template key is `<base>__<method>` (base = resolved type name, else enum/struct name), preferring sema's resolved_symbol when it names an existing template/spec; the call resolves to the best-matching specialization keyed by the receiver's type-args combined with the method's type-args.",
      "evidence": ["src/compiler/mono_clone.cpp#L4008-L4080"]
    },
    {
      "id": "mono.dispatch.combined-type-args",
      "domain": "mono",
      "title": "Specialization key combines receiver and method type-args",
      "statement": "Specialization selection for a concrete-receiver method combines the receiver type's type-arguments followed by the method-level type-arguments into a single ordered argument list used for spec lookup and mangling.",
      "evidence": ["src/compiler/mono_clone.cpp#L4021-L4022", "src/compiler/mono_clone.cpp#L4053-L4065"]
    },
    {
      "id": "mono.dispatch.self-generic-template-mangle",
      "domain": "mono",
      "title": "Self-typed method-generic call mangles resolved template directly",
      "statement": "A trait-default body call to another method-generic method on a `Self`-typed generic-struct receiver, where the resolved symbol is a method-generic template (contains `__g__`) with method-level type-params and all call type-args are concrete, is resolved by mangling the resolved template with the call's type-args and enqueuing it; impl-only-generic methods (no method-level type-params) are excluded from this path.",
      "evidence": ["src/compiler/mono_clone.cpp#L4083-L4139"],
      "divergence": "Logos-specific monomorphization fixup"
    },
    {
      "id": "mono.subst.const-arg-method-specialize",
      "domain": "mono",
      "title": "Const-argument specialization includes the method receiver",
      "statement": "For a concrete method call kept as a method call, const-argument specialization treats the receiver as parameter index 0 (a combined [receiver, args...] view) so a const-wanting parameter at any position, including the receiver, is mapped and the callee specialized.",
      "evidence": ["src/compiler/mono_clone.cpp#L4144-L4156"]
    },
    {
      "id": "mono.subst.closure-substitution",
      "domain": "mono",
      "title": "Closure substitution rewrites param/return/capture types",
      "statement": "Substituting a closure applies the type substitution to each parameter type, the return type, and each capture type, while preserving the closure id, move-ness, fn-pointer-ness, escape (heap-env) flag, capture names, per-capture mutability flags, per-capture field paths (RFC-2229), and per-capture narrow field types (also substituted).",
      "evidence": ["src/compiler/mono_clone.cpp#L4164-L4208"],
      "divergence": "RFC-2229 disjoint-closure-capture metadata preserved through mono"
    }
  ]
}
