{
  "unit": "mono/mono_clone/logos.part6",
  "layer": "mono",
  "source": "src/compiler/mono_clone.cpp#L2686-L3220",
  "source_hash": "67b7917da387049b",
  "rules": [
    {
      "id": "intrinsic.reflect.tuple-count-of",
      "domain": "intrinsic",
      "title": "tuple_count_of::<T>() yields tuple arity",
      "statement": "tuple_count_of::<T>() evaluates at compile time to an i64 literal equal to the number of element types of T when T is a tuple type, and to 0 for any non-tuple T.",
      "evidence": ["src/compiler/mono_clone.cpp#L2686-L2698"]
    },
    {
      "id": "intrinsic.reflect.field-count-of",
      "domain": "intrinsic",
      "title": "field_count_of::<T>() yields struct field count",
      "statement": "field_count_of::<T>() evaluates at compile time to an i64 literal equal to the number of declared fields of T when T is a struct (or zoned struct) type; for any non-struct or unresolvable T it is 0. The struct template is matched by name, preferring a package-qualified match (T.pkg) and falling back to name-only.",
      "evidence": ["src/compiler/mono_clone.cpp#L2703-L2730"]
    },
    {
      "id": "intrinsic.reflect.field-names-of",
      "domain": "intrinsic",
      "title": "field_names_of::<T>() yields array of field-name strings",
      "statement": "field_names_of::<T>() evaluates at compile time to an array [&str; N] whose elements are the declared field names of struct T in declaration order; for non-struct or unresolvable T it is the empty array. Struct lookup prefers a package-qualified match and falls back to name-only.",
      "evidence": ["src/compiler/mono_clone.cpp#L2733-L2768"]
    },
    {
      "id": "intrinsic.reflect.args-of",
      "domain": "intrinsic",
      "title": "args_of::<T>() yields T's generic type arguments",
      "statement": "args_of::<T>() produces a [Type; N] descriptor array of the generic type-arguments of T (in order); for a non-generic T the array is empty.",
      "evidence": ["src/compiler/mono_clone.cpp#L2780-L2783"],
      "related": ["intrinsic.reflect.type-descriptor-array"]
    },
    {
      "id": "intrinsic.reflect.typelist-tail",
      "domain": "intrinsic",
      "title": "typelist_tail::<T>() drops the first type argument",
      "statement": "typelist_tail::<T>() produces a [Type; N] descriptor array of T's generic type-arguments excluding the first (i.e. the tail beginning at index 1); empty when T has fewer than two type arguments.",
      "evidence": ["src/compiler/mono_clone.cpp#L2784-L2789"],
      "related": ["intrinsic.reflect.type-descriptor-array"]
    },
    {
      "id": "intrinsic.reflect.tuple-elems-of",
      "domain": "intrinsic",
      "title": "tuple_elems_of::<T>() yields tuple element types",
      "statement": "tuple_elems_of::<T>() produces a [Type; N] descriptor array of the element types of T when T is a tuple; empty otherwise.",
      "evidence": ["src/compiler/mono_clone.cpp#L2790-L2796"],
      "related": ["intrinsic.reflect.type-descriptor-array"]
    },
    {
      "id": "intrinsic.reflect.field-types-of",
      "domain": "intrinsic",
      "title": "field_types_of::<T>() yields substituted struct field types",
      "statement": "field_types_of::<T>() produces a [Type; N] descriptor array of the field types of struct (or zoned struct) T in declaration order, with the struct template's type parameters substituted by T's actual type arguments (positional binding of template params to T.type_args); empty for non-struct or unresolvable T.",
      "evidence": ["src/compiler/mono_clone.cpp#L2797-L2827"],
      "related": ["intrinsic.reflect.type-descriptor-array"]
    },
    {
      "id": "intrinsic.reflect.type-descriptor-array",
      "domain": "intrinsic",
      "title": "type-reflection intrinsics produce [Type; N] descriptors",
      "statement": "args_of::<T>(), type_refs_of, tuple_elems_of, typelist_tail, and field_types_of each evaluate at compile time to an array [Type; N] of struct literals. Each Type element has fields {kind: u32 = the type's kind tag, name: &str = the type's printed name, size: i64 = size_of, align: i64 = align_of, uid: u64 = a canonical 64-bit type hash recorded into a uid->type map}. N and the per-element source types are determined per-intrinsic (see related rules); type_refs_of uses its call-site type_args verbatim.",
      "evidence": ["src/compiler/mono_clone.cpp#L2828-L2869"]
    },
    {
      "id": "mono.pack.type-pack-expansion-args",
      "domain": "mono",
      "title": "Type-pack expansion in call arguments",
      "statement": "A pack-expansion call argument over a type-pack variable (static type Kind::TypeVar, bound in cur_packs_) expands at monomorphization to one argument per bound pack element, each a per-element variable reference named by make_pack_arg_name(pack_var, index) into the callee signature; when the callee is a generic template (present in templates_), the pack's element types are also appended to the call's type arguments.",
      "evidence": ["src/compiler/mono_clone.cpp#L2871-L2904"]
    },
    {
      "id": "mono.pack.const-pack-expansion-args",
      "domain": "mono",
      "title": "Const-pack expansion in call arguments",
      "statement": "A pack-expansion call argument over a const-pack variable (static type Kind::ConstVar) expands at monomorphization to one literal-int argument per bound element that carries a const_val(), typed by that element's pointee type (or the element type itself if it has no pointee); elements without a const_val fall back to the type-pack var_ref expansion.",
      "evidence": ["src/compiler/mono_clone.cpp#L2877-L2899"]
    },
    {
      "id": "mono.dispatch.trait-static-callee-rewrite",
      "domain": "mono",
      "title": "Generic trait-static dispatch rewrites the receiver-type prefix",
      "statement": "A call whose mangled callee is `[pkg.]<DT>__<method...>` where DT is a generic type parameter bound by the substitution map is rewritten to dispatch on the concrete bound type: the `<DT>` prefix is replaced by the concrete receiver name and the substituted type's package (falling back to the original callee package). The concrete name is the concrete struct name for a struct, the mangled enum name `<base>__<arg>...` for an enum, `$ref_`/`$mut_ref_` prefixed for a `&T`/`&mut T` receiver (struct pointee -> concrete_struct_name, else -> printed type name), and the printed type name otherwise; a `&[u8]` receiver name is normalized to `str`.",
      "evidence": ["src/compiler/mono_clone.cpp#L2909-L2968"],
      "testability": "transitive"
    },
    {
      "id": "mono.dispatch.method-tparam-inference",
      "domain": "mono",
      "title": "Inferring method type-args at trait-static dispatch",
      "statement": "When a rewritten trait-static call carries no explicit type arguments but the target method has method-level type parameters, the callee's method name is recovered by stripping a trailing `__g__...`/`__f__...` signature suffix, the generic template is located (struct_method_templates_ keyed by `[pkg.]<struct-or-enum-base>`, exact or `<method>__g__`-prefixed match; else a templates_ scan for `<base>__<method>__g__`-prefixed keys for primitive receivers), and the call is re-formed into a template callee with inferred type arguments so the correct specialization is instantiated; without this a `.collect::<C>()`-style call would lower to an un-parameterized callee that is never emitted.",
      "evidence": ["src/compiler/mono_clone.cpp#L2969-L3049"]
    },
    {
      "id": "mono.dispatch.assoc-type-projection-disambig",
      "domain": "mono",
      "title": "Associated-type projection disambiguates multi-impl trait-static dispatch",
      "statement": "When trait-static dispatch on `Self::method` is ambiguous among multiple trait-qualified candidates `<Self>__<Trait>$G..__<m>` distinguished only by a trait type-argument that is not a method parameter but an associated type of an argument, the correct candidate is selected by associated-type projection: for each argument type, find non-blanket impls whose target unifies with it, substitute the impl's type-params, and mangle the impl's trait type-args; pick the unique candidate whose `$G..` token an argument carries. A re-instantiable template is preferred over an already-monomorphized spec sharing the same token.",
      "evidence": ["src/compiler/mono_clone.cpp#L3067-L3181"],
      "testability": "transitive"
    },
    {
      "id": "mono.dispatch.multi-impl-arg-sig-disambig",
      "domain": "mono",
      "title": "Argument-signature disambiguation among same-Self multi-impl trait methods",
      "statement": "When several impls of a multi-parameter trait `Trait<A> for Self` exist for the same Self, mangled `<Self>__<method>__[fg]__<A-sig>`, the bare retargeted callee `<Self>__<method>` (which would otherwise resolve to whichever impl is registered first) is disambiguated using the call's own, now-concrete argument types: a want_sig is formed by joining mangle_type(arg_type) over the call arguments with `__`, and among registered symbols matching prefix `<Self>__<method>__[fg]__` the one whose suffix equals want_sig is selected as the exact callee.",
      "evidence": ["src/compiler/mono_clone.cpp#L3183-L3220"],
      "uncertainty": "Slice ends mid-lambda at L3220 (selection loop body not fully visible in this unit); rule inferred from the complete preceding comment (L3183-L3193) and the want_sig construction plus prefix-scan setup.",
      "testability": "transitive"
    }
  ]
}
