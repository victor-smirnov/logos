{
  "unit": "mono/mono_clone/find_best_struct_spec",
  "layer": "mono",
  "source": "src/compiler/mono_clone.cpp#L5632-L6412",
  "source_hash": "77a96368a2ac61d2",
  "rules": [
    {
      "id": "mono.struct-spec.best-by-specificity",
      "domain": "mono",
      "title": "Generic struct instantiation selects the most specific matching specialization",
      "statement": "For a generic struct base B applied to type-args A, among all declared specializations of B whose pattern arity equals |A| and each of whose patterns matches the corresponding A[i], the one selected is that with the lexicographically greatest specificity vector. Non-matching or arity-mismatched specializations are excluded.",
      "evidence": ["src/compiler/mono_clone.cpp#L5643-L5662"],
      "related": ["mono.struct-spec.ambiguous-error"],
      "testability": "behavioral"
    },
    {
      "id": "mono.struct-spec.ambiguous-error",
      "domain": "mono",
      "title": "Two equally-specific matching struct specializations is an error",
      "statement": "If two or more matching struct specializations share the maximal specificity vector (no strict winner), an Error diagnostic \"ambiguous specializations for struct '<base_name>'\" is emitted; instantiation still proceeds using one of the tied specs.",
      "evidence": ["src/compiler/mono_clone.cpp#L5659-L5668"],
      "related": ["mono.struct-spec.best-by-specificity"],
      "testability": "diagnostic"
    },
    {
      "id": "mono.struct-spec.fallback-template",
      "domain": "mono",
      "title": "Absent a matching specialization, the bare generic template is used",
      "statement": "When no declared specialization matches a struct instantiation, the generic template found by package-first lookup for (pkg, base) is used instead, and its type-params are bound positionally to the instantiation's type-args; a trailing variadic type-param absorbs all remaining type-args as a pack. If no template exists either, the needed instantiation is silently dropped.",
      "evidence": ["src/compiler/mono_clone.cpp#L5895-L5914"],
      "related": ["mono.subst.variadic-pack-absorb"],
      "testability": "behavioral"
    },
    {
      "id": "mono.subst.variadic-pack-absorb",
      "domain": "mono",
      "title": "Variadic type-param absorbs trailing type-args as a pack",
      "statement": "During positional binding of a template's type-params to type-args, a variadic type-param `...T` binds to a pack consisting of all remaining type-args from its position onward; non-variadic params bind one-to-one. Applies identically to struct-template and enum-template instantiation.",
      "evidence": [
        "src/compiler/mono_clone.cpp#L5904-L5913",
        "src/compiler/mono_clone.cpp#L6058-L6073"
      ],
      "testability": "behavioral"
    },
    {
      "id": "mono.inst.pkg-identity-from-generic",
      "domain": "mono",
      "title": "An instantiation's package identity comes from the generic's home package",
      "statement": "A monomorphized struct instance is assigned the package of its generic template, not that of the matched specialization; a specialization living in a different package contributes layout only, not conceptual identity.",
      "evidence": ["src/compiler/mono_clone.cpp#L5919-L5929"],
      "testability": "transitive"
    },
    {
      "id": "mono.inst.explicit-type-code",
      "domain": "mono",
      "title": "Explicit instantiation annotation sets a fixed type_code",
      "statement": "A declared explicit instantiation annotation matching an instance's mangled name and carrying a nonzero type_code (e.g. `#[type_code=100] eidos Array<AnyVal>;`) overrides that instance's type_code with the annotated value.",
      "evidence": ["src/compiler/mono_clone.cpp#L5936-L5943"],
      "testability": "behavioral"
    },
    {
      "id": "mono.enum.instance-drops-metadata",
      "domain": "mono",
      "title": "Enum instances carry only name/pkg/zoned2/variants",
      "statement": "A monomorphized enum instance preserves its name, package, zoned2 (niche/at-rest-relative) marker, and variants; type_params, backing_type, borrow_carrying, and doc are dropped on the instance.",
      "evidence": ["src/compiler/mono_clone.cpp#L5992-L5994", "src/compiler/mono_clone.cpp#L5996-L6031"],
      "testability": "transitive"
    },
    {
      "id": "mono.enum.variadic-variant-payload",
      "domain": "mono",
      "title": "Variadic enum variant payload expands its pack",
      "statement": "An enum variant whose first payload type is a variadic type-var (e.g. `Multi(...T)`) expands to one payload per element of the bound pack, each substituted; a non-pack-bound variadic payload substitutes as a single type. Non-variadic variants substitute each declared payload type directly.",
      "evidence": ["src/compiler/mono_clone.cpp#L6003-L6021"],
      "testability": "behavioral"
    },
    {
      "id": "mono.enum.void-payload-spec-skip",
      "domain": "mono",
      "title": "Enum instantiation with a void type-arg emits no methods",
      "statement": "When a generic enum is instantiated with a `()`/void type-argument (e.g. `Option<()>`, `Result<(),E>`), the enum's variants are still materialized but no impl methods are eagerly cloned for that instantiation, since a void-typed bound value carries no codegen slot and cannot be passed or returned.",
      "evidence": ["src/compiler/mono_clone.cpp#L6077-L6098"],
      "uncertainty": "Spec-level: such instances are usable only for marker/discriminant matching, not value-carrying method calls.",
      "testability": "transitive"
    },
    {
      "id": "mono.enum.self-referential-method-skip",
      "domain": "mono",
      "title": "Eager clone of self-referential enum methods is skipped",
      "statement": "A generic enum method is not eagerly cloned when any of its parameter or return types references the same enum base with a type-arg that is structurally larger than a bare type-var AND still contains a type-var (e.g. `Option::as_ref(&Option<T>) -> Option<&T>`); fully-concrete recursive args (`Result<(),Error>`) do not trigger the skip. Real call sites still enqueue the needed specialization via the ordinary call-site path.",
      "evidence": ["src/compiler/mono_clone.cpp#L6103-L6167", "src/compiler/mono_clone.cpp#L6248"],
      "testability": "transitive"
    },
    {
      "id": "mono.enum.method-bound-gate",
      "domain": "mono",
      "title": "Generic enum method clone is gated by type-param bound satisfaction",
      "statement": "An impl-derived generic enum method (e.g. `impl<T: Echo> Echo for Option<T>`) is cloned for a concrete instantiation only if each bounded type-param's concrete binding satisfies its trait bound; auto-traits are checked by auto-satisfaction and ordinary bounds by recursion-aware concrete-satisfies-bound (which recurses into nested generic type args, e.g. `X` inside `Wrapper<X>`). Failing bounds suppress the clone.",
      "evidence": ["src/compiler/mono_clone.cpp#L6299-L6358"],
      "testability": "transitive"
    },
    {
      "id": "mono.enum.method-fully-bound-gate",
      "domain": "mono",
      "title": "A blanket method with unbound type-params is not cloned",
      "statement": "A generic (blanket free-fn / impl) enum method is cloned only if every non-variadic type-param receives a binding (subst or pack) from this instantiation; if any param remains unbound (e.g. an output `D` in `impl<S, D: From<S>> Into<D> for S`), the clone is suppressed and the correct specialization is emitted by the full call site instead.",
      "evidence": ["src/compiler/mono_clone.cpp#L6359-L6373"],
      "testability": "transitive"
    },
    {
      "id": "mono.enum.method-partial-spec-target",
      "domain": "mono",
      "title": "Partial-spec impl target binds type-params by unification",
      "statement": "When a generic enum method carries an impl_target_pattern that is not the bare enum (e.g. `Result<Vec<T>, E>` vs `Result<T, E>`), its impl-level type-params are bound by unifying the concrete receiver type-args against the pattern, rather than by positional assignment from the enum's declared type-params.",
      "evidence": ["src/compiler/mono_clone.cpp#L6251-L6286"],
      "testability": "behavioral"
    },
    {
      "id": "mono.enum.method-dual-mangling",
      "domain": "mono",
      "title": "Generic enum methods are emitted under both cname-insert and type-arg-append names",
      "statement": "A generic enum method may be demanded under two mangled forms: the cname-insert form (`Option__i8__eq`, used for direct concrete-receiver calls and `==`/`!=` operator lowering, and every non-generic method) and the type-arg-append form (`Option__eq__g__sig__i8`, used for trait-bound dispatch, where the impl's type parameter was flattened into the method's own type_params at sema-collect time). The same substituted body is emitted under whichever of the two names a caller demands but no path has yet produced (aliased under both when both are needed).",
      "evidence": ["src/compiler/mono_clone.cpp#L6203-L6247"],
      "testability": "transitive"
    },
    {
      "id": "mono.struct-spec.bound-str-canonical",
      "domain": "mono",
      "title": "&[u8] canonicalizes to str for bound checking",
      "statement": "When checking whether a type bound to a generic type-param satisfies a required trait bound, the concrete type is canonicalized to a name string: struct/zoned-struct types use their concrete mangled name with any `$G...` generic-instance suffix stripped, enum types use their bare enum name, and `&[u8]` (the wire representation of `str`) canonicalizes to `\"str\"` to match the trait engine's registration key.",
      "evidence": ["src/compiler/mono_clone.cpp#L6317-L6332"],
      "testability": "transitive"
    }
  ]
}
