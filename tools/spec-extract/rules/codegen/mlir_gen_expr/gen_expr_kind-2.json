{
  "unit": "codegen/mlir_gen_expr/gen_expr_kind-2",
  "layer": "codegen",
  "source": "src/compiler/mlir_gen_expr.cpp#L3167-L3782",
  "source_hash": "ab978941e574ed84",
  "rules": [
    {
      "id": "coerce.cast.int-null-to-trait-object",
      "domain": "coerce",
      "title": "Integer (null) cast to trait object yields zeroed fat pair",
      "statement": "`E as T` where T is a trait object (`*mut dyn`/`&dyn`) and E has an integer type (IntLit/i32/u32/i64/u64/isize/usize) produces a 16-byte {data,vtable} fat pair with both halves null. This makes null-handle sentinels (`0 as *mut dyn`) and `… as *mut u64 == 0` null checks behave under the uniform-fat dyn model.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3170-L3193"
      ],
      "divergence": "Logos uniform-fat model: `*mut dyn`/`&dyn` are both 16-byte {data,vtable}; integer-to-dyn null cast is a Logos extension for null sentinels (no Rust analog)."
    },
    {
      "id": "coerce.cast.supertrait-upcast",
      "domain": "coerce",
      "title": "Supertrait upcast preserves data, swaps to super vtable",
      "statement": "`&dyn Sub`/`dyn Sub` cast to `&dyn Super` (Sub ≠ Super, Super a supertrait of Sub) keeps the SAME data pointer and replaces the vtable with Super's vtable, recovered from a stored super-vtable-pointer slot in Sub's vtable at index `3 + |methods(Sub)| + idx(Super)`. Identity dyn casts (Sub == Super) fall through to the no-op reinterpret.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3254-L3303"
      ],
      "divergence": "Rust-conformant (trait upcasting); vtable layout {drop,size,align, methods…, super-vtables…} is Logos-specific."
    },
    {
      "id": "coerce.unsize.box-array-to-box-slice",
      "domain": "coerce",
      "title": "Box<[T;N]> unsizes to owning Box<[T]>",
      "statement": "`Box<[T;N]>` cast to an owning `Box<[T]>` (Slice with owning_slice) builds a {data,len} fat pair: data = the box's heap pointer (field 0 of the Box struct), len = N (the array's compile-time size). This is CoerceUnsized for `Box::new([..]) as Box<[T]>`.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3305-L3337"
      ],
      "related": [
        "coerce.unsize.thin-array-ptr-to-slice"
      ]
    },
    {
      "id": "coerce.cast.u8-slice-to-u8-ptr",
      "domain": "coerce",
      "title": "&[u8]/str to *const u8 extracts data field",
      "statement": "`E as *const u8`/`*mut u8` where E has type Slice<u8> (str is Slice<u8>) extracts field 0 (the data pointer) of the {ptr,len} fat pair. Evaluated before the identity short-circuit because both the fat-struct alloca and `*const u8` are the same MLIR pointer type.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3339-L3351"
      ]
    },
    {
      "id": "coerce.unsize.struct-dyn-tail-to-dstref",
      "domain": "coerce",
      "title": "Pointer to struct with concrete tail unsizes to DstRef with dyn tail",
      "statement": "`*mut/*const/& ConcreteStruct<…, Sized>` cast to a DstRef whose tail type-arg is a trait object (`*mut Inner<dyn Tr>`) builds a {data,vtable} fat pair: data = the source thin pointer to the whole struct; vtable = the concrete tail type's vtable for the tail trait (the tail binding is the source instance's last type-arg). This is CoerceUnsized for a struct with an unsized (`dyn`) tail field.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3356-L3394"
      ]
    },
    {
      "id": "coerce.unsize.ref-concrete-to-trait-object",
      "domain": "coerce",
      "title": "Reference/pointer to concrete unsizes to bare trait object",
      "statement": "`&T`/`&mut T`/`*const T`/`*mut T` (T a concrete struct or primitive) cast to a bare trait object synthesizes a {data,vtable} fat pair; the vtable keys on T's concrete struct name (or the primitive's bare type name for a blanket-impl `&i64 as &dyn`). Only fires when the source pointee is concrete; a `&dyn`→`dyn` reinterpret (pointee already a trait object) is a no-op.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3406-L3432"
      ],
      "divergence": "Uniform-fat model: `&dyn` and `*mut dyn` are both 16-byte fat pairs (Logos), unlike Rust where only references unsize."
    },
    {
      "id": "coerce.unsize.smart-ptr-to-box-dyn",
      "domain": "coerce",
      "title": "Box/Rc/Arc<T> unsizes to owning dyn fat pair",
      "statement": "`Box<T>`/`Rc<T>`/`Arc<T>` (T concrete) cast to a dyn smart pointer builds a value fat pair {data,vtable}: for Box, data = field 0 (the heap pointer); for Rc/Arc, data = field0 + round_up(8, align(T)) (skipping the 2×i32 RcInner strong/weak header). vtable[0..2] = drop/size/align; drop is kind-specific (Box→free; Rc/Arc→dec strong + free RcInner). A dyn-payload smart pointer is already a handle and is not re-wrapped.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3434-L3491"
      ]
    },
    {
      "id": "coerce.cast.fat-to-thin-pointer",
      "domain": "coerce",
      "title": "Fat pointer to thin/void pointer extracts data half",
      "statement": "A fat value cast to a thin pointer extracts field 0 (the data half): (a) `*mut/*const dyn` (Ptr<TraitObject>) → `*mut/*const ()` (void only); (b) any bare dyn/closure VALUE or `&dyn`/`&mut dyn` → void or any non-fat thin pointer; (c) `*const/*mut [T]` (Slice) → any non-fat thin pointer; (d) a non-self-describing DstRef → thin pointer; (e) a fat zone-mut `&mut T` → `*mut/*const T`. Runs before the identity check since both sides are MLIR pointer type.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3493-L3591"
      ]
    },
    {
      "id": "coerce.unsize.thin-array-ptr-to-slice",
      "domain": "coerce",
      "title": "Thin array pointer to slice pointer synthesizes len=N",
      "statement": "`*const [T;N]`/`*mut [T;N]` (Ptr<Array>) cast to `*const [T]`/`*mut [T]` (Slice) synthesizes a {ptr, len=N} fat pair on the stack, where N is the array's compile-time size; without this the cast would be a no-op leaving array contents misread as the data field.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3592-L3616"
      ],
      "related": [
        "coerce.unsize.box-array-to-box-slice"
      ]
    },
    {
      "id": "coerce.cast.identity-noop",
      "domain": "coerce",
      "title": "Same-representation cast is identity",
      "statement": "If the source value's representation equals the target representation, `E as T` is the identity (the value is returned unchanged).",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3617"
      ]
    },
    {
      "id": "coerce.cast.int-widen-by-signedness",
      "domain": "coerce",
      "title": "Integer widening sign- or zero-extends per source signedness",
      "statement": "`E as T` where both are integers and width(T) > width(E): zero-extend if the source is unsigned (u8/u16/u24/u32/u56/u64/u128, or i1) else sign-extend.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3619-L3635"
      ]
    },
    {
      "id": "coerce.cast.int-truncate",
      "domain": "coerce",
      "title": "Integer narrowing truncates",
      "statement": "`E as T` where both are integers and width(T) < width(E) truncates to the low width(T) bits; equal widths are the identity.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3636-L3638"
      ]
    },
    {
      "id": "coerce.cast.int-to-float-by-signedness",
      "domain": "coerce",
      "title": "Integer to float conversion respects source signedness",
      "statement": "`E as F` (E integer, F float) uses unsigned-to-float if the source is unsigned (u8/u16/u24/u32/u56/u64/u128) or i1 (bool), else signed-to-float. Bool must be treated as unsigned: signed conversion of i1(1) gives -1.0.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3640-L3656"
      ],
      "testability": "untestable"
    },
    {
      "id": "coerce.cast.float-to-float",
      "domain": "coerce",
      "title": "Float to float truncates or extends by width",
      "statement": "`E as F` (both float): truncate if width(F) < width(E), else extend.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3657-L3665"
      ],
      "testability": "untestable"
    },
    {
      "id": "coerce.cast.float-to-int-by-target-signedness",
      "domain": "coerce",
      "title": "Float to integer conversion respects target signedness",
      "statement": "`E as T` (E float, T integer) uses float-to-unsigned-int if T is unsigned (u8/u16/u24/u32/u56/u64/u128) else float-to-signed-int.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3666-L3679"
      ]
    },
    {
      "id": "coerce.cast.int-to-ptr",
      "domain": "coerce",
      "title": "Integer to pointer widens to 64-bit then reinterprets",
      "statement": "`E as *T` (E integer) first widens E to 64-bit (zero-extend if the source is unsigned, else sign/value-coerce) then reinterprets the integer as an address.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3681-L3697"
      ]
    },
    {
      "id": "coerce.cast.ptr-to-int",
      "domain": "coerce",
      "title": "Pointer to integer reinterprets address",
      "statement": "`E as T` (E pointer, T integer) reinterprets the address as an integer of T's width.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3698-L3700"
      ]
    },
    {
      "id": "expr.if.branch-result-coercion",
      "domain": "expr",
      "title": "If-expression coerces both branch values to the result type",
      "statement": "An if-expression of type T evaluates the condition then both branches; each non-diverging branch value is numerically coerced to T and stored into a shared result slot, whose value is the if-expression's result. Aggregate branch values are spilled to a stack slot so both branches store a pointer when T is pointer-represented.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3710-L3780"
      ]
    },
    {
      "id": "expr.if.divergent-branch-skips-merge",
      "domain": "expr",
      "title": "Diverging if-branch omits its merge edge",
      "statement": "If a branch body diverges (e.g. `break`/`return` that already terminates the block), the if-expression omits that branch's result-store and merge branch; the merge point's predecessors simply exclude the diverging edge.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3742-L3759",
        "src/compiler/mlir_gen_expr.cpp#L3763-L3773"
      ]
    },
    {
      "id": "expr.if.void-branches-still-evaluated",
      "domain": "expr",
      "title": "Void if-expression still evaluates both branches",
      "statement": "An if-expression of unit type `()` still emits and evaluates both branch bodies (for their side effects such as panics/writes) and yields a synthetic unit value; the branches are not dropped despite producing no value.",
      "evidence": [
        "src/compiler/mlir_gen_expr.cpp#L3715-L3724",
        "src/compiler/mlir_gen_expr.cpp#L3775-L3779"
      ]
    }
  ]
}
