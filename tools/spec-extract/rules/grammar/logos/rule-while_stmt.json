{
  "unit": "grammar/logos/rule-while_stmt",
  "layer": "grammar",
  "source": "tools/peg_gen_cpp/grammars/logos.peg#L2340-L2941",
  "source_hash": "4536acbd5125d9a3",
  "rules": [
    {
      "id": "stmt.assign.place-general",
      "domain": "stmt",
      "title": "General place-assignment",
      "statement": "`place = expr ;` where `place` is an arbitrary postfix-chain read-expression (`atom`: chained index/field/tuple-index/deref — e.g. `a[i][j] = v`, `(*p).0 = v`) is a valid assignment; the place's address is computed from the read-expr shape and a deref-write emitted. Tried after the specialized single/double-level write productions (which win on the shapes they cover) and after the bare-`IDENT` `stmt.assign.simple` rule.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2355-L2364"
      ],
      "testability": "behavioral"
    },
    {
      "id": "stmt.assign.compound",
      "domain": "stmt",
      "title": "Compound assignment over any place",
      "statement": "Compound-assignment operators: `+= -= *= /= %= &= |= ^= <<= >>=`. `place OP= expr ;` is valid for any postfix-chain place (`atom`); a bare variable takes a direct update path, any other place desugars to `place = place OP expr` (or an in-place-op call). A bare-deref place `*p OP= v` is handled separately (`stmt.assign.deref-write`), since `*p` alone is not an `atom`.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2384-L2387"
      ],
      "testability": "behavioral"
    },
    {
      "id": "stmt.assign.deref-write",
      "domain": "stmt",
      "title": "Dereference write and compound-through-deref",
      "statement": "`*p = v ;` writes value `v` through dereferenced place `p` (a `unary_expr`). `*p OP= v ;` is compound assignment through a bare dereference, defined to lower to `*p = *p OP v`.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2395-L2400"
      ],
      "testability": "behavioral"
    },
    {
      "id": "expr.if.plain",
      "domain": "expr",
      "title": "if / if-else expression",
      "statement": "`if cond { THEN }` and `if cond { THEN } else (if_expr | { ELSE })` are expressions; `cond` is a bare `expr_ns` (no-struct-lit). `else` may chain to another `if_expr`, forming an if/else-if/…/else ladder.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2425-L2428"
      ],
      "testability": "behavioral"
    },
    {
      "id": "expr.comprehension.list",
      "domain": "expr",
      "title": "List comprehension",
      "statement": "`[expr for x in iter if pred]` and `[expr for x in iter]` (guard optional) produce a list by iterating `iter`, binding `x`, and — if present — filtering by `pred`.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2936-L2939"
      ],
      "divergence": "A6 — Logos addition: Python-style comprehension syntax, not present in Rust."
    },
    {
      "id": "metaprog.macro.fn-style-call",
      "domain": "metaprog",
      "title": "Function-style macro invocation in operand position",
      "statement": "Function-style macros invoke as `name!(…)` or `name![…]` in operand/expression position (`if cfg!(unix) && … {}`, `while m!(x) {}`); the ordinary (non-no-struct-lit) primary additionally admits the brace form `name!{…}`, which is excluded from the no-struct-lit grammar since it would swallow the enclosing if/while body block. Contents between the balanced delimiters are captured as raw source text and re-interpreted at sema time per the callee's macro kind.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2616-L2619",
        "tools/peg_gen_cpp/grammars/logos.peg#L2809-L2814"
      ]
    },
    {
      "id": "generic.pack.sizeof-and-expand",
      "domain": "generic",
      "title": "Variadic pack size and expansion in expression position",
      "statement": "`P...(N)` (`SIZEOF_PACK`) yields the length of variadic pack `P` bound to name `N`; `P...` in expression position (`PACK_EXPAND`) expands the pack `P`.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2605-L2606",
        "tools/peg_gen_cpp/grammars/logos.peg#L2632",
        "tools/peg_gen_cpp/grammars/logos.peg#L2834"
      ]
    },
    {
      "id": "expr.array.literal-forms",
      "domain": "expr",
      "title": "Array literal and fill forms",
      "statement": "An array literal `[ ... ]` has two forms, tried in this order (fill before list, to resolve the shared `[ expr` prefix ambiguity): (1) fill form `[value; N]`, repeating `value` N times, where `N` is one of: an integer literal, a named constant/identifier, a variadic pack length `sizeof...(P)` written `IDENT...(IDENT)`, or a `metacall { ... }` block evaluated to produce the size; (2) element-list form `[e1, e2, ...]` - zero or more comma-separated exprs with an optional trailing comma.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2863-L2873",
        "tools/peg_gen_cpp/grammars/logos.peg#L2703-L2704",
        "tools/peg_gen_cpp/grammars/logos.peg#L2926-L2933"
      ]
    },
    {
      "id": "expr.block.as-value",
      "domain": "expr",
      "title": "Block / control constructs as expressions",
      "statement": "`{ … }` (bare block), `unsafe { … }`, `loop { … }`, `if … {} else {}`, and `match … {}` are all primary expressions producing a value (block/loop yield their tail/break value). In the no-struct-lit primary (`primary_expr_ns`, used e.g. for an `if`/`while`/`match` condition) a *bare* `{ … }` block is NOT admitted as a primary — only the keyword-prefixed forms `unsafe`/`loop`/`if`/`match` are — to avoid ambiguity with the enclosing control construct's own opening brace.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2711-L2715",
        "tools/peg_gen_cpp/grammars/logos.peg#L2771-L2775",
        "tools/peg_gen_cpp/grammars/logos.peg#L2576-L2588"
      ]
    },
    {
      "id": "expr.cast.as-chain",
      "domain": "expr",
      "title": "as-cast chaining",
      "statement": "`v as T` binds below unary operators and chains left-associatively, so `x as T1 as T2` folds as `(x as T1) as T2`; a bare unary_expr with zero `as`-suffixes passes through unchanged.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2521-L2529",
        "tools/peg_gen_cpp/grammars/logos.peg#L2693-L2706",
        "tools/peg_gen_cpp/grammars/logos.peg#L2638-L2646",
        "tools/peg_gen_cpp/grammars/logos.peg#L2632"
      ]
    },
    {
      "id": "expr.cmp.non-chainable",
      "domain": "expr",
      "title": "Comparison operators are non-chainable",
      "statement": "Comparison operators are non-chainable: at most one comparison per level is well-formed. A chain of 2+ comparators (e.g. `a < b < c`), in both the struct-literal-permitting (`cmp_expr`) and no-struct-literal (`cmp_expr_ns`) expression grammars, is grammatically distinguished as a dedicated `CHAINED_CMP` node — tried before the single-comparison alt — so sema can reject it with a specific diagnostic (\"chained comparisons not supported; use `a < b && b < c`\", B-ex-08) rather than a generic syntax error.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2484-L2491",
        "tools/peg_gen_cpp/grammars/logos.peg#L2653-L2660",
        "tools/peg_gen_cpp/grammars/logos.peg#L2424-L2431",
        "tools/peg_gen_cpp/grammars/logos.peg#L2589-L2600",
        "tools/peg_gen_cpp/grammars/logos.peg#L290"
      ],
      "divergence": "Rust-conformant outcome (chained comparison is rejected); Logos additionally detects it grammatically (rather than only in sema) to produce a dedicated diagnostic instead of a generic parse error."
    },
    {
      "id": "expr.control.never-position",
      "domain": "expr",
      "title": "Diverging control-flow as expression",
      "statement": "In full expression position (`primary_expr`): `return [e]`, `break ['label] [e]`, bare `break`, and `continue ['label]` may appear as an expression producing the never type `!`, enabling forms like `let x = if c { v } else { return e };`, `_ => break`, and `match … { … => continue }`. In the no-struct-lit primary (`primary_expr_ns`, used in if/while/for condition position) only bare `break` is admitted as an expression — labeled/value-carrying `break`, and `continue`/`return` in any form, are excluded there because they would greedily consume following tokens or shadow the statement-level forms (e.g. `return *v;` would otherwise parse as a bare RETURN_EXPR followed by an orphan `*v`).",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2716-L2728",
        "tools/peg_gen_cpp/grammars/logos.peg#L2781-L2788",
        "tools/peg_gen_cpp/grammars/logos.peg#L2589-L2596"
      ],
      "examples": [
        "let x = if c { v } else { return e };",
        "match m { _ => break }"
      ]
    },
    {
      "id": "expr.enum-lit.forms",
      "domain": "expr",
      "title": "Enum variant literal forms",
      "statement": "Enum variants are written `E::V` (unit), `E::V(args)` (tuple payload; optional turbofish `E::V::<T,…>(args)`), `E::V { f: e, … }` (struct-shape payload; field names resolved to canonical positional indices via the variant's payload field names), or bare turbofish `E::V::<T,…>` (generic variant reference without a call). The qualified-as form `<T as Trait>::V` and the dotted-package-prefix form `pkg.path.E::V` (unit variant; the last dotted segment is the enum type) are also accepted.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2787-L2816",
        "tools/peg_gen_cpp/grammars/logos.peg#L2847-L2876"
      ]
    },
    {
      "id": "expr.field.tuple-index",
      "domain": "expr",
      "title": "Field / tuple-index access",
      "statement": "Postfix `.field` (`FIELD_READ`) reads a named field (`new`/`null` also accepted as field names); `.N` (integer) reads the Nth tuple/tuple-struct element (`TUPLE_INDEX`); `.#(expr)` and `.#ident` read a field whose name is computed from a bound expression/antiquote variable.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2554-L2565",
        "tools/peg_gen_cpp/grammars/logos.peg#L2738-L2749",
        "tools/peg_gen_cpp/grammars/logos.peg#L2684-L2685",
        "tools/peg_gen_cpp/grammars/logos.peg#L2678-L2679"
      ]
    },
    {
      "id": "expr.if.let-chain",
      "domain": "expr",
      "title": "if let-chain",
      "statement": "An `if` may chain conditions with `&&` where the first segment is a `let` binding: `if let P = e && seg (&& seg)* { THEN } [else …]`. The `let` scrutinee is parsed at `cmp_expr_ns` (one level below `&&`) so the `&&` belongs to the chain rather than the scrutinee expression. Each subsequent seg is either `let P = e` (scrutinee also at `cmp_expr_ns`) or a bare condition (`cmp_expr_ns`). The chain requires the first segment to be a `let` and at least two `&&`-joined segments — enforced so the legacy single-let and bare-cond `if` forms are not shadowed — and is followed by a lookahead on `{` so it does not swallow tokens past the if-block opening. Desugars to nested matching: all let-patterns must match and all bare conditions must hold for THEN to execute (equivalent to `match e { P if <trailing-cond> => THEN, _ => ELSE }` when there is exactly one trailing bare condition).",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2402-L2416",
        "tools/peg_gen_cpp/grammars/logos.peg#L2430-L2441"
      ]
    },
    {
      "id": "expr.if.no-struct-lit-cond",
      "domain": "expr",
      "title": "if/while/for condition restricts struct literals",
      "statement": "In `if`/`while`/`for` condition position the scrutinee uses the no-struct-lit expression grammar (`expr_ns`): a top-level `IDENT { … }` is NOT parsed as a struct literal, so the brace opens the control-flow block instead. The restriction applies only to the top-level primary — inside parens/brackets/calls, full `expr` resumes. To use a struct literal in condition position it must be parenthesized: `if (Foo { x: 1 }.ok) { }`.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2471-L2477",
        "tools/peg_gen_cpp/grammars/logos.peg#L2572-L2576"
      ]
    },
    {
      "id": "expr.if.single-let-guard",
      "domain": "expr",
      "title": "if-let with single guard condition",
      "statement": "`if let P = e && cond { THEN } [else ELSE]` (single let plus one trailing condition) desugars to `match e { P if cond => THEN, _ => ELSE }`; the let scrutinee is parsed at `cmp_expr_ns` so the `&&` belongs to the guard, and the trailing `cond` is a full `expr_ns` (itself possibly an `&&` chain).",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2417-L2420"
      ]
    },
    {
      "id": "expr.index.read",
      "domain": "expr",
      "title": "Index expression",
      "statement": "`e[i]` (`INDEX_READ`) is a postfix index-read, available identically in the ordinary and no-struct-lit postfix chains (`LBRACKET expr RBRACKET => INDEX_READ`). When the index is a range expression (`s[a..b]`, `s[a..]`, `s[..b]`, `s[..]`) the result is a slice rather than a single element.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2566-L2567",
        "tools/peg_gen_cpp/grammars/logos.peg#L2750-L2751",
        "tools/peg_gen_cpp/grammars/logos.peg#L2690-L2691",
        "tools/peg_gen_cpp/grammars/logos.peg#L2394-L2396"
      ]
    },
    {
      "id": "expr.literal.kinds",
      "domain": "expr",
      "title": "Primary literal forms",
      "statement": "The primary/atom literal forms, identical in both `primary_expr` (normal expression position) and `primary_expr_ns` (struct-lit-suppressed position used in `if`/`while` conditions etc.): `RAW_STRING`/`STRING` -> `LIT_STR`, `BYTE_STRING` -> `LIT_BYTES`, `CHAR_LIT` -> `LIT_CHAR`, `KW_TRUE`/`KW_FALSE` -> `LIT_BOOL`, `FLOAT` -> `LIT_FLOAT`, `INTEGER` -> `LIT_INT`. A `BYTE_STRING` literal lowers at sema to a `[u8; N]` array literal of its decoded bytes (escapes `\\n \\t \\r \\0 \\\\ \\\" \\x..` supported, matching the `PAT_BYTES` decoder).",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2822-L2834",
        "tools/peg_gen_cpp/grammars/logos.peg#L2624-L2632"
      ]
    },
    {
      "id": "expr.postfix.chain",
      "domain": "expr",
      "title": "Postfix operator chain",
      "statement": "A primary expression may be followed by zero or more left-associative postfix suffixes: method call `.m(args)` (optionally `.m::<T>(args)` with explicit turbofish, or the `.new(args)`/`.null(args)` reserved-name-method spellings), expression-callee invocation `e(args)` (arbitrary receiver, e.g. `arr[i](x)`, `(get_fn())(x)`, `(|| body)(x)`), field read `.field` (also `.new`/`.null` as field names, and dynamic-name forms `.#(expr)` / `.#ident`), tuple index `.N`, indexing `[i]`, and the try operator `?`. Chains parse left-to-right via a fold accumulator (`a.b.c`, `a.f().b`).",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2539-L2570",
        "tools/peg_gen_cpp/grammars/logos.peg#L2722-L2754"
      ]
    },
    {
      "id": "expr.range.family",
      "domain": "expr",
      "title": "Range expressions",
      "statement": "Range value-expressions: `lo..hi` (half-open), `lo..=hi` (inclusive), `lo..` (from), `..hi` (to), `..=hi` (to-inclusive), `..` (full). Grammar: `expr <- range_expr`, i.e. range is the top-level value-expression production — it binds loosest of all operators in the precedence cascade (below it: logical operators). An omitted side leaves the corresponding bound unspecified/unfilled; sema fills the open side. Sema lowers each form to a stdlib `RangeI64`/`RangeI32` struct implementing `Iterator<T>`.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2452-L2469"
      ],
      "examples": [
        "let r = 0..10;",
        "let r = 0..=10;",
        "s[a..b]",
        "s[..n]",
        "s[a..]",
        "s[..]"
      ]
    },
    {
      "id": "expr.struct-lit.field-init",
      "domain": "expr",
      "title": "Struct field initializers and shorthand",
      "statement": "A field initializer is `name: expr`, or shorthand `name` (`FIELD_SHORTHAND`, binds the in-scope variable of that name). Tuple-struct fields may be initialized by numeric name, `S { 0: a, 1: b }`, since fields of `struct S(T0, T1)` are named \"0\"/\"1\" (matching `.0`/`.1` access). The reserved words `new`/`null` are valid field names (`new: e`, `null: e`). Antiquote alts `#(field_init),*` (cursor-expanded field-init list) and `#(expr): expr` / `#IDENT: expr` (placeholder field name) are valid only inside a `quote_expr!` body.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2900-L2921"
      ]
    },
    {
      "id": "expr.struct-lit.forms",
      "domain": "expr",
      "title": "Struct literal forms",
      "statement": "Struct literals: `T { f: e, … }`, generic `T::<A,…> { f: e, … }`, and functional-update `T { f: e, .. base }` / `T { .. base, f: e }` / `T { .. base }` (explicit fields always override `base` regardless of source field order); plus the antiquote forms `#(expr) { … }` / `#IDENT { … }` (struct name supplied by a bound variable — valid only inside `quote_expr!`).",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2818-L2838",
        "tools/peg_gen_cpp/grammars/logos.peg#L2823-L2831",
        "tools/peg_gen_cpp/grammars/logos.peg#L2880-L2898"
      ]
    },
    {
      "id": "expr.turbofish.generic-ref",
      "domain": "expr",
      "title": "Turbofish generic reference and static call",
      "statement": "`IDENT::<T,…>` is a generic reference (`GENERIC_REF`, explicit type arguments applied to a function/item), available in both the ordinary and no-struct-lit expression grammars. `IDENT::<T,…>::METHOD` is a static call (`STATIC_CALL`) on the type-applied receiver.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2621-L2622",
        "tools/peg_gen_cpp/grammars/logos.peg#L2756-L2760",
        "tools/peg_gen_cpp/grammars/logos.peg#L2817-L2820"
      ]
    },
    {
      "id": "expr.unary.operator-set",
      "domain": "expr",
      "title": "Unary / prefix operators",
      "statement": "Prefix unary operators bind tighter than `as`-cast (and thus tighter than every binary operator): `*` deref, `&` borrow, `&mut` mutable borrow, `-` negate, `!` not. `&&v` lexes as the single AND token but denotes a double reference, lowering to nested address-of (mirrors DOUBLE_REF_TYPE at the type level).",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2531-L2537",
        "tools/peg_gen_cpp/grammars/logos.peg#L2708-L2716"
      ]
    },
    {
      "id": "intrinsic.offset-of.form",
      "domain": "intrinsic",
      "title": "offset_of! intrinsic",
      "statement": "`offset_of!(Type, field)` yields the byte offset of `field` within `Type`.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2597-L2598",
        "tools/peg_gen_cpp/grammars/logos.peg#L2729-L2730",
        "tools/peg_gen_cpp/grammars/logos.peg#L2789-L2790"
      ]
    },
    {
      "id": "stmt.assign.destructure",
      "domain": "stmt",
      "title": "Destructuring assignment into existing places",
      "statement": "`(n1, n2, …) = expr;` and `[n1, n2, …] = expr;` destructure-assign a tuple/array RHS into existing bindings n1, n2, … (reusing `pat_binding_list`); `Name { f1, f2, … } = expr;` destructure-assigns a struct RHS by field (reusing `pat_field_list`). Per RFC 2909, this writes into EXISTING places — unlike `let (a,b) = e;`, it introduces no new bindings. Ordered before the general expression-statement rule so a parenthesised/bracketed LHS followed by `=` is recognized as destructuring assign; the parser backtracks to an ordinary expr-statement when no `=` follows.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2366-L2375"
      ],
      "related": [
        "stmt.let.forms",
        "stmt.assign.simple",
        "stmt.assign.place-general",
        "stmt.assign.compound"
      ]
    },
    {
      "id": "stmt.assign.simple",
      "domain": "stmt",
      "title": "Simple variable assignment",
      "statement": "`IDENT = expr ;` assigns to a plain (unqualified) variable place.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2352-L2353"
      ]
    },
    {
      "id": "stmt.return.form",
      "domain": "stmt",
      "title": "return statement",
      "statement": "`return [expr] (;|,)` exits the enclosing function, optionally yielding a value; terminated by `;` or `,`.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2349-L2350"
      ],
      "uncertainty": "COMMA as an alternate terminator (besides SEMI) is unusual; likely to admit `return e` inside a macro/list-expansion context — not confirmed from this slice alone."
    },
    {
      "id": "stmt.while.forms",
      "domain": "stmt",
      "title": "while-loop statement forms",
      "statement": "`while` has four forms, tried in this order: (1) `while <if_let_chain> { body }` — a multi-segment `&&`-joined let-chain (≥2 segments, first must be `let`), ordered first so it is not shadowed by the single-let form; (2) `while let P = e && guard { body }` — single let-binding plus a trailing guard (guard parsed as `expr_ns`, scrutinee `e` at `cmp_expr_ns`); (3) `while let P = e { body }`; (4) `while cond { body }` with `cond` a bare `expr_ns` (no-struct-lit). The let/let-chain forms loop while the pattern(s) match / all chain segments hold; form (4) is a plain conditional loop. The let-chain and while-let productions reuse the same `if_let_chain` parser as the `if`-expression let-chain form.",
      "evidence": [
        "tools/peg_gen_cpp/grammars/logos.peg#L2340-L2347",
        "tools/peg_gen_cpp/grammars/logos.peg#L2277-L2287"
      ]
    }
  ]
}
