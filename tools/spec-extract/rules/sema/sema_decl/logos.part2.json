{
  "unit": "sema/sema_decl/logos.part2",
  "layer": "sema",
  "source": "src/compiler/sema_decl.cpp#L570-L1153",
  "source_hash": "24be40d740cfdb14",
  "rules": [
    {
      "id": "type.never.param-uninhabited",
      "domain": "type",
      "title": "Never type forbidden in parameter position",
      "statement": "A function parameter typed `!` (never) is rejected: `!` is uninhabited, so no value can be supplied. `!` remains valid in return position (a diverging function).",
      "evidence": ["src/compiler/sema_decl.cpp#L589-L595"],
      "examples": ["fn f(x: !) {}  // error", "fn g() -> ! { loop {} }  // ok"]
    },
    {
      "id": "item.fn-param.struct-pattern",
      "domain": "item",
      "title": "Struct-pattern function parameter",
      "statement": "A parameter may be an irrefutable struct pattern `Name { a, b, ... }: Name`. Each named field (or its `f: binding` rename) becomes a body-visible binding of the field's type; `..` rest is ignored. The pattern is desugared to a synthetic parameter plus prologue `let bind = synth.field;`.",
      "evidence": ["src/compiler/sema_decl.cpp#L604-L648", "src/compiler/sema_decl.cpp#L999-L1045"],
      "examples": ["fn f(Point { x, y }: Point) -> i32 { x + y }"]
    },
    {
      "id": "item.fn-param.tuple-pattern",
      "domain": "item",
      "title": "Tuple-destructure function parameter",
      "statement": "A parameter may be an irrefutable tuple pattern `(a, b, ...): (T1, T2, ...)`. Each non-`_` element name becomes a body-visible binding of the corresponding tuple element type, desugared to a synthetic parameter plus prologue `let a = synth.0;` etc.",
      "evidence": ["src/compiler/sema_decl.cpp#L654-L684", "src/compiler/sema_decl.cpp#L975-L994"],
      "examples": ["fn f((a, b): (i32, i32)) -> i32 { a + b }"]
    },
    {
      "id": "item.fn-param.self-reserved",
      "domain": "item",
      "title": "`self` reserved for impl receivers",
      "statement": "A parameter named `self` is an error outside an impl-block; `self` is only the magic receiver inside impl methods.",
      "evidence": ["src/compiler/sema_decl.cpp#L687-L694"],
      "examples": ["fn f(self: i32) {}  // error outside impl"]
    },
    {
      "id": "item.fn-param.datanode-by-value",
      "domain": "item",
      "title": "DataNode eidos cannot be passed by value",
      "statement": "A parameter whose type is (or contains) a DataNode datatype (one holding relative-pointer fields) is rejected by value; it must be passed as `DataRef<T>` because the relative pointers require a zone base pointer.",
      "evidence": ["src/compiler/sema_decl.cpp#L700-L713"],
      "divergence": "Logos addition (zoned/DataNode model); no Rust analog"
    },
    {
      "id": "item.fn-param.mut-binding",
      "domain": "item",
      "title": "`mut` parameter binding",
      "statement": "A typed parameter `mut x: T` makes `x` a mutable, caller-invisible local binding: the body may reassign or take `&mut` of it. Desugared to an immutable synthetic parameter plus prologue `let mut x = synth;` (a move of the param value into the user local).",
      "evidence": ["src/compiler/sema_decl.cpp#L714-L741", "src/compiler/sema_decl.cpp#L1046-L1060"],
      "examples": ["fn f(mut x: i32) { x += 1; }"]
    },
    {
      "id": "item.fn-param.owning-box-dyn",
      "domain": "item",
      "title": "By-value Box<dyn Trait> param owns the box",
      "statement": "A by-value parameter of owning trait-object type (`Box<dyn Trait>`) makes the callee own the box: it is dropped at the callee epilogue (vtable drop_in_place + dealloc) and call sites coerce the argument to a heap fat handle.",
      "evidence": ["src/compiler/sema_decl.cpp#L742-L759"],
      "examples": ["fn f(b: Box<dyn Trait>) {}"]
    },
    {
      "id": "item.fn-param.unique-names",
      "domain": "item",
      "title": "Parameter names must be unique",
      "statement": "All parameter names within one function signature must be distinct.",
      "evidence": ["src/compiler/sema_decl.cpp#L765-L768"]
    },
    {
      "id": "region.elision.ambiguous-output",
      "domain": "region",
      "title": "Output lifetime elision (E0106) ambiguity",
      "statement": "When the return type structurally contains an unannotated reference (`&T`, `&&T`, `(&T,..)`, `[&T;N]`, `&[T]`; references inside generic type-args are excluded), elision requires a source: it is an error (E0106) if there are >=2 input lifetime positions and no `&self`/`&mut self` receiver. With exactly one input lifetime or a `&self` receiver, elision succeeds; the zero-input case is left to explicit annotation.",
      "evidence": ["src/compiler/sema_decl.cpp#L772-L836"],
      "examples": ["fn h(a:&i32, b:&i32) -> &i32  // error E0106", "fn f(a:&i32) -> &i32  // ok (rule 2)"],
      "uncertainty": "Zero-input elided-ref return (e.g. returning &'static) is not flagged here; deferred to dangling-borrow check."
    },
    {
      "id": "item.fn.tail-match-as-return",
      "domain": "item",
      "title": "Tail match arms are return values",
      "statement": "When a non-void function's body ends with a `match` expression, each EXPR arm of that tail match is treated as the function's return value.",
      "evidence": ["src/compiler/sema_decl.cpp#L953-L972"]
    },
    {
      "id": "item.fn.impl-trait-return-infer",
      "domain": "item",
      "title": "impl Trait return inferred from body",
      "statement": "A function declared `-> impl Trait` resolves its return type to the single concrete type inferred from the body's return expressions; if none can be inferred it is an error.",
      "evidence": ["src/compiler/sema_decl.cpp#L1061-L1070"],
      "examples": ["fn f() -> impl Iterator { 0..3 }"]
    },
    {
      "id": "item.fn.all-paths-return",
      "domain": "item",
      "title": "All paths must return a value",
      "statement": "A function with a non-void, non-error return type must have every control-flow path produce a value (trailing tail expressions count as implicit returns); otherwise it is an error.",
      "evidence": ["src/compiler/sema_decl.cpp#L1071-L1082"]
    },
    {
      "id": "item.fn.param-drop-epilogue",
      "domain": "item",
      "title": "By-value params dropped at function epilogue",
      "statement": "By-value droppable (move-type) parameters are dropped at the function epilogue, equivalently to a `let` binding, when the body falls off the end without an explicit terminating return/break/continue. A parameter that was moved on any branch is conservatively skipped from the static epilogue drop (to avoid double-free on the move path).",
      "evidence": ["src/compiler/sema_decl.cpp#L1083-L1117"],
      "examples": ["fn consume(_x: Move) {}  // _x dropped at end"]
    }
  ]
}
