{
  "unit": "sema/sema_stmt/lower_loop",
  "layer": "sema",
  "source": "src/compiler/sema_stmt.cpp#L6868-L7606",
  "source_hash": "3d42cf1ccc6652eb",
  "rules": [
    {
      "id": "stmt.loop.conservative-init",
      "domain": "stmt",
      "title": "Loop body may run zero times for init analysis",
      "statement": "A `loop { ... }` body is treated as possibly executing zero times: any variable that becomes definitely-initialized only inside the loop body remains uninitialized in the enclosing scope after the loop (the pre-loop uninit set is snapshotted and restored once the body is lowered).",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L6877-L6894"
      ],
      "testability": "untestable"
    },
    {
      "id": "stmt.loop.diverges-never",
      "domain": "stmt",
      "title": "Loop with no reachable break diverges (type !)",
      "statement": "A `loop` whose body contains no `break` reaching the loop diverges; in expression position its type is `!` (never). A loop is non-diverging iff some `break` targeting it carries a value (giving a result type) or breaks without value.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L6887-L6889",
        "src/compiler/sema_stmt.cpp#L6895-L6897"
      ]
    },
    {
      "id": "stmt.loop.break-value-type",
      "domain": "stmt",
      "title": "Loop expression value comes from break values",
      "statement": "If any `break <expr>` targets the loop, the loop expression's type is the unified type recorded on its break-frame, and lowering allocates a break-value slot the loop yields at exit; `break` without value marks the frame non-diverging but gives no result type.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L6887-L6889",
        "src/compiler/sema_stmt.cpp#L6901-L6904"
      ]
    },
    {
      "id": "stmt.loop.label-scope",
      "domain": "stmt",
      "title": "Loop label active only within its body",
      "statement": "A loop label is bound (pushed onto the active label set) only for the duration of lowering the loop body, and is captured before body lowering so the label of the immediately-enclosing loop is the pending label; nested loops push/pop their labels with their bodies.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L6871-L6872",
        "src/compiler/sema_stmt.cpp#L6883-L6885",
        "src/compiler/sema_stmt.cpp#L6891"
      ]
    },
    {
      "id": "expr.assign.place-only",
      "domain": "expr",
      "title": "Assignment LHS must be an assignable place",
      "statement": "The left side of a compound place assignment must be a genuine lvalue shape: an index `a[i]`, field access `a.f`, tuple index `a.N`, or dereference `*p`. Any other LHS (call result, literal, arithmetic) is rejected with 'invalid assignment target: left side is not an assignable place'.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7350-L7361",
        "src/compiler/sema_stmt.cpp#L6908-L6919"
      ]
    },
    {
      "id": "expr.assign.index-mut-desugar",
      "domain": "expr",
      "title": "Indexed assignment uses IndexMut",
      "statement": "For a type implementing `IndexMut`, `a[i] = v` desugars to a store through the trait's produced reference (`*<Type>__index_mut(&mut a, i)` or `*a.index_mut(i)`, index widened to the trait's index-type parameter); the receiver `a` must be a mutable binding, else 'index write to immutable struct' is diagnosed.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7130-L7187",
        "src/compiler/sema_stmt.cpp#L7140-L7141",
        "src/compiler/sema_stmt.cpp#L7364-L7373"
      ]
    },
    {
      "id": "borrow.assign.immutable-var",
      "domain": "borrow",
      "title": "Write to immutable binding rejected",
      "statement": "A write whose place is rooted at a non-`mut` local variable is rejected ('assignment to immutable variable').",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7019-L7023"
      ]
    },
    {
      "id": "borrow.assign.shared-ref",
      "domain": "borrow",
      "title": "Write through shared reference rejected",
      "statement": "A write through a place rooted at a shared reference `&T` (or a shared `&DstStruct`) is rejected; a write through `&mut T` (or a `&mut DstStruct`) is permitted.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L6982-L6993",
        "src/compiler/sema_stmt.cpp#L7035-L7038"
      ]
    },
    {
      "id": "borrow.assign.raw-ptr-unsafe",
      "domain": "borrow",
      "title": "Write through raw pointer requires *mut and unsafe",
      "statement": "A write through a place rooted at a raw pointer `*const T` is rejected; through `*mut T` it is permitted only inside an `unsafe` context.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L6994-L7004",
        "src/compiler/sema_stmt.cpp#L7025-L7046"
      ]
    },
    {
      "id": "borrow.assign.static-mut-unsafe",
      "domain": "borrow",
      "title": "static mut write requires unsafe; immutable static not writable",
      "statement": "A write to a place rooted at a `static mut` is permitted (storage is mutable) but requires an `unsafe` block; a write to a plain immutable `static` is rejected.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7005-L7018"
      ],
      "divergence": "Rust-conformant (items.static.mut.safety)"
    },
    {
      "id": "borrow.assign.shared-slice-elem",
      "domain": "borrow",
      "title": "Element write through shared slice rejected",
      "statement": "For a field/index/tuple-index place whose receiver resolves to `Slice` type, writing is permitted only through a `&mut [T]` slice; a shared `&[T]` receiver is rejected ('cannot write through a shared `&[T]` slice'). The same receiver-kind switch also governs a receiver resolving to `MutRef` (writable) or `Ptr` (needs `*mut` + unsafe).",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7055-L7077"
      ]
    },
    {
      "id": "expr.assign.place-nesting-bound",
      "domain": "expr",
      "title": "Deeply-nested assignment targets rejected",
      "statement": "A place-write target is accepted only for shapes the address-of machinery can lower: a bare variable or `*p` bottoming out a recursion, INDEX_READ recursing to arbitrary depth over its receiver, and FIELD_READ/TUPLE_INDEX bounded to a receiver that is itself var/deref, a field chain over one, or an index into a supported place. Deeper/other nestings are rejected with 'assignment target too deeply nested to assign in place yet' (suggesting an intermediate `&mut` binding) rather than mis-lowered.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L6927-L6964",
        "src/compiler/sema_stmt.cpp#L7455-L7463"
      ],
      "divergence": "Compiler-side lowering limitation: Rust places arbitrary-depth field/index/tuple-index nesting; this compiler's general place-write path currently accepts only the bounded shapes above, erroring (with a workaround) on deeper nestings rather than treating the program as ill-formed.",
      "uncertainty": "The exact accepted shape set is defined by place_write_supported/place_field_base_ok recursion; bound is an implementation limitation, not a language-design boundary."
    },
    {
      "id": "expr.assign.dataref-field-unsafe",
      "domain": "expr",
      "title": "DataRef<ZonedStruct> field write desugars via mut_ptr and needs unsafe",
      "statement": "`p.field = v` where `p: DataRef<Z>` with `Z` a zoned struct desugars to `{ let t = p.mut_ptr(); (*t).field = v; }` (the DerefMut analog); it requires an `unsafe` context, `p` must be a mutable binding, and `v` must be type-compatible with the field type.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7194-L7235"
      ],
      "divergence": "Logos-specific: DataRef<T> is a zoned-memory smart pointer with no direct Rust counterpart; unlike Rust's DerefMut (auto-deref without an unsafe requirement), this ergonomic field-write path mandates an enclosing `unsafe` block.",
      "testability": "untestable"
    },
    {
      "id": "expr.assign.drop-before-replace",
      "domain": "expr",
      "title": "Field assignment drops old value first",
      "statement": "Assigning to a field place over an owned local root drops the place's prior value before the store, provided the value is live (root owned, definitely-initialized, no overlapping moved-out path) and droppable; assigning to a path also lifts drop-suppression for the covered (equal-or-deeper) moved paths so the scope-end drop releases the new value.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7386-L7436",
        "src/compiler/sema_stmt.cpp#L7592-L7604"
      ],
      "divergence": "Rust-conformant (expr.assign.drop-target / B8)"
    },
    {
      "id": "expr.assign.type-mismatch",
      "domain": "expr",
      "title": "Assignment value must match place type",
      "statement": "The assigned value's type must be compatible with the place's type (modulo `#[rel_ptr]`↔`*T` relations); otherwise a type-mismatch error is raised. Before the store the value is integer-widened to the place type, and the place type hints enum/struct literal RHS resolution.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7493-L7515"
      ]
    },
    {
      "id": "const.assign.intlit-fits",
      "domain": "const",
      "title": "Integer-literal RHS must fit the place's integer type",
      "statement": "An integer-literal RHS (scalar, or each element of an array/tuple literal RHS, recursively through one level of nested array/tuple literals) must fit the corresponding narrow integer place type; a value that does not fit is rejected.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7516-L7589"
      ]
    },
    {
      "id": "expr.assign.union-field-safe",
      "domain": "expr",
      "title": "Writing a union field is safe",
      "statement": "Writing to a union field is safe (no `unsafe` required for the write): the place-write LHS sets `in_place_write_lhs_`, suppressing the union unsafe gate that otherwise applies when reading a union field.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7467-L7473"
      ],
      "divergence": "Rust-conformant (items.union.fields.write-safety)"
    },
    {
      "id": "writ.schema.field-write-desugar",
      "domain": "writ",
      "title": "schema struct field write desugars to a TOM WMap::set",
      "statement": "For `p.field = v` where `p`'s (deref'd) base type is a registered `schema` struct, the write lowers to `(&mut *p.m).set(KEY, wany)`, where `p.m` is the schema's fixed-capacity `WMap<Wu6,WAny>` view (reinterpreted `*const`→`&mut`), KEY is the field's assigned schema key, and `wany` is either `v` verbatim (if the field's declared type is `WAny`) or the result of `<FieldType>::to_wany(v, z)` (`z` = the schema's carried arena allocator, cast to `*mut Allocator`); a generic field's type still a TypeVar emits the bare `T__to_wany` symbol for mono to retarget at instantiation. The RHS type is checked compatible with the (possibly type-substituted) field type before conversion.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7241-L7347"
      ],
      "testability": "untestable"
    },
    {
      "id": "writ.schema.field-type-must-be-writfield",
      "domain": "writ",
      "title": "Non-WAny schema field type must resolve a to_wany conversion",
      "statement": "For a concrete (non-TypeVar, non-`WAny`) schema field type with no discoverable `<Type>__to_wany` symbol, the field write is rejected: \"schema write '<recv>.<field>': field type '<T>' is not a WritField (no T→WAny conversion)\".",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7296-L7319"
      ]
    },
    {
      "id": "writ.schema.unknown-field",
      "domain": "writ",
      "title": "Writing an undeclared schema field is a compile error",
      "statement": "Assigning to `p.field` where `field` is not among the schema's registered field list is diagnosed: \"schema '<name>' has no field '<field>'\", and lowering yields an error placeholder expression rather than a write.",
      "evidence": [
        "src/compiler/sema_stmt.cpp#L7257-L7264"
      ]
    }
  ]
}
