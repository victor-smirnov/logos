{
  "unit": "sema/borrow_check/merge_moves.part2",
  "layer": "sema",
  "source": "src/compiler/borrow_check.cpp#L1176-L1703",
  "source_hash": "4b713ca9257fc707",
  "rules": [
    {
      "id": "borrow.field.whole-borrow-blocks-field",
      "domain": "borrow",
      "title": "Whole-value borrow blocks any field borrow",
      "statement": "take_field_borrow(target,path): rejected if target is already mut_borrowed (blocks ANY new field borrow, shared or mut) — 'target.path: target is already mutably borrowed'. Else, if the new borrow is mut and target.shared_borrows>0, rejected — 'target.path as mutable: target has shared borrows'.",
      "evidence": ["src/compiler/borrow_check.cpp#L1176-L1186"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.field.mutability-through-reference-root",
      "domain": "borrow",
      "title": "Field mutation legality from reference-typed root",
      "statement": "When the root of field place target.path has reference type (&T/&mut T), mutation legality is decided by the reference's TYPE, not target's `mut` binding: a `&mut`-typed root permits `&mut target.path`; a `&`-typed (shared) root REJECTS `&mut target.path` outright (rustc E0596) regardless of it->is_mut_binding.",
      "evidence": ["src/compiler/borrow_check.cpp#L1187-L1201"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.field.mut-binding-required",
      "domain": "borrow",
      "title": "&mut of a field requires a mut binding (non-reference root)",
      "statement": "`&mut target.path` where target's root is NOT a reference type is rejected ('not declared as mut') unless target is declared `mut` or is a known function parameter.",
      "evidence": ["src/compiler/borrow_check.cpp#L1202-L1209"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.field.overlapping-path-conflict",
      "domain": "borrow",
      "title": "Field borrows conflict on overlapping paths",
      "statement": "A new borrow of target.path is rejected if it overlaps (paths_overlap) an existing tracked entry: a mut request conflicting with any live shared_field_borrows entry, or ANY request conflicting with any mut_field_borrows entry. Two paths overlap iff equal or one is a dot-prefix of the other; disjoint sibling paths do not conflict. On success, the borrow is recorded (mut_field_borrows.insert / shared_field_borrows[path]++) and registered on the current scope for end-of-scope release.",
      "evidence": ["src/compiler/borrow_check.cpp#L1210-L1233"],
      "related": ["borrow.move.path-overlap"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.take.moved-cannot-borrow",
      "domain": "borrow",
      "title": "Cannot borrow a moved value",
      "statement": "take_borrow(target): taking any borrow (shared or mut) of target is rejected ('cannot borrow moved value') if target.moved.",
      "evidence": ["src/compiler/borrow_check.cpp#L1245-L1249"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.take.mut-requires-mut-binding",
      "domain": "borrow",
      "title": "&mut requires a mut binding",
      "statement": "take_borrow(target, is_mut=true): unless skip_mut_binding_check, rejected ('not declared as mut') unless target.is_mut_binding or target is a known function parameter. skip_mut_binding_check is set only by the bare-receiver elision recorder (tracks exclusivity only, leaving binding-mut legality of bare-place receivers permissive).",
      "evidence": ["src/compiler/borrow_check.cpp#L1259-L1264"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.take.field-borrow-blocks-whole-mut",
      "domain": "borrow",
      "title": "Any field borrow blocks a whole-value &mut",
      "statement": "take_borrow(target, is_mut=true): rejected ('field of target is already borrowed') if target has any tracked field-path borrow, shared or mut (mut_field_borrows or shared_field_borrows non-empty).",
      "evidence": ["src/compiler/borrow_check.cpp#L1265-L1272"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.take.mut-exclusive",
      "domain": "borrow",
      "title": "&mut is exclusive with existing mut, reservation, and shared borrows",
      "statement": "take_borrow(target, is_mut=true): rejected ('already mutably borrowed') if target.mut_borrowed is set, or if target.mut_reservations>0 (an in-flight &mut reservation from a sibling call argument is itself a conflict, matching rustc's rejection of `f(&mut x, &mut x)`); outside call-argument evaluation, also rejected if target.shared_borrows>0.",
      "evidence": ["src/compiler/borrow_check.cpp#L1273-L1284", "src/compiler/borrow_check.cpp#L1322-L1329"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.take.call-arg-mut-reservation",
      "domain": "borrow",
      "title": "Two-phase borrow reservation during call-argument evaluation",
      "statement": "Inside function-call argument evaluation (in_call_args_>0), take_borrow(target,is_mut=true) records a reservation (target.mut_reservations++) instead of setting mut_borrowed. A shared borrow of target pre-existing from an OUTER scope conflicts with the reservation ('N shared borrow(s) active'); a shared borrow taken within the SAME call's argument-evaluation frame does not conflict with it.",
      "evidence": ["src/compiler/borrow_check.cpp#L1285-L1321"],
      "uncertainty": "Approximates two-phase-borrow activation with a call-frame heuristic (reservation compatible only with borrows recorded in the current scope frame) rather than per-use-point activation; edge cases spanning nested calls are not exercised in this slice.",
      "testability": "diagnostic"
    },
    {
      "id": "borrow.take.shared-vs-mut",
      "domain": "borrow",
      "title": "Shared borrow excludes an active mut or mut-field borrow",
      "statement": "take_borrow(target, is_mut=false): rejected if target.mut_borrowed ('already mutably borrowed') or target.mut_field_borrows non-empty ('field of target is mutably borrowed'); else target.shared_borrows++ and the borrow is recorded on the current scope (multiple shared borrows coexist).",
      "evidence": ["src/compiler/borrow_check.cpp#L1329-L1345"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.move.path-overlap",
      "domain": "borrow",
      "title": "Move-path overlap semantics",
      "statement": "Two dotted paths a,b overlap iff a==b or one is a dot-prefixed ancestor of the other (e.g. \"i\" overlaps \"i.s\"); disjoint siblings (\"i.t\",\"i.s\") do not overlap. Reading/moving a moved leaf, a path under a moved subtree, or a parent containing a moved leaf are all treated as uses of (partially) moved data.",
      "evidence": ["src/compiler/borrow_check.cpp#L1350-L1360"],
      "testability": "transitive"
    },
    {
      "id": "borrow.move.reinit-clears-deeper",
      "domain": "borrow",
      "title": "Re-initialization clears equal and deeper move records",
      "statement": "Assigning to path p clears moved-tracking entries equal to p or strictly deeper (dot-prefixed by p) — e.g. writing `o.i` refills `o.i.s`. A moved entry SHALLOWER than p is unaffected: assigning `o.i.s` does not un-move a moved `o.i`.",
      "evidence": ["src/compiler/borrow_check.cpp#L1369-L1381"],
      "testability": "behavioral"
    },
    {
      "id": "borrow.move.partial-move-blocks-use",
      "domain": "borrow",
      "title": "Use of a partially- or fully-moved value is rejected",
      "statement": "consume(name): rejected ('use of partially moved value (field F moved on line L)') if name.moved_fields is non-empty. Else rejected ('use of moved value', citing the prior move line if known) if name.moved.",
      "evidence": ["src/compiler/borrow_check.cpp#L1383-L1401"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.move.borrowed-cannot-move",
      "domain": "borrow",
      "title": "Cannot move a borrowed value; binding mut-ness survives the move",
      "statement": "consume(name): rejected ('cannot move while it is borrowed') if name.mut_borrowed, name.shared_borrows>0, or name.mut_reservations>0. Rejected (rustc E0505) if any field of name has a live field borrow (field_borrow_conflicts, need_exclusive=true, empty path) — a live borrow of ANY field blocks moving the whole value, since the move would invalidate the field reference. On success, name's tracked state resets to fresh (clearing borrow/move bookkeeping) but is_mut_binding is preserved across the reset, and name is marked moved at the current line — reassigning a moved-from `let mut` binding still admits `&mut` of it afterward.",
      "evidence": ["src/compiler/borrow_check.cpp#L1402-L1424"],
      "testability": "diagnostic"
    },
    {
      "id": "borrow.use.place-base-suppresses-value-use",
      "domain": "borrow",
      "title": "Visiting a projection's receiver base is place-forming, not a value-use",
      "statement": "Visiting the receiver/base sub-expression of a place projection (x.f, x[i], recv.method(), t.N) suppresses field-borrow VALUE-use conflict checks for the duration (in_addr_source_=true): the precise place was already conflict-checked at the projection site itself. Liveness and move-state checks (check_live, moved_fields) are NOT suppressed and still run on the base chain.",
      "evidence": ["src/compiler/borrow_check.cpp#L1426-L1439"],
      "testability": "behavioral"
    },
    {
      "id": "borrow.region.dangling-after-scope-exit",
      "domain": "borrow",
      "title": "Use of reference after referent leaves scope (E0597)",
      "statement": "check_live(name): if name has a tracked dangling_ entry (its borrow-source local went out of scope while name still borrows it), rejected with E0597 ('name does not live long enough: it is borrowed by SRC, which is used here after SRC goes out of scope'). The diagnostic fires once per binding — the dangling_ entry is erased after report.",
      "evidence": ["src/compiler/borrow_check.cpp#L1441-L1449"],
      "uncertainty": "Release of dangling-tracking is line-granular (per formerly-DIVERGENCES-§B6, now closed): multiple borrow-conflicting statements on one physical line can under-report.",
      "testability": "diagnostic"
    },
    {
      "id": "borrow.use.moved-or-mut-borrowed",
      "domain": "borrow",
      "title": "Use of moved or mutably-borrowed value",
      "statement": "check_live(name): rejected ('use of moved value', citing the prior move line if known) if name.moved; rejected ('cannot use while it is mutably borrowed') if name.mut_borrowed.",
      "evidence": ["src/compiler/borrow_check.cpp#L1450-L1463"],
      "testability": "diagnostic"
    },
    {
      "id": "pat.bind.variant-and-wild-introduce-bindings",
      "domain": "pat",
      "title": "Pattern bindings introduced into scope",
      "statement": "declare_pat_bindings(pat): a VariantData pattern declares each of its sub-bindings (by name, at its bind-slot) into the current scope; a Wild pattern declares its own binding only when its name is non-empty and not \"_\".",
      "evidence": ["src/compiler/borrow_check.cpp#L1487-L1512"],
      "testability": "behavioral"
    },
    {
      "id": "region.pat.by-ref-binding-inherits-borrows",
      "domain": "region",
      "title": "By-reference match binding inherits scrutinee borrows",
      "statement": "propagate_pat_sources: for a VariantData pattern matched against a scrutinee with tracked borrow-sources srcs, each sub-binding whose static type is a reference kind or a #[borrow_carrying] type inherits srcs as its own ref-borrow-sources (recorded at the match line), so the borrow cannot be smuggled past the referent's scope via the binding. A by-value binding (neither reference nor borrow-carrying) does not inherit — it copies out and carries no borrow.",
      "evidence": ["src/compiler/borrow_check.cpp#L1514-L1536"],
      "testability": "transitive"
    },
    {
      "id": "region.elision.method-result-borrows-self",
      "domain": "region",
      "title": "Elided &self -> &T (or borrow-carrying) ties result to receiver",
      "statement": "is_self_borrowing(f) holds iff f's first parameter is a reference kind, f has NO explicit lifetime parameters, AND f's return type is a reference kind or a #[borrow_carrying] type. A method with explicit lifetime parameters may tie its result to a non-self argument instead, so it is classified NOT self-borrowing (avoids over-conservative borrowing of self).",
      "evidence": ["src/compiler/borrow_check.cpp#L1539-L1556"],
      "testability": "transitive"
    },
    {
      "id": "region.elision.operator-desugar-self-borrow",
      "domain": "region",
      "title": "Operator-desugared call self-borrowing by name agreement",
      "statement": "result_borrows_self(call): if call.resolved_symbol resolves to a function, defer to is_self_borrowing on it. Otherwise (operator-desugared/trait calls — `v[i]`->Index, `*p`->Deref — carry an empty resolved_symbol), fall back to ALL functions sharing the call's unmangled method name: classified self-borrowing only if EVERY same-named method is self-borrowing; any disagreement or no match yields NOT self-borrowing (conservative).",
      "evidence": ["src/compiler/borrow_check.cpp#L1558-L1575"],
      "testability": "transitive"
    },
    {
      "id": "borrow.recv.bare-place-self-conflict",
      "domain": "borrow",
      "title": "Bare-place method receiver self-borrow conflict",
      "statement": "method_self_kind(call) resolves the call's receiver self-kind (0=by-value,1=&self,2=&mut self) via resolved_symbol, falling back to the base method name (all same-named overloads must agree on param-0 kind, else ambiguous->0). check_recv_conflict(bp,is_mut): for a bare whole-variable receiver (bp.path empty) whose root is not a raw-pointer type, rejected if the root is already mut_borrowed; a mut call is also rejected if the root has shared_borrows>0 or any tracked field borrow. Raw-pointer roots are unchecked; reference-typed roots ARE checked, so a &mut-self call through a &mut-typed variable still conflicts with a live borrow of that variable.",
      "evidence": ["src/compiler/borrow_check.cpp#L1577-L1632"],
      "testability": "diagnostic"
    },
    {
      "id": "region.escape.borrow-carrying-type",
      "domain": "region",
      "title": "Borrow-carrying types and transitive containers are escape-tracked",
      "statement": "is_borrow_carrying_type(t): a named struct/zoned-struct/niche-enum type is borrow-carrying iff registered in ts_.borrow_carrying, UNLESS its name is registered in ts_.residency_exempt (laundered escape wrappers Held<T>/HeldAny, holding an Rc/Arc that keeps the arena alive, are NEVER borrow-carrying — even through their type-args, e.g. Held<WArray<WAny>>). A generic container whose element type-arg is itself borrow-carrying (Vec<WAny>, Option<WAny>, Box<WAny>) is transitively borrow-carrying. A raw pointer to such a type (*mut WAny) has no type-args on the pointer itself and is NOT checked.",
      "evidence": ["src/compiler/borrow_check.cpp#L1634-L1659"],
      "divergence": "Logos-only extension: #[borrow_carrying] generalizes escape/lifetime tracking to non-reference (arena-view) types; Rust has no analogous whole-value lifetime annotation.",
      "testability": "transitive"
    },
    {
      "id": "region.escape.value-local-root-walk",
      "domain": "region",
      "title": "Value-local root of a borrow place",
      "statement": "value_local_root(e): strips one optional leading AddrOfTemp, then follows a FieldRead/TupleIndex/IndexRead/Deref chain to its terminal. A Deref of a raw-pointer-typed operand stops the walk and returns empty (the pointee is not tied to the pointer's stack lifetime, e.g. box_leak's `&mut *into_raw(b)`). If the terminal is a VarRef naming a tracked VALUE local that is NOT a function parameter and NOT a tracked ref-binding (absent from prov_), its name is returned; otherwise empty. A reference rooted at such a local is treated as dangling once the local's scope ends.",
      "evidence": ["src/compiler/borrow_check.cpp#L1661-L1697"],
      "testability": "transitive"
    }
  ]
}
