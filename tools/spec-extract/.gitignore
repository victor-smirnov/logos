# Transient / deterministically-derived run files (regenerated by chunk.py).
# The durable ground truth is rules/*.json (carries stable ids + source_hash);
# manifest.json and the run plan are pure derived state and add only churn.
.run-plan.json
manifest.json
.collisions.json
.dedup-decisions.json
.test-plan.json
.testability.json
.mono-plan.json
