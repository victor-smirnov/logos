1
0
42
1000000
true
false
"hi"
"a longer string"
$p
$count
a
a.b
a.b.c.d
-x
!flag
!!x
a + b
a - b
a * b
a / b
a % b
a + b * c
(a + b) * c
a < b
a <= b && c >= d
a == b || c != d
a ? b : c
a == b ? c + 1 : d - 2
!a || b && c == d + e * f
1 + 2 * 3
a.b + 10
f()
g(x)
contains(a, b)
clamp(x, lo, hi)
outer(inner(a, b), c)
round(x.price, 2)
[x for x in items]
[x.price for x in items if x.qty > 0]
[f(x) for x in rows if x.ok && x.n < 100]

# Numeric-literal shapes. The suffixed forms are what real deem! bodies write;
# they went unnoticed for four grammars because el.peg under-declared INTEGER
# (`/[0-9]+/`) and only ONE backend read the pattern. The last two pin the
# error-returning literal policy: > i64::MAX must decode to the i64::MIN poison
# in BOTH backends, not to 0 (std::from_chars leaves its output untouched).
1000000i64
o.amt * 100i64
x.n + 1u32
0.0f64
1.5 * qty
key * 1000000i64 + n
99999999999999999999
9223372036854775807
