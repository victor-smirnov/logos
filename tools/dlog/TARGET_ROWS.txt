TARGET ROWS — written for the NEXT round, from the classified inventory.
No compiler source was edited this round (build hash 0e8ecc17b99f4077 43,
READ before and after); the deliverable was the inventory itself.

WHAT THE INVENTORY SAYS THE REMAINING REPAIR IS: 18 confirmed decisions across
all five groups, of which 13 are the owner's documented back-compat aliases.
FIVE are anybody's to price, and only one of them is a call site:

1. mlir_gen_expr.cpp:5890  emit_dst_len            R2, the single DECLINE
   `resolve_method_symbol(sname, "dst_len", dstref_pkg)` passes the package and
   DECLINES the out-parameter `pkg_owns_struct`. `mlir_gen_impl.hpp:640` and
   both mlir_gen_stmt.cpp drop sites take it since `3cad21dcc`; this one does
   not. Same mechanism, one hop from a landing — the shape that has paid.
   ⚠ It is NOT drop glue, so predict the population before pricing: it bites
   only where a package owns a struct of this name that has no `dst_len`.

2-5. mlir_gen_dyn.cpp:962, :982  (dyn_vtable_methods_)
     sema_collect.cpp:5234, :5235 (impls_, impls_all_)      R3, bare-key
   A key concatenated from a bare entity name, landing in a registry that is
   NOT a local scope map and that gets no qualified key beside it. Unlike the
   13 alias-pair sites these have no qualified sibling at all, so the bare key
   IS the only key — which makes them a different and possibly worse shape.

NOT chosen, and why the next round should not re-open them:
 · R1's 102 `4-no-qual-param` rows. The callee has no package parameter; the
   bare name is the interface. Repairing one is an interface change, not a call
   site change. That is a bigger question than the ledger is asking.
 · The 13 alias-pair sites (mono x9, mlir_gen x4). Documented, deliberate,
   owner's. ⚠ But RECORD the asymmetry: mlir_gen guards its bare alias with
   `!count(bare)` (first-wins) and mono does not (last-wins). That difference
   is a question for the owner, not a repair.

THE NEXT `dlog` INCREMENT, IN ORDER OF WHAT IT BUYS:
 a. Deepen `name_key.dl`'s `name_val`/`name_lit` from 3 to 8. MEASURED: it adds
    251 sites to R3 (165 -> 416), including the pkg-qualified halves of the very
    alias pairs the class is about. Do it in a round that re-pins the counts.
 b. The second hop for R3's `6-stored` (65 sites): a key built into a local and
    subscripted with it later. `pkg_var` already follows exactly one initialiser
    hop; this is the same hop pointed the other way.
