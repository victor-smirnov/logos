# bc_admits_blocked.ledger — ADMITTED PROGRAMS THAT NO COMPILER FIX CLOSES.
#
# THE SPLIT, AND WHY IT IS NOT A SKIP LIST. `bc_admits.ledger` is the queue: a
# row there is a borrow-check DEFECT with a root, and the intended way for that
# file to change is to SHRINK. This file holds the rows that are not defects and
# never were — a round that prices them is buying nothing, and four rounds in a
# row spent their pricing phase rediscovering that. Three kinds, and nothing
# else may be filed here:
#
#   LEGAL-AS-PORTED   the PORT is legal Rust as written, whatever upstream did.
#                     A fix that closed it would refuse a correct program. The
#                     owner's move is to re-port it (restore the turbofish, the
#                     non-Copy field, the closure, the parameterless `loop{}`)
#                     or to retire it — NOT to change the checker.
#   DIVERGENCE-*      Logos deliberately admits the shape, canonised in
#                     docs/DIVERGENCES.md or named by a `docs/spec/` clause.
#   OWNER-CORPUS      closing it would red LEGAL fixtures or the stdlib. The
#                     wall is named in the row, and the wall is what must be
#                     re-measured — not the row.
#
# ⚠ THE COVERAGE IS UNCHANGED. These programs did NOT move off the admit shelf.
# Each still has its own registered test `logos_00_bc_admit_<dir>_<name>` from
# the CMake glob, and each still asserts that the compile is SILENT. If one of
# them ever starts being refused that is still RED and still news — being a
# non-defect is not permission to change behaviour quietly.
#
# ⚠ THIS FILE IS RE-VERIFIED, NOT CARRIED. A DECLINE IS A MEASUREMENT WITH A
# TIMESTAMP AND A WALL DECAYS. This file exists because a "five owner fixtures
# wall this block" verdict was carried for six rounds after four of the five had
# been REPAIRED, and re-pricing came back 5 -> 1. So every row carries the DATE
# its reason was last checked against the tree, and a row whose reason cannot be
# re-checked TODAY goes back to `bc_admits.ledger`, where the queue can see it.
# Re-verifying a row means opening the PORT and the WALL, not reading this file.
#
# Format:  <fixture> <root-id> <path relative to the repo root, no .logos>
#          # <REASON-TAG> <date verified> — <the evidence, in one line>
# The row itself is the same three columns `bc_admits.ledger` uses and is parsed
# by the same loop in `bc_admits_ledger_gate.sh`; the reason is a comment, so
# there is one row format in this tree and not two.
#
# Held by `logos_00_bc_admits_ledger` exactly as the queue is: every row has a
# file, every file under tests/imported/admit/ has a row in ONE of the two
# files, and EACH file's own `# TOTAL` is held against its own row count.
#
# ── 2026-09-04, the split: 124 -> 99 queue rows + 25 here ───────────────────
# EVERY ROW BELOW WAS RE-VERIFIED ON 2026-09-04 BY OPENING THE PORT, AND EVERY
# "walled by X" CLAIM BY OPENING X. What the re-verification CONTRADICTED, so
# that the next reader does not re-inherit it:
#   · E0713 x3 (enum-drop-access, issue-52059-…, issue-53773) are recorded
#     "retired, spec, by analogy with E0509". THE ANALOGY IS REFUTED AND THEY
#     ARE NOT HERE. `intrinsic.drop.skip-moved-out-paths` says drop SUPPRESSES a
#     moved-out sub-value; it says nothing about a `&mut` handed out PAST a
#     destructor that then writes through it, which is what these three do
#     (`drop_enum(d)` returns `opt.r` and `DropOpt::drop` stores 9 through it).
#     They stay in the queue as the aliasing holes they are.
#   · `do-not-ignore-lifetime-bounds-in-copy-proj--b` is recorded as a third A16
#     row. It is NOT on the admit shelf at all any more — it is a fail fixture,
#     tests/imported/fail/nll/, with a pinned diagnostic. The A16 group is TWO.
#   · the six `lifereg.R18` M-SIG rows are recorded "rejected twice" and are NOT
#     here. That decline decayed: every PROBES census through 09-07 prints
#     "lifereg.R18 8" and the root is SIX rows today, and M-SIG §6.1 (`Self`
#     substituted at the conformance site) LANDED in round 2026-09-04y after the
#     rejection. A partially-landed compiler direction is a queue row.

# ── 2026-09-07, STAGE 3 OF THE RE-PORT: 25 -> 12 ────────────────────────────
# EVERY ONE OF THE 25 ROWS WAS RE-PORTED AS-IS FROM ITS UPSTREAM TEST at the
# commit its port cites (scripts/report-worksheet.py, rustc checkout at
# 4b0c9d76a). THIRTEEN ROWS LEFT THIS FILE, and the split of the thirteen is the
# finding: EIGHT of them were not blocked at all — the PORT had moved the
# construct to where the rule flips, and restoring the construct makes them
# ordinary admitted DEFECTS, now rows in bc_admits.ledger. FIVE re-ported to a
# REFUSAL at the same construct and are fail fixtures with the sentence pinned.
# ⚠ NO COMPILER SOURCE WAS TOUCHED IN THAT ROUND. `git diff --stat` over src/
# and include/ is empty; every verdict below is the unmodified compiler's.
# What each surviving row now carries: the BUCKET it landed in, and the file
# itself carries the full audit trail (`// Original path:`, the upstream
# construct with its line numbers, and `// Modifications:`).
#   BUCKET 3, blessed divergence (7) — the construct cannot exist here, and the
#     clause is cited BY ID in the port. Five E0509 rows against
#     `intrinsic.drop.skip-moved-out-paths` (docs/spec/expressions.md), and the
#     clause was RE-VERIFIED BY RUNNING IT: a two-Drop struct with `let g = s.f`
#     prints INNER_DROPS=1 S_DROPS=1 — no leak, no double free. Two A16 rows
#     against docs/DIVERGENCES.md A16 (structural auto-Copy, canonised 2026-08-24).
#   BUCKET 4, implementation hole (3) — the re-port needs something the language
#     does not have. Both `multiple-sources-*` rows need an explicit LIFETIME
#     argument in a turbofish (`f::<'_>(..)` -> "unexpected type node code 131",
#     while `call::<i64>()` compiles); `issue-64173-unused-lifetimes` needs a
#     const-expression array length. Both are entries in unrowed_backlog.ledger.
#   OWNER-CORPUS (2) — the two `regions-*-anon` ports are FAITHFUL as they stand
#     (an elided `&` in a declaration, verbatim) and their wall was re-measured
#     by direct listing: 195 bare-`&` declaration sites in 124 files (was
#     recorded as 134 in 101 — it GREW), plus the explicit pass pin
#     `bc_enumpldlt_placeholder_payload_lifetime`. ⚠ NOT a blessed divergence:
#     no clause canonises it, searched 2026-09-07.
# ⚠ THE BUCKET-3 DROP EVIDENCE WAS TAKEN AT ONE DOOR, AND FIVE PORTS WERE BLESSED
#   ON IT. Re-measured later the same day with a per-type destructor-counter sweep
#   over SEVEN doors (build f812a8b998ecde2b): the clause
#   `intrinsic.drop.skip-moved-out-paths` is implemented at the DOTTED-PATH door
#   (`let g = s.f;` -> 111, the shape the original verification ran), at the
#   TUPLE-STRUCT-PATTERN door (`let S(x, _) = s;` -> 111) and at the FRU door
#   (`T { f: .., ..s0 }` -> 212, two T values) — and it is NOT implemented at the
#   STRUCT-PATTERN door, in a `let` or in a `match`: the user `Drop::drop` never
#   runs at all, and any field the pattern discards leaks (110 / 100 where the
#   clause requires 111). The discriminator is measured both ways — the identical
#   programs with NO `impl Drop` on the owner print 110, i.e. the discarded field
#   DOES drop. Three of the five ports (-with-dtor, --b, --t17) use doors that are
#   correct and their rows are untouched; the two `--r13`/`--t13` rows carry the
#   correction. ⚠ NO ROW MOVED BETWEEN FILES FOR THIS: what bucket 3 blesses is the
#   ADMISSION (no E0509), which is unchanged and still correct. The RUN-TIME
#   misbehaviour is a different defect and is now counted where the queue can see
#   it — soundness_queue.ledger row `letstruct_destructure_skips_user_drop` (tier 1,
#   run 100), same class as the already-open `enum_payload_partial_move_leak`,
#   which is the same failure at the enum-variant door.
# ⚠ WHERE THE E0392 WALL STANDS, RE-MEASURED 2026-09-07 rather than carried:
#   6559 pass fixtures scanned for a struct/enum declaring a lifetime parameter
#   its body never mentions — FIVE, in five files. The recorded "five pass
#   fixtures" has NOT decayed.

# 2026-09-07 (stage 4) — 12 -> 13. ONE row ARRIVED from bc_admits.ledger:
# `issue-75904-move-closure-loop`, BUCKET 3 against docs/DIVERGENCES.md A16. Re-ported
# as-is it keeps upstream's `struct NotCopy;`, which is field-less and carries no
# `impl Drop` — implicitly Copy under A16 — so `let m = a;` is a COPY and the
# program is LEGAL Logos. Measured one-variable pair, only the `impl Drop` differs:
# with it the SAME loop program REFUSES ("use of moved value 'a'"). The
# closure-in-a-loop move IS checked; only this port's TYPE is Copy, and swapping
# the type would change the subject. A ROW MOVED, NOT A ROW CLOSED.


# ── 2026-09-08 — THE `DIVERGENCE-DROP` BUCKET IS GONE, ALL SIX ROWS MOVED OUT ──
#
# The owner decided on 2026-09-08 that Logos's drop semantics are Rust-canonical:
# `Drop::drop` takes `&mut self`, and moving a sub-value out of a value that
# implements `Drop` is E0509 and REFUSED. That retires the spec clause
# `intrinsic.drop.skip-moved-out-paths` in the form that blessed these six rows
# (the clause survives, scoped to NON-`Drop` owners, which is Rust's own drop-flag
# behaviour and not a divergence — see docs/spec/expressions.md and the new
# `intrinsic.drop.move-out-of-drop-type-refused`).
#
# So the divergence is not a divergence any more and the six rows are DEFECTS
# again. Every one of them moved BECAUSE OF THE CLAUSE — none had a second
# reason to be filed here — and all six are now in bc_admits.ledger with a root.
# Three of them carry an ADDITIONAL blocker which the clause's retirement does
# not remove (a `match`-pattern door, and A16 structural auto-Copy); that is
# recorded on the rows themselves, in the actionable file, where it belongs.
#
# 15 -> 9. THE ACTIONABLE LEDGER GROWING BY SIX IS THE CORRECT RESULT.

copy-modulo-regions lifereg.NEW-L5 tests/imported/admit/lifetimes/copy-modulo-regions  # BUCKET-3 DIVERGENCE-A16 2026-09-07 — docs/DIVERGENCES.md A16 structural auto-Copy: Foo{f:&i64} is Copy at EVERY region
do-not-ignore-lifetime-bounds-in-copy nllmoves.NEW-S7-1 tests/imported/admit/nll/do-not-ignore-lifetime-bounds-in-copy  # BUCKET-3 DIVERGENCE-A16 2026-09-07 — same clause; impl Copy for Foo<'static> restricts nothing here
issue-64173-unused-lifetimes lifereg.L4 tests/imported/admit/lifetimes/issue-64173-unused-lifetimes  # BUCKET-4 HOLE 2026-09-07 — needs a const-expression array length ([(); size_of::<&Self>()]); backlog const_expr_array_len_absent. E0392 wall re-measured: still 5 fixtures
multiple-sources-for-outlives-requirement--c24b lifereg.N1 tests/imported/admit/regions/multiple-sources-for-outlives-requirement--c24b  # BUCKET-4 HOLE 2026-09-07 — needs an explicit LIFETIME turbofish arg; backlog lt_turbofish_absent (an invariance probe IS admitted — a row waits behind it)
multiple-sources-for-outlives-requirement--t24 lifereg.N1 tests/imported/admit/regions/multiple-sources-for-outlives-requirement--t24  # BUCKET-4 HOLE 2026-09-07 — same missing feature, same backlog entry
regions-in-enums-anon lifereg.R17 tests/imported/admit/regions/regions-in-enums-anon  # OWNER-CORPUS 2026-09-07 — port is FAITHFUL; wall re-measured 195 bare-& sites in 124 files (was 134/101), pinned by pass/bc_enumpldlt_placeholder_payload_lifetime E1
regions-in-structs-anon lifereg.NEW-L1 tests/imported/admit/regions/regions-in-structs-anon  # OWNER-CORPUS 2026-09-07 — port is FAITHFUL; same re-measured wall, 174 of the 195 sites are struct fields
issue-75904-move-closure-loop nllmoves.C tests/imported/admit/moves/issue-75904-move-closure-loop  # BUCKET-3 DIVERGENCE-A16 2026-09-07 (stage 4) — docs/DIVERGENCES.md A16 structural auto-Copy: upstream's `struct NotCopy;` is field-less with no `impl Drop`, so it IS Copy here and `let m = a;` is a COPY. LEGAL Logos as written. ONE-VARIABLE PAIR: adding `impl Drop` to the SAME loop program REFUSES ("use of moved value 'a'") — the closure-in-a-loop move IS checked, only this port's TYPE is Copy

# 2026-09-07 (stage 5) — 13 -> 15. TWO rows ARRIVED from bc_admits.ledger, neither
# by a compiler change, both after being RE-PORTED AS-IS first:
# `borrowck-move-error-with-note--b`, BUCKET 3 against docs/spec/expressions.md
#   `intrinsic.drop.skip-moved-out-paths` — upstream's `move_in_match` moves both
#   fields out of a `Drop` type (E0509) and this clause blesses exactly that. Its
#   four `*-with-dtor*` siblings are already here under the same clause; the row had
#   carried `bck.B` ("does not follow a projection") on a program with no projection
#   in it, RE-ROOTED `bck.E`. RUN EVIDENCE TAKEN AT THIS PORT'S OWN DOOR, not
#   inherited: with a `putchar` body in `Drop::drop` and markers either side of the
#   call the program prints `AB` — the destructor NEVER RAN, which is not what the
#   clause's own text requires. That half is already rowed: soundness_queue
#   `letstruct_destructure_skips_user_drop` enumerates this exact match door.
# `enum-drop-access`, BUCKET 4 — needs NESTED PATTERNS INSIDE ENUM-VARIANT PAYLOADS.
#   Upstream is `DropOption::Some(&mut ref mut r)` and the nested pattern IS the
#   test (the two OK cases in the same upstream file differ from it only in whether
#   the dropped value can reach the reference). MEASURED 2026-09-07 on the upstream
#   shape, generic enum and `Drop` impl and all: "pattern DropOption::Some: nested
#   patterns inside enum-variant payloads are not yet supported; bind to a name and
#   match in the body". The generic enum with a `Drop` impl compiles on its own, so
#   it is the nested pattern alone that has no path; following the compiler's own
#   suggestion substitutes the construct. Backlog `enum_payload_nested_pattern_absent`.
#   ⚠ THE FILE BELOW IS STILL THE OLD PORT, kept so the shelf entry has a program,
#   and its header says so: it is `finish_1` from `issue-52059` under another name.
#   A ROW MOVED, NOT A ROW CLOSED — both times.

enum-drop-access nllmoves.E tests/imported/admit/nll/enum-drop-access  # BUCKET-4 HOLE 2026-09-07 (stage 5) — needs nested patterns inside enum-variant payloads (`Some(&mut ref mut r)`); backlog enum_payload_nested_pattern_absent. The file still holds the OLD port, which duplicates issue-52059's finish_1
# TOTAL 9
