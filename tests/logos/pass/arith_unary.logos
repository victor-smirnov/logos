package arith_unary;
// F02: unary minus, negation in expressions
fn negate(x: i32) -> i32 { return -x; }
fn main() -> i32 {
    let a: i32 = negate(-42);   // 42
    let b: i32 = -(-10);        // 10
    let c: i32 = 0 - 10;        // -10
    return a + b + c;            // 42 + 10 - 10 = 42
}
