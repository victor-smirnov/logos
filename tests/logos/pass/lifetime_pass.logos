package lifetime_pass;
// Correct: returning param with matching lifetime
fn identity<'a>(x: &'a i32) -> &'a i32 {
    return x;
}
unsafe fn main() -> i32 {
    let val: i32 = 42;
    let r = identity(&val);
    return 42;
}
