// Index bounds (Rust panics; Logos traps per A13/A7): a negative index.
package index_negative_array_traps;
struct S { a: [i64; 3] }
fn at(x: i64) -> i64 { return x; }
fn main() -> i32 {
    let a: [i64; 3] = [1, 2, 3]; return a[at(-1)] as i32;
}
