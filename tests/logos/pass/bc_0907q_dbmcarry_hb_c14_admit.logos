// hand battery: round 2026-09-07q-dbmcarry, program c14 — caught: verdict moved base -> landed — PROBES.md "HAND MATRIX, base -> armed ... 2 -> 1 (nine): c01 ... c19" (landed)
// legality: by reading, no rustc binary
// harvested 2026-09-14h from src/compiler/probes/2026-09-07q-dbmcarry/hand/c14.logos
package bc_0907q_dbmcarry_hb_c14_admit;
use logos.std.fmt;
struct S { pub n: i64 }
impl Drop for S { fn drop(self: &mut S) { println!("D{}", self.n); } }
fn main() -> i32 {
    let p: (Option<S>, i64) = (Option::Some(S { n: 5i64 }), 9i64);
    let mut out: i64 = 0i64;
    if let (Option::Some(a), b) = &p { out = a.n + *b; }
    if out != 14i64 { return 1i32; }
    return 0i32;
}
