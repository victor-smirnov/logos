// Variadic generics: head/tail recursive sum
package variadic_recursive;

fn sum_all() -> i32 { return 0; }

fn sum_all<H, T...>(first: H, rest: T...) -> i32 {
    return (first as i32) + sum_all(rest...);
}

unsafe fn main() -> i32 {
    return sum_all::<i32, i32, i32>(10, 12, 20);
}
