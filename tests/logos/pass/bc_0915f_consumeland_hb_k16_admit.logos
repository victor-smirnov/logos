// hand battery: round 2026-09-15f-consumeland, program k16 — caught: its destructor count moved to Rust's answer between base 7eb6735b16576c45 and the landing (a by-value operator / *Assign operand now marked moved)
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15f-consumeland/rust/k16_place_addassign_loop.rs: compiles, runs, stdout `r=5 n=111`
package bc_0915f_consumeland_hb_k16_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
struct W { x: D, y: D, z: i64 }
impl Add for D {
    type Output = D;
    fn add(self: D, o: D) -> D {
        return D { v: 100i64, c: self.c };
    }
}
impl Neg for D {
    type Output = D;
    fn neg(self: D) -> D {
        return D { v: 1000i64, c: self.c };
    }
}
impl AddAssign for D {
    fn add_assign(self: &mut D, o: D) {
        self.v = self.v + 0i64;
    }
}
fn eat(d: D) -> i64 { return d.v; }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
fn g(p: *mut i64) -> i64 {
    let mut w: W = W { x: D { v: 1i64, c: p }, y: D { v: 10i64, c: p }, z: 5i64 };
    let mut i: i64 = 0i64;
    while i < 2i64 {
        let d: D = D { v: 50i64, c: p };
        w.x += d;
        i = i + 1i64;
    }
    return w.z;
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let r: i64 = g(p);
    let got: i64 = rd(p);
    unsafe { printf("r=%ld n=%ld\n".as_ptr(), r, got); }
    if got != 111i64 { return 1i32; }
    return 0i32;
}
