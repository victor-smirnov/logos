// hand battery: round 2026-09-15d-argrefland, program q29 — caught: verdict moved base b96b980fb (cc=0 run=29 vg=0) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_q29_admit;
struct D { v: i64, n: *mut i64 }
impl Drop for D {
    fn drop(self: &mut Self) { unsafe { *self.n = *self.n + 1i64; } }
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let pn: *mut i64 = &mut n as *mut i64;
    {
        let d: D = D { v: 6i64, n: pn };
        let rd: &D = &d;
        let mut s: i64 = 0i64;
        match rd { q => { s = s + q.v; } }
        let rrd: &&D = &rd;
        match rrd { &q => { s = s + q.v; } }
        if s != 12i64 { return 29i32; }
    }
    return (n as i32) - 1i32;
}
