package d6_at_with_or;
fn main() -> i32 {
    let x: i32 = 5i32;
    let r: i32 = match x {
        v @ (1i32 | 5i32) => v * 2i32,
        _ => 0i32,
    };
    if r != 10i32 { return 1i32; }
    return 0i32;
}
