// hand battery: round 0915_shadowland, program c26 — caught: verdict moved base -> shadowslot landing
// legality: by reading, no rustc binary
package bc_0915_shadowland_hb_c26_admit;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
fn eatd(x: D) -> i64 { return x.v; }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
struct H { a: D, b: D }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        let h: H = H { a: D { v: 1i64, c: p }, b: D { v: 2i64, c: p } };
        let k: i64 = eatd(h.a);
        let h: D = D { v: 3i64, c: p };
        if h.v + k != 4i64 { return 9i32; }
    }
    if rd(p) != 132i64 { return 1i32; }
    return 0i32;
}
