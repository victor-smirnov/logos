// hand battery: round 2026-09-08-fatrepr-fix, program G8 — caught: verdict moved base -> landed — RESULT.md "CLOSED (REFUSED -> rc 0, compiled + linked + RUN, answer checked): G1 ... G8"
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: DIVERGENCE (REFUSED, error[E0106]); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0908_fatrepr_fix_hb_g8_admit.rs
// ⚠ THE AUDIT RECORDS THIS FIXTURE AS RESTING ON A BLESSED DIVERGENCE, NOT AS WRONG — rustc refuses the literal twin and the canonising row/clause is named in docs/rustc-fixture-audit-2026-09.md §bc_0908_fatrepr_fix_hb_g8_admit. The assertion is UNCHANGED: a corpus decision with an owner.
// harvested 2026-09-14h from src/compiler/probes/2026-09-08-fatrepr-fix/hand/G8.logos
// G8 LEGAL — a NON-ZERO argument index, with a thin arg BEFORE and AFTER the
// fat one, so a fix that only looks at arg 0 shows here.
package bc_0908_fatrepr_fix_hb_g8_admit;
enum E<'a> { N, S(i64, &'a [i64], i64) }
fn main() -> i32 {
    let a: [i64; 3] = [1i64, 2i64, 3i64];
    let e: E = E::S(11i64, &a, 22i64);
    match e {
        E::N => { return 1i32; }
        E::S(x, s, y) => { if x != 11i64 { return 10i32; } if s[2] != 3i64 { return 11i32; } if y != 22i64 { return 12i32; } }
    }
    return 0i32;
}
