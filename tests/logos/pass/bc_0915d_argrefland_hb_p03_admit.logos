// hand battery: round 2026-09-15c-argref (re-run on this landing), program p03 — caught: verdict moved base b96b980fb (cc=0 run=139 vg=1) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915d_argrefland_hb_p03_admit.rs
package bc_0915d_argrefland_hb_p03_admit;
struct P { x: i32 }
fn deref2b(p: &&P) -> i32 {
    let x: &P = *p;
    return x.x;
}
fn main() -> i32 {
    let p = P { x: 5i32 };
    let r = &p;
    if deref2b(&r) != 5i32 { return 3i32; }
    return 0i32;
}
