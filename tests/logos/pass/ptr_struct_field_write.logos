package ptr_struct_field_write;
// Regression: field write through *mut T pointer
struct Cell {
    val: i32,
}
unsafe fn main() -> i32 {
    let mut c: Cell = Cell { val: 0 };
    let p: *mut Cell = &mut c;
    p.val = 42;
    return c.val;  // 42
}
