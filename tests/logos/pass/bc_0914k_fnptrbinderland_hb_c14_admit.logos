// hand battery: round 2026-09-14k-fnptrbinderland, program c14 — caught: legal, refused on base (for<'a,'b> from first<'x,'y>), admitted by the landing
// legality: by reading, no rustc binary
package bc_0914k_fnptrbinderland_hb_c14_admit;
fn first<'x, 'y>(a: &'x i64, b: &'y i64) -> &'x i64 { return a; }
fn main() -> i32 {
    let f: for<'a, 'b> fn(&'a i64, &'b i64) -> &'a i64 = first;
    let x: i64 = 14i64;
    let y: i64 = 1i64;
    return *f(&x, &y) as i32;
}
