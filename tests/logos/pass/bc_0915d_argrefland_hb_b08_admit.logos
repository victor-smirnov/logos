// hand battery: round 2026-09-15c-argref (re-run on this landing), program b08 — caught: verdict moved base b96b980fb (cc=0 run=139 vg=1) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_b08_admit;
struct D { v: i64 }
fn inner(a: &&D) -> i64 {
    let x: &D = *a;
    return x.v;
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    let f = |rd: &D| -> i64 { return inner(&rd); };
    if f(&d) != 6i64 { return 8i32; }
    return 0i32;
}
