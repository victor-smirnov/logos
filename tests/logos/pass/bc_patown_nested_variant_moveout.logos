// `if let Some(Some(s)) = oo { eat(s) }`: the nested payload is dropped once (was 2, then 3 with arm drops: the nested-variant synth is marked moved) PROBES.md 2026-09-06d: `if let` / `while let` / let-chains lower BY DELEGATION to lower_match (temp hoist, per-arm scrutinee move, arm drops, cond-move merge); the count is a destructor tally through a raw pointer.
package bc_patown_nested_variant_moveout;
struct S { v: i64, i: *mut i64 }
impl Drop for S { fn drop(self: &mut S) { unsafe { *self.i = *self.i + self.v; } } }
fn eat(s: S) -> i64 { return s.v; }
fn mk(p: *mut i64, v: i64) -> Option<S> { return Option::Some(S { v: v, i: p }); }
fn main() -> i32 {
    let mut c: i64 = 0i64;
    let p: *mut i64 = &mut c;
    {
        let oo: Option<Option<S>> = Option::Some(mk(p, 1i64));
        if let Option::Some(Option::Some(s)) = oo { let _k: i64 = eat(s); }
    }
    return c as i32;
}
