// `s.swap(i, j)` on a `&mut [T]` (std's slice swap, bounds-checked).
package slice_swap_method;
fn main() -> i32 {
    let mut a: [i64; 5] = [1, 2, 3, 4, 5];
    let s: &mut [i64] = &mut a;
    s.swap(1, 4);
    return (a[1] - 5) as i32;
}
