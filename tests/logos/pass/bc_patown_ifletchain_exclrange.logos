// an exclusive range pattern in a let-chain lowers (was: 'do not parse as an expression') PROBES.md 2026-09-06d: `if let` / `while let` / let-chains lower BY DELEGATION to lower_match (temp hoist, per-arm scrutinee move, arm drops, cond-move merge); the count is a destructor tally through a raw pointer.
package bc_patown_ifletchain_exclrange;
fn main() -> i32 {
    let x: i64 = 5i64;
    let mut hit: i64 = 0i64;
    if let 0i64..5i64 = x && x > 0i64 {
        hit = 1i64;
    }
    if hit != 0i64 { return 1i32; }
    return 0i32;
}
