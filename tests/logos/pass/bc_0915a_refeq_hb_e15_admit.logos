// hand battery: round 0915a_refeq, program e15 — caught: verdict moved base (wrong answer at run time, reference pair compared by address) -> refeq landing (Rust's answer)
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915a_refeq_hb_e15_admit.rs
package bc_0915a_refeq_hb_e15_admit;
use logos.lang.cmp;
struct S { v: i64, c: *mut i64 }
impl Drop for S {
    fn drop(self: &mut S) {
        unsafe { *self.c = *self.c + 1i64; }
    }
}
impl Eq for S {
    fn eq(&self, other: &S) -> bool {
        return self.v == other.v;
    }
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let mut hit: i64 = 0i64;
    {
        let a: S = S { v: 3i64, c: p };
        let b: S = S { v: 3i64, c: p };
        let ra: &S = &a;
        let rb: &S = &b;
        if ra == rb { hit = 1i64; }
        if ra != rb { hit = 5i64; }
    }
    let got: i64 = unsafe { n };
    if got != 2i64 { return 10i32 + got as i32; }
    if hit != 1i64 { return 1i32; }
    return 0i32;
}
