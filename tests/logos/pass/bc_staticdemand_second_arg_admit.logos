// Both arguments 'static; it runs.
// TWIN of fail/bc_staticdemand_second_arg_refuse, one token apart. src/compiler/PROBES.md 2026-09-13d-staticdemand.
package bc_staticdemand_second_arg_admit;
static V: i64 = 2i64;
static N: i64 = 3i64;
fn both<'a>(x: &'a i64, y: &'a i64) -> i64 where 'a: 'static { return *x + *y; }
fn g(u: &'static i64) -> i64 {
    return both(&V, u);
}
fn main() -> i32 { return g(&N) as i32; }
