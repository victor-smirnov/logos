// hand battery: round 2026-09-08-fatrepr, program B4 — caught: refuted an arm — FINDINGS.md:129 "B4 — ... runs rc 0 at base and SEGFAULTS (rc 139) under the arm [fatbind]"
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: DIVERGENCE (REFUSED, error[E0106]); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0908_fatrepr_hb_b4_admit.rs
// ⚠ THE AUDIT RECORDS THIS FIXTURE AS RESTING ON A BLESSED DIVERGENCE, NOT AS WRONG — rustc refuses the literal twin and the canonising row/clause is named in docs/rustc-fixture-audit-2026-09.md §bc_0908_fatrepr_hb_b4_admit. The assertion is UNCHANGED: a corpus decision with an owner.
// harvested 2026-09-14h from src/compiler/probes/2026-09-08-fatrepr/hand/B4.logos
// B4 (fatbind) — a `&dyn Tr` FIELD with a METHOD called through the by-value
// binder. Legal and correct today (a FatDyn field at the same door); the arm
// touches the same branch, so this is its blast-radius control.
package bc_0908_fatrepr_hb_b4_admit;
trait Tr { fn v(&self) -> i64; }
struct A { x: i64 }
impl Tr for A { fn v(&self) -> i64 { return self.x; } }
struct H<'a> { d: &'a dyn Tr, n: i64 }
fn main() -> i32 {
    let a: A = A { x: 17i64 };
    let h: H = H { d: &a, n: 7i64 };
    match h {
        H { d, n } => {
            if d.v() != 17i64 { return 10i32; }
            if n != 7i64 { return 11i32; }
        }
    }
    return 0i32;
}
