// hand battery: round 0915b_refeqland, program f13 — caught: verdict moved base (wrong answer at run time) -> refeq landing (Rust's answer)
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915b_refeqland_hb_f13_admit.rs
package bc_0915b_refeqland_hb_f13_admit;
use logos.lang.cmp;
struct D { v: i64 }
impl Eq for D {
    fn eq(&self, other: &D) -> bool {
        return self.v == other.v;
    }
}
fn main() -> i32 {
    let mut v: Vec<D> = vec_new::<D>();
    v.push(D { v: 2i64 });
    v.push(D { v: 3i64 });
    v.push(D { v: 2i64 });
    let t: D = D { v: 2i64 };
    let mut n: i64 = 0i64;
    for x in v.iter() {
        if x == &t {
            n = n + 1i64;
        }
    }
    if n != 2i64 { return 1i32 + n as i32; }
    return 0i32;
}
