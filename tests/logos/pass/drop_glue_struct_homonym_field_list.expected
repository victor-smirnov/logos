# ONE `INNER DROP` LINE — the correct count, RE-AIMED 2026-08-22 when #110
# landed. Until then this file asserted TWO and said so in its own header: one
# logical value, two stack slots, both dropped — a double free with a
# heap-owning field. #110's root was `compute_auto_copy_types`' enum arm in
# sema.cpp asking `struct_name()` (empty on an Enum TypeRef) against the BARE
# `enums_` key (only ever written qualified): both misses landed on a generous
# `return true`, so EVERY enum-typed field read as Copy, `OptionIter<T> { state:
# Option<T> }` was auto-promoted into `copy_types_`, no move was ever tracked
# against it — while `has_droppable_fields` still put drop glue on every slot.
#
# WHAT THIS FIXTURE PINS IS STILL #103/#98, NOT #110. It is the only shape that
# REACHES the drop-glue field-list consult that round fixed (mlir_gen_stmt.cpp
# `find_struct_def_it(ty)` qualified-first): a plain-scope program never
# executes that site, so a plain-scope pin would sit over a branch that never
# ran. The discriminating count is therefore 0-vs-1 today, where it used to be
# 0-vs-2. RE-PROVEN BY CONTROL REVERT 2026-08-22 on the post-#110 tree: with the
# bare lookup restored bare-first at mlir_gen_stmt.cpp, this fixture printed
# ZERO `INNER DROP` lines while its renamed `_control` twin printed ONE.
#
# ⚠ BOTH DIRECTIONS. The line must be present exactly once: zero is the #103
# regression (drop glue silently skipped), two is the #110 regression (one value
# freed twice). Do not "fix" this file by relaxing the count in either.
exit: 0
stdout:
INNER DROP n=7
count=1
