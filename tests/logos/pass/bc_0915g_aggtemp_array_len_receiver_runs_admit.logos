// soundness queue row array_len_builtin_discards_receiver_run — CLOSED by round 2026-09-15g-aggtemp, commit of this fixture.
// asserts: `mkarr(p).len()` CALLS its receiver and drops the array: n=111 (base n=0, exit 1 — the call never ran)
// legality: rustc 1.98.1 --edition 2024, twin cited in the round's rust/ directory; measured, not read.
package bc_0915g_aggtemp_array_len_receiver_runs_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
fn mkarr(p: *mut i64) -> [D; 2] {
    unsafe { *p = *p + 100i64; }
    return [D { v: 1i64, c: p }, D { v: 10i64, c: p }];
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        let k: i64 = mkarr(p).len();
        if k != 2i64 { return 3i32; }
    }
    let got: i64 = unsafe { n };
    unsafe { printf("n=%ld\n".as_ptr(), got); }
    if got != 111i64 { return 1i32; }
    return 0i32;
}
// rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/rust/rows/array_len_builtin_discards_receiver_run.rs: rustc=0 run=0 out=n=111
