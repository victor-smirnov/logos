package test;
trait Animal { fn legs(&self) -> i32; }
trait Dog: Animal { fn bark(&self) -> i32; }
struct Lab { n: i32 }
impl Animal for Lab { fn legs(&self) -> i32 { return 4; } }
impl Dog for Lab { fn bark(&self) -> i32 { return self.n; } }
fn via(d: &dyn Dog) -> i32 { return d.legs() + d.bark(); }
fn main() -> i32 {
    let l: Lab = Lab { n: 3 };
    return via(&l);
}
