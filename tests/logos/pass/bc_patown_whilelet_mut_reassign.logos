// `while let Some(mut s) = v.pop() { s = S{..} }` drops the popped AND the assigned value (was: the new one leaked; queue row whilelet_mut_reassign_leak). ONE TOKEN from fail/bc_patown_whilelet_reassign_no_mut_fail. PROBES.md 2026-09-06d: `if let` / `while let` / let-chains lower BY DELEGATION to lower_match (temp hoist, per-arm scrutinee move, arm drops, cond-move merge); the count is a destructor tally through a raw pointer.
package bc_patown_whilelet_mut_reassign;
use logos.mem.collections.vec;
struct S { v: i64, i: *mut i64 }
impl Drop for S { fn drop(self: &mut S) { unsafe { *self.i = *self.i + self.v; } } }
fn main() -> i32 {
    let mut c: i64 = 0i64;
    let p: *mut i64 = &mut c;
    {
        let mut vs: Vec<S> = Vec::new();
        vs.push(S { v: 1i64, i: p });
        while let Option::Some(mut s) = vs.pop() {
            s = S { v: 2i64, i: p };
            s.v = s.v + 5i64;
        }
    }
    if c != 8i64 { return 1i32; }
    return 0i32;
}
