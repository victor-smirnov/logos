// hand battery: round 2026-09-15d-argrefland, program q11 — caught: verdict moved base b96b980fb (cc=0 run=11 vg=0) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_q11_admit;
struct P { x: i32 }
fn main() -> i32 {
    let p = P { x: 5i32 };
    let r: &P = &p;
    let rr: &&P = &r;
    let ppp: &&&P = &rr;
    match ppp { &&&P { x } => { if x != 5i32 { return 11i32; } } }
    return 0i32;
}
