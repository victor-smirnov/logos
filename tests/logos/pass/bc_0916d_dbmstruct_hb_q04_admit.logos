// hand battery: round 2026-09-16d-landrulings, program q04 — caught: an UNUSED binder still double-dropped — the arm body never mentions it (base 'D4 D4')
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: ACCEPTED, exit 0, stdout 'D4' — twin src/compiler/probes/2026-09-16d-landrulings/rust/q04.rs
package bc_0916d_dbmstruct_hb_q04_admit;
use logos.std.fmt;
struct S { pub n: i64 }
impl Drop for S { fn drop(self: &mut S) { println!("D{}", self.n); } }
enum E { V { f: S }, Z }
fn run() -> i64 {
    let p: E = E::V { f: S { n: 4i64 } };
    let mut out: i64 = 0i64;
    match &p {
        E::V { f } => { out = 4i64; },
        E::Z => {}
    }
    return out;
}
fn main() -> i32 {
    if run() != 4i64 { return 1i32; }
    return 0i32;
}
