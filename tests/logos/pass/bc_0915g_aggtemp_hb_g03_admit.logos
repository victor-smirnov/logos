// hand battery: round 2026-09-15g-aggtemp (landing), program g03 — caught: an array-literal temporary in a RETURN expression was never dropped (n 0 -> 11)
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/battery-land/rust/g03.rs: compiles, runs, stdout `r=1 n=11`
package bc_0915g_aggtemp_hb_g03_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
fn g(p: *mut i64) -> i64 {
    return [D { v: 1i64, c: p }, D { v: 10i64, c: p }][0].v;
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let r: i64 = g(p);
    let got: i64 = unsafe { n };
    unsafe { printf("r=%ld n=%ld\n".as_ptr(), r, got); }
    if r != 1i64 { return 3i32; }
    if got != 11i64 { return 1i32; }
    return 0i32;
}
