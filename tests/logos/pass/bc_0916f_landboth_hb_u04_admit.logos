// hand battery: round 2026-09-16f-landboth, program u04 — caught: cost-direction control: the loan is of a DIFFERENT vec, read by the index; the arm must NOT refuse it
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (ACCEPTED, exit 9); twin src/compiler/probes/2026-09-16f-landboth/rust/u04.rs
package bc_0916f_landboth_hb_u04_admit;
fn main() -> i32 {
    let mut v: Vec<i64> = Vec::new();
    let mut w: Vec<i64> = Vec::new();
    v.push(1i64); v.push(0i64);
    w.push(0i64); w.push(7i64);
    let e: &i64 = &w[0u64];
    v[*e as u64] = 9i64;
    return v[0u64] as i32;
}
