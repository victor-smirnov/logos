// hand battery: round 0915a_refeq, program e51 — caught: verdict moved base (wrong answer at run time, reference pair compared by address) -> refeq landing (Rust's answer)
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915a_refeq_hb_e51_admit.rs
package bc_0915a_refeq_hb_e51_admit;
use logos.lang.cmp;
use logos.mem.collections.vec;
struct D { v: i64 }
impl Eq for D {
    fn eq(&self, other: &D) -> bool {
        return self.v == other.v;
    }
}
fn main() -> i32 {
    let mut v: Vec<D> = vec_new::<D>();
    v.push(D { v: 4i64 });
    v.push(D { v: 4i64 });
    let r0: &D = &v[0];
    let r1: &D = &v[1];
    if r0 == r1 {
        return 0i32;
    }
    return 1i32;
}
