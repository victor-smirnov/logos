// soundness queue row aggregate_extended_borrow_temp_never_dropped — CLOSED by round 2026-09-15g-aggtemp, commit of this fixture.
// asserts: a temporary extended through an aggregate `let` initialiser drops at the END OF THE BLOCK: inside=0 n=1000 (base n=0, exit 1)
// legality: rustc 1.98.1 --edition 2024, twin cited in the round's rust/ directory; measured, not read.
package bc_0915g_aggtemp_extended_borrow_temp_dropped_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct C { v: i64, c: *mut i64 }
impl Drop for C { fn drop(self: &mut C) { unsafe { *self.c = *self.c + self.v; } } }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let mut inside: i64 = 0i64;
    {
        let t: (&C, i64) = (&C { v: 1000i64, c: p }, 1i64);
        if t.0.v != 1000i64 { return 3i32; }
        inside = unsafe { n };
    }
    let got: i64 = unsafe { n };
    unsafe { printf("inside=%ld n=%ld\n".as_ptr(), inside, got); }
    if inside != 0i64 { return 2i32; }
    if got != 1000i64 { return 1i32; }
    return 0i32;
}
// rustc 1.98.1 --edition 2024 (round 2026-09-15f-rustcaudit, class CONFIRMED): COMPILES and RUNS exit 0 (stdout "inside=0 n=1000"). Twin src/compiler/probes/2026-09-15f-rustcaudit/rust/aggregate_extended_borrow_temp_never_dropped.rs. Audit note: tuple-operand &temp is lifetime-extended to block end; control n=0 exit 1.
