// a Copy scrutinee is not moved by the if-let and is reused PROBES.md 2026-09-06d: `if let` / `while let` / let-chains lower BY DELEGATION to lower_match (temp hoist, per-arm scrutinee move, arm drops, cond-move merge); the count is a destructor tally through a raw pointer.
package bc_patown_copy_scrut_reused;
fn main() -> i32 {
    let o: Option<i64> = Option::Some(3i64);
    let mut n: i64 = 0i64;
    if let Option::Some(v) = o { n = n + v; }
    if let Option::Some(w) = o { n = n + w; }
    return n as i32;
}
