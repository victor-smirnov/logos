// hand battery: round 2026-09-15f-consumeland, program b20 — caught: its destructor count moved to Rust's answer between base 7eb6735b16576c45 and the landing (a by-value operator / *Assign operand now marked moved); pricing round 2026-09-15e-consume's program, statements re-flowed one per line
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15e-consume/rust/b20_op_in_arrlit.rs: compiles, runs, exit 0 (the twin asserts the same destructor count)
package bc_0915f_consumeland_hb_b20_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
struct W { x: D, y: D }
#[derive_copy]
#[derive_clone]
struct P { v: i64 }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
impl Add for D { type Output = D; fn add(self: D, o: D) -> D { return D { v: self.v * 0i64 + 100i64, c: self.c }; } }
impl Neg for D { type Output = i64; fn neg(self: D) -> i64 { return 0i64 - self.v; } }
impl Add for P { type Output = P; fn add(self: P, o: P) -> P { return P { v: self.v + o.v }; } }
fn g(p: *mut i64) -> i64 {
    let a: D = D { v: 1i64, c: p };
    let b: D = D { v: 10i64, c: p };
    { let arr: [D;
    1] = [a + b];
    if arr[0].v != 100i64 { return 7i64;
    } } return 0i64;
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let r: i64 = g(p);
    let got: i64 = rd(p);
    unsafe { printf("r=%ld n=%ld\n".as_ptr(), r, got); }
    if got != 111i64 { return 1i32; }
    return 0i32;
}
