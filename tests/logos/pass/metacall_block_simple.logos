// metacall { ... } — single-tail-expr block at compile time.
package metacall_block_simple;

fn compute() -> i32 { return 42; }

fn main() -> i32 {
    let r: i32 = metacall { compute() };
    return r;
}
