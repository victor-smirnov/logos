// THE LEGAL TWIN of fail/bc_b6ptr_param_escape, one token apart: the stored
// borrow names a `static`, whose storage outlives every caller. (2026-09-02c)
package bc_b6ptr_param_static;
static G: i64 = 9i64;
fn store(out: &mut &i64) {
    let local: i64 = 1i64;
    *out = &G;
}
fn main() -> i32 {
    let mut p: &i64 = &G;
    store(&mut p);
    return 0i32;
}
