// soundness queue row call_result_index_projection_invalid_mlir_refused — CLOSED by round 2026-09-15g-aggtemp, commit of this fixture.
// asserts: `mkarr(p)[1].v` / `mktup(p).1.v` compile and run: n=22 (base: backend GEP verify failure, refused)
// legality: rustc 1.98.1 --edition 2024, twin cited in the round's rust/ directory; measured, not read.
package bc_0915g_aggtemp_call_result_projection_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
fn mkarr(p: *mut i64) -> [D; 2] { return [D { v: 1i64, c: p }, D { v: 10i64, c: p }]; }
fn mktup(p: *mut i64) -> (D, D) { return (D { v: 1i64, c: p }, D { v: 10i64, c: p }); }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        let a: i64 = mkarr(p)[1].v;
        let b: i64 = mktup(p).1.v;
        if a + b != 20i64 { return 3i32; }
    }
    let got: i64 = unsafe { n };
    unsafe { printf("n=%ld\n".as_ptr(), got); }
    if got != 22i64 { return 1i32; }
    return 0i32;
}
// rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/rust/rows/call_result_index_projection_invalid_mlir_refused.rs: rustc=0 run=0 out=n=22
