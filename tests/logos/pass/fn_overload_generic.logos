package fn_overload_generic;
// Edge case: calling same function with different concrete types
fn identity<T>(x: T) -> T { return x; }
unsafe fn main() -> i32 {
    let a: i32 = identity::<i32>(42);
    let b: bool = identity::<bool>(true);
    if b { return a; }
    return 0;
}
