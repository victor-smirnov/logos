// hand battery: round 2026-09-10dropord, program c14 — caught: verdict moved base -> landed — PROBES.md "c1 struct, 3 fields 321 -> 123 ... c17 nested struct + user Drop 3n921 -> n9123" (drop order, landed 1fad68657)
// legality: by reading, no rustc binary
// harvested 2026-09-14h from snapshot hand-harvest-2026-09-14/25aa8421-fce1-4a11-8a89-5d2ba5981c88/dropord/cex/c14.logos
package bc_0910dropord_hb_c14_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
fn p(fmt: str, v: i32) { unsafe { printf(fmt.as_ptr(), v); } }
struct O { id: i32 }
impl Drop for O { fn drop(self: &mut O) { p("%d", self.id); } }
struct S2 { a: O, b: O }
fn mk() -> S2 { return S2 { a: O { id: 1 }, b: O { id: 2 } }; }
fn main() -> i32 {
    {
        let s: S2 = mk();
        p("[%d]", 0);
    }
    unsafe { printf("\n".as_ptr()); }
    return 0;
}
