package index_compound_assign;
// Regression: arr[i] op= val compound assignment

fn main() -> i32 {
    let mut arr: [i32; 3] = [41, 0, 0];
    arr[0] += 1;
    arr[1] += 20;
    arr[2] += 21;
    return arr[0] + arr[1] + arr[2];
}
