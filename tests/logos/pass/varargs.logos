package varargs;

extern fn printf(fmt: *const u8, ...) -> i32;

fn printi(fmt: str, v: i32) { unsafe { printf(fmt.as_ptr(), v); } }

unsafe fn main() -> i32 {
    printi("hello %d\n", 42);
    return 0;
}
