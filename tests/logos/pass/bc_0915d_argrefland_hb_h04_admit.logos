// hand battery: round 2026-09-15d-argrefland, program h04 — caught: verdict moved base b96b980fb (cc=0 run=139 vg=1) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915d_argrefland_hb_h04_admit.rs
package bc_0915d_argrefland_hb_h04_admit;
struct D { v: i64 }
fn inner(a: &&D) -> i64 {
    let x: &D = *a;
    return x.v;
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    let mut it: Option<&D> = Option::Some(&d);
    let mut sum: i64 = 0i64;
    while let Option::Some(rd) = it {
        sum = sum + inner(&rd);
        it = Option::None;
    }
    if sum != 6i64 { return 4i32; }
    return 0i32;
}
