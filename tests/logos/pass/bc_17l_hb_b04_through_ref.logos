// hand battery: round 2026-09-17l, program b04_through_ref — caught: array field reached through a reference parameter: verdict moved REFUSED to exit 0
// legality MEASURED with rustc 1.98.1 --edition 2024 (twin beside the round record).
package bc_17l_hb_b04_through_ref;
extern fn printf(fmt: *const u8, ...) -> i32;
struct S { arr: [i64; 2], tag: i64 }
fn rd(r: &S) -> i64 {
    match r.arr {
        [_, y] => { return y; }
    }
}
fn main() -> i32 {
    let s: S = S { arr: [1i64, 2i64], tag: 7i64 };
    let out: i64 = rd(&s);
    unsafe { printf("got=%ld\n".as_ptr(), out); }
    if out != 2i64 { return 1i32; }
    return 0i32;
}
