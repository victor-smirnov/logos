// hand battery: round 0915a_refeq, program e49 — caught: verdict moved base (wrong answer at run time, reference pair compared by address) -> refeq landing (Rust's answer)
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915a_refeq_hb_e49_admit.rs
package bc_0915a_refeq_hb_e49_admit;
use logos.lang.cmp;
struct D { v: i64 }
impl Eq for D {
    fn eq(&self, other: &D) -> bool {
        return self.v == other.v;
    }
}
fn main() -> i32 {
    let a: D = D { v: 2i64 };
    let b: D = D { v: 2i64 };
    let ra: &D = &a;
    let rb: &D = &b;
    let k: i64 = 1i64;
    match k {
        1i64 if ra == rb => { return 0i32; }
        _ => { return 1i32; }
    }
}
