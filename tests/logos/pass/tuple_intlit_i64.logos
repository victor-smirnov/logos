package tuple_intlit_i64;
// Bug: tuple literal (10000000000, 5) truncated first element to i32.
// Fix: upgrade tuple element type from IntLit to i64 when value overflows i32.
fn main() -> i32 {
    let t = (10000000000, 5);
    if t.0 == 10000000000 { return 0; }
    return 1;
}
