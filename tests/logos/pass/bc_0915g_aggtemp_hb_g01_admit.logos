// hand battery: round 2026-09-15g-aggtemp (landing), program g01 — caught: an array-literal temporary in an IF CONDITION was never dropped (n 0 -> 11)
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/battery-land/rust/g01.rs: compiles, runs, stdout `s=1 n=11`
package bc_0915g_aggtemp_hb_g01_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let mut s: i64 = 0i64;
    {
        if [D { v: 1i64, c: p }, D { v: 10i64, c: p }][1].v == 10i64 { s = 1i64; }
    }
    let got: i64 = unsafe { n };
    unsafe { printf("s=%ld n=%ld\n".as_ptr(), s, got); }
    if s != 1i64 { return 3i32; }
    if got != 11i64 { return 1i32; }
    return 0i32;
}
