package for_continue;
// F08: continue inside for loop
unsafe fn main() -> i32 {
    let mut sum: i32 = 0;
    for i in 1..11 {
        if i % 2 == 0 { continue; }   // skip evens
        sum = sum + i;   // 1+3+5+7+9 = 25
    }
    // 25 + 17 = 42
    return sum + 17;
}
