// hand battery: round 2026-09-16d-landrulings, program r04 — caught: `&*ra < rb` — the repair the new diagnostic names — must stay legal
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: ACCEPTED, exit 0 — twin src/compiler/probes/2026-09-16d-landrulings/rust/r04.rs
package bc_0916d_refrel_hb_r04_admit;
use logos.lang.cmp;
fn main() -> i32 {
    let mut a: (bool, u8) = (true, 3u8);
    let b: (bool, u8) = (true, 4u8);
    let ra: &mut (bool, u8) = &mut a;
    let rb: &(bool, u8) = &b;
    if &*ra < rb { return 0i32; }
    return 1i32;
}
