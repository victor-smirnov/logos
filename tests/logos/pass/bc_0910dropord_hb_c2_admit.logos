// hand battery: round 2026-09-10dropord, program c2 — caught: verdict moved base -> landed — PROBES.md "c1 struct, 3 fields 321 -> 123 ... c17 nested struct + user Drop 3n921 -> n9123" (drop order, landed 1fad68657)
// legality: by reading, no rustc binary
// harvested 2026-09-14h from snapshot hand-harvest-2026-09-14/25aa8421-fce1-4a11-8a89-5d2ba5981c88/dropord/cex/c2.logos
package bc_0910dropord_hb_c2_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
fn p(fmt: str, v: i32) { unsafe { printf(fmt.as_ptr(), v); } }
struct O { id: i32 }
impl Drop for O { fn drop(self: &mut O) { p("%d", self.id); } }
struct In { x: O, y: O }
struct Out { m: In, n: In }
fn main() -> i32 {
    {
        let o: Out = Out { m: In { x: O { id: 1 }, y: O { id: 2 } },
                           n: In { x: O { id: 3 }, y: O { id: 4 } } };
        p("[%d]", 0);
    }
    unsafe { printf("\n".as_ptr()); }
    return 0;
}
