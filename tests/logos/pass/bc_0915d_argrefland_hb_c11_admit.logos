// hand battery: round 2026-09-15c-argref (re-run on this landing), program c11 — caught: verdict moved base b96b980fb (cc=0 run=139 vg=1) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_c11_admit;
struct D { v: i64 }
fn inner(a: &&D) -> *const D {
    let x: &D = *a;
    return x as *const D;
}
fn inner_m(a: &&mut D) -> i64 { return a.v; }
fn main() -> i32 {
    let mut d: D = D { v: 6i64 };
    let f = |rd: &mut D| -> i64 { return inner_m(&rd); };
    if f(&mut d) != 6i64 { return 11i32; }
    return 0i32;
}
