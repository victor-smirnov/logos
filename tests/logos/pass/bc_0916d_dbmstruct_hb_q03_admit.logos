// hand battery: round 2026-09-16d-landrulings, program q03 — caught: a `..` rest that SKIPS a Drop field: only the BOUND field doubled (base 'D1 D1 D2') — the duplicate is per BINDER, not per value
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: ACCEPTED, exit 0, stdout 'D1 D2' — twin src/compiler/probes/2026-09-16d-landrulings/rust/q03.rs
package bc_0916d_dbmstruct_hb_q03_admit;
use logos.std.fmt;
struct S { pub n: i64 }
impl Drop for S { fn drop(self: &mut S) { println!("D{}", self.n); } }
enum E { V { f: S, g: S }, Z }
fn run() -> i64 {
    let p: E = E::V { f: S { n: 1i64 }, g: S { n: 2i64 } };
    let mut out: i64 = 0i64;
    match &p {
        E::V { f, .. } => { out = f.n; },
        E::Z => {}
    }
    return out;
}
fn main() -> i32 {
    if run() != 1i64 { return 1i32; }
    return 0i32;
}
