// hand battery: round 0915b_refeqland, program f29b — caught: verdict moved base (wrong answer at run time) -> refeq landing (Rust's answer)
// legality: by reading, no rustc binary
package bc_0915b_refeqland_hb_f29b_admit;
use logos.lang.cmp;
struct D { v: i64 }
impl Eq for D {
    fn eq(&self, other: &D) -> bool {
        return self.v == other.v;
    }
}
fn same<T: Eq>(a: &T, b: &T) -> bool {
    let x: &&T = &a;
    let y: &&T = &b;
    return x == y;
}
fn main() -> i32 {
    let a: D = D { v: 6i64 };
    let b: D = D { v: 6i64 };
    if !same::<D>(&a, &b) { return 1i32; }
    return 0i32;
}
