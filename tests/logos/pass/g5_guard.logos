package g5_guard;

fn check(x: i32) -> i32 {
    match x {
        n if n > 50 => { return 100; }
        n if n > 20 => { return 42; }
        _ => { return 0; }
    }
}

fn main() -> i32 {
    return check(30);
}
