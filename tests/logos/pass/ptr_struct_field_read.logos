package ptr_struct_field_read;
// Regression: field read through *mut T pointer
struct Point {
    x: i32,
    y: i32,
}
unsafe fn main() -> i32 {
    let mut p: Point = Point { x: 20, y: 22 };
    let ptr: *mut Point = &mut p;
    return ptr.x + ptr.y;  // 42
}
