// Sprint 6.2 / B-ty-07: `&&T` and `&&mut T` parse despite lexer
// collapse of `&&` to AND.
package double_ref_type;

fn helper(p: &&i32) -> i32 { return 0; }
fn helper2(p: &&mut i32) -> i32 { return 0; }

fn main() -> i32 { return 0; }
