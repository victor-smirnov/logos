package loop_continue;
// RC4 smoke: continue skips even numbers — sum of odd 1..=9 = 1+3+5+7+9 = 25

fn main() -> i32 {
    let mut sum: i32 = 0;
    let mut i: i32 = 0;
    while i < 10 {
        i = i + 1;
        if i % 2 == 0 { continue; }
        sum = sum + i;
    }
    return sum;
}
