// soundness queue row extended_field_base_temp_dropped_at_let_end_run — CLOSED by round 2026-09-15g-aggtemp, commit of this fixture.
// asserts: an extended borrow of a FIELD of a temporary keeps the temporary to the end of the block: seen=0 n=11 (base seen=11, exit 1)
// legality: rustc 1.98.1 --edition 2024, twin cited in the round's rust/ directory; measured, not read.
package bc_0915g_aggtemp_extended_field_base_block_owner_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
struct W { x: D, y: D, z: i64 }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let mut seen: i64 = -1i64;
    {
        let k: &D = &W { x: D { v: 1i64, c: p }, y: D { v: 10i64, c: p }, z: 3i64 }.y;
        seen = unsafe { *p };
        if k.v != 10i64 { return 3i32; }
    }
    let got: i64 = unsafe { n };
    unsafe { printf("seen=%ld n=%ld\n".as_ptr(), seen, got); }
    if got != 11i64 { return 2i32; }
    if seen != 0i64 { return 1i32; }
    return 0i32;
}
// rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/rust/rows/extended_field_base_temp_dropped_at_let_end_run.rs: rustc=0 run=0 out=seen=0 n=11
