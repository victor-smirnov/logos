// Mixed fixed and variadic parameters
package variadic_mixed;
fn apply<U, T...>(x: U, extra: T...) -> i32 {
    return (x as i32) + sum(extra...);
}
fn sum(a: i32, b: i32) -> i32 { return a + b; }
unsafe fn main() -> i32 { return apply::<i32, i32, i32>(2, 20, 20); }
