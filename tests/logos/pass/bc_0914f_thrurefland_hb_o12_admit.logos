// hand battery: round 2026-09-14f-thrurefland, program o12 — caught: verdict moved base -> landed — PROBES.md "I read d15 / o11 / o12 / v1-v7 as ILLEGAL programs opened by D2. They are not illegal in Logos ... auto-Copy under blessed divergence A1...
// legality: by reading, no rustc binary
// harvested 2026-09-14i from snapshot hand-harvest-2026-09-14b/land14f_own.1gCs/o12_place_retarget_move_owner_illegal.logos
package bc_0914f_thrurefland_hb_o12_admit;
struct H<'x> { r: &'x (i64, i64) }
fn eat(p: (i64, i64)) -> i64 { return p.0; }
fn main() -> i32 {
    let s1: (i64, i64) = (1i64, 2i64);
    let s2: (i64, i64) = (3i64, 4i64);
    let mut h: H = H { r: &s1 };
    let a: &i64 = &h.r.0;
    h.r = &s2;
    let m: i64 = eat(s1);
    return (*a + m + h.r.0) as i32;
}
