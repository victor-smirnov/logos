package i05;
fn main() -> i32 {
    let mut v: Vec<i32> = Vec::<i32>::new();
    v.push(1i32); v.push(2i32); v.push(3i32); v.push(4i32);
    let mut it = v.iter().filter(|x| **x % 2i32 == 0i32);
    let n: Option<&i32> = it.next();
    if n.is_none() { return 1i32; }
    return 0i32;
}
