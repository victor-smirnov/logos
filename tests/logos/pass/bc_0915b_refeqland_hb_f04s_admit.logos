// hand battery: round 0915b_refeqland, program f04s — caught: verdict moved base (wrong answer at run time) -> refeq landing (Rust's answer)
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915b_refeqland_hb_f04s_admit.rs
package bc_0915b_refeqland_hb_f04s_admit;
use logos.lang.cmp;
struct D { v: i64 }
impl Eq for D {
    fn eq(&self, other: &D) -> bool {
        return self.v == other.v;
    }
}
fn main() -> i32 {
    let mut a: D = D { v: 1i64 };
    let b: D = D { v: 9i64 };
    let rb: &D = &b;
    let ra: &mut D = &mut a;
    ra.v = 9i64;
    let r1: bool = ra == rb;
    if !r1 { return 1i32; }
    return 0i32;
}
