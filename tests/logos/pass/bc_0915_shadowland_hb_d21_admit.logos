// hand battery: round 0915_shadowland, program d21 — caught: exit 1 on base AND under shslotev (uninit drop state read by NAME); reads Rust on the landing
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915_shadowland_hb_d21_admit.rs
package bc_0915_shadowland_hb_d21_admit;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
fn eatd(x: D) -> i64 { return x.v; }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
// d21 NEW binding declared uninit, early return before its assignment. Rust: c=true 1; c=false 2 then 1 => 21
fn g(p: *mut i64, c: bool) -> i64 {
    let x: D = D { v: 1i64, c: p };
    let x: D;
    if c { return 5i64; }
    x = D { v: 2i64, c: p };
    return x.v;
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    if g(p, true) != 5i64 { return 9i32; }
    if rd(p) != 1i64 { return 1i32; }
    unsafe { *p = 0i64; }
    if g(p, false) != 2i64 { return 8i32; }
    if rd(p) != 21i64 { return 2i32; }
    return 0i32;
}
