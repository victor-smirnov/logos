// hand battery: round 2026-09-15d-argrefland, program h02 — caught: verdict moved base b96b980fb (cc=0 run=139 vg=1) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_h02_admit;
struct D { v: i64 }
struct W { k: i64, inner: D }
fn inner(a: &&D) -> i64 {
    let x: &D = *a;
    return x.v;
}
fn main() -> i32 {
    let w: W = W { k: 1i64, inner: D { v: 6i64 } };
    match w {
        W { k: _, inner: ref q } => {
            if inner(&q) != 6i64 { return 2i32; }
        }
    }
    return 0i32;
}
