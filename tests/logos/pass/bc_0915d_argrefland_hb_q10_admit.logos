// hand battery: round 2026-09-15d-argrefland, program q10 — caught: verdict moved base b96b980fb (cc=0 run=132 vg=0) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_q10_admit;
struct P { x: i32 }
fn main() -> i32 {
    let p = P { x: 5i32 };
    let r: &P = &p;
    let pp: &&P = &r;
    let mut n: i32 = 0i32;
    while let &&P { x } = pp {
        n = n + x;
        if n > 9i32 { break; }
    }
    if n != 10i32 { return 10i32; }
    return 0i32;
}
