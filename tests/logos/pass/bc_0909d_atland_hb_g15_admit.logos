// hand battery: round 2026-09-09d-atland, program g15 — caught: verdict moved base -> landed — tables/hand_head.txt vs hand_final.txt: run=1 or cc=1 on HEAD -> cc=0 run=0 landed; PROBES.md "ELEVEN of fifteen were already wrong before a line w...
// legality: by reading, no rustc binary
// harvested 2026-09-14h from src/compiler/probes/2026-09-09d-atland/hand/g15.logos
// Two at-bindings on the SAME struct, one field-level and one whole-value.
package bc_0909d_atland_hb_g15_admit;
struct P { x: i64, y: i64 }
fn main() -> i32 {
    let p: P = P { x: 5i64, y: 2i64 };
    let mut out: i64 = 0i64;
    match p {
        w @ P { x: a @ _, y } => { out = w.y + a + y; }
    }
    if out != 9i64 { return 1i32; }
    return 0i32;
}
