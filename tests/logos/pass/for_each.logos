package for_each;

fn sum_arr(arr: [i32; 4]) -> i32 {
    let mut total: i32 = 0;
    for item in arr {
        total = total + item;
    }
    return total;
}

unsafe fn main() -> i32 {
    let a: [i32; 4] = [10, 20, 30, 40];
    return sum_arr(a);
}
