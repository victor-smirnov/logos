// Variadic generics: pack forwarding
package variadic_forward;

fn sum(a: i32, b: i32) -> i32 { return a + b; }

fn forward<T...>(args: T...) -> i32 {
    return sum(args...);
}

unsafe fn main() -> i32 {
    return forward::<i32, i32>(20, 22);
}
