// hand battery: round 2026-09-15c-argref (re-run on this landing), program c17 — caught: verdict moved base b96b980fb (cc=0 run=17 vg=0) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_c17_admit;
struct D { v: i64 }
fn inner(a: &&D) -> *const D {
    let x: &D = *a;
    return x as *const D;
}
impl D {
    fn me(self: &Self) -> bool {
        let s: &D = self;
        return inner(&s) == self as *const D;
    }
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    if !d.me() { return 17i32; }
    return 0i32;
}
