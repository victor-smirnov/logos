// hand battery: round 2026-09-17k (landed by 2026-09-17l), program x04_matchfield_copy — caught: ABUSE for the match-door change: mutating s.arr in the arm must not be visible through a by-value binder (rustc got=2)
// legality MEASURED with rustc 1.98.1 --edition 2024. Owed by round 17k, which measured it and could not land it.
package bc_17l_hb_x04_matchfield_copy;
extern fn printf(fmt: *const u8, ...) -> i32;
struct S { arr: [i64; 2], tag: i64 }
fn main() -> i32 {
    let mut s: S = S { arr: [1i64, 2i64], tag: 7i64 };
    let mut out: i64 = 0i64;
    match s.arr {
        [_, y] => {
            s.arr[1] = 50i64;
            out = y;
        }
    }
    unsafe { printf("got=%ld\n".as_ptr(), out); }
    if out != 2i64 { return 1i32; }
    return 0i32;
}
