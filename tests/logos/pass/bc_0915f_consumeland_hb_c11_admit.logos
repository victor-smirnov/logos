// hand battery: round 2026-09-15f-consumeland, program c11 — caught: its destructor count moved to Rust's answer between base 7eb6735b16576c45 and the landing (a by-value operator / *Assign operand now marked moved); pricing round 2026-09-15e-consume's program, statements re-flowed one per line
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15e-consume/rust/c11_op_chain.rs: compiles, runs, exit 0 (the twin asserts the same destructor count)
package bc_0915f_consumeland_hb_c11_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
struct H { arr: [D; 2] }
struct W { x: D, y: D }
enum E { V(D, D), U }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
fn eat(d: D) -> i64 { return d.v * 0i64; }
fn eat2(a: [D; 2]) -> i64 { return a[0].v * 0i64; }
impl Add for D { type Output = D; fn add(self: D, o: D) -> D { return D { v: 100i64, c: self.c }; } }
impl Not for D { type Output = i64; fn not(self: D) -> i64 { return self.v; } }
impl AddAssign for D { fn add_assign(self: &mut D, o: D) { self.v = self.v + 0i64 * o.v; } }
fn g(p: *mut i64) -> i64 {
    let a: D = D { v: 1i64, c: p };
    let b: D = D { v: 10i64, c: p };
    let c: D = D { v: 1000i64, c: p };
    let s: D = a + b + c;
    return s.v;
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let r: i64 = g(p);
    let got: i64 = rd(p);
    unsafe { printf("r=%ld n=%ld\n".as_ptr(), r, got); }
    if got != 1211i64 { return 1i32; }
    return 0i32;
}
