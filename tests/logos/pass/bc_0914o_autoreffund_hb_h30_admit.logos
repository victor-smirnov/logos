// hand battery: round 2026-09-14o-autoreffund, program h30 — caught: (round 2026-09-14n) construction and drop ORDER of temporaries on both sides: base leaked both (12); Rust's 1265 under the landing
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (ACCEPTED, exit:0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0914o_autoreffund_hb_h30_admit.rs
package bc_0914o_autoreffund_hb_h30_admit;
use logos.lang.cmp;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, s: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.s = *self.s * 10i64 + self.v + 4i64; } } }
impl Eq for D { fn eq(&self, other: &D) -> bool { return self.v > 0i64 && other.v > 0i64; } }
fn mk(v: i64, s: *mut i64) -> D {
    unsafe { *s = *s * 10i64 + v; }
    return D { v: v, s: s };
}
fn main() -> i32 {
    let mut q: i64 = 0i64;
    let s: *mut i64 = &mut q;
    if !(mk(1i64, s) == mk(2i64, s)) { return 3i32; }
    let got: i64 = unsafe { q };
    unsafe { printf("seq=%ld\n".as_ptr(), got); }
    if got != 1265i64 { return 1i32; }
    return 0i32;
}
