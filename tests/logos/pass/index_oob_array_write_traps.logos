// Index bounds (Rust panics; Logos traps per A13/A7): a runtime index past a [T; N] write.
package index_oob_array_write_traps;
struct S { a: [i64; 3] }
fn at(x: i64) -> i64 { return x; }
fn main() -> i32 {
    let mut a: [i64; 3] = [1, 2, 3]; a[at(2)] = 9; a[at(10)] = 7; return 0;
}
