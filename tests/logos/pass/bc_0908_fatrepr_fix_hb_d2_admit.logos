// hand battery: round 2026-09-08-fatrepr-fix, program D2 — caught: verdict moved base -> landed — RESULT.md "CLOSED (REFUSED -> rc 0, RUN): D1 inherent method · D5 TRAIT method · D8 ... · D2 closure -> fn ptr"
// legality: by reading, no rustc binary
// harvested 2026-09-14h from src/compiler/probes/2026-09-08-fatrepr-fix/hand/D2.logos
// D2 — CLASS PROBE: CFLAG_CLOSURE_TO_FNPTR is in the MethodArg mask. Does the
// candidate SELECTOR admit a closure at an `fn(i64)->i64` parameter?
package bc_0908_fatrepr_fix_hb_d2_admit;
struct H { n: i64 }
impl H { pub fn ap(&self, f: fn(i64) -> i64) -> i64 { return f(self.n); } }
fn main() -> i32 {
    let h: H = H { n: 41i64 };
    if h.ap(|x: i64| -> i64 { return x + 1i64; }) != 42i64 { return 10i32; }
    return 0i32;
}
