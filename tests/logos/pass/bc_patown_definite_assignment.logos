// definite assignment merges across the if-let's two arms PROBES.md 2026-09-06d: `if let` / `while let` / let-chains lower BY DELEGATION to lower_match (temp hoist, per-arm scrutinee move, arm drops, cond-move merge); the count is a destructor tally through a raw pointer.
package bc_patown_definite_assignment;
fn main() -> i32 {
    let o: Option<i64> = Option::Some(4i64);
    let mut x: i64 = 99i64;
    if let Option::Some(v) = o { x = v; } else { x = 0i64; }
    return x as i32;
}
