// hand battery: round 0915_shadowland, program d16 — caught: verdict moved base -> shadowslot landing
// legality: by reading, no rustc binary
package bc_0915_shadowland_hb_d16_admit;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
fn eatd(x: D) -> i64 { return x.v; }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        let x: D = D { v: 1i64, c: p };
        let x: D = D { v: 2i64, c: p };
        let _k: i64 = eatd(x);
        let x: D = D { v: 3i64, c: p };
        if x.v != 3i64 { return 9i32; }
    }
    if rd(p) != 231i64 { return 1i32; }
    return 0i32;
}
