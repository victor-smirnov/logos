// `w.push(1)` on a `for mut w in vs` binding: legal, runs.
// PROBES.md 2026-09-08a: a place in a mutable-use position takes the DerefMut/IndexMut step at every level, a Deref-only/Index-only step is refused, and the bare `deref_mut()` receiver is asked the binding-mut question.
package bc_mutplace_vec_push_for_mut_binding;
fn main() -> i32 {
    let v: Vec<i64> = Vec::new();
    let mut vs: Vec<Vec<i64>> = Vec::new();
    vs.push(v);
    let mut n: i64 = 0i64;
    for mut w in vs { w.push(1i64); n = n + w.len(); }
    if n != 1i64 { return 1i32; }
    return 0i32;
}
