// hand battery: round 2026-09-15c-argref, program b02 — caught: argrefpos / argrefneg REFUTED: `let p: &&mut D = &rd;` over an immutable `&mut D` binding read exit 2 under both; right on base and under argrefland / argreflandx
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915c_argref_hb_b02_admit.rs
package bc_0915c_argref_hb_b02_admit;
struct D { v: i64 }
fn main() -> i32 {
    let mut d: D = D { v: 6i64 };
    let rd: &mut D = &mut d;
    let p: &&mut D = &rd;
    if p.v != 6i64 { return 2i32; }
    return 0i32;
}
