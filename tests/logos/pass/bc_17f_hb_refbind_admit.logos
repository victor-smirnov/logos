// hand battery: round 2026-09-17f, program hb_refbind_admit — caught: a LEGAL `ref` spelling under a &-chain that the BASE binary
// refused with "undefined variable 'q'"; it compiles and runs only with both halves of the door armed.
// legality: rustc 1.98.1 --edition 2024 MEASURED 2026-09-17: compiles (rc 0), exit 0.
package bc_17f_hb_refbind_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
struct H { d: D }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let h: H = H { d: D { v: 2i64, c: p } };
    let r: &H = &h;
    let rr: &&H = &r;
    let k: i32 = 1i32;
    match (rr, k) {
        (&&H { d: ref q }, j) => { unsafe { printf("got=%ld %d\n".as_ptr(), q.v, j); } }
    }
    return 0i32;
}
