// Compiles and runs as in rustc 1.98.1 (squeue row of the same name).
package self_tuple_struct_ctor_refused;
struct T(i64);
impl T {
    fn mk(x: i64) -> Self {
        return Self(x);
    }
}
fn main() -> i32 {
    let t = T::mk(53i64);
    return t.0 as i32;
}
