// hand battery: round 2026-09-15c-argref (re-run on this landing), program d06 — caught: verdict moved base b96b980fb (cc=0 run=6 vg=0) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_d06_admit;
struct D { v: i64 }
fn inner(a: &&D) -> *const D {
    let x: &D = *a;
    return x as *const D;
}
static G: D = D { v: 6i64 };
fn main() -> i32 {
    let rd: &D = &G;
    if inner(&rd) != rd as *const D { return 6i32; }
    return 0i32;
}
