// hand battery: round 2026-09-08-fatrepr-fix, program G3 — caught: verdict moved base -> landed — RESULT.md "CLOSED (REFUSED -> rc 0, compiled + linked + RUN, answer checked): G1 ... G8"
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: DIVERGENCE (REFUSED, error[E0106]); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0908_fatrepr_fix_hb_g3_admit.rs
// ⚠ THE AUDIT RECORDS THIS FIXTURE AS RESTING ON A BLESSED DIVERGENCE, NOT AS WRONG — rustc refuses the literal twin and the canonising row/clause is named in docs/rustc-fixture-audit-2026-09.md §bc_0908_fatrepr_fix_hb_g3_admit. The assertion is UNCHANGED: a corpus decision with an owner.
// harvested 2026-09-14h from src/compiler/probes/2026-09-08-fatrepr-fix/hand/G3.logos
// G3 LEGAL — the ctor is not on the RHS of a `let`: it is built directly at a
// CALL ARGUMENT position, so no LetInit hint reaches the payload check.
package bc_0908_fatrepr_fix_hb_g3_admit;
enum E<'a> { N, S(&'a [i64]) }
fn take(e: E) -> i64 {
    match e {
        E::N => { return 0i64; }
        E::S(s) => { return s[2]; }
    }
}
fn main() -> i32 {
    let a: [i64; 3] = [4i64, 5i64, 6i64];
    if take(E::S(&a)) != 6i64 { return 10i32; }
    return 0i32;
}
