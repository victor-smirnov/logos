// format() via std.fmt — variadic generics + Display trait
package variadic_format;

use std.fmt;
use std.io;
use std.lang.text;

unsafe fn main() -> i32 {
    let s: String = format::<str, i32, bool>("{} says: x={}, ok={}", "logos", 42, true);
    println_string(&s);
    return 42;
}
