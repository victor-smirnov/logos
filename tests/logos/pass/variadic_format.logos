// format() via std.fmt — variadic generics + Display trait
package variadic_format;

use std.fmt;
use std.io;

unsafe fn main() -> i32 {
    let s: String = format::<*const u8, i32, bool>("{} says: x={}, ok={}", "logos", 42, true);
    println(s.as_ptr());
    return 42;
}
