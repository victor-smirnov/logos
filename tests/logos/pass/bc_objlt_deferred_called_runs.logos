// A generic fn returning `Box<dyn Tr>`, CALLED from main: the 09-01g §7 SIGSEGV (rc 139 before 2026-09-02y — the deferred `Box<T>` reached codegen).
package bc_objlt_deferred_called_runs;
trait Tr { fn m(self: &Self) -> i64; }
struct S { v: i64 }
impl Tr for S { fn m(self: &Self) -> i64 { return self.v; } }
fn f<A: Tr + 'static>(v: A) -> Box<dyn Tr> {
    return Box::new(v);
}
fn main() -> i32 { let b = f(S { v: 2i64 }); return (b.m() - 2i64) as i32; }
