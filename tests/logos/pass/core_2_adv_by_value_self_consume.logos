// `fn consume(self)` — the by-value receiver shorthand — is legal Rust; it was pinned as a syntax error (the fail
// fixture core_2_adv_by_value_self_syntax) until the grammar gained the shorthand. rustc: compiles, exit 0.
package core_2_adv_by_value_self_consume;
struct V { v: i32 }
impl V { fn consume(self) -> i32 { return self.v; } }
fn main() -> i32 { return 0i32; }
