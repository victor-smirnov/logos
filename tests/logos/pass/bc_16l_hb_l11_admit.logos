// hand battery: round 2026-09-16l-refptee, program l11 — caught: verdict MOVED base->landed (base: `&&'a D` was a parse error; landed: compiles and runs exit 0). This is the AND-LIFETIME half of the arm.
// legality MEASURED, rustc 1.98.1 --edition 2024: compiles, RUNS exit 0 — twin src/compiler/probes/2026-09-16l-refptee/rust/r11_amplt_double.rs.
package l11_amplt_double;
struct D { v: i64 }
fn read<'a>(src: &&'a D) -> i64 {
    return src.v;
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    let rd: &D = &d;
    return (read(&rd) - 6i64) as i32;
}
