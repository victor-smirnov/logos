// hand battery: round 2026-09-17f, program hb_tuple_admit — caught: it is the VALUE oracle at this door, where the exit code is blind.
// The sema half alone made this print got=0 while exiting 0 (PROBES.md 2026-09-16p); only the printed
// value separates a real closure from a miscompile.
// legality: rustc 1.98.1 --edition 2024 MEASURED 2026-09-17: compiles, prints got=6, exit 0.
package bc_17f_hb_tuple_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct P { x: i32 }
fn main() -> i32 {
    let p = P { x: 5i32 };
    let r: &P = &p;
    let pp: &&P = &r;
    let k: i32 = 1i32;
    match (pp, k) {
        (&&P { x }, j) => {
            unsafe { printf("got=%d\n".as_ptr(), x + j); }
            if x + j != 6i32 { return 1i32; }
        }
    }
    return 0i32;
}
