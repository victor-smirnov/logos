// hand battery: round 2026-09-15g-aggtemp (landing), program g17 — caught: a METHOD CALL on an element of an array-literal temporary never dropped the temporary (n 0 -> 11)
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/battery-land/rust/g17.rs: compiles, runs, stdout `k=10 n=11`
package bc_0915g_aggtemp_hb_g17_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
impl D {
    fn get(self: &D) -> i64 { return self.v; }
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let mut k: i64 = 0i64;
    {
        k = [D { v: 1i64, c: p }, D { v: 10i64, c: p }][1].get();
    }
    let got: i64 = unsafe { n };
    unsafe { printf("k=%ld n=%ld\n".as_ptr(), k, got); }
    if k != 10i64 { return 3i32; }
    if got != 11i64 { return 1i32; }
    return 0i32;
}
