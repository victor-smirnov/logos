// hand battery: round 0915_shadowland, program d01 — caught: refused on base AND under shslotev (`let x = x` move recorded after define); reads Rust on the landing
// legality: by reading, no rustc binary
package bc_0915_shadowland_hb_d01_admit;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
fn eatd(x: D) -> i64 { return x.v; }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        let x: D = D { v: 1i64, c: p };
        let x: D = x;
        if x.v != 1i64 { return 9i32; }
    }
    if rd(p) != 1i64 { return 1i32; }
    return 0i32;
}
