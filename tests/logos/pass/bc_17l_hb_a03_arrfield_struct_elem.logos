// hand battery: round 2026-09-17k (landed by 2026-09-17l), program a03_arrfield_struct_elem — caught: refuted the fatslarr arm 17k priced: that arm regressed this legal program 7 to 2
// legality MEASURED with rustc 1.98.1 --edition 2024. Owed by round 17k, which measured it and could not land it.
package bc_17l_hb_a03_arrfield_struct_elem;
extern fn printf(fmt: *const u8, ...) -> i32;
struct P { v: i64 }
struct R { a: [P; 2], y: i64 }
fn main() -> i32 {
    let r: R = R { a: [P { v: 5i64 }, P { v: 6i64 }], y: 1i64 };
    let mut out: i64 = 0i64;
    match r {
        R { a: v, y } => { out = v[1].v + y; }
    }
    unsafe { printf("got=%ld\n".as_ptr(), out); }
    if out != 7i64 { return 1i32; }
    return 0i32;
}
