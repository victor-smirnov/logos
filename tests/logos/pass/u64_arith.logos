package u64_arith;
// u64 and u32 arithmetic, downcast to i32
fn main() -> i32 {
    let a: u64 = 1000000000000;
    let b: u64 = a + 1;
    let delta: i32 = (b - a) as i32;  // 1
    let x: u32 = 41;
    return delta + x as i32;           // 42
}
