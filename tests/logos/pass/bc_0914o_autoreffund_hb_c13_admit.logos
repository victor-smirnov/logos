// hand battery: round 2026-09-14o-autoreffund, program c13 — caught: `let ref y = <rvalue>` in a for-loop body leaked every iteration on base (222); 262626 under the landing
// legality: by reading, no rustc binary
package bc_0914o_autoreffund_hb_c13_admit;
use logos.lang.cmp;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, s: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.s = *self.s * 10i64 + self.v + 4i64; } } }
impl Eq for D { fn eq(&self, other: &D) -> bool { return self.v > 0i64 && other.v > 0i64; } }
fn mk(v: i64, s: *mut i64) -> D {
    unsafe { *s = *s * 10i64 + v; }
    return D { v: v, s: s };
}
fn side(s: *mut i64) -> i64 {
    unsafe { *s = *s * 10i64 + 1i64; }
    return 1i64;
}
fn pick<'a>(d: &'a D, s: *mut i64) -> &'a D {
    unsafe { *s = *s * 10i64 + 1i64; }
    return d;
}
fn rd(s: *mut i64) -> i64 { return unsafe { *s }; }

fn main() -> i32 {
    let mut q: i64 = 0i64;
    let s: *mut i64 = &mut q;
    for i in 0i64..3i64 {
        let ref y = mk(2i64, s);
        if y.v + i < 2i64 { return 3i32; }
    }
    let got: i64 = rd(s);
    unsafe { printf("seq=%ld\n".as_ptr(), got); }
    if got != 262626i64 { return 1i32; }
    return 0i32;
}
