// <Varchar[,AnyVal]>{...} is the ObjectMap spelled with a key type (writ).
package writ_typed_map_default_materialize;
use logos.lang.writ.container;
use logos.lang.writ.anyval;
use logos.lang.writ.wmap;
use logos.lang.writ.wstring;
use logos.mem.writ.parser;
use logos.lang.rc;
use logos.lang.str;

unsafe fn main() -> i32 {
    let rc: Rc<Writ> = writ_rc(8192);
    let h: &Writ = rc.deref();

    let m1: WAny = parse(h, "<Varchar>{\"a\":1,\"b\":2}");
    if !m1.is_map() { return 1; }
    let mm1: &WMap<WString, WAny> = &*(m1.resolve() as *const WMap<WString, WAny>);
    if mm1.get("a").as_i64() != 1i64 { return 2; }
    if mm1.get("b").as_i64() != 2i64 { return 3; }

    let m2: WAny = parse(h, "<Varchar,AnyVal>{\"x\":7}");
    if !m2.is_map() { return 4; }
    let mm2: &WMap<WString, WAny> = &*(m2.resolve() as *const WMap<WString, WAny>);
    if mm2.get("x").as_i64() != 7i64 { return 5; }
    return 0;
}
