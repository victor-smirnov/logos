// hand battery: round 0915b_refeqland, program f53 — caught: verdict moved base (wrong answer at run time) -> refeq landing (Rust's answer)
// legality: by reading, no rustc binary
package bc_0915b_refeqland_hb_f53_admit;
use logos.lang.cmp;
struct D { v: i64 }
impl Eq for D {
    fn eq(&self, other: &D) -> bool {
        return self.v == other.v;
    }
}
fn main() -> i32 {
    let a: D = D { v: 4i64 };
    let b: D = D { v: 4i64 };
    let ra: &D = &a;
    let rb: &D = &b;
    let f = move || -> bool { return ra == rb; };
    if !f() { return 1i32; }
    return 0i32;
}
