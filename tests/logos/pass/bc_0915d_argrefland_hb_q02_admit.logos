// hand battery: round 2026-09-15d-argrefland, program q02 — caught: verdict moved base b96b980fb (cc=1 run=- vg=-) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_q02_admit;
struct P { x: i32, y: i32 }
fn main() -> i32 {
    let p = P { x: 5i32, y: 6i32 };
    let r: &P = &p;
    let pp: &&P = &r;
    let s: i32 = match pp { &&P { x, y } => x * 10i32 + y };
    if s != 56i32 { return 2i32; }
    return 0i32;
}
