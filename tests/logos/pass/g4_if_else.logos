package g4_if_else;

fn classify(x: i32) -> i32 {
    if x < 0 {
        return 0 - 1;
    } else if x == 0 {
        return 0;
    } else {
        return 1;
    }
}

fn main() -> i32 {
    let pos: i32 = classify(5);
    let neg: i32 = classify(0 - 3);
    return (pos - neg) * 21;
}
