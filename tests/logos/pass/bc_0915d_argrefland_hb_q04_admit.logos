// hand battery: round 2026-09-15d-argrefland, program q04 — caught: verdict moved base b96b980fb (cc=0 run=4 vg=0) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_q04_admit;
fn main() -> i32 {
    let o: Option<i32> = Option::Some(5i32);
    let r: &Option<i32> = &o;
    let pp: &&Option<i32> = &r;
    let mut got: i32 = 0i32;
    match pp {
        &&Option::Some(x) => { got = x; }
        &&Option::None => { got = 77i32; }
    }
    if got != 5i32 { return 4i32; }
    return 0i32;
}
