// B64: `&T` is covariant in T (and in lifetime). Shortening lifetimes
// through stacked shared refs is legal — Rust accepts; Logos accepts.
package variance_ref_covariant;

fn shorten<'a, 'b>(p: &'b &'static i32) -> &'b &'a i32 { return p; }

fn main() -> i32 { return 0; }
