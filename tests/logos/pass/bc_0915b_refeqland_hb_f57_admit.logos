// hand battery: round 0915b_refeqland, program f57 — caught: verdict moved base (refused: 'arith.cmpi' op operand #0 must be signless-integer-like) -> refeq landing (runs, Rust's answer)
// legality: by reading, no rustc binary
package bc_0915b_refeqland_hb_f57_admit;
use logos.lang.cmp;
fn same(a: &(i64, i64), b: &(i64, i64)) -> bool {
    return a <= b && a >= b;
}
fn main() -> i32 {
    let x: (i64, i64) = (2i64, 3i64);
    let y: (i64, i64) = (2i64, 3i64);
    let z: (i64, i64) = (2i64, 4i64);
    if !same(&x, &y) { return 1i32; }
    if same(&x, &z) { return 2i32; }
    if !(&x < &z) { return 3i32; }
    return 0i32;
}
