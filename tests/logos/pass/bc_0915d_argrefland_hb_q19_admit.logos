// hand battery: round 2026-09-15d-argrefland, program q19 — caught: verdict moved base b96b980fb (cc=0 run=19 vg=0) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_q19_admit;
struct P { x: i32, y: i32 }
impl Copy for P {}
impl Clone for P { fn clone(self: &Self) -> P { return P { x: self.x, y: self.y }; } }
fn main() -> i32 {
    let p = P { x: 5i32, y: 6i32 };
    let r: &P = &p;
    let pp: &&P = &r;
    match pp { &&q => { if q.y != 6i32 { return 19i32; } } }
    match pp { &q => { if q.y != 6i32 { return 20i32; } } }
    return 0i32;
}
