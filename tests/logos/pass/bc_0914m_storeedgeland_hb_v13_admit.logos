// hand battery: round 2026-09-14m-storeedge-land, program v13 — caught: legal, refused on base (`y = 20` after a read, the Vec dead), compiles and runs under the landing
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (ACCEPTED, exit:0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0914m_storeedgeland_hb_v13_admit.rs
package bc_0914m_storeedgeland_hb_v13_admit;
fn main() -> i32 {
    let mut x: i64 = 3i64;
    let mut y: i64 = 4i64;
    let mut buffer: Vec<&i64> = Vec::new();
    buffer.push(&x);
    buffer.push(&y);
    let s: i64 = *buffer[0u64] + *buffer[1u64];
    x = 10i64;
    y = 20i64;
    return (s + x + y) as i32 - 37i32;
}
