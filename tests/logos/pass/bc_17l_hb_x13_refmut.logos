// hand battery: round 2026-09-17l, program x13_refmut — caught: refuted the spill-a-copy arm the pricing round recommended: a ref mut element binder over an array place must write THROUGH to s.arr (rustc src=99)
// legality MEASURED with rustc 1.98.1 --edition 2024 (twin beside the round record).
package bc_17l_hb_x13_refmut;
extern fn printf(fmt: *const u8, ...) -> i32;
struct S { arr: [i64; 2], tag: i64 }
fn main() -> i32 {
    let mut s: S = S { arr: [1i64, 2i64], tag: 7i64 };
    match s.arr {
        [_, ref mut y] => { *y = 99i64; }
    }
    let src: i64 = s.arr[1];
    unsafe { printf("src=%ld\n".as_ptr(), src); }
    if src != 99i64 { return 1i32; }
    return 0i32;
}
