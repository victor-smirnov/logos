// hand battery: round 2026-09-14k-fnptrbinderland, program c15 — caught: legal, refused on base (for<'p> into for<'q>, alpha-renamed) and with fnptrnohrtb; admitted by the landing
// legality: by reading, no rustc binary
package bc_0914k_fnptrbinderland_hb_c15_admit;
fn id<'k>(x: &'k i64) -> &'k i64 { return x; }
fn main() -> i32 {
    let f: for<'p> fn(&'p i64) -> &'p i64 = id;
    let g: for<'q> fn(&'q i64) -> &'q i64 = f;
    let v: i64 = 15i64;
    return *g(&v) as i32;
}
