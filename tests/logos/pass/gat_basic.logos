package gat_basic;

trait Wrapper {
    type Inner<T>;
}
struct Box<V> { val: V, }
impl<V> Wrapper for Box<V> {
    type Inner<T> = T;
}
fn identity(x: Box<i32>::Inner<i32>) -> i32 { return x; }
unsafe fn main() -> i32 {
    return identity(42);
}
