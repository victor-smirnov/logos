package cmp_negative;
// F03: comparisons with negative numbers
fn abs(x: i32) -> i32 {
    if x < 0 { return -x; }
    return x;
}
fn main() -> i32 {
    let a: i32 = abs(-42);   // 42
    let b: i32 = abs(42);    // 42
    let ok: bool = a == b;
    if ok { return a; }
    return 0;
}
