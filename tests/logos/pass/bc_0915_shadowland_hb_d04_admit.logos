// hand battery: round 0915_shadowland, program d04 — caught: REFUTED the priced shslotev: exit 0 on base, exit 1 under it (closure drop group keyed by the old name)
// legality: by reading, no rustc binary
package bc_0915_shadowland_hb_d04_admit;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
fn eatd(x: D) -> i64 { return x.v; }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        let x: D = D { v: 1i64, c: p };
        let f = move || -> i64 { return x.v; };
        let y: D = D { v: 2i64, c: p };
        let f: i64 = 3i64 + y.v;
        if f != 5i64 { return 9i32; }
    }
    if rd(p) != 21i64 { return 1i32; }
    return 0i32;
}
