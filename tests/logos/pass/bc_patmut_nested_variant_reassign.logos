// `Some(Some(mut n))` — the nested-variant let-else re-extraction (emit_nested_variant_lets) is the fifth door of the same fact — then `n = n + 10`: legal, runs. ONE TOKEN from fail/bc_patmut_nested_variant_no_mut_reassign_fail. PROBES.md 2026-09-06b: the by-value pattern `mut` is carried into the LIR for match/if-let/while-let/for bindings.
package bc_patmut_nested_variant_reassign;
fn main() -> i32 {
    let o: Option<Option<i64>> = Option::Some(Option::Some(1i64));
    let mut out: i64 = 0i64;
    match o {
        Option::Some(Option::Some(mut n)) => { n = n + 10i64; out = n; },
        Option::Some(Option::None) => { out = -1i64; },
        Option::None => { out = -2i64; }
    }
    if out != 11i64 { return 1i32; }
    return 0i32;
}
