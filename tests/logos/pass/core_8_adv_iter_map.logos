package i04;
fn main() -> i32 {
    let mut v: Vec<i32> = Vec::<i32>::new();
    v.push(1i32); v.push(2i32); v.push(3i32);
    let mut it = v.iter().map(|x| *x * 2i32);
    let n: Option<i32> = it.next();
    if n.unwrap() != 2i32 { return 1i32; }
    return 0i32;
}
