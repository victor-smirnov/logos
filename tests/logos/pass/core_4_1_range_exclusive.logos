// logos-core §4 — half-open range pattern `a..b` matches [a, b).
package core_4_1_range_exclusive;
fn main() -> i32 {
    let x: i32 = 7i32;
    let r: i32 = match x {
        1i32..10i32 => 100i32,
        _ => 200i32,
    };
    if r != 100i32 { return 1i32; }
    return 0i32;
}
