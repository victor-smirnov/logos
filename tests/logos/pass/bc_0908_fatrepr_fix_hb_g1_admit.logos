// hand battery: round 2026-09-08-fatrepr-fix, program G1 — caught: verdict moved base -> landed — RESULT.md "CLOSED (REFUSED -> rc 0, compiled + linked + RUN, answer checked): G1 ... G8"
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: DIVERGENCE (REFUSED, error[E0106]); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0908_fatrepr_fix_hb_g1_admit.rs
// ⚠ THE AUDIT RECORDS THIS FIXTURE AS RESTING ON A BLESSED DIVERGENCE, NOT AS WRONG — rustc refuses the literal twin and the canonising row/clause is named in docs/rustc-fixture-audit-2026-09.md §bc_0908_fatrepr_fix_hb_g1_admit. The assertion is UNCHANGED: a corpus decision with an owner.
// harvested 2026-09-14h from src/compiler/probes/2026-09-08-fatrepr-fix/hand/G1.logos
// G1 LEGAL — GENERIC enum: the payload type is a substituted `&[T]`, so the
// unsize is decided AFTER subst_type_sema. A shape the pricing round did not use.
package bc_0908_fatrepr_fix_hb_g1_admit;
enum E<'a, T> { N, S(&'a [T]) }
fn main() -> i32 {
    let a: [i64; 3] = [7i64, 8i64, 9i64];
    let e: E<i64> = E::S(&a);
    match e {
        E::N => { return 1i32; }
        E::S(s) => { if s[0] != 7i64 { return 10i32; } if s[2] != 9i64 { return 11i32; } }
    }
    return 0i32;
}
