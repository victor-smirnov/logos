// hand battery: round 2026-09-08-fatrepr-fix, program D8 — caught: verdict moved base -> landed — RESULT.md "CLOSED (REFUSED -> rc 0, RUN): D1 inherent method · D5 TRAIT method · D8 ... · D2 closure -> fn ptr"
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: DIVERGENCE (REFUSED, error[E0592]); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0908_fatrepr_fix_hb_d8_admit.rs
// ⚠ THE AUDIT RECORDS THIS FIXTURE AS RESTING ON A BLESSED DIVERGENCE, NOT AS WRONG — rustc refuses the literal twin and the canonising row/clause is named in docs/rustc-fixture-audit-2026-09.md §bc_0908_fatrepr_fix_hb_d8_admit. The assertion is UNCHANGED: a corpus decision with an owner.
// harvested 2026-09-14h from src/compiler/probes/2026-09-08-fatrepr-fix/hand/D8.logos
// D8 — the wrapper unsize at an OVERLOADED method name: two candidates, one
// `Rc<dyn Sp>` and one `i64`. A selector that admits too much picks wrong.
package bc_0908_fatrepr_fix_hb_d8_admit;
use logos.lang.rc;
trait Sp { fn v(&self) -> i64; }
struct A { x: i64 }
impl Sp for A { fn v(&self) -> i64 { return self.x; } }
struct H { n: i64 }
impl H {
    pub fn eat(&self, k: i64) -> i64 { return k * 2i64; }
    pub fn eat(&self, r: Rc<dyn Sp>) -> i64 { return r.v() + self.n; }
}
fn main() -> i32 {
    let h: H = H { n: 1i64 };
    let rc: Rc<A> = rc_new(A { x: 17i64 });
    if h.eat(rc) != 18i64 { return 10i32; }
    if h.eat(5i64) != 10i64 { return 11i32; }
    return 0i32;
}
