// hand battery: round 0915a_refeq, program e05 — caught: verdict moved base (wrong answer at run time, reference pair compared by address) -> refeq landing (Rust's answer)
// legality: by reading, no rustc binary
package bc_0915a_refeq_hb_e05_admit;
use logos.lang.cmp;
struct D { v: i64 }
impl Eq for D {
    fn eq(&self, other: &D) -> bool {
        return self.v == other.v;
    }
}
fn main() -> i32 {
    let mut a: D = D { v: 4i64 };
    let mut b: D = D { v: 4i64 };
    let ra: &mut D = &mut a;
    let rb: &mut D = &mut b;
    if ra == rb {
        return 0i32;
    }
    return 1i32;
}
