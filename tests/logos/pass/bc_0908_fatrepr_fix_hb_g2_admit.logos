// hand battery: round 2026-09-08-fatrepr-fix, program G2 — caught: verdict moved base -> landed — RESULT.md "CLOSED (REFUSED -> rc 0, compiled + linked + RUN, answer checked): G1 ... G8"
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: DIVERGENCE (REFUSED, error[E0106]); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0908_fatrepr_fix_hb_g2_admit.rs
// ⚠ THE AUDIT RECORDS THIS FIXTURE AS RESTING ON A BLESSED DIVERGENCE, NOT AS WRONG — rustc refuses the literal twin and the canonising row/clause is named in docs/rustc-fixture-audit-2026-09.md §bc_0908_fatrepr_fix_hb_g2_admit. The assertion is UNCHANGED: a corpus decision with an owner.
// harvested 2026-09-14h from src/compiler/probes/2026-09-08-fatrepr-fix/hand/G2.logos
// G2 LEGAL — the MUTABLE unsize `&mut [i64;3]` -> `&mut [i64]` in a payload,
// written THROUGH the binder and read back from the array afterwards.
package bc_0908_fatrepr_fix_hb_g2_admit;
enum E<'a> { N, S(&'a mut [i64]) }
fn main() -> i32 {
    let mut a: [i64; 3] = [1i64, 2i64, 3i64];
    let e: E = E::S(&mut a);
    match e {
        E::N => { return 1i32; }
        E::S(s) => { s[1] = 42i64; }
    }
    if a[1] != 42i64 { return 10i32; }
    return 0i32;
}
