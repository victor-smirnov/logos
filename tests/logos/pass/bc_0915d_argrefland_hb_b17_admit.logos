// hand battery: round 2026-09-15c-argref (re-run on this landing), program b17 — caught: verdict moved base b96b980fb (cc=0 run=139 vg=1) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_b17_admit;
struct D { v: i64 }
fn pick<'a, 'b>(a: &'b &'a D) -> &'b D {
    return *a;
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    let rd: &D = &d;
    let back: &D = pick(&rd);
    if back.v != 6i64 { return 17i32; }
    return 0i32;
}
