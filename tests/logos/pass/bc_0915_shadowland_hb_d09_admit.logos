// hand battery: round 0915_shadowland, program d09 — caught: verdict moved base -> shadowslot landing
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915_shadowland_hb_d09_admit.rs
package bc_0915_shadowland_hb_d09_admit;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
fn eatd(x: D) -> i64 { return x.v; }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
fn g(p: *mut i64, a: bool, b: bool) {
    let x: D = D { v: 1i64, c: p };
    if a { let _k: i64 = eatd(x); }
    let x: D = D { v: 2i64, c: p };
    if b { let _j: i64 = eatd(x); }
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    g(p, true, false);
    if rd(p) != 12i64 { return 1i32; }
    unsafe { *p = 0i64; }
    g(p, false, true);
    if rd(p) != 21i64 { return 2i32; }
    return 0i32;
}
