// format() — std.fmt (variadic generics + Format trait)
package format_basic;

use std.fmt;
use std.io;
use std.mem;

fn main() -> i32 {
    let s: *mut u8 = format::<i32, i32>("n={}, m={}", 40, 2);
    println(s as *const u8);
    dealloc(s);
    return 42;
}
