// hand battery: round 2026-09-17l, program c01_matchexpr — caught: the match-as-expression door's by-value carrier, the control that separates c02's miscompile from a plain refusal
// legality MEASURED with rustc 1.98.1 --edition 2024 (twin beside the round record).
package bc_17l_hb_c01_matchexpr;
extern fn printf(fmt: *const u8, ...) -> i32;
struct S { arr: [i64; 2], tag: i64 }
fn main() -> i32 {
    let s: S = S { arr: [1i64, 2i64], tag: 7i64 };
    let out: i64 = match s.arr {
        [_, y] => y
    };
    unsafe { printf("got=%ld\n".as_ptr(), out); }
    if out != 2i64 { return 1i32; }
    return 0i32;
}
