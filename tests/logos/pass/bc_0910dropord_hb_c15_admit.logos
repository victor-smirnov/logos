// hand battery: round 2026-09-10dropord, program c15 — caught: verdict moved base -> landed — PROBES.md "c1 struct, 3 fields 321 -> 123 ... c17 nested struct + user Drop 3n921 -> n9123" (drop order, landed 1fad68657)
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (ACCEPTED, exit:0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen4/bc_0910dropord_hb_c15_admit.rs
// harvested 2026-09-14h from snapshot hand-harvest-2026-09-14/25aa8421-fce1-4a11-8a89-5d2ba5981c88/dropord/cex/c15.logos
package bc_0910dropord_hb_c15_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
fn p(fmt: str, v: i32) { unsafe { printf(fmt.as_ptr(), v); } }
struct O { id: i32 }
impl Drop for O { fn drop(self: &mut O) { p("%d", self.id); } }
struct S2 { a: O, b: O }
trait Tr { fn go(self: &Self) -> i32; }
impl Tr for S2 { fn go(self: &S2) -> i32 { return 1; } }
fn main() -> i32 {
    {
        let d: Box<dyn Tr> = Box::new(S2 { a: O { id: 1 }, b: O { id: 2 } });
        p("[%d]", d.go());
    }
    unsafe { printf("\n".as_ptr()); }
    return 0;
}
