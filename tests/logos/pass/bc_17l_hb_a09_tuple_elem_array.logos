// hand battery: round 2026-09-17k (landed by 2026-09-17l), program a09_tuple_elem_array — caught: the TUPLE-ELEMENT spelling of the same place scrutinee (match t.0), a second carrier the row header names
// legality MEASURED with rustc 1.98.1 --edition 2024. Owed by round 17k, which measured it and could not land it.
package bc_17l_hb_a09_tuple_elem_array;
extern fn printf(fmt: *const u8, ...) -> i32;
fn main() -> i32 {
    let t: ([i64; 2], i64) = ([1i64, 2i64], 7i64);
    let mut out: i64 = 0i64;
    match t.0 {
        [_, y] => { out = y; }
    }
    unsafe { printf("got=%ld\n".as_ptr(), out); }
    if out != 2i64 { return 1i32; }
    return 0i32;
}
