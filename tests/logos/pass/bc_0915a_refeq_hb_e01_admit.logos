// hand battery: round 0915a_refeq, program e01 — caught: verdict moved base (wrong answer at run time, reference pair compared by address) -> refeq landing (Rust's answer)
// legality: by reading, no rustc binary
package bc_0915a_refeq_hb_e01_admit;
use logos.lang.cmp;
struct D { v: i64 }
impl Eq for D {
    fn eq(&self, other: &D) -> bool {
        return self.v == other.v;
    }
}
fn main() -> i32 {
    let a: i64 = 4i64;
    let b: i64 = 4i64;
    let mut ra: &i64 = &a;
    let mut rb: &i64 = &b;
    let x: &mut &i64 = &mut ra;
    let y: &mut &i64 = &mut rb;
    if x == y {
        return 0i32;
    }
    return 1i32;
}
