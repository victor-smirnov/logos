// hand battery: round 2026-09-14e-thruref, program t3 — caught: verdict moved base -> landed — PROBES.md "asgref (7 moved, all LEGAL, all RUN): m1 m2 m5 t1 t2 t3"; landed 2026-09-14f, measured base REFUSED -> today RUN
// legality: by reading, no rustc binary
// harvested 2026-09-14i from snapshot hand-harvest-2026-09-14b/walk.ek13/t3_tuple_door_reassign_refvar.logos
package bc_0914e_thruref_hb_t3_admit;
fn main() -> i32 {
    let s1: (i64, i64) = (1i64, 2i64);
    let s2: (i64, i64) = (3i64, 4i64);
    let mut r: &(i64, i64) = &s1;
    let mut t: i64 = 0i64;
    match r {
        (a, b) => {
            r = &s2;
            t = *a + r.0;
        }
    }
    return (t - 4i64) as i32;
}
