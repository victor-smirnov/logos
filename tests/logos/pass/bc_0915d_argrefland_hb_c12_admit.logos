// hand battery: round 2026-09-15c-argref (re-run on this landing), program c12 — caught: verdict moved base b96b980fb (cc=0 run=12 vg=0) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_c12_admit;
struct D { v: i64 }
fn inner(a: &&D) -> *const D {
    let x: &D = *a;
    return x as *const D;
}
fn apply<F: Fn(&D) -> bool>(f: F, d: &D) -> bool { return f(d); }
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    let want: *const D = &d as *const D;
    if !apply(|rd: &D| -> bool { return inner(&rd) == want; }, &d) { return 12i32; }
    return 0i32;
}
