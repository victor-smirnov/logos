// hand battery: round 0915a_refeq, program e29 — caught: verdict moved base (wrong answer at run time, reference pair compared by address) -> refeq landing (Rust's answer)
// legality: by reading, no rustc binary
package bc_0915a_refeq_hb_e29_admit;
use logos.lang.cmp;
struct D { v: i64 }
impl Eq for D {
    fn eq(&self, other: &D) -> bool {
        return self.v == other.v;
    }
}
impl D {
    fn same(&self, o: &D) -> bool {
        return self == o;
    }
}
fn main() -> i32 {
    let a: D = D { v: 4i64 };
    let b: D = D { v: 4i64 };
    if a.same(&b) {
        return 0i32;
    }
    return 1i32;
}
