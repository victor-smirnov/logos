// hand battery: round 2026-09-16f-atbind, program c01 — caught: FOUND THE THIRD SITE: a PLAIN binder, no `@` at all, in EXPRESSION position (139 -> 0) — its statement twin was already correct
// legality: MEASURED vs rustc 1.98.1 --edition 2024 (twin beside the round record); accepted, exit 0.
package bc_0916f_atbind_hb_c01_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
struct W { a: D, b: i64 }
fn g(p: *mut i64) -> i64 {
    let w: W = W { a: D { v: 2i64, c: p }, b: 3i64 };
    let k: i64 = match w { y => y.b };
    return k;
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let k: i64 = g(p);
    unsafe { printf("k=%ld n=%ld\n".as_ptr(), k, rd(p)); }
    if k != 3i64 { return 2i32; }
    if rd(p) != 2i64 { return 1i32; }
    return 0i32;
}
