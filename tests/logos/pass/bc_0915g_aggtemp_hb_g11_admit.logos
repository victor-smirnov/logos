// hand battery: round 2026-09-15g-aggtemp (landing), program g11 — caught: a generic one-element array literal double-dropped its element (n 2 -> 1)
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/battery-land/rust/g11.rs: compiles, runs, stdout `n=1`
package bc_0915g_aggtemp_hb_g11_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
fn one<T>(x: T) -> [T; 1] { return [x]; }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        let arr: [D; 1] = one(D { v: 1i64, c: p });
        if arr[0].v != 1i64 { return 3i32; }
    }
    let got: i64 = unsafe { n };
    unsafe { printf("n=%ld\n".as_ptr(), got); }
    if got != 1i64 { return 1i32; }
    return 0i32;
}
