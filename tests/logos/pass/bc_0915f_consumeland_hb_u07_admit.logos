// hand battery: round 2026-09-15f-consumeland, program u07 — caught: REFUTED the pricing's recommended arm: right on base and on the landing, but LEAKED its whole array (n -> 0) under the first candidate b3b9ae03 that marked array-literal elements moved at SemaChecker::lower_arr_lit / lower_arr_fill_lit — right on base only by cancellation (row aggregate_literal_temp_place_base_never_dropped_run)
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15f-consumeland/rust/u07_fill1_temp_index.rs: compiles, runs, stdout `r=1 n=1`
package bc_0915f_consumeland_hb_u07_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
struct W { x: D, y: D, z: i64 }
impl Add for D {
    type Output = D;
    fn add(self: D, o: D) -> D {
        return D { v: 100i64, c: self.c };
    }
}
impl Neg for D {
    type Output = D;
    fn neg(self: D) -> D {
        return D { v: 1000i64, c: self.c };
    }
}
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
fn g(p: *mut i64) -> i64 {
    let a: D = D { v: 1i64, c: p };
    let k: i64 = [a; 1][0].v;
    return k;
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let r: i64 = g(p);
    let got: i64 = rd(p);
    unsafe { printf("r=%ld n=%ld\n".as_ptr(), r, got); }
    return 0i32;
}
