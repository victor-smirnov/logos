// `mut xs @ ..` parses and binds a mutable sub-array (rustc 1.98.1: exit 0).
package let_array_named_rest_mut;
fn main() -> i32 {
    let [mut m @ .., _y] = [7i64, 8, 9];
    m[1] = m[1] + 10;
    return (m[0] + m[1] - 25) as i32;
}
