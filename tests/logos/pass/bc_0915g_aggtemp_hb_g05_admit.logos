// hand battery: round 2026-09-15g-aggtemp (landing), program g05 — caught: a call result's TUPLE INDEX was REFUSED by the backend (GEP on an SSA aggregate) and now compiles and runs (k=10 n=11)
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/battery-land/rust/g05.rs: compiles, runs, stdout `k=10 n=11`
package bc_0915g_aggtemp_hb_g05_admit;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
fn mktup(a: D, b: D) -> (D, D) { return (a, b); }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let mut k: i64 = 0i64;
    {
        k = mktup(D { v: 1i64, c: p }, D { v: 10i64, c: p }).1.v;
    }
    let got: i64 = unsafe { n };
    unsafe { printf("k=%ld n=%ld\n".as_ptr(), k, got); }
    if k != 10i64 { return 3i32; }
    if got != 11i64 { return 1i32; }
    return 0i32;
}
