// hand battery: round 0915a_refeq, program e31 — caught: verdict moved base (wrong answer at run time, reference pair compared by address) -> refeq landing (Rust's answer)
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (accepts, exit 0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard2-hb-late/bc_0915a_refeq_hb_e31_admit.rs
package bc_0915a_refeq_hb_e31_admit;
use logos.lang.cmp;
fn same<T: Eq>(a: &T, b: &T) -> bool {
    return a == b;
}
fn main() -> i32 {
    let a: char = 'k';
    let b: char = 'k';
    if !same::<char>(&a, &b) {
        return 1i32;
    }
    return 0i32;
}
