// hand battery: round 2026-09-14o-autoreffund, program h34 — caught: (round 2026-09-14n) `String::from(..) == "…"`: 34 bytes definitely lost on base (valgrind), 0 errors under the landing
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (ACCEPTED, exit:0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0914o_autoreffund_hb_h34_admit.rs
package bc_0914o_autoreffund_hb_h34_admit;
use logos.lang.cmp;
use logos.mem.string;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
impl Eq for D { fn eq(&self, other: &D) -> bool { return self.v > 0i64 && other.v > 0i64; } }

fn body(p: *mut i64) -> i64 {
    let a: D = D { v: 1001i64, c: p };
    if !(String::from("hello-world-long") == "hello-world-long") { return 3i64; }
    return 0i64;
}

fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let r: i64 = body(p);
    let got: i64 = unsafe { n };
    unsafe { printf("n=%ld r=%ld\n".as_ptr(), got, r); }
    if r != 0i64 { return 2i32; }
    if got != 1001i64 { return 1i32; }
    return 0i32;
}
