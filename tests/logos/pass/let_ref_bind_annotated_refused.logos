// Compiles and runs as in rustc 1.98.1 (squeue row of the same name).
package let_ref_bind_annotated_refused;
fn main() -> i32 {
    let x: i64 = 7i64;
    let ref y: i64 = x;
    let ref z: i64 = 5i64;
    if *y + *z != 12i64 { return 1i32; }
    return 0i32;
}
