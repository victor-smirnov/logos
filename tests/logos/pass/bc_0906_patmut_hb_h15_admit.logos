// hand battery: round 2026-09-06-patmut, program h15 — caught: verdict moved base -> landed — PROBES.md "LEGAL, base REFUSED -> whole RUNS rc 0 (18)" + PROBES.md "The pricing's 42 hand shapes re-run on both builds: identical"
// legality: by reading, no rustc binary
// harvested 2026-09-14h from src/compiler/probes/2026-09-06-patmut/hand/h15_match_or_mut.logos
package bc_0906_patmut_hb_h15_admit;
struct S { v: i64 }
impl S { fn get(&mut self) -> &mut i64 { return &mut self.v; } }
enum E { A(S), C(S), B }
fn main() -> i32 {
    let e: E = E::C(S { v: 1i64 });
    match e {
        E::A(mut s) | E::C(mut s) => { let r: &mut i64 = s.get(); *r = 8i64; if s.v != 8i64 { return 1i32; } },
        E::B => {}
    }
    return 0i32;
}
