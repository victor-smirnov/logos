// hand battery: round 2026-09-16d-landrulings, program q12 — caught: an arm that RETURNS EARLY out of the match double-dropped (base 'D2 D2')
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: ACCEPTED, exit 0, stdout 'D2' — twin src/compiler/probes/2026-09-16d-landrulings/rust/q12.rs
package bc_0916d_dbmstruct_hb_q12_admit;
use logos.std.fmt;
struct S { pub n: i64 }
impl Drop for S { fn drop(self: &mut S) { println!("D{}", self.n); } }
enum E { V { f: S }, Z }
fn run() -> i64 {
    let p: E = E::V { f: S { n: 2i64 } };
    match &p {
        E::V { f } => { return f.n; },
        E::Z => {}
    }
    return 0i64;
}
fn main() -> i32 {
    if run() != 2i64 { return 1i32; }
    return 0i32;
}
