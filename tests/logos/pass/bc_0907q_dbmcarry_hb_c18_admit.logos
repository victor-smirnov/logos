// hand battery: round 2026-09-07q-dbmcarry, program c18 — caught: verdict moved base -> landed, predicted wrong — PROBES.md "REFUSED -> 1 (one, a GAIN NOT PREDICTED): c18 ... refused ... on base, compiles and writes through the scrutinee now" | (c) PREDICTION.m...
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (ACCEPTED, exit:0); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard1-hb-early/gen3/bc_0907q_dbmcarry_hb_c18_admit.rs
// harvested 2026-09-14h from src/compiler/probes/2026-09-07q-dbmcarry/hand/c18.logos
package bc_0907q_dbmcarry_hb_c18_admit;
use logos.std.fmt;
struct S { pub n: i64 }
impl Drop for S { fn drop(self: &mut S) { println!("D{}", self.n); } }
fn main() -> i32 {
    let mut p: (Option<S>, i64) = (Option::Some(S { n: 5i64 }), 9i64);
    let mut out: i64 = 0i64;
    match &mut p {
        (Option::Some(a), b) => { a.n = a.n + 1i64; out = a.n + *b; },
        (Option::None, _) => {}
    }
    if out != 15i64 { return 1i32; }
    return 0i32;
}
