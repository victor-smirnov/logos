// hand battery: round 2026-09-15d-argrefland, program h07 — caught: verdict moved base b96b980fb (cc=0 run=139 vg=1) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_h07_admit;
fn inner(a: &&(i64, i64)) -> i64 {
    let x: &(i64, i64) = *a;
    return x.1;
}
fn main() -> i32 {
    let arr: [(i64, i64); 2] = [(1i64, 2i64), (3i64, 4i64)];
    let s: &[(i64, i64)] = &arr;
    let mut sum: i64 = 0i64;
    for x in s {
        sum = sum + inner(&x);
    }
    if sum != 6i64 { return 7i32; }
    return 0i32;
}
