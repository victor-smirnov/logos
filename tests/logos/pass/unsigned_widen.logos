package unsigned_widen;
// Verify u32 -> i64 cast uses zero-extension
fn widen(x: u32) -> i64 { return x as i64; }
fn main() -> i32 {
    let y: i64 = widen(4000000000);
    if y == 4000000000 {
        return 0;
    }
    return 1;
}
