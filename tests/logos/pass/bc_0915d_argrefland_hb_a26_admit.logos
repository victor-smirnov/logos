// hand battery: round 2026-09-15c-argref (re-run on this landing), program a26 — caught: verdict moved base b96b980fb (cc=0 run=139 vg=1) -> landed argrefland (exit 0, valgrind 0 errors: Rust's answer)
// legality: by reading, no rustc binary
package bc_0915d_argrefland_hb_a26_admit;
struct D { v: i64 }
fn inner<T>(a: &T) -> *const T {
    return a as *const T;
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    let rd: &D = &d;
    let slot: *const &D = inner::<&D>(&rd);
    let back: &D = unsafe { *slot };
    if back.v != 6i64 { return 27i32; }
    return 0i32;
}
