// Fail: &[u64] slice cast to <I32>[] — element type mismatch.
package hermes_as_array_elem_mismatch;
use std.hermes.ctr;

unsafe fn main() -> i32 {
    let arr: [u64; 2] = [1, 2];
    let s: &[u64] = &arr;
    let ctr: Hermes = s as <I32>[];
    return 0;
}
