package hermes_map_comp_guard_bad;

use std.hermes.ctr;
use logos.lang.hermes.anyval;

fn main() -> i32 {
    let xs: [i32; 2] = [1, 2];
    let d: Hermes = @{"k": AnyVal::embed_i24(x) for x in xs if x + 1};
    return 0;
}
