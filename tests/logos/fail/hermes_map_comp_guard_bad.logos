package hermes_map_comp_guard_bad;

use logos.lang.hermes.comp_builder;
use logos.lang.hermes.container;
use logos.lang.hermes.anyval;
use logos.lang.rc;

fn main() -> i32 {
    let xs: [i32; 2] = [1, 2];
    let d: Rc<Hermes> = @{"k": x for x in xs if x + 1};
    return 0;
}
