// Fail: as <T>[] with unsupported element type.
package writ_as_array_bad_type;
use logos.lang.writ.typed_arr;
use logos.lang.writ.tmpl;
use logos.lang.writ.container;
use logos.lang.rc;

unsafe fn main() -> i32 {
    let arr: [i32; 2] = [1, 2];
    let s: &[i32] = &arr;
    let ctr: Rc<Writ> = s as <Bool>[];
    return 0;
}
