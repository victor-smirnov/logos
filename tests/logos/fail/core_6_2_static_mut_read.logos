// logos-core §6.2: reading a `static mut` outside `unsafe` is
// rejected — Rust spec `items.static.mut.safety`.
package core_6_2_static_mut_read;

static mut COUNTER: i32 = 0i32;

fn main() -> i32 {
    let v: i32 = COUNTER;
    return v;
}
