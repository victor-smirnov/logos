// hand battery: round 2026-09-15f-consumeland, program i03 — caught: its sentence moved between base and the landing: base borrow_check "use of moved value 'a' (moved on line 27)", landing sema "use of moved variable 'a'" (a conditionally-moved operator operand is now marked moved at SemaChecker::lower_binop)
// legality: rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15f-consumeland/rust/i03_cond_op_then_op.rs: REFUSED error[E0382]: use of moved value: `a`
package bc_0915f_consumeland_hb_i03_refuse;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
struct W { x: D, y: D, z: i64 }
impl Add for D {
    type Output = D;
    fn add(self: D, o: D) -> D {
        return D { v: 100i64, c: self.c };
    }
}
impl Neg for D {
    type Output = D;
    fn neg(self: D) -> D {
        return D { v: 1000i64, c: self.c };
    }
}
impl AddAssign for D {
    fn add_assign(self: &mut D, o: D) {
        self.v = self.v + 0i64;
    }
}
fn eat(d: D) -> i64 { return d.v; }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
fn g(p: *mut i64) -> i64 {
    let a: D = D { v: 1i64, c: p };
    let b: D = D { v: 10i64, c: p };
    if rd(p) == 0i64 {
        let s: D = a + b;
    }
    let t: D = a + b;
    return t.v;
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let r: i64 = g(p);
    let got: i64 = rd(p);
    unsafe { printf("r=%ld n=%ld\n".as_ptr(), r, got); }
    if got != 0i64 { return 1i32; }
    return 0i32;
}
