// Fail: capture a non-capturable type (struct with no coercion).
package hermes_capture_bad_type;
use logos.lang.hermes.typed_arr;
use logos.lang.hermes.tmpl;
use logos.lang.hermes.container;
use logos.lang.rc;

pub struct Point {
    x: i32,
    y: i32,
}

unsafe fn main() -> i32 {
    let p: Point = Point { x: 1, y: 2 };
    let doc: Rc<Hermes> = @{"pt": $p};
    return 0;
}
