// hand battery: round 2026-09-17d-arrlit, program X5 — caught: array REPEAT [&n; 2] — PREDICTED possibly unmoved (separate node kind) and the prediction was REFUTED: this arm refuses it; base ran exit 2.
// legality: MEASURED with rustc 1.98.1 --edition 2024 (REFUSES, error[E0515]).
// Twin: src/compiler/probes/2026-09-17d-arrlit/rust/X5_*.rs
package bc_esc_arrlit_repeat_dangle;
fn pick2<'a>(x: [&'a i64; 2]) -> &'a i64 {
    return x[0u64];
}
fn escape() -> &'static i64 {
    let n: i64 = 2i64;
    return pick2([&n; 2]);
}
fn main() -> i32 {
    return (*escape()) as i32;
}
