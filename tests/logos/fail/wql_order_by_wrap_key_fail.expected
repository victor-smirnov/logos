wql!: `order by`: a key of type `u64` cannot be collected into the key vector the backend emits (`i64`) — a `u64`/`usize` value is normalized to i64 by the EL, and that cast REINTERPRETS rather than converts
