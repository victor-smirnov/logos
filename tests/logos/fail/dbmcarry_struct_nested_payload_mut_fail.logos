// The Rust-2024 modifier rule at the STRUCT door, two pattern levels down.
// PAIR: pass/dbmcarry_struct_nested_payload_drop_once, one token apart (`mut a` -> `a`).
package dbmcarry_struct_nested_payload_mut_fail;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
struct W { pub o: Option<D>, pub k: i64 }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let mut r: i64 = 0i64;
    {
        let w: W = W { o: Option::Some(D { v: 1i64, c: p }), k: 9i64 };
        match &w {
            W { o: Option::Some(mut a), k } => { r = a.v + *k; },
            W { o: Option::None, k: _ } => {}
        }
    }
    if r != 10i64 { return 2i32; }
    unsafe { printf("%ld\n".as_ptr(), n); }
    return 0i32;
}
