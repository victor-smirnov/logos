// hand battery: round 2026-09-17d-arrlit, program X3 — caught: NESTED array literal [[&n]] — proves the merge recurses; base ran exit 6.
// legality: MEASURED with rustc 1.98.1 --edition 2024 (REFUSES, error[E0515]).
// Twin: src/compiler/probes/2026-09-17d-arrlit/rust/X3_*.rs
package bc_esc_arrlit_nested_dangle;
fn pick<'a>(x: [[&'a i64; 1]; 1]) -> &'a i64 {
    return x[0u64][0u64];
}
fn escape() -> &'static i64 {
    let n: i64 = 6i64;
    return pick([[&n]]);
}
fn main() -> i32 {
    return (*escape()) as i32;
}
