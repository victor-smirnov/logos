// hand battery: round 2026-09-16p-tupdoor, program x02 — caught: the ABUSE twin of the widened tuple
// door. Its verdict MOVED between the base binary and the landed one, in the direction that matters.
// An EXPLICIT `&` pattern defeats match ergonomics, so the struct field is bound BY VALUE out of a
// shared reference. rustc 1.98.1 --edition 2024 refuses it E0507 ("cannot move out of `r.0.d` which is
// behind a shared reference"); twin src/compiler/probes/2026-09-16p-tupdoor/rust/x02_move_out_of_ref_pattern.rs.
// ⚠ ON THE BASE BINARY THIS PROGRAM WAS REFUSED ONLY BY ACCIDENT — "undefined variable 'd'", because
// the tuple door bound nothing at all. Adding Struct to that door's whitelist ADMITTED it (measured:
// compiled rc 0), trading an over-refusal for an admitted illegal program. The `byval_` walker in
// bind_pattern_ref's RefPat case gained Tuple and Struct cases in the SAME commit, and the sentence
// pinned below is the one that now refuses it — a real E0507, not the old accident.
// One token separates this from a legal program: `ref d` in place of `d` (rustc says so itself).
package bc_0916p_tupdoor_hb_x02_refuse;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
struct H { d: D }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let mut t: i64 = 0i64;
    let h: H = H { d: D { v: 7i64, c: p } };
    let sv: (H, i64) = (h, 1i64);
    let r: &(H, i64) = &sv;
    match r {
        &(H { d }, k) => { t = d.v + k; }
    }
    if t != 8i64 { return 1i32; }
    return 0i32;
}
