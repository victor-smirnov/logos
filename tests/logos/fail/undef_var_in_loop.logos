package undef_var_in_loop;
// Sema fail: undefined variable used in a loop body
fn main() -> i32 {
    let mut i: i32 = 0;
    while i < 10 {
        i = i + missing_var;  // error: missing_var not defined
    }
    return i;
}
