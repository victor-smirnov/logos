// hand battery: round 2026-09-09k-selfrecv, program h5 — caught: verdict moved base -> landed — selfrecvland/ROUND.md:130 "REFUSED after the arm (10) h1 h2 h5 h7 h9 h10 h13 n9 n10 n12"
// legality: by reading, no rustc binary
// harvested 2026-09-14h from src/compiler/probes/2026-09-09k-selfrecv/hand/h5.logos
// h5: by-value receiver on a GENERIC struct.
package bc_0909k_selfrecv_hb_h5_refuse;
struct G<T> { v: T, c: *mut i64 }
impl<T> Drop for G<T> {
    fn drop(self: G<T>) { unsafe { *self.c = *self.c + 1i64; } }
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    { let g: G<i64> = G { v: 7i64, c: p }; }
    let got: i64 = unsafe { n };
    return got as i32;
}
