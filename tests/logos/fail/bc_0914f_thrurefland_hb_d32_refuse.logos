// hand battery: round 2026-09-14f-thrurefland, program d32 — caught: verdict moved base -> landed — PROBES.md "HAND, UNCERTAIN in the predictions: ... d07 d17 d26 d27 d32 stay refused with the owner's sentence"; measured base first error "cannot a...
// legality: by reading, no rustc binary
// harvested 2026-09-14i from snapshot hand-harvest-2026-09-14b/land14f_d2.978t/d32_closure_captures_field_loan_then_reassign_illegal.logos
package bc_0914f_thrurefland_hb_d32_refuse;
struct P { a: i64, b: i64 }
fn main() -> i32 {
    let mut s1: P = P { a: 1i64, b: 2i64 };
    let mut r: &mut P = &mut s1;
    let a: &mut i64 = &mut r.a;
    r = &mut s1;
    let c = || -> i64 { *a };
    return (c() + r.b) as i32;
}
