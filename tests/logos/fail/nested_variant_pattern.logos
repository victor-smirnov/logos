package nested_variant_pattern;
struct Pair { a: i32, b: i32 }
enum E { Some(Pair), None }
fn main() -> i32 {
    let e: E = E::Some(Pair { a: 5, b: 7 });
    match e {
        E::Some(Pair { a, b }) => { return a + b; }
        E::None => { return 0; }
    }
}
