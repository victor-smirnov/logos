package class_method_arg_overflow;
class Counter {
    val: i32,
    unsafe fn add(self: *mut Counter, n: i32) -> i32 {
        return self.val + n;
    }
}
unsafe fn main() -> i32 {
    let c = new Counter { val: 0 };
    return c.add(10000000000);
}
