// FNPTRBIND pair (2026-09-14k-fnptrbinderland), one token apart from bc_fnptrbind_let_named_as_elided_admit.
// Closes soundness queue fnptr_elided_param_let_from_named_admits: `fn(&i64) -> i64` is `for<'x> fn(&'x i64) -> i64`, and
// `g` accepts only 'a. rustc E0308 (MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (REFUSED, E0308); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard4-fail/twins/bc_fnptrbind_let_named_as_elided_refuse.rs).
package bc_fnptrbind_let_named_as_elided_refuse;
fn test<'a>(g: fn(&'a i64) -> i64) -> i32 {
    let f: fn(&i64) -> i64 = g;
    return 0i32;
}
fn main() -> i32 { return 0i32; }
