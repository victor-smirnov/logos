package struct_unknown_field_init;
// Sema fail: struct literal with unknown field
struct Point { x: i32; y: i32; }
fn main() -> i32 {
    let p: Point = Point { x: 1, y: 2, z: 3 };  // z doesn't exist
    return p.x;
}
