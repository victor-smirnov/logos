// hand battery: round 2026-09-12b-rpitbound, program c5 — caught: verdict moved base -> landed — rpitbound/RESULT.md "c1/c2/c3/c5 illegal, rc 0 before, refused after"
// legality: by reading, no rustc binary
// harvested 2026-09-14h from src/compiler/probes/2026-09-12b-rpitbound/hand/c5.logos
package bc_0912b_rpitbound_hb_c5_refuse;
struct S { base: i64 }
impl S {
    // ILLEGAL: RPIT in a METHOD position, mutating capture.
    fn maker(&self) -> impl Fn() -> i64 {
        let mut n: i64 = self.base;
        return move || -> i64 { n += 1i64; return n; };
    }
}
fn main() -> i32 {
    let s: S = S { base: 3i64 };
    let f = s.maker();
    return f() as i32;
}
