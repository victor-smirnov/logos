// ILLEGAL twin of pass/bc_patmut_for_tuple_pat_reassign (E0384). PROBES.md 2026-09-06b.
package bc_patmut_for_tuple_pat_no_mut_reassign_fail;
fn main() -> i32 {
    let arr: [(i64, i64); 2] = [(1i64, 10i64), (2i64, 20i64)];
    let mut acc: i64 = 0i64;
    for (a, b) in arr {
        a = a + b;
        acc = acc + a;
    }
    if acc != 33i64 { return 1i32; }
    return 0i32;
}
