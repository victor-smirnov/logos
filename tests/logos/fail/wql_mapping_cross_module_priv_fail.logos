// A NON-pub mapping is private to its package: consuming it across the module
// boundary is a named error telling the user the fix.
package test;

use maplib;
use logos.lang.writ.container;
use logos.std.wql.wql;
use logos.std.wql.mapping_item;
use logos.mem.collections.vec;

resource bad = deem!(w: Hidden) { from leaf l select l.v };

fn main() -> i32 { return 0i32; }
