package return_i32_overflow;
// Bug: return 10000000000 from fn -> i32 silently truncated — no error.
// Fix: sema now rejects return values that don't fit in the declared return type.
fn small() -> i32 {
    return 10000000000;
}
fn main() -> i32 {
    return small();
}
