// Without `mut` the rest binder is immutable (rustc 1.98.1: E0594).
package let_array_named_rest_immutable_write_refused;
fn main() -> i32 {
    let [m @ .., _y] = [7i64, 8, 9];
    m[1] = m[1] + 10;
    return (m[0] + m[1] - 25) as i32;
}
