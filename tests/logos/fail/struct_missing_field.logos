package struct_missing_field;
// Sema fail: struct literal with missing field initialization
struct Point { x: i32; y: i32; }
fn main() -> i32 {
    let p: Point = Point { x: 1 };  // y not initialized
    return p.x;
}
