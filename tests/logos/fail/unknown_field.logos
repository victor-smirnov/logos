package unknown_field;
// Sema fail: accessing a field that doesn't exist on a struct
struct Point { x: i32; y: i32; }
fn main() -> i32 {
    let p: Point = Point { x: 1, y: 2 };
    return p.z;  // error: no field 'z' on Point
}
