// PAIR C, fail half — the ARRAY carrier: a whole-array write while the array is
// reserved by an argument-position `&mut`. One token apart from
// pass/bc_argresvassign_array_admit: the assigned NAME. rustc: E0506.
package bc_argresvassign_array_refuse;
fn take(a: &mut [i64; 2], b: i64) -> i64 { return a[0] + b; }
fn main() -> i32 {
    let mut arr: [i64; 2] = [1i64, 2i64];
    let mut arr2: [i64; 2] = [3i64, 4i64];
    let r: i64 = take(&mut arr, { arr = [8i64, 9i64]; 0i64 });
    return r as i32;
}
