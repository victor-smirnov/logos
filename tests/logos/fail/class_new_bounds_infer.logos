package class_new_bounds_infer;

trait Valid {
    fn ok(self: *mut Self) -> i32;
}

class Box<T: Valid> {
    val: T,
}

struct Bad {
    x: i32,
}

unsafe fn main() -> i32 {
    let b = new Box { val: Bad { x: 1 } };
    return 0;
}
