// The same body with `x`: `x` does not outlive the impl's region: rustc errors.
// TWIN of pass/bc_selfregion_typearg_anon_let_self_admit, one token apart. src/compiler/PROBES.md 2026-09-13b-selfregion-land.
package bc_selfregion_typearg_anon_let_self_refuse;
struct W<T> { t: T }
impl W<&'_ i64> {
    fn dup(&self, x: &i64) -> Self {
        let g: Self = W { t: x };
        return g;
    }
}
fn main() -> i32 {
    let v: i64 = 20i64;
    let u: i64 = 1i64;
    let w = W { t: &v };
    let y = w.dup(&u);
    return *y.t as i32;
}
