// Fail: as <I32>[] with non-slice source.
package hermes_as_array_not_slice;
use logos.lang.hermes.typed_arr;
use logos.lang.hermes.tmpl;
use logos.lang.hermes.container;
use logos.lang.rc;

unsafe fn main() -> i32 {
    let x: i32 = 5;
    let ctr: Rc<Hermes> = x as <I32>[];
    return 0;
}
