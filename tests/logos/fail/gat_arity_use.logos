package gat_arity_use;
trait Wrapper { type Inner<T>; }
struct Box<V> { val: V, }
impl<V> Wrapper for Box<V> { type Inner<T> = T; }
fn foo(x: Box<i32>::Inner<i32, bool>) -> i32 { return x; }
unsafe fn main() -> i32 { return foo(42); }
