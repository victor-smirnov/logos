package compound_assign_tuple_bool;
// Bug: lower_tuple_field_compound_assign had no types_compatible check.
// t.0 += true compiles and silently gives t.0 = t.0 - 1.  Must be a type error.

fn main() -> i32 {
    let mut t: (i32, i32) = (10, 20);
    t.0 += true;
    return t.0;
}
