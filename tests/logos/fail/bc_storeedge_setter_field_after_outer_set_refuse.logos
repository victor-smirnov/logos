// STOREEDGE pair (2026-09-14m-storeedge-land): was soundness_queue setter_field_store_after_outer_set_rehomed_admits. One token apart from
// pass/bc_storeedge_setter_field_after_outer_set_admit: `*h.r` is read after the block (rustc E0597; MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (REFUSED, E0597); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard4-fail/twins/bc_storeedge_setter_field_after_outer_set_refuse.rs).
package bc_storeedge_setter_field_after_outer_set_refuse;
struct H<'a> { r: &'a i64 }
impl<'a> H<'a> {
    fn set(self: &mut H<'a>, r: &'a i64) { self.r = r; }
}
fn main() -> i32 {
    let x: i64 = 5i64;
    let y: i64 = 6i64;
    let mut h: H = H { r: &y };
    h.set(&x);
    {
        let d: i64 = 1i64;
        h.set(&d);
    }
    let v: i64 = *h.r;
    return v as i32;
}
