package assoc_const_missing;

trait Limit {
    const MAX: i32;
    fn check(self: Self, v: i32) -> bool;
}
struct Buffer {}
impl Limit for Buffer {
    fn check(self: Buffer, v: i32) -> bool { return v <= 100; }
}
unsafe fn main() -> i32 { return 0; }
