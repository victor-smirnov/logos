// `pub(module)` is the middle visibility tier: a mapping so marked is visible
// to its module's packages but NOT across the module boundary.
package test;

use maplib;
use logos.lang.writ.container;
use logos.std.wql.wql;
use logos.std.wql.mapping_item;
use logos.mem.collections.vec;

resource bad = deem!(w: Internal) { from leaf l select l.v };

fn main() -> i32 { return 0i32; }
