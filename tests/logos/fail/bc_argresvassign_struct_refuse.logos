// PAIR B, fail half — the STRUCT carrier of the same arm: the whole-value write
// is a struct literal, not a scalar store. One token apart from
// pass/bc_argresvassign_struct_admit: the assigned NAME. rustc: E0506.
package bc_argresvassign_struct_refuse;
struct Holder { n: i64 }
fn take(a: &mut Holder, b: i64) -> i64 { return a.n + b; }
fn main() -> i32 {
    let mut h: Holder = Holder { n: 1i64 };
    let mut h2: Holder = Holder { n: 2i64 };
    let r: i64 = take(&mut h, { h = Holder { n: 9i64 }; 0i64 });
    return r as i32;
}
