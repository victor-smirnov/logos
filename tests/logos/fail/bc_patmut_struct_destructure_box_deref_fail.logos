// 2026-09-05a: ex-soundness-queue row destructure_no_mut_box_deref (tier 2 ADMIT) — '&mut *bb' through a no-'mut' destructured Box is E0596; the pat_bound mask that admitted it is gone. Pass twin: bc_patmut_struct_destructure_box_deref.
package bc_patmut_struct_destructure_box_deref_fail;
struct W { b: Box<i64> }
fn main() -> i32 {
    let w: W = W { b: Box::new(1i64) };
    let W { b: bb } = w;
    let r: &mut i64 = &mut *bb;
    *r = 6i64;
    return 0i32;
}
