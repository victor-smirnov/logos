// soundness queue row generic_unbounded_typevar_reuse_after_array_literal_admits — CLOSED by round 2026-09-15g-aggtemp, commit of this fixture.
// asserts: `let a: [T; 1] = [x]; return x;` is REFUSED (rustc E0382); base compiled it
// legality: rustc 1.98.1 --edition 2024, twin cited in the round's rust/ directory; measured, not read.
package bc_0915g_aggtemp_typevar_reuse_after_arrlit_refuse;
fn f<T>(x: T) -> T {
    let a: [T; 1] = [x];
    return x;
}
fn main() -> i32 {
    let q: i64 = f(7i64);
    if q != 7i64 { return 1i32; }
    return 0i32;
}
// rustc 1.98.1 --edition 2024 (round 2026-09-15f-rustcaudit, class CONFIRMED): REFUSED — error[E0382]: use of moved value: `x`. Twin src/compiler/probes/2026-09-15f-rustcaudit/rust/generic_unbounded_typevar_reuse_after_array_literal_admits.rs. Audit note: already rustc-checked 09-15e; twin re-run from src/compiler/probes/2026-09-15e-consume/rust.
