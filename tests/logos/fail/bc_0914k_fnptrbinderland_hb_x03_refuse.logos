// hand battery: round 2026-09-14k-fnptrbinderland, program x03 — caught: illegal, admitted on base (match-join of two fn(&'r) params into fn(&i64)), refused by the landing
// legality: by reading, no rustc binary
package bc_0914k_fnptrbinderland_hb_x03_refuse;
fn t<'r>(g: fn(&'r i64) -> i64, h: fn(&'r i64) -> i64) -> i32 {
    let n: i32 = 1i32;
    let f: fn(&i64) -> i64 = match n { 0i32 => g, _ => h };
    return 0i32;
}
fn main() -> i32 { return 0i32; }
