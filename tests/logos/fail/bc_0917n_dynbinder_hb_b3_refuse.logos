// hand battery: round 2026-09-17n-dynbinder, program b3 — caught: ARRAY-literal arg, no dyn: COMPILED AND RAN exit 5 on the SHIPPED binary
// legality: MEASURED with rustc 1.98.1 --edition 2024 — REFUSED, error[E0597].
package bc_0917n_dynbinder_hb_b3_refuse;
fn pa2<'q>(x: [&'q i64; 1]) -> &'q i64 { return x[0]; }
fn store<'a>(out: &mut &'a i64) {
    let l: i64 = 5i64;
    *out = pa2([&l]);
}
fn main() -> i32 {
    let z: i64 = 0i64;
    let mut r: &i64 = &z;
    store(&mut r);
    return *r as i32;
}
