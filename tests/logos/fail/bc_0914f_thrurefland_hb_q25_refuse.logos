// hand battery: round 2026-09-14f-thrurefland, program q25 — caught: verdict moved base -> landed — PROBES.md "reference layers over a pointer (`&*mut &'a` via `let rx = &x`, q25) CLOSED (peel)"; measured base RUN 0 -> today REFUSED
// legality: by reading, no rustc binary
// harvested 2026-09-14i from snapshot hand-harvest-2026-09-14b/land14f_r2.sFtP/q25_let_inferred_ref_peel_mutptr_illegal.logos
package bc_0914f_thrurefland_hb_q25_refuse;
fn f<'a, 'b>(x: *mut &'a i64, y: *mut &'b i64) -> bool {
    let rx = &x;
    let ry = &y;
    return rx == ry;
}
fn main() -> i32 {
    return 0i32;
}
