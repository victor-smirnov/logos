// wql! diagnostic: a call name that is neither an EL builtin nor a top-level
// fn declared in the trigger module is a compile-time error naming the fn
// (imported fns are outside metacall reflection's reach — documented MVP).
package test;

use logos.std.wql.wql;
use logos.mem.collections.vec;

struct P { pub v: i64 }

resource bad = deem!(xs: &[P]) { from xs x where nope(x.v) > 0 select x.v };

fn main() -> i32 { return 0i32; }
