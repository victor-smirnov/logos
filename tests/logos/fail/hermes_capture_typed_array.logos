// Fail: capture inside a typed @<T>[] array (not supported — typed arrays store raw T, not AnyVal).
package hermes_capture_typed_array;
use logos.lang.hermes.typed_arr;
use logos.lang.hermes.tmpl;
use logos.lang.hermes.container;
use logos.mem.rc;

unsafe fn main() -> i32 {
    let x: i32 = 5;
    let arr: Rc<Hermes> = @<I32>[$x, 2, 3];
    return 0;
}
