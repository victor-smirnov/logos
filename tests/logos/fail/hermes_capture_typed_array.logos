// Fail: capture inside a typed @<T>[] array (not supported — typed arrays store raw T, not AnyVal).
package hermes_capture_typed_array;
use hermes.ctr;

unsafe fn main() -> i32 {
    let x: i32 = 5;
    let arr: HermesCtr = @<I32>[$x, 2, 3];
    return 0;
}
