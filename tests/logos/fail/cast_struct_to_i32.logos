package cast_struct_to_i32;
// Bug: casting a struct to i32 was silently accepted and produced garbage.
// Fix: sema now rejects aggregate-to-scalar casts.
struct Foo { x: i32; y: i32; }
fn main() -> i32 {
    let f = Foo { x: 1, y: 2 };
    return f as i32;
}
