// hand battery: round 2026-09-12b-rpitbound, program c1 — caught: verdict moved base -> landed — rpitbound/RESULT.md "c1/c2/c3/c5 illegal, rc 0 before, refused after"
// legality: by reading, no rustc binary
// harvested 2026-09-14h from src/compiler/probes/2026-09-12b-rpitbound/hand/c1.logos
package bc_0912b_rpitbound_hb_c1_refuse;
// ILLEGAL: `impl Fn` whose body mutates a capture, signature has a parameter.
fn mk(step: i64) -> impl Fn(i64) -> i64 {
    let mut acc: i64 = 0i64;
    return move |d: i64| -> i64 { acc = acc + d + step; return acc; };
}
fn main() -> i32 {
    let f = mk(1i64);
    return f(2i64) as i32;
}
