// B-ca-06: silent acceptance of unbound `<type:Q>` in WritStatic literal.
package type_lit_unbound;

use logos.lang.writ.wstatic;

pub const X: WritStatic = @{ "key": <type:NotAType> };

pub unsafe fn main() -> i32 { return 0; }
