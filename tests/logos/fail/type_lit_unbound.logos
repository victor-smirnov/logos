// B-ca-06: silent acceptance of unbound `<type:Q>` in HermesStatic literal.
package type_lit_unbound;

use logos.lang.hermes.view;
use std.hermes.view;

pub const X: HermesStatic = @{ "key": <type:NotAType> };

pub unsafe fn main() -> i32 { return 0; }
