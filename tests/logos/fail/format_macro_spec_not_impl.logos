// Slice 4.4d landed width/align/fill; precision / sign / `#` / `0`
// remain gated until slice 4.4e.
package format_macro_spec_not_impl;

use std.fmt;
use std.lang.text;

fn main() -> i32 {
    let s = format!("{:.3}", "hello");
    return 0;
}
