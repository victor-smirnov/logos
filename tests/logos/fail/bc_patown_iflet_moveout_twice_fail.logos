// the payload binding is a by-value owner: a second move of it is E0382. ONE TOKEN from pass/bc_patown_iflet_moveout. PROBES.md 2026-09-06d: `if let` / `while let` / let-chains lower BY DELEGATION to lower_match (temp hoist, per-arm scrutinee move, arm drops, cond-move merge); the count is a destructor tally through a raw pointer.
package bc_patown_iflet_moveout_twice_fail;
struct R { i: *mut i64 }
impl Drop for R { fn drop(self: &mut R) { unsafe { *self.i = *self.i + 1i64; } } }
fn eat(r: R) -> i64 { return 1i64; }
fn main() -> i32 {
    let mut c: i64 = 0i64;
    let p: *mut i64 = &mut c;
    {
        let o: Option<R> = Option::Some(R { i: p });
        if let Option::Some(r) = o { let _k: i64 = eat(r); let _m: i64 = eat(r); }
    }
    if c != 1i64 { return 1i32; }
    return 0i32;
}
