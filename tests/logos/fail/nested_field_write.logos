package nested_field_write;
// o.inner.v = x  — nested lvalue not supported in grammar
struct Inner { v: i32; }
struct Outer { inner: Inner; }
fn bump(o: *mut Outer) {
    o.inner.v = 42;
}
fn main() -> i32 { return 0; }
