// a preceding fn's T: 'static does not leak into a struct declaration's scope: struct S<T> { x: &'static T } is refused (2026-09-03a)
package bc_wf_field_scope_fail;
fn f<T: 'static>(t: &'static T) -> i32 { return 0i32; }
struct S<T> { x: &'static T }
fn main() -> i32 { return 0i32; }
