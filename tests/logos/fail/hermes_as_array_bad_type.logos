// Fail: as <T>[] with unsupported element type.
package hermes_as_array_bad_type;
use hermes.ctr;

unsafe fn main() -> i32 {
    let arr: [f64; 2] = [1.0, 2.0];
    let s: &[f64] = &arr;
    let ctr: Hermes = s as <F64>[];
    return 0;
}
