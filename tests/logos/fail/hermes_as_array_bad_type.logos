// Fail: as <T>[] with unsupported element type.
package hermes_as_array_bad_type;
use hermes.ctr;

unsafe fn main() -> i32 {
    let arr: [i32; 2] = [1, 2];
    let s: &[i32] = &arr;
    let ctr: Hermes = s as <Bool>[];
    return 0;
}
