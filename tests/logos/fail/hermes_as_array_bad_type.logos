// Fail: as <T>[] with unsupported element type.
package hermes_as_array_bad_type;
use logos.lang.hermes.typed_arr;
use logos.lang.hermes.tmpl;
use logos.lang.hermes.container;
use logos.mem.rc;

unsafe fn main() -> i32 {
    let arr: [i32; 2] = [1, 2];
    let s: &[i32] = &arr;
    let ctr: Rc<Hermes2> = s as <Bool>[];
    return 0;
}
