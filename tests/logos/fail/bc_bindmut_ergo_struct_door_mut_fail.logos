package bc_bindmut_ergo_struct_door_mut_fail;
struct P { x: i64, y: i64 }
fn main() -> i32 {
    let p: P = P { x: 1i64, y: 2i64 };
    let mut out: i64 = 0i64;
    match &p {
        P { mut x, y } => { x = x + 1i64; out = x + *y; }
    }
    if out != 4i64 { return 1i32; }
    return 0i32;
}
