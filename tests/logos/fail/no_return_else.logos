package no_return_else;
// Sema fail: if/else where the else branch doesn't return
fn check(x: i32) -> i32 {
    if x > 0 {
        return 1;
    } else {
        let y: i32 = x * 2;  // does not return
    }
}
fn main() -> i32 { return check(5); }
