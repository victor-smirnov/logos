// 2026-09-05a: two LIVE reborrows through an immutable &mut local — E0499, and the sentence is the exclusivity one (before this round the binding-mut line masked it). Pass twin: bc_recvaot_mutref_sequential_reborrow.
package bc_recvaot_two_live_reborrows_fail;
struct S { v: i64 }
impl S {
    fn get(&mut self) -> &mut i64 { return &mut self.v; }
}
fn main() -> i32 {
    let mut s: S = S { v: 1i64 };
    let sr: &mut S = &mut s;
    let r1: &mut i64 = sr.get();
    let r2: &mut i64 = sr.get();
    *r1 = 2i64;
    *r2 = 3i64;
    return 0i32;
}
