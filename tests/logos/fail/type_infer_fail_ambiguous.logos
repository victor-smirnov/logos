// Error: T cannot be inferred — no args of type T, no let-annotation hint.
package type_infer_fail_ambiguous;

fn make<T>() -> T { return 0; }

fn main() -> i32 {
    let x = make();  // no annotation → no return-type hint → T not inferrable
    return 0;
}
