// hand battery: round 2026-09-14m-storeedge-land, program c09 — caught: illegal, admitted on base (E0506 `x = 9` after the second push re-homed onto `y`), refused by the landing
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (REFUSED, E0506); twin src/compiler/probes/2026-09-16a-fixtureaudit/rust/shard4-fail/twins/bc_0914m_storeedgeland_hb_c09_refuse.rs
package bc_0914m_storeedgeland_hb_c09_refuse;
fn main() -> i32 {
    let mut x: i64 = 5i64;
    let y: i64 = 7i64;
    let mut buffer: Vec<&i64> = Vec::new();
    buffer.push(&y);
    buffer.push(&x);
    x = 9i64;
    return buffer.len() as i32;
}
