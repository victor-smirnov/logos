// an unknown loop label is refused. ONE TOKEN from pass/bc_patown_labeled_continue_drops. PROBES.md 2026-09-06d: `if let` / `while let` / let-chains lower BY DELEGATION to lower_match (temp hoist, per-arm scrutinee move, arm drops, cond-move merge); the count is a destructor tally through a raw pointer.
package bc_patown_labeled_continue_unknown_fail;
struct S { v: i64, i: *mut i64 }
impl Drop for S { fn drop(self: &mut S) { unsafe { *self.i = *self.i + self.v; } } }
fn main() -> i32 {
    let mut c: i64 = 0i64;
    let p: *mut i64 = &mut c;
    let mut k: i64 = 0i64;
    'outer: loop {
        let s: S = S { v: 1i64, i: p };
        k = k + 1i64;
        if k > 3i64 { break; }
        loop {
            continue 'nope;
        }
    }
    return c as i32;
}
