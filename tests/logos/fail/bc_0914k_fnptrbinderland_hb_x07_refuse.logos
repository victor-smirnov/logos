// hand battery: round 2026-09-14k-fnptrbinderland, program x07 — caught: illegal, admitted on base (an impl lifetime param region into fn(&i64)), refused by the landing
// legality: by reading, no rustc binary
package bc_0914k_fnptrbinderland_hb_x07_refuse;
struct S<'a> { r: &'a i64 }
impl<'a> S<'a> {
    fn cb(&self, g: fn(&'a i64) -> i64) -> i32 {
        let f: fn(&i64) -> i64 = g;
        return 0i32;
    }
}
fn main() -> i32 { return 0i32; }
