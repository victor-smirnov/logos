wql!: `aggregate`: `avg` over a value of type `String` — the aggregate accumulates in `f64` and the argument reaches it through the EL's normalizing cast (`el_norm_cast`), the one an arithmetic operand, a sort key and a UDF result each get — and this type is not numeric, so there is no cast into the accumulator at all. The remedy is a numeric argument
