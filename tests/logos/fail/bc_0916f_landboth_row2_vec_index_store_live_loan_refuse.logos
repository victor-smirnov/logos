// SOUNDNESS QUEUE row vec_index_store_live_shared_loan_admits (tier 2) — CLOSED
// by this commit, so its program lands here as an ordinary fail fixture.
// `e` is a live shared loan of `v` and the indexed store needs `&mut v` while
// the INDEX expression still reads that loan. rustc 1.98.1 refuses with
// error[E0502]; base logosc compiled it and ran exit 9.
// legality: MEASURED 2026-09-16 vs rustc 1.98.1 --edition 2024: CONFIRMED (REFUSED, E0502).
package bc_0916f_landboth_row2_vec_index_store_live_loan_refuse;
fn main() -> i32 {
    let mut v: Vec<i64> = Vec::new();
    v.push(1i64); v.push(0i64);
    let e: &i64 = &v[1u64];
    v[*e as u64] = 9i64;
    return v[0u64] as i32;
}
