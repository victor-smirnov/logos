// hand battery: round 2026-09-17p-ordcmpland, program z08 — caught: the ABUSE direction of the raw-pointer ordering arm; a raw pointer against a reference must stay REFUSED, and this one is refused by a READ sema sentence rather than by the MLIR verifier the arm removes.
// legality: MEASURED, rustc 1.98.1 --edition 2024 refuses the twin with error[E0308]: mismatched types (src/compiler/probes/2026-09-17p-ordcmpland/rust/z08_ptr_vs_ref.rs).
package bc_0917p_ordcmpland_hb_z08_refuse;
fn main() -> i32 {
    let mut v: i64 = 1i64;
    let p: *mut i64 = &mut v as *mut i64;
    let r: &i64 = &v;
    if p < r { return 1i32; }
    return 0i32;
}
