# unrowed_backlog.ledger — FINDINGS THAT ARE NOT YET ROWS, AND WHAT EACH NEEDS TO BECOME ONE.
#
# WHY THIS FILE EXISTS. `bc_admits.ledger` and `soundness_queue.ledger` are the
# two files an agent may take work from, and a round is priced against a row.
# A finding that has no row is therefore invisible to the round machinery, and
# for several rounds the only place these lived was prose in a report or in
# memory. Prose does not survive its round. This file is the third counted
# list: 91 + 78 + this file's TOTAL is the whole of the remaining work (counts
# re-derived by direct listing 2026-09-08).
#
# ⚠ AN ENTRY HERE IS NOT A ROW AND MUST NOT BE PRICED AS ONE. Each entry names
# the ONE thing that must be measured before it can become a row (or be thrown
# away). That measurement is the entry's whole content. An entry whose gate
# says "verify the harness first" may turn out to be zero defects; an entry
# that says "group by allocation site" may turn out to be several rows.
# Promoting an entry means DELETING it here and adding the row(s) there.
#
# ⚠ RE-MEASURE, DO NOT CARRY. Same rule as bc_admits_blocked.ledger: every
# entry carries the DATE its evidence was taken. A recorded defect is a
# measurement with a timestamp, and the oracle is the emitted artifact, not
# this file. An entry that cannot be re-checked today is stale, not open.
#
# Format:  <id> <kind> <count-if-known> <where the evidence lives>
#          # <date measured> — <what must be measured to make it a row>
#
# kind:  sweep     a batch reading from one instrument run, count = raw hits
#        defect    one reproduced misbehaviour, seen but never rowed
#        recorded  carried in memory as open; needs re-measure before rowing
#        perf      a timing regression, not a correctness defect

let_tuple_struct_sub_diag  defect     1  round 2026-09-07u pricing report
    # 2026-09-07 — `let (P { x, y }, z) = sv;` is refused with a THIRD sentence,
    # "'let <pattern> = expr;' currently supports struct patterns only". Reproduced on
    # the base compiler. Needs a fixture pair and a tier-3 row; check first whether it
    # is the same missing Struct arm as match_tuple_door_nested_struct_binds_nothing
    # (doors in SERIES) or a fourth door.
lt_turbofish_absent        defect     2  tests/imported/admit/regions/multiple-sources-for-outlives-requirement--{c24b,t24}
    # 2026-09-07 — MISSING FEATURE, blocking a re-port. `f::<'_>(&v)` reports "unexpected
    # type node code 131" while `call::<i64>()` compiles: a turbofish takes TYPE arguments
    # only, no LIFETIME arguments. Both `multiple-sources-*` rows sit in
    # bc_admits_blocked as BUCKET-4 because upstream's whole test IS
    # `outlives_indir::<'_, 'b, _>(&mut 1u32)`. ⚠ A ROW IS ALREADY WAITING BEHIND IT:
    # two probes written this round that pin `'b` INVARIANTLY instead of with a turbofish
    # (`w: &mut &'b i64`, and `struct Inv<'b> { f: fn(&'b i64) -> &'b i64 }`, each against
    # `fn outlives_indir<'a,'b>(x: &'a mut i64, w: ..) where 'a: 'b` called with
    # `&mut 1i64`) are BOTH ADMITTED where rustc refuses. To make it a row: decide whether
    # the invariant form is a re-port (it is not — it substitutes the construct) or its own
    # finding, and if the latter, write it as a native fixture with its own root.
const_expr_array_len_absent defect     1  tests/imported/admit/lifetimes/issue-64173-unused-lifetimes
    # 2026-09-07 — MISSING FEATURE, blocking a re-port. An array length must be a literal:
    # `struct Foo<'s> { array: [i64; size_of_t::<i64>()] }` reports "syntax error near
    # 'struct'". Upstream's test is `[(); size_of::<&Self>()]` / `[(); foo::<&'a ()>()]`,
    # so neither annotated half has a shape here. To make it a row: it is a LANGUAGE
    # feature request, not a defect — decide with the owner whether const-expression array
    # lengths are wanted at all before this is priced.

enum_payload_nested_pattern_absent defect 1  tests/imported/admit/nll/enum-drop-access
    # 2026-09-07 — MISSING FEATURE, blocking a re-port. `match opt { DropOption::Some(&mut ref mut r) => .. }`
    # reports "pattern DropOption::Some: nested patterns inside enum-variant payloads are not
    # yet supported; bind to a name and match in the body". The generic enum with a `Drop` impl
    # compiles on its own, so it is the nested pattern alone that has no path. The row
    # `enum-drop-access` sits in bc_admits_blocked as BUCKET-4 because upstream's whole test IS
    # the reborrow-through-the-payload `&mut ref mut r`. To make it a row: decide whether this is
    # the same missing Struct/Ref arm as soundness_queue `match_tuple_door_nested_struct_binds_nothing`
    # and `let_at_binding_tuple_sub_unsupported` (doors in SERIES on ONE reader) or a third door —
    # if one reader, these are one row, not three.

tuplestruct_ctor_turbofish  defect     1  tests/imported/admit/lifetimes/constructor-lifetime-early-binding-error
    # 2026-09-08 — a turbofish on a TUPLE-STRUCT CONSTRUCTOR CALL does not parse:
    # `struct S<'a,'b>(&'a i64, &'b i64); let s = S::<'static>(&x, &x);` answers
    # `error [fn main]: unexpected type node code 131`. MISSING FEATURE: explicit
    # type/lifetime arguments on a tuple-struct constructor path expression. The SAME
    # turbofish on a struct LITERAL (`S::<'static> { .. }`) and on an ENUM VARIANT
    # constructor (`E::V::<'static>(&x)`) both parse and both ADMIT, so this is a
    # parser hole, not the arity hole. It blocks the STRUCT half of upstream
    # constructor-lifetime-early-binding-error.rs; the enum half is ported and is that
    # row. To make it a row: land the syntax, then re-port the struct half and see
    # whether it admits like the enum half does (predicted yes — same missing check).
lifetime_supertrait_bound   defect     1  tests/imported/admit/regions/regions-infer-bound-from-trait-self
    # 2026-09-08 — a LIFETIME SUPERTRAIT BOUND is a syntax error: `trait
    # InheritsFromStatic: 'static { .. }` answers `syntax error near 'trait' at line N
    # col 1`. MISSING FEATURE: a lifetime in a trait's supertrait list. It blocks the
    # four LEGAL CONTRAST traits of upstream regions-infer-bound-from-trait-self.rs
    # (InheritsFromStatic, InheritsFromStaticIndirectly, InheritsFromIs<'a>,
    # InheritsFromIsIndirectly<'a>); only the trait that inherits NOTHING — the
    # annotated case, and the row — is portable today. To make it a row: land the
    # syntax, then check whether `check_bound(x, self)` is refused in the four
    # inheriting traits and admitted in the fifth. Until then the row has no control
    # and the `A: 'a` bound is exercised in one direction only.
raw_ref_expression          defect     1  tests/imported/admit/moves/move-of-addr-of-mut
    # 2026-09-08 — a RAW-REFERENCE EXPRESSION does not parse: `let p: *mut S = &raw mut x;`
    # answers `syntax error near 'raw' at line N col 22`. MISSING FEATURE: `&raw const` /
    # `&raw mut` (what `core::ptr::addr_of{,_mut}!` expands to). It blocks the upstream
    # construct of tests/ui/moves/move-of-addr-of-mut.rs — "taking a mutable RAW ptr to an
    # uninitialised variable does not change its initializedness" — which is a rule about
    # raw references SPECIFICALLY. The row stands today on `&mut x` over the same
    # uninitialised binding, which rustc also refuses but by the ordinary borrow rule. To
    # make it a row: land the syntax, re-port the upstream line, and see whether the
    # `let y = x;` that follows is still admitted.
refmut_refmut_deref_coercion  defect   1  tests/imported/admit/borrowck/mut-borrow-of-mut-ref
    # 2026-09-08 — no deref coercion from `&mut &mut T` down to `&mut T` at a call:
    # `fn h(x: &mut i64) {} fn f(mut b: &mut i64) { h(&mut b); }` answers `call to 'h' arg
    # 1: expected &mut i64, got &mut &mut i64`. rustc accepts. MISSING FEATURE: deref
    # coercion through `&mut`. It forces the port of mut-borrow-of-mut-ref.rs to give the
    # callee the un-coerced type `&mut &mut i64`; the borrow site `&mut b` is unchanged, so
    # the row is not blocked, only the callee's spelling. To make it a row: land the
    # coercion, then check whether the E0596 at `&mut b` still goes unreported.
box_deref_coercion_at_assignment  defect  1  tests/imported/admit/nll/issue-62007-assign-differing-fields--c
    # 2026-09-08 — no deref coercion from `&mut Box<T>` to `&mut T` at an assignment:
    # `let mut r: &mut L; r = &mut bb;` with `bb: Box<L>` answers `assignment to 'r': type
    # mismatch — expected &mut L, got &mut Box<L>`. rustc accepts. MISSING FEATURE: Box
    # deref coercion in assignment position. The port writes `&mut **n` instead; same
    # place, same assignment. To make it a row: land the coercion and re-check.
doubled_ampersand_type_lexing  defect  1  tests/imported/admit/nll/type-check-pointer-coercions
    # 2026-09-08 — `&&` is one token in TYPE position: `fn f<'a,'b>(x: &&'a i64) -> *const
    # &'b i64` answers `syntax error near 'fn' at line 2 col 1`, while `& &'a i64` with a
    # space parses and compiles. MISSING FEATURE: splitting `&&` into two `&` in a type.
    # Cosmetic for the corpus (the space is written) but it silently mis-locates the error
    # to the previous line, which cost a bisect here. To make it a row: decide whether a
    # parse-only defect belongs in bc_admits at all.
closure_move_of_refmut_local_over_refusal  defect  1  round 2026-09-08 (stage 7 re-port controls)
    # 2026-09-08 — SUSPECTED OVER-REFUSAL of legal Rust, found as a control while
    # re-porting issue-27282. `let b: &mut bool = &mut t; let c = || { let bar = b; *bar =
    # false; }; c();` is REFUSED with "use of moved value 'b' (moved on line 5)". The
    # closure captures `b` by move and is called ONCE, which is an FnOnce call rustc
    # accepts. Same shape with a second `&mut` local (`d`) gives the same refusal. ⚠ NOT
    # CONFIRMED AGAINST rustc: there is NO rustc binary on this box (`which rustc` empty,
    # no stage1 under /home/logos/cxx/rust). To make it a row: run these two programs
    # through a real rustc; if rustc accepts them this is a tier-3 `refuses` row for
    # soundness_queue.ledger, and an over-refusal is the most expensive kind here.
vg_leak_records            sweep     39  sandbox/vg-round3/{leaks.tsv,recs3.pkl}
    # 2026-09-11 RE-SWEPT WHOLE on build 19ff93338332ba46 43, same 93 fixtures, same
    # per-fixture CMake ENVIRONMENT. ⚠ THE 09-08 DATA IS DEAD AND MUST NOT BE INHERITED:
    #   09-08  87 LEAK   3 OK   3 CORRUPT   270 recs  39 sites  75 pairs  110 stacks
    #   09-11  57 LEAK  34 OK   2 CORRUPT   137 recs  36 sites  42 pairs   53 stacks
    # 30 fixtures went LEAK -> OK and NONE went the other way (diffed both ways). The
    # entry's own headline root `replace_site_skips_field_drop_glue` IS NOT OPEN — it was
    # closed by 1979d72f4 — and the question this entry said must not be claimed is now
    # MEASURED: of the 32 fixtures in the `vec$vec_new`-from-`mk` class, 30 are clean
    # (all 11 `cond_move_*`, all 7 `dupown_*`, all 11 `rawdup_*`, `no_auto_drop_sibling_ctl`)
    # and 3 remain, 2 of them for reasons that are not that root at all.
    #
    # ATTRIBUTED AND CLOSED — 72 of the 137 records are NOT DEFECTS, with the evidence:
    #   26 recs / 18 fixtures  bare `malloc` in the fixture's own `unsafe fn main()`
    #                          (zone_mut_*, zoned_relative, rel_ptr, dst/layout probes);
    #                          zone_mut_fat_ref even names one `_spacer` "intervening
    #                          alloc (clobber)". Raw-pointer tests, freed by process exit.
    #   29 recs /  5 fixtures  fixture-local `unsafe fn` allocators never freed:
    #                          layout_dst_prefix_and_offset_of$zeroed, layout_clike_enum_
    #                          backing$zeroed, fs_meta$cstr, process_lines$cstr,
    #                          bst$new_node. fs_meta's own comment says "Leak the buffer
    #                          for the lifetime of this call (test-only convenience)".
    #   13 recs /  1 fixture   no_auto_drop_sibling: `#[no_auto_drop]` is a SUPPRESSION and
    #                          13 is EXACTLY its 13 suppressed values. Its `_ctl` sibling,
    #                          the same shapes without the attribute, is now CLEAN — the
    #                          control holds in both directions.
    #    3 recs /  2 fixtures  arenas: relany$arena_new and zvec$arena_new, 4096 b each,
    #                          plus one indirect zvec.Heap__z_alloc inside the second.
    #    1 rec  /  1 fixture   box_into_raw: `box_leak` — "never freed", by design.
    #
    # ATTRIBUTED TO ROWS THAT ALREADY EXIST — 14 more records, no new row owed:
    #   11 recs / 3 fixtures   soundness_queue `operator_autoref_temp_never_dropped`.
    #                          RE-VERIFIED today by hand: `s == String::from("hej")` loses
    #                          8 b while the by-value argument and the method receiver are
    #                          CLEAN. wql_el_cmp_measured's `m_string` compares SIX pairs
    #                          of `k_string(..)` temporaries with `==` (8 recs);
    #                          string_eq_empty has one; field-replace-in-struct-with-drop-
    #                          b154, which that row's header already names, has two.
    #    3 recs / 3 fixtures   soundness_queue `weak_local_never_dropped`, which already
    #                          names all three (arc_weak_upgrade, rc_weak_upgrade,
    #                          rc_weak_dangling). One-variable control re-run today:
    #                          `Rc` alone CLEAN, `r.clone()` CLEAN, `r.downgrade()` 12 b.
    #
    # ROWED 2026-09-11, CLOSED 2026-09-11b — 1 record, tier 1: soundness_queue
    # `boxdyn_arg_deref_borrow_kills_box_drop` (tests/spec/pass/coerce_box_dyn.logos).
    # Root: `mark_moved_expr` read the NAME-keyed `owning_dyn` bit without asking the
    # EXPRESSION's type, and `&b` on a `Box<dyn Tr>` local lowers to that local's own
    # VarRef re-typed OwningKind::Borrow — so a BORROW was marked moved and the local's
    # scope-exit drop was deleted. `coerce_box_dyn` re-measured on the fixed binary:
    # 8 b definitely lost -> 0. Fixtures tests/logos/{pass,fail}/
    # boxdyn_borrow_arg_keeps_box_drop. The record is RESOLVED, not moved.
    #
    # WHAT IS LEFT — 50 records, and the ONE thing each needs to become a row:
    #    CLOSED 2026-09-11c — 11 recs / 8 fixtures, and the CLASS WAS ONE MEMBER LARGER
    #                          THAN THIS ENTRY SAID. The entry's own class was
    #                          `buf_reader_wrap`/`buf_writer_wrap`, 9 recs over what it
    #                          recorded as 7 fixtures (MEASURED: SIX — adv3_generic_field_
    #                          method_call is valgrind-clean and exits 0, not 42). It was
    #                          defined by which fixtures valgrind caught. Enumerated by
    #                          PROPERTY instead — a stdlib struct whose VALUE-returning
    #                          ctor heap-allocates a buffer it stores, with an explicit
    #                          release fn and no `impl Drop` — the class is THREE:
    #                          BufWriter, BufReader, and **BTreeMap**, whose one leaking
    #                          fixture `deem_btreemap_source` this file had filed below
    #                          under "10 singles with no group yet". Five more allocating
    #                          types (Chan, FutureSlot, Scheduler, SpscRing, ThreadPool)
    #                          are NOT the class: their ctors return `*mut T`, which has
    #                          no drop glue for an `impl Drop` to hook, so that zero is
    #                          structural. 13 comparable types already declare Drop.
    #                          ⚠ "an API decision with an owner" is RETIRED as a verdict:
    #                          both divergence registries were grepped BY CONSTRUCT —
    #                          docs/DIVERGENCES.md is 17 letter rows A1..A17 with no
    #                          io/buffered row, and `bufreader|bufwriter|buf_reader|
    #                          buf_writer|io/buffered` over all 3854 docs/spec clauses
    #                          exits 1 — so the standing Rust rule settles it.
    #                          Fix: the release fn nulls what it freed and `Drop` performs
    #                          the same guarded release, at all three sites. `close` was
    #                          already half of that; `btreemap_free` was not, and a Drop
    #                          on top of it as it stood double-freed at
    #                          tests/logos/pass/btreemap_basic.logos:82.
    #                          9 recs / 6 fixtures (bufio_generic_reader 5 b, http_parse_
    #                          stream 96 b, http_framing_readers 80 b, http_chunked_reader
    #                          16 b, http_chunked_body_helper 64 b, http_serialize_stream
    #                          64 b) and 2 recs / 1 fixture (deem_btreemap_source 128 b)
    #                          ALL -> 0, every exit code unchanged, no Invalid free.
    #                          Pinned by two pairs one token apart: tests/logos/pass/
    #                          bufwriter_{drop_flushes_buffer,close_then_drop_flushes_once}
    #                          (a DESTRUCTOR COUNT — `flushes=0|1|2` — whose pass half is
    #                          RED on the pre-fix binary) and btreemap_{drop_frees_storage,
    #                          free_then_drop_frees_once}. ⚠ the BTreeMap pair pins only
    #                          the DOUBLE-FREE direction by exit code: that type runs no
    #                          element destructors, so no count exists and its leak
    #                          direction is held by the valgrind number alone.
    #                          DECLINED by name: element drops in BTreeMap (nothing drops
    #                          K/V anywhere — `remove` shifts and discards — so fixing it
    #                          on the drop path alone is a double-drop; 0 of the 11 records
    #                          closed were element leaks). See docs/probes/
    #                          bufdrop-2026-09-11/ROUND-LANDING.md.
    #   19 recs / 8 fixtures   writ/fabric/dview stores — DView__new_store (5/5),
    #                          DViewStore__storage_push (5/4), PrimVec push/insert_at
    #                          (4/3), WritStringStorage__storage_push (2/1), WAny and i64
    #                          __store_push (2/2), writ_new (1/1). NEEDS: read each store's
    #                          lifetime contract; these are arena-shaped and an arena freed
    #                          at process exit is not a leak, but nothing says so yet.
    #   12 recs / 1 fixture    deem_incr_static_retract_e2e — 7 vec_new from
    #                          `iq_incremental`, 4 indirect hashmap_with_capacity under
    #                          `iq_apply`, 1 tuple-element Vec. NEEDS: one reduction; it is
    #                          the only member of the old `vec_new`-from-`mk` class whose
    #                          caller is not a `mk`.
    #   10 recs / 9 fixtures   singles with no group yet: cross_pkg_dual_box_layout and
    #                          mono_6 (`box_new` + `String::from` in main — ⚠ eight hand
    #                          programs putting a String/Box/Vec local in `main`, in a
    #                          callee, in a block, in a struct, in a plain and an `unsafe`
    #                          main, and returned from a factory, are ALL CLEAN, so "a heap
    #                          owner in main is not dropped" is REFUTED and must not be
    #                          re-proposed), recursion-tail-call-no-arg-leak
    #                          (`str__to_string`), deem_btreemap_source — ⚠ CLOSED
    #                          2026-09-11c with the group above; it was NEVER a single,
    #                          it was the third member of the buffered class and this
    #                          file's own "no group yet" was the premature closure —
    #                          generic_chain_write_through_ptr (a fixture-local `test_rc_new`
    #                          x2), derive_branch_node_shuttle (3), podref_buffer (1),
    #                          ptr_drop_in_place_recurses (1).
    #
    # ⚠ THE 2 CORRUPT FIXTURES ARE NOT IN THIS ENTRY: custom_dst_smartptr_owning_drop has
    # its own entry below, and bc_objlt_str_literal has none yet.

custom_dst_smartptr_owning_drop_uaf valgrind 2 tests/logos/pass/custom_dst_smartptr_owning_drop
    # 2026-09-08. THE FIXTURE IS PINNED GREEN OVER A USE-AFTER-FREE. `exit: 42` holds on
    # the base binary and under the Rust-canonical drop landing of this round; NEITHER
    # HALF MOVES IT, in either direction, so it is not evidence for or against them.
    # It is here because it was READ this round, with valgrind, and it reproduces:
    #   Invalid read of size 8 at `__drop_in_place__A`, 16 bytes inside a 64-byte block
    #   free'd by `MyRc$G1$udyn_Tr__drop_me__g__refmut_MyRc$G1$T`
    #   Invalid read of size 8 at `test.A__drop__f__A`, same block
    # i.e. `MyRc::drop_me` FREES the block and the owning-custom-DST drop glue then reads
    # the prefix field and runs `A::drop` through the freed storage. Two invalid reads,
    # exit code 42 both times — the strongest oracle the corpus has (a destructor COUNT)
    # cannot see it either, because the destructor does run, once, on dead memory.
    # ⚠ NOT A SOUNDNESS-QUEUE ROW, and the reason is the queue's own oracle: `observed`
    # is `admits`/`refuses`/`run <rc>`/`diag`, and this program's rc is RIGHT. A row
    # would have to lie to be held. What it owes is either a queue `observed` kind that
    # a memory checker can answer, or a program that makes the same free observable to
    # an exit code (write a sentinel into the block before the free and read it after).
    # A previous round recorded "its recorded UAF is NOT reproduced by either arm". That
    # is CONTRADICTED here: it was not reproduced because it was not run under valgrind.

e0713_mutref_past_destructor_needs_a_new_carrier carrier 2 tests/imported/fail/nll/
    # 2026-09-08. TWO ROWS LOST THEIR CARRIER, NOT THEIR DEFECT. `enum-drop-access` and
    # `issue-52059-report-when-borrow-and-drop-conflict` were rowed for an E0713 hole: a
    # `&mut` handed OUT of a function past a destructor which then writes through it
    # (`DropOpt::drop` stores 9 through `self.r`, the caller then stores 5 through the
    # returned `&mut`). Both programs express it the same way — `fn f(x: DropStruct) ->
    # &mut T { return x.field; }` — and the E0509 landing of 2026-09-08 refuses exactly
    # that, correctly: rustc refuses it as E0509 too, before E0713 is ever consulted.
    # ⚠ SO THE PROGRAMS ARE ILLEGAL AND THE ROWS COULD NOT STAND, BUT THE HOLE IS OPEN AND
    # IS NOW UNOBSERVED. Nothing in the tree tests whether a `&mut` that outlives a
    # destructor is caught. What this owes is a CARRIER: a legal-but-for-E0713 program
    # that gets the reference out without moving a field out of a `Drop` value — e.g. a
    # `&mut self` method returning a reborrow (`fn take(&mut self) -> &mut i64 { &mut
    # *self.r }`) whose result is used after the owner is dropped, or a Drop-less wrapper
    # holding the Drop value. Write it, check whether logosc admits it, and row it.
    # ⚠ IT IS ALSO A LESSON ABOUT COVERAGE: a refusal landed for one rule DELETED the
    # only two observations of another. A row's program is its instrument, and a stronger
    # rule upstream of it can silently retire the instrument while leaving the defect.

dyn_vtable_bare_key_miscompile  defect     2  src/compiler/mlir_gen_dyn.cpp:962,982
    # 2026-09-11 — A CONFIRMED MISCOMPILE WITH NO ROW, BECAUSE ITS CARRIER CANNOT BE
    # WRITTEN UNDER THE CURRENT GATE. `dyn_vtable_methods_` keys on
    # `td.name() + "::" + ib.target_type()` — BARE — while the LIR carries
    # `ImplView::identity_trait()`. Measured by census over 4342 compiles:
    # 7 413 092 registrations, 413 603 DUPLICATE KEYS (5.58%; 5.80% on the 5288-file
    # population a day earlier, so the ratio is stable). A duplicate key overwrites a
    # vtable's method vector with a different one.
    # ⚠ CORRECTED 2026-09-11, and the correction is mine: "no multi-module carrier
    # exists" was FALSE OF THE TREE and true only of `soundness_queue_gate.sh`, which
    # compiles ONE FILE. The corpus already carries the scaffolding —
    # `tests/logos/trait_ident_chain/{lhom,hmid}` with `.module` manifests, wired by
    # `LOCAL_TRAIT_IDENT_CHAIN_USERS`, and its consumer `pass/trait_ident_pkg_chain`
    # exists for THIS VERY CLASS: "the consumer links both and must NOT see lhom's
    # `Hash` impls through the bare registry slot". Also present: `coex_from_a/b` (two
    # modules both declaring `widgets::Widget`) and the G156-1 `cross_pkg_*` set.
    # So the carrier belongs in the ORDINARY pass/fail corpus, not in the soundness
    # queue, and the same goes for the `type_aliases_` cross-package read of
    # 2026-09-11i. ⚠ A "cannot be written" recorded without grepping the tree is a
    # claim about my attention, not about the tree.
    # ⚠ NOT a row moved and not a defect closed: the defect is measured and standing.
    # ⚠ 2026-09-11, CARRIER WRITTEN AND RED — `tests/logos/pass/coex_dyn_bare_key`, on
    # the EXISTING `coex` mechanism (one module, two packages, `coex_binary`, a GLOB),
    # 12 lines appended to each of `coex/{alpha,beta}.logos` and one word of cmake.
    # On the unmodified compiler at build hash cccc3157d76a7856: `beta_dyn_mark=1005`,
    # rc 11, expected 0 — beta dispatched through ALPHA's method. The 09-10g scratch
    # repro on today's binary loses the OPPOSITE side (`alpha=2005`), so which
    # registration wins the key is collection order: EVERY first-wins repair is this
    # defect with a different victim, now measured in both directions and not argued.
    # ⚠ THE ENTRY IS STILL NOT A ROW, AND WHAT IT NEEDS HAS CHANGED: not a carrier
    # (it has one) but a decision on the SECOND repair arm — key the registration on
    # the QUALIFIED target (`ImplView::target_typeref` -> `concrete_struct_name`, the
    # `$M`-spelling `ensure_vtable_global` ALREADY probes first), additive at
    # mlir_gen_dyn.cpp:962/982. It closes the cross-package half only; the 834
    # `rekey.diff` events of 09-10g collide on the TRAIT axis with the same target
    # and still need the trait-plane change. The number that partitions them is one
    # bucket on 09-10g's own probe (do the two registrations' `concrete_struct_name`
    # differ?) and has NOT been run. See PROBES.md 2026-09-11 §5.

# TOTAL 15
