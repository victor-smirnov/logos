package mylib;

pub struct Point {
    pub x: i32,
    pub y: i32,
}

struct Secret {
    hidden: i32,
}

pub fn make_point(x: i32, y: i32) -> i32 {
    return x + y;
}

fn internal_helper(x: i32) -> i32 {
    return x * 2;
}
