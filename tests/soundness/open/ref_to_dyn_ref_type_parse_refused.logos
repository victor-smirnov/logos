// SOUNDNESS QUEUE row ref_to_dyn_ref_type_parse_refused (tier 3). LEGAL RUST REFUSED BY THE PARSER.
// A reference to a trait-object reference in TYPE position — `& &dyn Tr`, and `&&dyn Tr` alike — is a syntax error
// ("syntax error near 'fn'"), so no parameter can name the type. Neighbour in shape of ref_to_rawptr_type_parse_refused (`&*mut`).
// Found by round 2026-09-15c-argref (hands a27 b15) on build/ 98ca3fa669468808 43. Rust: exit 0. Legality MEASURED with rustc 1.98.1 (foot of this file).
package ref_to_dyn_ref_type_parse_refused;
trait Tr { fn get(self: &Self) -> i64; }
struct D { v: i64 }
impl Tr for D { fn get(self: &Self) -> i64 { return self.v; } }
fn inner(a: & &dyn Tr) -> i64 {
    let x: &dyn Tr = *a;
    return x.get();
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    let rd: &dyn Tr = &d;
    return (inner(&rd) - 6i64) as i32;
}
// rustc 1.98.1 --edition 2024 (round 2026-09-15f-rustcaudit, class CONFIRMED): COMPILES and RUNS exit 0. Twin src/compiler/probes/2026-09-15f-rustcaudit/rust/ref_to_dyn_ref_type_parse_refused.rs.
