// SOUNDNESS QUEUE row arg_ref_primitive_for_ref_struct_admits (tier 2). AN ILLEGAL PROGRAM IS ADMITTED.
// A call passes `&i64` to a parameter declared `&W`, where `W` is an ordinary struct. logosc compiles it
// with NO diagnostic (rc 0). rustc REFUSES: error[E0308], mismatched types.
// MEASURED 2026-09-16 (round 2026-09-16l-refptee) on the UNARMED base binary 53884ccdf5a135ca 43 —
// this is INHERITED, reachable with no grammar change; it was found as the base-reachable control for a
// hand program of that round's battery.
package arg_ref_primitive_for_ref_struct_admits;
struct W { p: i64 }
unsafe fn read(p: &W) -> i64 {
    return p.p;
}
unsafe fn main() -> i32 {
    let v: i64 = 7i64;
    let r: &i64 = &v;
    return read(r) as i32;
}
// rustc 1.98.1 --edition 2024: REFUSED, error[E0308]. Twin src/compiler/probes/2026-09-16l-refptee/rust/r16_illegal_argtype_ref_struct.rs.
