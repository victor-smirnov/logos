// SOUNDNESS QUEUE row user_type_alias_shadows_stdlib_type_param_refused (tier 3).
// LEGAL PROGRAM REFUSED: A USER PACKAGE'S TOP-LEVEL `type T = i64;` BREAKS THE STDLIB'S GENERIC SPECIALIZATIONS.
//
// The alias is never used. TODAY refused, build 4bc75379010a5384 43: 559 errors, every one inside the stdlib
// ("stdlib/lang/mem/mem.logos:60: error [fn swap (specialization)]: index read through raw pointer requires unsafe
// context", …) — the user's alias name resolves inside a stdlib generic body in place of that body's own type parameter
// `T`. Same shape, measured the same day on the same binary, one program each: `type F = i64;` (14 errors, MapIter),
// `type I = i64;` (71, RevIter), `type U = i64;` (28, iter_eq_by), `type E = i64;` (6, result_unwrap_or_default),
// `type R = i64;` (29, MapIter); `type K/V/S/Item/Output/Res/Rr = i64;` and `struct R` compile. Legal Rust: an item of the
// user crate is not in scope inside std. Legality MEASURED with rustc 1.98.1 (foot of this file).
// Found: round 2026-09-14k-fnptrbinderland, while writing the counter-example `type R = for<'r> fn(&'r i64) -> &'r i64;`
// (c21). Not a borrow-check defect. FIX = compiles and runs 3.
package user_type_alias_shadows_stdlib_type_param_refused;
type T = i64;
fn main() -> i32 {
    return 3i32;
}
// rustc 1.98.1 --edition 2024 (round 2026-09-15f-rustcaudit, class CONFIRMED): COMPILES and RUNS exit 3. Twin src/compiler/probes/2026-09-15f-rustcaudit/rust/user_type_alias_shadows_stdlib_type_param_refused.rs.
