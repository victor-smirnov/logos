// SOUNDNESS QUEUE row array_temp_elem_field_store_refused (tier 3, refuses). A STORE INTO A FIELD OF AN ELEMENT OF A TEMPORARY ARRAY IS REFUSED AS "TOO DEEPLY NESTED".
// `[D{1}, D{10}][0].v = 5;` is legal Rust (rustc n=15: the store lands, both elements drop at the statement end). TODAY
// "assignment target too deeply nested". Found BESIDE round 2026-09-15g-aggtemp (hand c06); NOT CLOSED by its arms (the same sentence under all
// nine; where the place-assign lowering refuses was NOT read — no disposition claimed beyond "unmoved").
package array_temp_elem_field_store_refused;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        [D { v: 1i64, c: p }, D { v: 10i64, c: p }][0].v = 5i64;
    }
    let got: i64 = unsafe { n };
    unsafe { printf("n=%ld\n".as_ptr(), got); }
    if got != 15i64 { return 1i32; }
    return 0i32;
}
// rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/rust/rows/array_temp_elem_field_store_refused.rs: rustc=0 run=0 out=n=15
