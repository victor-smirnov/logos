// SOUNDNESS QUEUE row nested_array_literal_index_initializer_lost_refused (tier 3, refuses). INDEXING AN ARRAY-LITERAL ELEMENT OF AN ARRAY LITERAL LOSES THE `let` INITIALISER.
// `[[D{1}], [D{10}]][1][0].v` is legal Rust (rustc n=11). TODAY "mlir_gen: internal: `let k` initializer produced no value (expr
// kind 14) — statement DROPPED" and COMPILE FAILED.
// NEIGHBOUR of aggregate_literal_temp_place_base_never_dropped_run, found by round 2026-09-15g-aggtemp (hand a09): the inner
// receiver hoisted by `ptidx` compiles and reads n=11 under ptidx / ptown / ptownxf (build 70a78549838d6b7f). It closes with the door.
package nested_array_literal_index_initializer_lost_refused;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        let k: i64 = [[D { v: 1i64, c: p }], [D { v: 10i64, c: p }]][1][0].v;
        if k != 10i64 { return 3i32; }
    }
    let got: i64 = unsafe { n };
    unsafe { printf("n=%ld\n".as_ptr(), got); }
    if got != 11i64 { return 1i32; }
    return 0i32;
}
// rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/rust/rows/nested_array_literal_index_initializer_lost_refused.rs: rustc=0 run=0 out=n=11
