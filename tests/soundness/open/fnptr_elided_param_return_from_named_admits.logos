// SOUNDNESS QUEUE row fnptr_elided_param_return_from_named_admits (tier 2).
// ILLEGAL PROGRAM ADMITTED: A FN POINTER WITH A NAMED PARAMETER REGION IS RETURNED AS `fn(&i64) -> i64`.
//
// The return-site twin of fnptr_elided_param_let_from_named_admits (same fact: an elided fn-pointer region is a wildcard,
// not the pointer's own higher-ranked binder). rustc refuses (E0308, one type is more general than the other); legality
// by reading, no rustc binary. TODAY admitted, build c52dcb19dff987ff 43. Moved by the same arm (fnptrelide) at the
// strict lower_return site, with hand i18 (a closure literal naming 'a returned as `fn(&i64) -> i64`).
// Found: hand i01, round 2026-09-14j-fnptrbinder. FIX = refused with a region sentence.
package fnptr_elided_param_return_from_named_admits;
fn f<'r>(g: fn(&'r i64) -> i64) -> fn(&i64) -> i64 {
    return g;
}
fn main() -> i32 { return 0i32; }
