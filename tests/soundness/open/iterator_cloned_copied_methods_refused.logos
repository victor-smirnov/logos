// SOUNDNESS QUEUE row iterator_cloned_copied_methods_refused (tier 2, `refuses`). Rust-parity audit 2026-09-26 (high): Iterator::cloned() / copied() are not methods
// Logos: error: method call: 'VecIter$G1$i32' has no method 'cloned'
// rustc: compiles and runs, exit=0
// fix hint: stdlib iter.logos: .copied() exists only as free fn iter_copied (KNOWN GAP comment); cloned is absent in every form.
package iterator_cloned_copied_methods_refused;
use logos.mem.collections.vec;
fn main() -> i32 {
    let v: Vec<i32> = vec![1,2,3];
    let r: Vec<i32> = v.iter().cloned().collect();
    if r.len() == 3 { return 0; } else { return 1; }
}
