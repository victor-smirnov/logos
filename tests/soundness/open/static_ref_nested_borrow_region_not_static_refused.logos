package static_ref_nested_borrow_region_not_static_refused;
static G: i64 = 9i64;
static R: &i64 = &G;
fn take(x: & &'static i64) -> i64 {
    return **x;
}
fn main() -> i32 {
    let r: & &'static i64 = &&G;
    let a: i64 = take(&R);
    return (**r + a - 18i64) as i32;
}
// SOUNDNESS QUEUE row static_ref_nested_borrow_region_not_static_refused (tier 3).
// Legal Rust: `&G` is `&'static i64`, and a static item's elided `&i64` is `&'static i64`
// (items.static), so both `&&G` and `&R` are `& &'static i64` (rustc 1.98.1 MEASURED, foot of this file).
// TODAY refused twice: "let 'r': variance mismatch — expected &&'static i64, got &&i64" and
// "call to 'take' arg 1: variance mismatch — expected &&'static i64, got &static &i64".
// Two spellings of one missing read: the region of a borrow OF A STATIC one level under a
// reference, and a static item's declared elided region at a READ (2026-09-13d filled it at
// WRITES only; its note names static_arg_pins_shared_callee_region_refused as the door in
// front of the declaration-site fill). Found 2026-09-14a-ptrcoerce (r14j, M10).
// rustc 1.98.1 --edition 2024 (round 2026-09-15f-rustcaudit, class CONFIRMED): COMPILES and RUNS exit 0. Twin src/compiler/probes/2026-09-15f-rustcaudit/rust/static_ref_nested_borrow_region_not_static_refused.rs.
