// SOUNDNESS QUEUE row tuple_of_arrays_debug_internal (tier 3, `refuses`). Found 2026-09-25 by the discovery battery.
// `{:?}` of `([1, 2, 3], [4, 5])`: rustc prints `([1, 2, 3], [4, 5])`. Logos: mlir_gen "'return' value lowered to no value"
// in `$tuple$variadic__fmt` over array elements.
package tuple_of_arrays_debug_internal;
use logos.std.fmt;
fn main() -> i32 {
    let pair = ([1i64, 2i64, 3i64], [4i64, 5i64]);
    println!("{:?}", pair);
    return 0;
}
