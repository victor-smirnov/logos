# rustc Test Import Provenance

This file is the **authoritative manifest** for every test under
[tests/imported/ui/](ui/) — the upstream commit it was sourced from,
the original path in rust-lang/rust, and a one-line summary of any
adaptations made.

When this file is empty (no rows in the manifest table), no tests
have been imported yet — the infrastructure is in place for the
first batch.

## Pinned upstream commit

Imports happen in **batches**; each batch pins a single rustc commit
that all files in that batch were sourced from. When a new batch
starts, a new commit row appears here and is referenced from the
per-file rows below.

| Batch | rustc commit (SHA) | Date | Imported by | Scope |
|---|---|---|---|---|
| B136 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/{drop,methods,traits,expr,cast,mir,numbers-arithmetic,self,generics,where-clauses,ufcs,coercion,autoref-autoderef,functions-closures,structs,enum} run-pass — 23 NEW DISTINCT run-pass tests from areas with remaining untapped run-pass coverage (skimmed against existing `pass/` to avoid source dupes). Global-unique suffix `-b136` on all files. Prelude-aware (NO `use`). All 23 compile + link + exit 0. **drop (2):** drop-on-ret (early `return` inside an `if` block runs a String local's destructor cleanly), conditional-drop-10734 (`if true { let _a=Foo{} }` AND `if false{}else{let _a}` each run the Foo dtor at block-end — count 1 then 2; local `trait Drop`+`*mut i64` cell). **methods (1):** inherent-methods-same-name (two inherent impls of generic `Foo<T>` at u64 vs i64 each define a same-named `bar`, dispatched by receiver type). **traits (2):** trait-default-method-on-primitive (issue-3683 — trait DEFAULT method `b` calling required `a` on self, impl'd for i64, dispatched on a value), anon-static-method (inherent associated fn `Foo::new()->Foo` constructor). **expr (2):** block-generic (a `{ expr }` block as the VALUE of a generic-typed local inside `test_generic<T:Copy>`, at bool+i64), if-generic (an `if/else` expr as that value, at bool+i64). **cast (1):** enum-to-numeric-cast (unit-only enum → i64/i32/u64/u32, yielding the variant ordinal; payload-variant error-case dropped). **mir (1):** if-let-converging-enum-ret (issue-66851 — `if let Some(r)=opt { Res(r) } else { Res(42) }` both arms construct the same single-payload enum; `"1".parse()` source → bare `Option<u64>`; enum equality via match, not `==`). **numbers-arithmetic (2):** arith-unsigned (u8/u16/u32 ordering+div+rem near each max), isize-base (i64 literal/cast/inference/fn-return forms all equal). **self (1):** uniq-self-in-mut-slot (`mut self: X` trait method mutates a field of the moved receiver and returns it; `Box<Self>`→stack). **generics (2):** generic-temporary (generic `apply<T:Copy>(produce:fn()->T, consume:fn(T))->T` wiring fn-ptrs; `static mut` observation→returned value), generic-tag-match (matching a single-payload generic enum `Foo<T>` inside `altfoo<T>` with turbofish ctor `Foo::Arm::<i64>(10)`). **where-clauses (1):** where-clause-method-substituion (impl-side `fn method<U>(self) where X: Foo<U>` discharged from `impl Foo<S> for X`; the trait-DECL-side `where` dropped — see G136-1). **ufcs (1):** ufcs-trait-method-call (UFCS `Greet::hi(&f)` on a struct receiver resolves to the same impl as `f.hi()`; UFCS on a PRIMITIVE receiver is known-open). **coercion (1):** coerce-reborrow-imm-ptr-arg (a `&mut i64` arg reborrows as `&i64` passed to a shared-ref fn). **autoref-autoderef (1):** auto-ref (auto-`&` of a by-value receiver for a `&self` trait method `x.getme()`). **functions-closures (3):** closure-returning-closure (zero-arg closure returning a `fn(i64,i64)->i64`; chained `g()(args)` is a parse error → let-pin the callable), bare-fn-implements-fn-mut (bare fn item satisfies `F:FnMut()` + `G:FnMut(i64,i64)->i64`), fn-item-type-cast (fn ITEM→fn-ptr alias `IntMap`, incl. an if/else casting two distinct items to the same ptr type). **structs (1):** field-destruction-order (both fields of `C{a:A,b:B}` run their dtor exactly once; Logos drops fields bottom-to-top — log==21 — vs rustc top-to-bottom, an implementation-defined-order DIVERGENCE per upstream's own comment; see G136-2). **enum (1):** enum-univariant-repr (univariant C-like enum explicit discriminant `X as i32==17` + implicit-zero `Y as u16==0`; mem::size_of/transmute facets dropped). Mechanical adaptations: `package <name>;` header; `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; negatives `0 - n`; `&self`→`self:&T` / `mut self`→`mut self:T`; `match self`→`match *self`; `Box<T>`/`Box<Self>` field+receiver → stack `T`; `String::len`→`.len()`; `static`/`static mut` → returned value or `*mut T` cell; local `trait Drop { fn drop(self: Self) }` + `*mut i64` for drop counting; `assert!`/`assert_eq!`/`panic!`/`println!` → distinct-nonzero return codes; `#[repr(..)]`/`#[derive(..)]` dropped; cross-crate `aux-build`/transmute/mem::size_of facets dropped. Gaps surfaced (2 NEW): **(G136-1)** a `where` clause on a trait-method DECLARATION (ending in `;`) is a PARSE error (`fn m<B>(..) -> T where A: Clone;` → "syntax error"), while the SAME clause on an IMPL-side DEFINITION (before `{..}`) parses + discharges fine — tractable as a missing-case in the trait-method-declaration grammar production (parallel-mapping to the definition rule). **(G136-2 — DIVERGENCE, not a bug)** struct field DROP order is bottom-to-top (`b` then `a` for `C{a,b}`) vs rustc's top-to-bottom; both fields drop exactly once (the real invariant); upstream's source itself calls the order implementation-defined. Re-confirmed known-open (NOT re-reported, candidates dropped/distilled): `static`/`static mut` items; chained postfix call `g()(args)` (same family as `arr[i](args)`); UFCS `Trait::method(&prim)` on a primitive (struct receiver works); generic struct literal with explicit type-args `Foo<u64>{..}` (use annotation-driven bare ctor); struct `T:Copy` auto-copy (MEMORY divergence B1 — block/if-generic re-targeted to scalar i64); inherent-vs-trait method collision (`impl-inherent-prefer-over-trait.rs`, not imported). (see notes/B136-ui-surfaced-gaps.md). |
| B135 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/{cast,enum,structs-enums,self,generics,where-clauses,coercion,functions-closures,expr,binop,numbers-arithmetic} run-pass — 23 NEW distinct tests from areas with remaining untapped run-pass coverage (skimmed against existing `pass/` to avoid source dupes). Global-unique suffix `-b135` on all files. Prelude-aware (NO `use`). All 23 compile + link + exit 0. **cast (2):** u8-to-char-cast (issue #9918 — `(0u8+0u8) as char` == `'\0'`), cast-char-int-roundtrip (`cast.rs` — char↔int casts: `'Q' as i64/u32/u8`, truncation, sign-reinterpret `as u8 as i8`, `0x51 as char`, `false as u32`; the `_`-inferred raw-ptr tail dropped). **enum (3):** enum-variants (heterogeneous tuple-payload + struct-payload variants; mutable enum local reassigned across variants; struct-variant field permutation), enum-discr (explicit C-like discriminants incl. NEGATIVE values, read via `as u64`/`as i64`), enum-discr-derived (issue-23304-2 — explicit discriminant value read via `as i64`; the `X::A as isize` const-expr initializer pre-evaluated to a literal). **structs-enums (4):** rec-extend (functional-record-update `Point{x:..,..origin}` leaving base intact), struct-field-shorthand (field-init shorthand `Foo{x,y:y,z}` in several orderings + trailing-comma `Bar{x,}`), borrow-tuple-fields (shared `&x.0` + mutable `&mut x.1` borrows of distinct tuple fields, write through the mut place; the positional tuple-struct half dropped), tag-custom-eq (`tag.rs` — hand-written `eq`/`ne` on an enum with a tuple-payload + a fieldless variant, `match *self`/`match *other`, fieldless inner arm via `_`). **self (3):** move-self (by-value `foo(self)` calling another by-value `bar(self)`, String field `.len()`), self-re-assign (issue #3290 — `x = x` self-assignment does not drop-before-take; Box/Rc halves → stack scalar), by-value-self-in-mut-slot (`mut self: X` trait method mutating a field of the moved receiver and returning it, via a `Changer` trait). **generics (1):** generic-exterior-unique (generic struct built inside a generic fn, instantiated at i64, field read back; `Box<T>` field → plain stack `T`). **where-clauses (1):** where-clauses-eq-fn (free generic fn with `where T: Eq` comparing two `&T` via `*x == *y`; the blanket-impl + trait-method facet of `where-clauses.rs` dropped). **coercion (1):** coerce-reborrow-mut-ptr-rcvr (reborrow `&mut SpeechMaker` for each of three successive `&mut self` calls, no move error, cumulative mutation observed). **functions-closures (2):** fn-lval (bare fn ITEM passed where a `fn(i64)->i64` POINTER param is expected — fn-item→fn-ptr coercion), fn-bare-coerce-to-block (bare fn item satisfying an `F: FnOnce()` generic bound, invoked through it). **expr (1):** block-fn (item-fn nested inside another fn body, bound to a local then called as a fn value). **binop (1):** binary-minus-without-space (issue #954 — `a-b` w/o spaces lexes as subtraction; negative literal match arm `-1`). **numbers-arithmetic (4):** div-mod (integer `/` and `%` vs literals and variables), u8-incr-decr (u8 add/sub reaching a common value), i8-incr (i8 +1/-1 round-trip to a negative value), floatlits (f64 literal parsing incl. scientific-notation literals + ordering). Mechanical adaptations: `package <name>;` header added (Logos requires it; was the first parse error); `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; negatives `0 - n`; `&self`→`self:&T` / `mut self`→`mut self:T`; `match self`→`match *self`; `Box<T>` field/receiver → stack `T`; `String::len`→`.len()`; assert/assert_eq!/panic!/println! → distinct-nonzero return codes; cross-crate `aux-build` tests dropped; scientific-notation float literals require a decimal-point mantissa (`5e-11`→`5.0e-11`, documented lexer shape). Gaps surfaced (1; re-confirms B111, NOT a fresh bug): **(G135-1)** `==` on `Option<T>` mis-evaluates — `None == None` AND `Some(3) == Some(3)` BOTH return FALSE (the operator path yields constant-false for enum operands; masked for the unequal case). Same root as B111 (stdlib `Option<i64> == Option<i64>` derived `==` mis-codegen), here a silently-wrong RUNTIME result rather than a codegen dump. Looks tractable as a missing-case/parallel-mapping fix: operator-`==` lowering for an enum operand likely isn't routing to the derived/variadic structural-eq the `assert_eq!`/`.eq()` channels use. `structs-enums/compare-generic-enums.rs` dropped on this. Re-confirmed known-open (NOT re-reported, candidates dropped/distilled): block-wrapped closure literal `{|i|i}` (parse error, `closure-inference2.rs`); generic tuple-struct `S<T>(T)` (G134, `generic-newtype-struct.rs`); positional tuple-struct + named-index field `S(u8,u16); S{0:a,1:b}` (`numeric-fields.rs`); payload-binding inside a nested-variant struct-field pattern in a match arm (`record-pat.rs`); in-language `mod` + `::path()` call (`generic-fn-twice.rs`, `expr/scope.rs`); cross-crate `aux-build` (`method-self-arg-aux{1,2}.rs`). (see notes/B135-ui-surfaced-gaps.md). |
| B134 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/{self,for-loop-while,expr,coercion,numbers-arithmetic,methods,generics,structs-enums,functions-closures,traits,where-clauses,cast} run-pass — 22 NEW distinct tests across areas with remaining untapped run-pass coverage. Per-area unique suffixes (`-slf4`/`-flw4`/`-xpr4`/`-coe4`/`-na4`/`-mth4`/`-gen4`/`-se4`/`-fc4`/`-tr4`/`-wc4`/`-cst4`) to avoid the global ctest-name clash. Prelude-aware (NO `use` except Vec). All 22 compile + link + exit 0. **self (2):** move-self-slf4 (by-value `foo(self)` calling another by-value `bar(self)` on the moved receiver, String field via `.len()`), by-value-self-mut-slf4 (a `mut self: Self` method mutating a field of the moved receiver and returning it, dispatched through a `Changer` trait). **for-loop-while (3):** labeled-break-flw4 (labeled `break 'lbl` to an OUTER loop from an inner `loop`, across loop/for/while), loop-break-cont-flw4 (`break`+`continue` in bare `loop`s incl. a `continue` skipping an even/odd assignment), nested-loop-break-unit-flw4 (issue #2642 — `loop { loop { break; } }` type-checks as a bound value; the fn is intentionally NOT called, matching upstream's empty main). **expr (3):** block-expr-xpr4 (standalone `{ .. }` blocks as VALUES: bool block, struct-lit block, while-mutated-local tail block), copy-struct-mutref-xpr4 (POD struct `let mut y = x` independent copy, mutated through `&mut` arg, source unaffected), early-return-in-binop-xpr4 (`return` inside a block used as the RIGHT operand of `+` short-circuits the whole expr). **coercion (3):** coerce-fn-if-else-coe4 (least-upper-bound coercion of two distinct fn ITEMS to a common `fn()->i64` via if/else, match, and a `[fn;2]` array — array elem bound to a local before call), coercion-generic-dyn-coe4 (coerce `&Struct` to `&dyn Trait` and dispatch a method), coerce-unify-return-coe4 (wrapping a fn item in `Some(..)` unifies to `Option<fn()->i64>`, then match-unwrap+call). **numbers-arithmetic (1):** shift-various-types-na4 (right-shift accepts a shift-amount of ANY integer width i8..u64 read from struct fields). **methods (1):** method-two-trait-defer-mth4 (selecting between two trait impls keyed on the Vec's element type: `impl MyFoo for Vec<u32>` vs `Vec<i32>`). **generics (2):** generic-fn-id-gen4 (identity `id::<T>(x)` at i64/char/3-field-struct), generic-tup-gen4 (generic `get_third<T>((T,T,T))->T` at i64/u8). **structs-enums (3):** rec-struct-copy-se4 (4-field record struct passed BY VALUE through fn args twice + field reads), tag-variant-disr-val-se4 (explicit enum discriminant hex VALUES + `as i64` readout; shift-derived disr pre-evaluated to literals), tuple-struct-fields-se4 (concrete tuple struct `Pair(i64,i64)` positional construction + `.0`/`.1`). **functions-closures (1):** closure-calls-fn-fc4 (a closure capturing a value and forwarding it to a free fn, bound then invoked). **traits (1):** default-method-simple-tr4 (a trait DEFAULT method `f` calling another required method `g` on self; unit struct `A`->`A {}`). **where-clauses (1):** where-clauses-method-wc4 (method-level `where T: Eq` on a generic struct `Foo<T>` adding an out-of-scope bound, then `==`). **cast (1):** cast-to-char-compare-cst4 (issue #41998 — `char as char` casts + `<` ordering between two chars). Mechanical adaptations: `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; negatives `0 - n`; `&self`→`self:&T` / `mut self`→`mut self:Self`; `String::len`→`.len()` (returns i64); `Vec::new`→`vec_new::<T>()` + `use logos.mem.collections.vec`; `Vec::len`→`.length()`; `for x in &v`→`*x` deref; assert/println!→distinct-nonzero return codes; `#[derive(Copy,Clone)]` dropped (POD structs copy by value); generic trait type-params distilled to concrete where the lifetime/str machinery wasn't the facet; `Box<dyn>` dropped in favor of `&dyn`. Gaps surfaced (1 NEW; rest re-confirmed known-open): **(G134-1)** `for x in &v` over a `Vec<Struct>` (multi-field struct elements) yields the WRONG element per iteration — iteration COUNT is correct (= `v.length()`) but the dereferenced struct read in each arm is wrong (reads the LAST element's fields, not each successive element's): a 3-element accumulator `acc=acc*10+(*elt).x` over `Pair{x:1},{x:2},{x:4}` produced `102` not `124`. Direct indexed `v.get(i)` is CORRECT, and `for i in &[T;N]` (array, scalar) works, so the bug is specific to the `for ... in &Vec<Struct>` iterator-element deref lowering (candidate `for-loop-while/for-destruct.rs` dropped). Re-confirmed known-open (NOT re-reported, candidate dropped/distilled): never-typed `if`/`loop` arm as a sub-expression value (`if c { return 7; } else { 5 }` → "branches have incompatible types: void vs i64", `expr/if-bot.rs` dropped); GENERIC tuple-struct `S<T>(T)` ("call to undefined function 'S'" — concrete `Pair(i64,i64)` works, re-distilled); shift-expr in enum discriminant position (`Purple = 1<<1` syntax error — pre-evaluated to literal); radix int literal with `_` before suffix (`0xBEEF_i64` malformed — supported form `0xBEEFi64`; `integer-literal-radix` already exists, dropped as dupe); unit struct `struct A;` (use `struct A {}`); zero-arg closure without explicit ret-type/block (`|| f(v)` syntax error — use `|| -> T { ... }`); block-wrapped closure literal `{ || ... }`; chained `arr[i](args)` (bind `let f=arr[i];` first). (see notes/B134-ui-surfaced-gaps.md). |
| B133 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/{unboxed-closures,ops/binop,methods,mir,structs-enums,functions-closures} run-pass — 23 NEW tests targeting areas thin in `pass/`: `unboxed-closures/` (had only 1 imported) and `ops/` (had only a coretest), plus distinct `methods`/`mir`/`structs-enums`/`functions-closures` facets. Suffix `-b133`. Prelude-aware. All 23 compile + link + exit 0. **unboxed-closures (11):** uc-simple (two-arg closure literal called directly), uc-generic-call-it (`fn(i32,i32)->i32`-ptr HOF + fn-item→fn-ptr coercion at call site), uc-single-word-env (move-closure capturing one upvar `z`, single-word env codegen path), uc-infer-fnmut (closure mutating a captured local, FnMut inferred, called twice, observed after scope), uc-infer-upvar (generic `f<F:FnMut()>` invoking a capture-mutating closure through the bound), uc-by-ref-kinds (by-ref capture across Fn/FnMut: Fn-bound HOF reads a captured var, FnMut-bound HOF mutates it), uc-all-traits (one closure shape satisfies Fn/FnMut/FnOnce generic bounds in turn), uc-extern-fn (a bare fn ITEM `square` satisfies the full Fn family threaded through three bounded HOFs), uc-fnonce-capture-struct (move-closure captures an owned non-Copy struct by value, reads fields, through `F:FnOnce()->i64`), uc-zero-args (zero-arg unit-returning closure called for effect twice + a zero-arg value-returning closure), uc-generic-call-twice (`apply_twice<F:FnMut(i64)->i64>` calling the bound closure twice, threading the intermediate). **ops/binop (3):** operator-overloading (user `+`/`-` (Add/Sub), unary `-` (Neg), custom-PartialEq-driven `==` on a Point struct), operator-mul-rem-div (`*`/`/`/`%` Mul/Div/Rem on an i32 newtype), augmented-assign-struct (`+=`/`-=`/`*=` dispatch to AddAssign/SubAssign/MulAssign on a struct). **methods (3):** method-where-clause (trait method dispatch on concrete `Option<u32>` via method syntax + prelude `unwrap_or`), method-projection (issue #20469 — method-notation dispatch onto an associated-type projection `Self::F: Doubler`), method-self-arg-trait-ufcs (UFCS `Bar::foo1(&x)`/`Bar::foo2(x)`/`Bar::bar1(&x)` with by-ref-required / by-value-required / by-ref-DEFAULT self kinds on a concrete receiver). **mir (2):** mir-match-arm-guard (#30527 — boolean GUARD over a bound payload `Some(xyz) if xyz>100` alongside plain + fieldless arms), mir-call-converging (a converging call assigned inside `loop{ x=f(); break; }` then read out). **structs-enums (2):** class-poly-methods (generic struct `Cat<U>` with a method-level-generic `speak<T>` + `&mut self` mutator/reader over a Vec field, U=i64), rec-tup (type alias `Rect=(Point,Point)`, destructuring `let (a,_)=r;`/`let (_,b)=r;` to project a STRUCT out of a tuple + `.x`/`.y`), either-construct-match (generic two-param enum `Either<T,U>` with Left(T)/Right(U), constructed + matched). **functions-closures (1):** closure-immediate (a three-mixed-int-width-arg `\|a:u8,b:u16,d:u8\|` closure called immediately — the immediate-logic struct-reopt path). Mechanical adaptations: `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; negatives `0 - n`; `&self`→`self:&T` / bare `self`→`self:Self` / `&mut self`→`self:&mut T`; `match self`→`match *self`; `assert_eq!`/`assert!`/`panic!`/`println!`→distinct-nonzero return-code checks; `F: Fn/FnMut/FnOnce` bounds kept as generic Fn-family bounds OR lowered to `fn(..)->..` pointer params (bare fn items coerce); `Vec::new`→`vec_new::<T>()` + `use logos.mem.collections.vec`; `Vec::len`→`.length()`; operator traits via `use logos.lang.ops`; `Box<T>`/`Box<Self>` receivers dropped; `static mut` counters → a local or `*mut`; `PhantomData` dropped; assoc-type `get(&self)->&Self::F` returns a fresh value; operator MULTIDISPATCH (`impl Add<isize>`) dropped (Logos ops traits are single-type Self=RHS=Output divergence). Gaps surfaced (2 NEW; rest re-confirmed known-open): **(G133-1)** user operator overloading is wired for only a SUBSET — `+`/`-`/`*`/`/`/`%`/unary-`-` DO dispatch to the trait impl on a struct, but the BITWISE operators `^` (BitXor) and (same hardcoded path) `&`/`\|`/`<<`/`>>` on a user struct are NOT routed to their `ops` trait impls (sema: `operator '^': left must be integer or bool, got <Struct>`); the BitAnd/BitOr/BitXor/Shl/Shr traits exist but the operator lowering for these does not consult them for non-primitive operands. **(G133-2)** assigning to an OUTER mutable local the closure didn't already capture, from inside a non-`mut` (`Fn`-kind) closure body, fails (`mlir_gen: assign to undefined '<name>'`); `mut`-closure capture-mutation works (worked around via return value). Re-confirmed known-open / dropped: operator multidispatch + distinct-RHS/Output (single-type `ops` divergence); `feature(fn_traits)` manual `impl Fn/FnMut/FnOnce for S` w/ `extern "rust-call"` tuple-args (overloaded-calls-simple/zero-args); Rc/RefCell/Box overloaded-Deref auto-deref chains (overloaded-deref/autoderef); method autoderef THROUGH a user `Index` place `f[1].inc()` (notes/B108); scalar move-closure-capture VALUE-isolation (a `move ||` mutating a captured scalar mutated the OUTER var → exit 1 → that one candidate dropped, not re-reported as a confirmed gap). (see notes/B133-ui-surfaced-gaps.md). |
| B132 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/{binding,tuple} run-pass — 22 NEW tests distilling DISTINCT let/match-binding + tuple features (skimmed against the large existing `pass/{binding,tuple,tuples}/`; the `tuple/` source dir is nearly exhausted so most come from `binding/` plus distilled tuple facets). Basenames suffixed `-bn2` (binding, into `pass/binding/`) / `-tp2` (tuples, into `pass/tuples/`) to avoid the global ctest-name clash. Prelude-aware (NO `use`). All 22 compile + link + exit 0. **binding (12, -bn2):** match-with-ret-arm (diverging-arm type unification — `return` in one arm, a value in the other), pattern-in-closure (closure params that are a tuple PATTERN `\|(x,_)\|` and a struct PATTERN `\|Foo{x,y}\|`), match-at-binding-range (`name @ <inclusive-range>` at-bindings + catch-all `@`), match-phi (fieldless-enum match, per-arm assignment join + a closure invoked in one arm), match-ref-binding-in-guard (`Some(ref z) if *z` — `ref`-binding read inside a match-arm GUARD + re-read in body; Mutex distilled out), match-tag-multi-payload (enum with 3 tuple-payload variants of differing arity, a distinct positional field bound per arm), while-let-option (`while let Some(x) = ..` draining loop), let-else-binding (`let Some(x) = .. else { diverge };` refutable binding, both arms), nested-tuple-let (`let (a,(b,c)) = ..` + `let ((p,q),r) = ..`), match-guard-binding (binding + boolean GUARD arms over overlapping ranges + a literal arm + a catch-all), if-let-else-chain (`if let .. else if let .. else ..`), struct-rest-pattern (struct `..` rest in both a destructuring `let` and a `match` arm). **tuples (10, -tp2):** tuple-in-struct-field (tuple-typed struct field + chained `s.field.0` projection), tuple-in-enum-payload (tuple as an enum-variant payload, bound WHOLE then projected — the nested tuple-destructuring sub-pattern form is N2), tuple-eq-compare (structural `==`/`!=` on 2- and 3-tuples via the variadic-tuple Eq prelude), tuple-return-destructure (fn returning a 2-tuple consumed by destructuring `let`, both branches), tuple-deep-nested-field (`.1.1.0` / `.1.1.1` chained projection through a 4-deep nested tuple), tuple-index-on-borrow (`.0`/`.1` on a `&(i64,i64)` reference param), tuple-mixed-types (`(i64, bool, char)` heterogeneous tuple), tuple-mut-field-write (writing through a tuple field place `t.0 = ..` on a `mut` local), tuple-arg-ret-chain (3-tuple through fn arg + return, composed `shift(shift(t))`), unit-tuple-value (`()` as a value: fn `-> ()` returning `()`, `let u: () = ()`). Mechanical adaptations: `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; negatives written `0 - n`; `assert_eq!`/`assert!`/`panic!`/`println!`→distinct-nonzero return-code checks; Mutex/`lock().unwrap()` distilled to a bare `Option<bool>`; `"str".parse::<usize>().ok()` distilled to a bare `Option<u32>`. Gaps surfaced (4 NEW; the rest re-confirmed known-open): **(N1)** a 2-tuple `match` where the FIRST element pattern is a NO-PAYLOAD variant (`None`) short-circuits the second element's discriminant check — `(None, Some(9))` wrongly matches a `(None, None)` arm placed before the `(None, Some(_))` arm (arm-ordering-dependent silent miscompile; correct when first element is `Some(_)`); **(N2)** a nested tuple DESTRUCTURING sub-pattern inside a `match` arm (`E::Pair((a,b))` AND plain `(a,(b,c))`) drops the inner bindings (`undefined variable`) — the same pattern in a `let` WORKS, so it's match-arm-specific; **(N3)** a `ref` ARGUMENT pattern `fn f(ref _s: S)` is parsed as a `&`-typed param (`expected &<error>, got S`); **(N4)** `name @ <enum-variant>` at-binding (`x @ Some(_)`) confuses the exhaustiveness checker (range-`@` works). Re-confirmed known-open (NOT re-reported, source dropped/distilled): `mut` binding-mode inside a destructuring `let`/fn-param/`mut x @ pat` (B109 gap #9 — `mut-in-ident-patterns.rs`); `match return X {}` bot-in-scrutinee (parse error, `match-bot-2.rs`); string-literal `match` patterns (`match-str`/`match-borrowed_str`); reference patterns `&Pat` (`borrowed-ptr-pattern-infallible`/`match-unsized`); never-type `!` patterns (`empty-types-in-patterns`); the `&mut self` stdlib `Drop` not firing glue (use local `trait Drop { fn drop(self: Self) }`); bare-rest tuple patterns `(.., W)` (`pat-tuple-5`). (see notes/B132-ui-surfaced-gaps.md). |
| B131 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/{structs,structs-enums,fn,functions-closures} run-pass — 22 NEW tests distilling DISTINCT struct + function features (skimmed against existing `pass/{struct,structs-enums,functions-closures,fn}/`; dropped two that turned out already-imported: struct-update-syntax-2463, nested-function-names-8587). Basenames suffixed `-st2` (structs, into `pass/struct/`) / `-fn2` (functions, into `pass/functions-closures/`) to avoid the global ctest-name clash. Prelude-aware. All 22 compile + link + exit 0. **structs (11, -st2):** struct-new-field-name (field literally named `new`), struct-pattern-matching (full field-binding pattern + `Foo{..}` rest-pattern), numeric-fields-named (rustc tuple-struct `S(u8,u16)` → named fields, out-of-order literal + `..`-rest field pattern), large-records (12-field literal + field access), newtype-fnptr-field (rustc tuple-struct newtype → named struct with a `fn(T)->R` field, called via `(s.data.compute)(s)`), nested-struct-destructure (`Y{y:X}` nested literal + nested binding pattern `let Y{y:X{x}}=`), generic-struct-methods (`Wrap<T>` inherent get/make on i64 + bool instantiations), method-mut-self (`self:&mut T` field-mutating methods + by-value-self reader), struct-field-update (direct mixed-type field writes on a `mut` local), generic-fnptr-field (generic `R<F>{field:F}` with `F=fn()->i64`, fn item coerced in + called back). **functions (11, -fn2):** fun-call-variants (HOF taking `fn(i64)->i64`, direct + indirect call), expr-fn (fns whose body is match/if/block/early-return/let-block-tail), fn-item-type-coerce (fn ITEM→fn-POINTER alias `type IntMap=fn(i64)->i64` at let + generic-arg positions), fnptr-struct-field (rustc `extern "Rust" fn()` field → `fn()->i64` field, bare item + ptr local stored & called), mutual-recursion (is_even/is_odd), return-tuple (`->(i64,i64)` + destructure + `.0/.1`), return-struct (build + read a returned struct, translate chain), higher-order-apply (two fn items through one HOF + apply-twice composition), recursion-factorial (factorial + tail-accumulator sum), fnptr-mut-outparam (rustc `&mut bool` out-param → `*mut bool` cell written through a `fn(i64,*mut bool)` pointer), many-params (5-param mixed-type fn + struct-by-value dot product), fnptr-in-array (`[fn(i64)->i64;3]` dispatched by index — index let-pinned per G131-2). Mechanical adaptations: `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; tuple structs `S(..)`→named-field structs (Logos has no positional tuple-struct field/pattern syntax); `&self`→`self:&T`, `&mut self`→`self:&mut T`; `Box<isize>`→stack/scalar; `extern "Rust" fn()` ABI annotation dropped; `&mut`-out-param through a fn pointer→`*mut` cell; `F: FnOnce()/FnMut()` bounds→`fn(..)->..` pointer params (bare fn items coerce); `static`/`&'static` const instances dropped (§A1); `Default::default()`-driven inference built explicitly; `assert_eq!`/`panic!`/`println!`→distinct-nonzero return-code checks; `match self`→`match a.foo`/by-value. Gaps surfaced (2 NEW, ALL §B catch-up; NO new §A): **(G131-1)** matching a GENERIC-enum-typed STRUCT FIELD (`struct R{foo: Opt<u64>}; match a.foo {…}`) SIGSEGVs at runtime — even when the field is first let-pinned to a local; standalone generic-enum match AND non-generic-enum-as-struct-field match BOTH work, so the gap is the conjunction (generic-enum monomorph layout when reached through a struct-field projection). **(G131-2)** the chained postfix `arr[i](args)` (array index immediately followed by a call) is a PARSE error (`syntax error near ']'`); let-pinning the indexed fn-pointer to a local first works. Re-confirmed known-open (NOT re-reported, source dropped): FRU with a field-shadow where the FRU base supplies a moved-then-superseded field (`S{f0:.., ..S{f0:moved,..}}` → spurious "use of moved variable"); unit-like struct `struct Foo;` bare form (Logos uses `struct Foo {}`, which changes the match-as-unit-value shape). (see notes/B131-ui-surfaced-gaps.md). |
| B130 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/closures + numbers-arithmetic run-pass — 11 tests (import agent stalled mid-batch; partials verified + salvaged by hand). closures (7, -cl3): block-wrapping fnptr, explicit param/ret types, generic FnOnce unit-ret, in-tuple-struct-destructure, noparam block+expr, ref-param pattern, return-boxed-fnmut. numbers (4, -na2): float infinity arith, float-int cast roundtrip, float-signature fn, int-width cast chain. All compile+link+exit 0. No new gaps recorded (agent stalled before the report). |
| B129 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/{traits,issues} run-pass — 25 NEW tests distilling DISTINCT trait + scattered-issue features (skimmed against the large existing `pass/{traits,issues}/` corpus to avoid dupes). Basenames suffixed `-tr3` (traits) / `-is2` (issues) to avoid the global ctest-name clash. Prelude-aware. All 25 compile + link + exit 0. **traits/ (15):** copy-trait-implicit-copy-tr3 (`T: Copy` type-param read by-value twice — implicit copy, §B1 now-DONE), default-method-arg-required-9123-tr3 (default method `b` = required `a()`+const), region-pointer-as-dyn-tr3 (`(&a) as &dyn Foo` cast + dispatch), coercion-generic-trait-arg-tr3 (generic `Trait<T>` concrete impl, value as `&dyn Trait<i64>`), multi-typaram-trait-tuple-ret-tr3 (`trait B<T,U> { fn get(&self)->(T,U) }`), supertrait-assoc-fn-returns-self-4107-tr3 (no-self assoc fn `-> Self` via `Mat::identity(..)` type path), impl-trait-for-primitive-5280-tr3 (impl a trait for `u32`, call on a value), inherent-impl-on-generic-enum-tr3 (`impl<T> Opt<T>` inherent method on a generic enum), blanket-via-supertrait-bound-5988-tr3 (`impl<U: T> B for U` blanket-via-supertrait dispatched through `&dyn B`), default-method-chain-3level-tr3 (3-level default-method chain meow→scratch→purr, impl supplies only the leaf), default-method-calls-supertrait-bounded-fn-tr3 (subtrait default method calls a free fn bounded over the supertrait, passing self), mut-default-method-via-supertrait-3979-tr3 (subtrait `&mut self` default method mutates via supertrait getter/setter), multidispatch-convert-target-tr3 (`Convert<Target>` two impls + `doit<T:Convert<U>,U>` dispatch), assoc-const-read-in-method-body-tr3 (assoc const read from inside the impl's own method body via concrete type path), blanket-impl-all-T-via-generic-fn-tr3 (unconstrained `impl<T> Foo for T` dispatched through `foo<T:Foo>(t)` on a STRUCT receiver — primitive-receiver path is G4). **issues/ (10):** issue-16671-fnonce-move-closure-is2 (move-closure capturing+mutating an owned Vec passed to `FnOnce`-bounded fn; `Vec::new`→`vec_new::<T>()`), issue-17816-closure-field-call-is2 (struct field is a generic `F: Fn(..)->..`, called via `(s.f)(x)`), issue-21634-parse-mutate-block-value-is2 (float arith + mutate-then-yield block value `{ y+=5.0; y }`; str→f64 parse distilled out), issue-21922-operator-ufcs-is2 (operator-trait method via UFCS `P::add(a,b)` ≡ `+`; primitive `Add` distilled to a user struct), issue-22258-add-output-assoc-is2 (`fn sum<T: Add>(a,b)->T { a+b }`; primitive Add→user struct impl), issue-26709-drop-writes-through-field-is2 (struct with a `*mut` cell field whose `Drop` writes through it, observed after an inner block; `&mut i32`→`*mut i64`), issue-36260-closure-captures-string-is2 (move-closure captures an owned String — drop-glue), issue-43923-array-of-fn-items-is2 (same fn item stored 3× in an array + called; `?Sized` slice-field tail distilled out), issue-4759-1-byval-self-on-primitive-is2 (trait method taking `self` by value on a primitive, `4.f()`), issue-50415-range-inclusive-is2 (inclusive range `a..=b` iterated). Mechanical adaptations: `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; `&self`→`self:&Self`/`self:&T`; `Box<T>`→stack, `Box<dyn>`→`&dyn`; `Vec::new`→`vec_new::<T>()` (+`use logos.mem.collections.vec`); operator traits→`use logos.lang.ops` + user-struct impls (no primitive impls); `assert_eq!`/`panic!`/`println!`→distinct-nonzero return-code checks; FnOnce/FnMut→fn-ptr or closure params; trait names renamed off stdlib collisions (`U`→`ByVal`). Gaps surfaced (4 NEW, ALL §B catch-up; NO new §A): **(G1)** UFCS `Trait::method(obj)` through a `&dyn Trait` receiver → `call to undefined static method` (concrete-receiver UFCS works), **(G2)** `impl Trait for fn(..)->..` parse error in the impl-target position, **(G3)** trait-object upcast `&dyn Sub`→`&dyn Super` compiles but SIGSEGVs (wrong vtable slot), **(G4)** unconstrained blanket `impl<T> Foo for T` + a PRIMITIVE receiver through a generic-bound fn fails to emit (`'i64__xyz' does not reference a valid function`; struct receiver + direct primitive call both work). Re-confirmed known-open (NOT re-reported, source dropped): string-literal `match` patterns (issue-3574 — syntax error at the arm); for-over-`&[&dyn]` array iteration (issue-5666 distilled — SIGSEGV); in-language `mod` item decls (issue-29668 — `syntax error near 'mod'`, no module-decl grammar). (see notes/B129-ui-surfaced-gaps.md). |
| B128 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/{match,borrowck} run-pass — 16 NEW tests distilling DISTINCT match-pattern and run-pass borrowck features (checked against the mature existing `pass/{match,binding,borrowck,patterns}/` corpus — most of `ui/match/`'s run-pass set was already imported, so this batch pulls the remaining DISTINCT match facets plus the previously-untouched run-pass `ui/borrowck/` facets). Basenames suffixed `-m2` (match) / `-bc` (borrowck) to avoid the global-ctest-name clash. Prelude-aware. All 16 compile + link + exit 0. **borrowck/** (10): disjoint-field-borrows-bc (two distinct fields of one struct borrowed `&mut` simultaneously — field sensitivity; + imm-then-mut disjoint), field-mut-borrow-survives-other-field-use-bc (a `&mut x.b` borrow stays live across a copy-use of `x.a` then writes through), borrow-from-cloned-temp-bc (borrow `&temp` of a fresh temporary passed into a higher-order fn-ptr param — distinct storage from the original), mut-in-imm-is-freezable-bc (a `&mut` held inside a struct read through `&` is frozen — multiple shared reads coexist; `&mut [T]` slice→a `*mut i64` cell), move-then-reinit-bc (move a binding/field out, re-initialize the whole binding or the field, use again — valid), nested-ref-mut-match-bc (match `&mut Struct` binding TWO `ref mut` fields simultaneously — disjoint mutable interior borrows), reborrow-ref-mut-option-no-move-bc (#28839 — match `&mut Option<&mut T>`, bind inner `ref mut x`, `*x` is a REBORROW not a move; outer still usable+mutated), borrow-interior-box-elem-bc (#3026 — shared `*const` reference into a local's interior stored in a struct + read back), nll-early-end-borrow-bc (NLL: a shared borrow ends at its last use, a later `&mut`/write of the same local is allowed without a scope), two-phase-method-receiver-bc (two-phase borrow: `a.add(a.cur())` — `&mut a` for `add` activates only after the shared `a.cur()` read). **match/** (6): ref-some-pattern-m2 (#3500 — match a borrowed `&Option<T>` by `match *x` over the variants; the arm-side `&Some(_)` reference-PATTERN form is G128-1), or-pattern-ref-mut-distinct-parts-m2 (#51348 — single arm OR-pattern `E::Ok(ref mut v,_) | E::Err(_,ref mut v)` binding the SAME name to DIFFERENT parts per alternative, guard reads `*v`, right part fires per alt), ref-mut-binding-stable-across-guard-m2 (a `ref mut z` binding has the same storage address in the guard and the arm body — raw-ptr identity), rvalue-match-equiv-binding-m2 (#7660 — `ref a`/`ref b` interior borrows of struct fields via `match v { A{x:ref a,y:ref b} => … }`; statement-position form, the match-AS-EXPRESSION value form is G128-2), nested-match-borrowed-payload-m2 (#39963 distilled — match `&Outer` deref, build + nested-match an inner enum), match-tuple-of-bools-guard-m2 (match a 2-bool tuple with literal tuple patterns + a guarded alt — exhaustive 2×2 grid). Mechanical adaptations: `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; `&self`→`self:&T`/`&mut self`→`self:&mut T`; `match self`→`match *self`; `Box<T>`/`Box<dyn>`→stack value / `&dyn`; `Vec`/`HashMap`/`String` interior-borrow shapes distilled to scalar/struct/`*const`/`*mut` cells; `assert_eq!`/`panic!`/`println!`→distinct-nonzero return-code checks; FnOnce/FnMut closure args→fn-ptr params; `static mut` items dropped. Gaps surfaced (2 NEW, ALL §B catch-up; NO new §A): **(G128-1)** a `&Pat` reference-pattern in a match ARM over a `&Enum`/`&Struct` scrutinee crashes mlir-gen (`'llvm.load' operand must be pointer, got i32`) — the leading-`&` arm desugar (peel one borrow ≡ deref-then-match) is missing; the scrutinee-side `match *x` deref form works. **(G128-2)** a struct field-binding pattern (value or `ref`) used in a match-AS-EXPRESSION (`let r = match v { A{x:a,y:b} => … }`) miscompiles / SIGSEGVs / types the arm as `<error>` — `EMatchExprView` arm lowering does not bind struct-field patterns into the arm scope (statement-position `EMatchExpr` does; the gap is the expression-side mirror of the B5 slice split). Re-confirmed known-open (NOT re-reported, facet trimmed/source skipped): byte-string/byte-array `match` patterns (`b"."` arms — pattern-deref-miscompile skipped); `box`-patterns + `Box<T>` fields (issue-42679 / borrowck-box-sensitivity / borrowck-mut-uniq → stack values); dynamic `&[T]` slice-as-value + `&[T;N]`→`&[T]` coercion (borrowck-freeze-frozen-mut → `*mut i64` cell; borrowck-slice-pattern-element-loan-rpass skipped); `match`-over-`&Enum` no-deref (G128-1). Skipped sources: match/issue-2699[6]/issue-27021 (`//@ ignore-test`); match/postfix-match/* (postfix `.match` sugar); borrowck/two-phase-bin-ops (operator `*Assign` traits via `macro_rules!` — §A3); borrowck `static`/`static mut` items. (see notes/B128-ui-surfaced-gaps.md). |
| B127 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/{generics,regions,variance} run-pass — 30 NEW tests distilling DISTINCT generic + region/lifetime + variance features (checked against the mature existing `pass/{generics,regions,variance,nll}/` corpus; basenames suffixed `-g3` (generics) / `-rg` (regions/variance) to avoid the global-ctest-name clash). Prelude-aware. All 30 compile + link + exit 0. **generics/** (11): generic-triple-box-it-g3 (generic free fn taking + returning a 3-field generic struct `Triple<T>`, turbofish `box_it::<i64>`), default-type-param-concrete-impls-g3 (one generic struct with TWO concrete-typearg inherent impl blocks `impl Foo<i64>` + `impl Foo<bool>`), generic-clone-bound-method-g3 (`impl<A: Clone> Foo<A>` method bound satisfied by a Copy arg, clone-by-value), generic-nested-type-alias-zst-g3 (4-level-deep nested generic struct `L<L<L<L<i64>>>>` round-trip — mono no-overflow; size_of dropped §A const-eval), generic-prev-param-in-field-g3 (3-param generic struct where a later field's type uses an earlier type param nested in another generic), generic-fn-returns-generic-via-fn-g3 (generic fn builds a generic struct then threads it through another generic identity fn `g::<Pair<T>>`), generic-fn-ptr-produce-consume-g3 (generic fn over a producer `fn()->T` + consumer `fn(T)->T`, fn-item→fn-ptr coercion via typed lets, turbofish), generic-method-extra-typaram-g3 (generic-struct assoc fn `make<U>` adds an extra method-level type param, mid-path type-level turbofish `S::<i64>::make(..)`; same via a trait method), generic-many-field-struct-byval-g3 (generic head field + many mixed-width scalar fields, passed by value to `bar<T>`), generic-trait-typearg-as-dyn-g3 (generic trait `Foo<T>` impl'd at a concrete typearg, used as `&dyn Foo<i64>`), generic-recursive-list-enum-g3 (generic recursive enum `List<T>=Cons(T,*const List<T>)|Nil`, recursively summed). **regions/** (17): region-fn-lt-as-type-arg-bound-rg (fn lifetime param as a type-arg in a trait bound `G: GetRef<'a,i64>`), region-early-bound-trait-lt-param-rg (early-bound lifetime as a trait generic param `Trait<'a>` w/ `&'a self` long + `<'b>&'b self` short methods, via `poly_invoke<'c,T:Trait<'c>>`), region-reborrow-ref-ref-mut-rg (reborrow through `&'a &'b mut i64` returning `&**y`), region-enum-ref-self-recursive-rg (recursive enum `Ast<'a>` carrying `&'a Ast<'a>` self-refs, built by a fn returning `Ast<'r>`, eval'd), region-enum-bind-by-ref-match-rg (enum variant `Ctor(&'a i64)` bound out by a match into `&i64`), region-lub-match-deref-arm-rg (2-variant enum `Cached<'a>` w/ Ref(&'a i64)|Owned(i64), `&self` method returns ref from either arm — LUB), region-trait-object-from-struct-method-rg (fn builds a `&dyn M` whose lifetime ties to an input ref, wrapping a struct built from a borrowed-method result), region-self-ref-impl-trait-rg (trait w/ lifetime param impl'd for a ref-holding struct, method returns own borrowed field at trait lifetime), region-close-over-ref-into-object-rg (generic `make_object<'a,A:SomeTrait+'a>` closing a reference type into a `&dyn`), region-reassign-shorter-to-longer-rg (mut let-bound ref reassigned longer→shorter borrow, intersection region), region-two-refs-same-region-pick-rg (`max<'r>(&'r Boxed,&'r i64)` constrains struct-ref + bare-ref to same region, contravariance via ret), region-ref-binding-into-interior-rg (`ref` binding into the interior of a borrowed temporary via match), region-where-outlives-static-id-rg (where-clause region-outlives bounds: `'a:'static`, `'static:'a`, transitive `'a:'b,'b:'static`; top-level `static` not ported → `&'static` from a fn-local), region-borrow-in-loop-body-fresh-rg (fresh borrow via generic `borrow<T>(&T)->&T` inside a loop body, confined to the iteration), region-trait-method-own-lt-vs-trait-lt-rg (trait `AstConv<'tcx>` whose method adds its own `<'a>&'a self -> &'a Ctxt<'tcx>` — two nested region binders / DeBruijn depth, via `&dyn`), region-slice-param-index-rg (fixed-size array passed by value + indexed; `&[i64;N]`→`&[T]` slice coercion + dynamic `&[T]`-as-value both KNOWN-OPEN), region-addr-of-interior-field-rg (returning `&c.pos.x` — borrow of an interior nested field outliving the fn body), region-dependent-addr-of-chained-rg (dependent region pointers: `let foo=&a.value;` then `&foo.v1`/`&foo.v2[i]`, nested-scope outlives). **variance/** (2): variance-option-ref-intersection-rg (struct w/ `&'l i32` + `Option<&'l i32>` fields built from two refs of intersecting lifetimes — covariance approximates to a common region). Mechanical adaptations: `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; `&self`→`self:&T`/`&mut self`→`self:&mut T`; `match self`→`match *self`; `Box<T>`→stack value, `Box<dyn>`→`&dyn`, recursive enum self-payload→`*const`; `assert!`/`assert_eq!`/`println!`→distinct-nonzero return-code checks; top-level `static` items dropped (borrow a fn-local for `&'static`); method-level turbofish on assoc-fn segments dropped (inferred). Gaps surfaced (2 NEW, ALL §B catch-up; NO new §A): **(G127-1)** impl-block lifetime param not substituted into a struct field's declaration-site lifetime during the method-return variance check — `struct E<'a>{f:&'a u8}` + `impl<'b> E<'b>{fn m(self:&E<'b>)->&'b u8{(*self).f}}` → spurious "expected &'b u8, got &'a u8" (matching the impl binder to the struct's name fixes it). **(G127-2)** a `&'b self` method on `enum E<'a>` returning the `&'a` payload of a ref-carrying variant is rejected because the implicit `'a:'b` outlives (`&'b T<'a>` ⇒ `'a:'b`) is not fed to region inference (collapsing to one lifetime fixes it). Re-confirmed known-open (NOT re-reported, facet trimmed): `&[i64;N]`→unsized `&[T]` slice-param coercion; method-level turbofish on an assoc-fn path segment (parser); deferred-init `let z:&T; z=…` in a match arm (immutable-assign); recursive enum payload by value (`box with *const`); top-level `static` items. (see notes/B127-ui-surfaced-gaps.md). |
| B126 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/issues run-pass — 26 NEW tests distilling DISTINCT features from scattered `issue-NNNN` regression tests (checked against existing `pass/issues/`; basenames suffixed `-is` to avoid the global-ctest-name clash with the pre-existing `issue-NNNN` files). Prelude-aware. All 26 compile + link + exit 0. Tests (feature): issue-3149-generic-struct-ctor-fn-is (generic free fn building a many-field generic struct via field-name struct literal), issue-3220-consume-self-drop-is (inherent `fn f(self: T)` consuming self + by-value local `trait Drop` over a `*mut i64`, parenthesized receiver `(z).f()`), issue-3895-guard-or-pattern-is (`match` enum: first arm wildcard `_ if true`, later arm or-pattern over variants), issue-3979-supertrait-default-method-is (supertrait `Movable: Positioned`; subtrait DEFAULT method calls supertrait required methods on self, empty subtrait impl, static dispatch), issue-4228-two-impl-blocks-is (two separate inherent `impl` blocks on one struct), issue-17905-impl-concrete-typeargs-is (inherent `impl Pair<i64,i64>` specialized to CONCRETE type args), issue-20174-destructure-closure-field-is (struct field holding a closure `F: Fn()->T`, extracted + called), issue-23699-fn-item-as-ptr-is (fn ITEM coerced to fn-POINTER via `as fn(T)->R` cast then called), issue-23898-enum-as-int-is (fieldless enum `Variant as i64` runtime cast), issue-24308-ufcs-assoc-fn-is (trait associated no-self fns; default-bodied fn calls a sibling required fn via `Self::`), issue-25279-ufcs-self-lifetime-is (method on lifetime-parametric struct calls sibling via `Type::bar(self)`, returns `&'a` borrow), issue-25343-labeled-loop-in-closure-is (`'label: loop { break; }` + same nested inside a closure body, value-returning), issue-25497-enum-match-extract-is (payload enum matched + payload extracted + catch-all → fieldless variant), issue-26468-enum-discr-nested-iflet-is (enums with explicit HEX discriminants + outer payload enum + nested fieldless-variant pattern in `match`), issue-26802-default-method-lifetime-arg-is (lifetime-parametric trait `Foo<'a>` DEFAULT method taking a `&u8` borrow arg, empty impl inherits it), issue-28550-nested-generic-ctor-is (nested generic struct ctor `B{a:A{v:x}}` wrapped in `Some`, matched back out), issue-28828-assoc-type-alias-is (trait assoc type fixed to a concrete type in the impl, value typed via the projection), issue-29071-if-value-as-callee-is (`if/else`-as-value result CALLED in place `if c { f1 as Fp } else { f2 as Fp }()`), issue-29522-closure-path-name-is (non-capturing closure to a generic fn; local name reused as a path segment is NOT captured), issue-30081-tuple-pattern-closure-is (closure param is a TUPLE PATTERN `|(a,b)|` destructuring the argument), issue-35423-binding-range-guard-is (`match`: `ref r if *r<0` guarded binding + `e @ 1..=100` inclusive-range `@`-binding + wildcard), issue-36936-cast-not-lexpr-is (`(a as i32)` is an rvalue — borrowing it copies, later write to `a` does not change it), issue-38942-repr-enum-cast-is (enum cast to `u64` read through a fn; `#[repr]` stripped §A5, const→runtime §A2), issue-41479-generic-tuple-fields-is (generic fn on tuple `(A,B)` reading `.0`/`.1` of nested tuples), issue-41604-array-index-method-is (`&mut self` method on an array element at a computed index `b[1-1].init()`), issue-43692-unicode-escape-char-is (high-codepoint `'\u{10FFFF}'` char-literal escapes compare equal). Mechanical adaptations: `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; `&self`→`self:&T`, `&mut self`→`self:&mut T`; `match self`→`match *self`; `Box<T>`→stack value, `Box<dyn>`→`&dyn`; `assert!`/`assert_eq!`/`println!`→distinct-nonzero return-code checks; in-file `mod` wrappers dropped (Logos uses packages); generic struct literals use bare `Name { … }` (no turbofish on the literal). Gaps surfaced (5 NEW, ALL §B catch-up; NO new §A): **(G126-1)** `Some(B { a: A { v: x } })` doubly-nested struct literal directly under an enum-variant ctor → spurious "use of moved variable 'x'" (let-pinned form works); **(G126-2)** nested fieldless-variant pattern (`Mode::Bar(BarMode::Check)`) rejected in `if let` though it works in `match` (extend the synthesized-payload-guard lowering to the `if let` desugar); **(G126-3)** fixed-size array `[T;N]` passed BY VALUE as a closure/fn-bound param crashes mlir-gen (`getelementptr` on `!llvm.array` — needs spill-to-alloca like the B104 struct-field array fix); **(G126-4)** `if/else`-as-value with a postfix INDEX (`if c {[…]} else {[…]}[0]`) crashes mlir-gen (postfix-CALL form works); **(G126-5)** a top-level `type` alias whose name collides with a stdlib generic TYPE-PARAMETER name (e.g. `type F = …` vs `MapIter<I,T,R,F>`) poisons the whole stdlib module — type-alias name resolution must be package-scoped, not shadow generic-param bindings. Re-confirmed known-open (NOT re-reported, candidate dropped): tuple-struct ctor as a first-class fn value (`let f = A; f(true)` → `undefined variable 'A'`); string-literal `match` patterns (`"foo" => …` → parser error); generic `T: Add` operator bound + `Add::add(x,y)` on primitives (operator traits are user-impl-only); digit-group underscores inside `'\u{…}'`. (see notes/B126-ui-surfaced-gaps.md). |
| B125 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/traits (dyn/object subset) + tests/ui/drop + tests/ui/impl-trait run-pass — 19 NEW tests distilling DISTINCT trait-object (`&dyn`), observable-Drop/drop-order, and `-> impl Trait` return features (checked against existing pass/{traits,closures,drop}; the traits/ dir already had 4 `-tr2` dyn tests — single+multi-method, dyn-as-arg-with-default, dyn-in-field, dyn-via-`as`-cast — and 7 drop/ tests, so this batch DISTILLS facets ABSENT before into NEW `pass/{dyn,drop,impl-trait}/` dirs). Suffix `-dyn` (trait-objects, 7) / `-dr` (drop, 7) / `-it2` (impl-trait, 5). Prelude-aware (Vec resolves implicitly; no `use` needed). **dyn/** (7): dyn-array-indexed-dispatch-dyn (`[&dyn Shape; N]` fixed array, two distinct impls, indexed `arr[i].area()` dispatch — `for s in &arr` over `&dyn` SIGSEGVs, see gap note, indexed used), dyn-returned-from-fn-dyn (a fn conditionally selects + returns one of two `&dyn` impls), dyn-mut-self-method-dyn (`&mut dyn Counter` with a `self: &mut Self` mutating method dispatched dynamically), dyn-two-fields-struct-dyn (struct with TWO distinct `&dyn Val` fields pointing at different impls), dyn-default-method-only-dyn (a DEFAULT/provided trait method called through `&dyn` → vtable thunk → required method on self), dyn-arg-and-local-handle-dyn (`let h: &dyn Meter = &m;` coercion AT the let binding (not `as`), multi-method trait), dyn-method-result-in-match-dyn (dyn dispatch result fed into a `match` expr w/ literal arms + catch-all, two impls drive distinct arms). **drop/** (7, all via the local `trait Drop { fn drop(self: Self) }` by-value idiom over a `*mut i64` counter — stdlib `&mut self` Drop fires no glue): drop-order-locals-dr (REVERSE-declaration drop order of 3 locals, tick-stamped per slot), drop-in-loop-iterations-dr (fresh value per iteration drops at end of EACH iteration → N drops), drop-nested-field-dr (outer-struct Drop + owned field's Drop BOTH fire, outer+100/inner+1=101), drop-move-into-fn-once-dr (value moved into a fn drops exactly once, no double-drop), drop-on-early-return-dr (live local dropped on EARLY return out of a conditional branch), drop-returned-value-not-in-fn-dr (value returned from a fn is moved out → NOT dropped in the producer, drops in caller), drop-nested-scope-order-dr (inner-block local drops BEFORE outer-block local, tick-stamped: inner=1, outer=2). **impl-trait/** (5, all return-position `-> impl Trait`): impl-trait-return-basic-it2 (fn returns a concrete value behind an opaque `-> impl Animal`, caller dispatches), impl-trait-return-from-arg-it2 (opaque return built from a runtime arg, two calls distinct results), impl-trait-return-then-generic-it2 (opaque result passed by ref into a generic `T: Trait` fn, unifies at call site), impl-trait-return-from-method-it2 (an inherent METHOD with `-> impl Trait` return), impl-trait-return-default-method-it2 (default/provided method called on the opaque result). Mechanical adaptations: `pub fn main()`→`fn main()->i32{…return 0i32;}`; isize/usize→i64/u64; literals suffixed; `&self`→`self:&T`, `&mut self`→`self:&mut T`; `match self`→`match *self`; `static mut` drop counters → a shared `*mut i64` passed in; `assert!`/`assert_eq!`/`println!`→distinct-nonzero return-code checks; `Box<dyn Tr>`→`&dyn Tr`. WORKING (confirmed, all 19 compile+link+exit 0): `&dyn` single/multi-method dispatch, as arg, returned/selected by a fn, in struct fields (two distinct), `[&dyn;N]` indexed array dispatch, let-binding coercion, `&mut dyn` mutating method, default-method-via-vtable, dyn-result-in-match; observable Drop order (reverse-decl, nested-scope inner-before-outer), per-iteration drop, nested-field drop (outer+field), move-into-fn-once, early-return drop, returned-value-not-dropped-in-fn; return-position `-> impl Trait` (basic, from-arg, into-generic-bound, from-method, default-method). Gaps surfaced (3 NEW, ALL §B catch-up; NO new §A): **(G125-1)** supertrait method NOT in the `&dyn Sub` vtable — `trait Sub: Super`; `s.base()` on `&dyn Sub` → `trait 'Sub' has no method 'base'` (the SAME call works through a generic `T: Sub` bound, so the supertrait machinery exists for static dispatch; only the dyn method-resolver lacks the supertrait walk). **(G125-2)** `impl Trait` at PARAMETER position rejected with a helpful diagnostic (`'impl Trait' is not yet supported at parameter position; use 'fn f<T: Animal>(x: T)' or '&dyn Animal'`) — pure APIT sugar over an existing capability; ALSO `&impl Trait` fails at the PARSER. **(G125-3)** `-> impl Fn(..)->..` (return-position impl-Trait over an Fn-family closure) fails to PARSE; non-Fn-family `-> impl Trait` parses + works (the 5 it2 tests). Re-confirmed known-open (NOT re-reported): `for s in &arr`/`&v` over `&dyn` elements SIGSEGVs (double-ref deref through borrow-iteration; indexed access used); stdlib `&mut self` Drop fires no glue (by-value local-`trait Drop` idiom used); enum Drop-glue does not fire (struct-only drop tests). (see notes/B125-ui-surfaced-gaps.md). |
| B124 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/numbers-arithmetic + tests/ui/expr + tests/ui/lifetimes run-pass — 30 NEW tests distilling DISTINCT integer/float arithmetic, expression, and lifetime/borrow features (checked against existing pass/{numbers-arithmetic,intops,floatops,expr,lifetimes,regions} — the int↔int op grid, float op grid, and the huge regions/ elision+outlives+GAT corpus were already imported, so this batch DISTILLS facets absent before). Suffix `-na` (numbers-arithmetic, 12) / `-ex` (expr, 9) / `-lt` (lifetimes, 9). Prelude-aware; wrapping/float-math tests carry `use logos.lang.{arith,math}`. **numbers-arithmetic/** (12): wrapping-overflow-na (`logos.lang.arith` free-fn `wrapping_add/sub/mul` two's-complement overflow — the named §A replacement for trapping `+`/`-`/`*`; `wrapping_neg` written as `wrapping_sub(0,x)`), checked-arith-na (`checked_add/sub/mul/div`→`Option<T>`, None on overflow), saturating-arith-na (`saturating_add/sub/mul` clamp to MAX/MIN), bit-methods-na (`count_ones`/`leading_zeros`/`trailing_zeros`/`rotate_left`/`rotate_right`/`swap_bytes`), abs-signum-pow-na (`abs`/`signum`/`pow`/`min`/`max`), shift-chains-na (left-assoc shift chain `1<<1<<1<<1<<1<<1==32`, shift-by-variable, cross-width shift amount), float-math-fns-na (`logos.lang.math` free-fn `sqrt_f64`/`floor_f64`/`ceil_f64`/`round_f64`/`trunc_f64`/`abs_f64`/`pow_f64`/`fmod_f64` — float METHOD form is a gap), unary-minus-precedence-na (`-a*b`==`(-a)*b`, double-neg, hex/octal/binary radix, INT_MIN as `-MAX-1`), div-mod-signs-na (signed div truncates toward zero, `%` sign follows dividend), cmp-bit-precedence-na (full `< <= > >= == !=` on int+float, `&` tighter than `|`, `^`, `!u32`), float-literal-forms-na (scientific `1.5e3`/`2.5e-1`, f32 vs f64, negative floats), int-literal-underscores-na (digit-group `_` separators across dec/hex/bin/oct). **expr/** (9): loop-break-value-ex (`loop { … break v; }` value-producing, unlabeled), if-else-chain-ex (if/else-if/else as value expr + block branch with intermediate lets), match-expr-ranges-ex (`match` expr with inclusive ranges + or-pattern + guard + catch-all), block-scope-shadow-ex (block-as-value + while-in-block-expr; shadow + deep-nest facets dropped per gaps), early-return-binop-ex (`return` inside a block sub-expr in call-arg + comparison-rhs positions), tuple-destructure-arith-ex (tuple + nested-tuple destructure, `.0`/`.1.1` index), short-circuit-ex (`&&`/`||` observable short-circuit via counter side effect), while-continue-break-ex (`continue` skip + `break` out of `while true`), fn-pointer-value-ex (`fn(i32)->i32` value bound + passed as a fn param). **lifetimes/** (9): two-distinct-lifetimes-struct-lt (`Pair<'a,'b>` two distinct lifetime params, methods read each borrow), enum-ref-variant-match-lt (lifetime-parametric enum w/ `&'a T` variant, `match *r` reads payload at runtime), return-lifetime-struct-lt (fn builds + returns `Wrap<'a>` carrying a borrow), chained-borrow-fns-lt (`id(id(x))` borrow threaded through identity-on-lifetime fns), longest-same-lifetime-lt (canonical `longest<'a>(&'a,&'a)->&'a` runtime-conditional select, both branches exercised), mut-borrow-modify-lt (`&mut T` fn params mutate caller + two-`&mut` swap), mut-self-method-lt (`self:&mut T` method mutates receiver across calls), nested-field-borrow-lt (return `&'a` of a nested struct field `&o.inner.val`), array-of-refs-read-lt (`[&i64;3]` iterated `for r in &arr` summed via `**r`). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32{…return 0i32;}`; assert/assert_eq→distinct-nonzero return codes (assert_eq! needs fmt/string/str imports — value checks used instead); `&self`→`self:&T`; `match self`→`match *self`; lifetime/`_` turbofish dropped; `static` const items folded to locals (§A1); INT_MIN as `-MAX-1`; float methods→`logos.lang.math` free fns; rustc macro-grid tests (num-wrapping, shift) distilled to representative value-semantics checks. WORKING (confirmed, all 30 compile+link+exit 0): wrapping/checked/saturating arith on i32/i64/u32/u64; bit-counting + rotate + byte-swap methods (wide widths); abs/signum/pow/min/max methods; shift chains + cross-width shift amounts; signed div/mod sign semantics; full comparison + bitwise precedence; scientific + radix + underscore literals; loop/if/match as value expressions; range + or + guard match arms; nested-tuple destructure + field-index; observable `&&`/`||` short-circuit; `continue`/`break`; fn-pointer values + higher-order calls; two-distinct-lifetime structs; ref-carrying enums matched + read; lifetime-parametric struct ctor/return; chained borrows; `longest` runtime select; `&mut` borrow + `&mut self` mutation; nested-field borrow return; array-of-refs runtime read. Gaps surfaced (ALL §B catch-up; NO new §A): **(G124-1)** a nested-block `let` SHADOW leaks into the enclosing scope (`{ let x=100 }` overwrites outer `x`); **(G124-2)** `if { return v } else { e }` used directly as an operator/call sub-expression types the return arm as `void` (no never-type coercion at if-branch level; block-wrapped form works); **(G124-3)** a block nested as another block's tail `{ { 7 } }` loses its value (yields 0; single-level works). Re-confirmed known-open (NOT re-reported): narrow-width integer METHODS (i8/i16/u8 receivers → `receiver is not a struct`); float `.sqrt()`/`.floor()` METHOD form (free fns used); `i32::MAX`/`u8::MAX` assoc-const projection (`unknown enum 'i32'`; literals used); lifetime turbofish `::<'_>` (dropped). (see notes/B124-ui-surfaced-gaps.md). |
| B123 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/iterators + tests/ui/option-result shapes (also binding/expr distinct exprs) run-pass — 31 NEW tests maximizing DISTINCT Option/Result combinator + iterator-adapter features (checked against existing pass/{option-result,iterators,binding,expr}; the existing option-result/ files are hand-rolled-enum `Opt`/`Res` shapes + the qmark `?` harness, and iterators/ is entirely `#[test]`-harness coretest style — so this batch DISTILLS the prelude-Option/Result combinator surface + standalone run-pass for-loop/free-fn iterator pipelines, both absent before). Suffix `-or` (option-result, 20) / `-itx` (iterators, 11). Prelude-aware: Option/Result/Some/None resolve implicitly; combinator tests carry `use logos.lang.{option,result}`, iterator tests `use logos.lang.iter`. **option-result/** (20): opt-and-then-or (`Option::and_then` monadic chain), opt-map-or-or (`map_or` w/ default), opt-map-or-else-or (`map_or_else` lazy default+map), opt-unwrap-or-else-or (`unwrap_or_else` lazy fallback), opt-filter-or (`filter` keep-Some-if-pred), opt-or-else-or (`or` eager + `or_else` lazy alternative), opt-and-or (`and` replace-Some/propagate-None), opt-ok-or-or (`ok_or`/`ok_or_else`→Result), opt-take-or (`take` moves value out leaving None), opt-flatten-or (`Option<Option<T>>`→`Option<T>`), opt-if-let-or (if-let in expr position w/ else), opt-while-let-or (while-let draining a producer to None), opt-map-chain-unwrap-or (chained `map().map().unwrap_or`), res-map-or (`Result::map`/`map_err`), res-and-then-or (`Result::and_then` short-circuit on Err), res-unwrap-or-or (`unwrap_or`/`unwrap_or_else`), res-ok-err-opt-or (`Result::ok`/`err`→Option), res-is-ok-unwrap-or (`is_ok`/`is_err`/`unwrap`), res-and-or-comb-or (`Result::and`/`or`), res-of-opt-match-or (nested match over `Result<Option<i32>,i32>`). **iterators/** (11): for-range-sum-even-itx (for-over-`0..n` conditional sum), for-range-product-itx (for-over-range factorial), for-ref-array-sum-itx (for-over-`&[T;N]` deref `*x`), filter-fold-pipeline-itx (`iter_filter`→`iter_fold`), map-fold-squares-itx (`iter_map`→`iter_fold` sum-of-squares), take-count-itx (`iter_take(...).count()`), skip-count-itx (`iter_skip(...).count()`), chain-count-itx (`iter_chain(...).count()`), zip-dot-product-itx (`iter_zip`→`iter_fold` over `ZipPair` dot product), enumerate-fold-itx (`iter_enumerate`→`iter_fold` over `ZipPair<i64,i32>` index sum), nested-for-range-itx (nested range for-loops product-sum). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32{…return 0i32;}`; assert/assert_eq→distinct-nonzero return codes; closures need explicit param/return types + `return`; iterator method-style adapters use the free-fn forms (`iter_map`/`iter_filter`/`iter_fold`/`iter_take`/`iter_skip`/`iter_chain`/`iter_zip`/`iter_enumerate` over `iter_over_slice(&arr)`) with full turbofish type-args incl. the closure-type slot on `MapIter<I,T,R,F>`; `.iter()` on `[T;N]` avoided (`for x in &arr` used); `println!`/`format!` dropped. WORKING (confirmed this batch, all 31 compile+link+exit 0 via ctest harness): prelude `Option`/`Result` combinator surface — `and_then`/`map`/`map_or`/`map_or_else`/`filter`/`or`/`or_else`/`and`/`ok_or`/`ok_or_else`/`take`/`flatten`/`unwrap`/`unwrap_or`/`unwrap_or_else`/`is_some`/`is_none`/`is_ok`/`is_err`/`ok`/`err`/`map_err`; chained combinator pipelines; if-let/while-let over prelude Option; nested match over `Result<Option<T>,E>`; for-over-range (excl), for-over-`&[T;N]` (deref), nested for; free-fn iterator adapters chained into fold/count terminals; `ZipPair` field access (`.first`/`.second`) in fold closures. Gaps surfaced: **NONE** — every distinct feature targeted works. (Note: `Option::zip`/`unzip` are deliberately free fns `option_zip`/`option_unzip` in stdlib, NOT methods — `a.zip(b)` → `receiver is not a struct (got Option)` is the documented stdlib API shape per `stdlib/lang/option/option.logos`, a §A-style replacement / divergence, NOT a gap; `.step_by` on a range literal needs `use std.lang.range` for `RangeI32` and was simply avoided.) (see notes/B123-ui-surfaced-gaps.md). |
| B122 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/array-slice-vec + tests/ui/cast run-pass — 29 NEW tests maximizing DISTINCT array/slice/Vec + cast features (checked against existing pass/{array-slice-vec,cast,casts} — the int↔int cast grid + char↔int + many fixed-array tests were already imported, so this batch DISTILLS distinct facets). Suffix `-av` (array-slice-vec, 14) / `-ca` (casts, 15→14). Prelude-aware: Vec/Option/Some/None resolve implicitly; no test needed `use`. **array-slice-vec/** (14): multidim-array-av (`[[i64;2];2]` nested index), nested-bool-array-av (`[[bool;2];3]`; from repeated-vector-syntax), array-of-structs-av (`[Foo;3]` + for-over-`&arr` deref `*f` + index field; from vec-tail-matching, slice-match tail dropped), fixed-array-copy-by-value-av (pass `[i64;4]` by value, sum, caller copy untouched; from fixed_length_copy/destructure-array-1), array-repeat-sum-av (`[7i64;4]` repeat array summed; from repeated-vector-syntax), array-struct-field-av (`[i64;4]` as a struct field), mut-array-element-write-av (`a[i]=v` index assignment; from mutability-inherits-through-fixed-length-vec), array-range-sum-helper-av (subrange `[lo,hi)` via index-loop helper — slice sugar `&a[lo..hi]` is KNOWN-OPEN; from issue-15730/vec-matching-fold), big-repeat-array-av (`[[bool;1];512]` repeat-of-repeat; from repeated-vector-syntax), mutable-alias-vec-av (`fn grow(v:&mut Vec<i64>)`, `.len()`→`.length()`; from mutable-alias-vec), vec-pop-drain-av (push then `while let Some(x)=v.pop()` LIFO drain), vec-of-structs-av (`Vec<P>` push + for-over-`&v` field sum; from nested-vec-2, Drop dropped), vec-grow-iterate-av (build Vec in a while loop, sum via for-over-`&v`; from vec-late-init), vec-first-or-default-av (`v.get(0)`-or-default via `&Vec` helper; from variance-vec-covariant, covariance/lifetime axis = not a runtime feature). **casts/** (14): cast-int-float-roundtrip-ca (`i64↔f64`), cast-float-saturate-ca (large `f32`/`f64`→int saturating bit-patterns), cast-f32-f64-ca (`f32↔f64` exact-representable roundtrip), cast-u64-narrow-signed-ca (large `u64`→`u8/i8/u32/i32` truncation + sign reinterpret, `0x80…f0 as i8 == -0x10`), cast-chained-ca (`l as u32 as u64 as u32` value-preserving), cast-bool-to-int-ca (`bool`→`u16/i64/u8`), cast-char-to-int-ca (`char`→`u32/u16/u8` incl. multibyte `'英' as i16 == -0x7d0f`), cast-int-to-char-ca (`u8`→`char`; byte literals `b'..'` dropped — parse gap G122-byte), cast-enum-as-int-ca (C-like enum explicit discriminants→`u8/i8/u16`, `H8=163 as i8 == -93`; literal discriminants, not `const`), cast-enum-signed-disc-ca (negative discriminant `M1=-1`→`u16==65535`/`i16==-1`), cast-ptr-addr-roundtrip-ca (`usize↔*const T`, no `unsafe` block needed), cast-ref-to-ptr-ca (`&T`→`*const T`→`usize`; from coercion-as-explicit-cast, dyn/unsize tail dropped), cast-int-widen-signext-ca (signed widen sign-extends, unsigned widen zero-extends), cast-coercion-trivial-ca (identity `T as T`; from coercion-as-explicit-cast). All sourced from cast-rfc0401 facets + coercion-as-explicit-cast + cast-region-to-uint. Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; assert_eq→return-code checks; `&self`→`self:&T`; `vec!`→`vec_new::<T>`+push; `.len()`→`.length()`; `println!`/`format!` dropped; const items→literals (§A1); `unsafe` blocks unneeded for ptr casts. WORKING (confirmed this batch): all int↔int/int↔float/float↔float/bool↔int/char↔int casts incl. multibyte-char narrowing + sign reinterpretation; chained casts; large-float→int saturation; enum-as-int (positive + negative discriminants, signed/unsigned target); `usize↔*const`/`&T→*const T→usize` ptr casts (no `unsafe` needed); multi-dim fixed arrays + index/mutate; `[v;N]` repeat (incl. repeat-of-repeat, 512-elem); array-by-value copy into fns; array as struct field; for-over-`&arr` (deref `*x`); Vec push/pop/length/get/grow/iterate; `&mut Vec` mutation. Gaps surfaced (ALL §B catch-up; NO new §A): **(G122-1)** nested `Vec<Vec<T>>` + `get` returning the inner Vec BY VALUE → runtime `double free` / SIGABRT (the returned temp + the still-owned element both Drop the shared buffer; construction-only is fine — non-Copy heap element read out by value is the trigger; needs borrowing `get`→`&T` or deep-clone). nested-vec-av DELETED; **(G122-2)** typed deferred-init local assigned in BOTH if/else branches then read (`let mut later: Vec<i64>; if…{later=…}else{…}; later.get(0)`) → SIGSEGV (assigned value never flows to the post-join slot for a heap type — the "full definite-assignment deferred" tail of B3-bg-01, now a concrete heap miscompile; ALSO `let x: T; x = v;` without `mut` is wrongly rejected `assignment to immutable` — first-write-is-init exemption missing). vec-late-init not imported. Re-confirmed (already documented, NOT re-reported): byte-char/byte-string literals `b'a'`/`b"…"`/`br"…"`→parse error (pending per docs/language/reference/lexical.md); `const` items→§A1 (literals used); dynamic `&[T]` slice-as-value + slicing sugar `&a[lo..hi]`→KNOWN-OPEN (index-loop helper used); `.iter()` on `[T;N]`→KNOWN-OPEN (`for x in &arr` used); Vec covariance/lifetimes (not runtime). Skipped (feature/surface, not new gaps): slice.rs (user `Index`/`IndexMut` for `Range<Foo>` overloads — large surface), huge-largest-array/fixed-size-arrays-zero-size-types-8898 (`mem::size_of`/ZST Debug §A5), vec-matching*/vec-tail-matching/vec-matching-fold (named slice-tail patterns `[ref head, ref tail @ ..]` — B5 remaining), cast-rfc0401-vtable-kinds/fat-ptr-cast-rpass/cast-to-box-arr/coercion-as-explicit-cast dyn-tail (`as &dyn Foo`/`as Box<[u32]>`/fat-ptr — dyn/unsize axis), nested-vec-2 (Drop-counting via `println!` §A3). (see notes/B122-ui-surfaced-gaps.md). |
| B121 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/traits run-pass — 31 NEW tests maximizing DISTINCT trait features (corpus ~1168, only ~87 imported; checked against existing pass/traits + pass/methods to avoid dupes). Suffix `-tr2` (ctest names are global). Prelude-aware; `assert_eq!` tests carry `use logos.{std.fmt,mem.string,lang.str}`. **Files (all under pass/traits/, all compile+link+exit 0):** multiple-inheritors-tr2 (`T: B + C`, both subtraits share supertrait A — diamond reach; from inheritance/multiple-inheritors), inheritance-three-level-tr2 (`Baz: Bar: Foo` grand-supertrait method via `T: Baz`; from inheritance/call-bound-inherited2), default-method-mutual-recursion-tr2 (required methods chaining into a default: meow→scratch→purr on `i64`; from default-method/trivial), blanket-via-supertrait-tr2 (`impl<T: Foo> Quux for T {}` exposes supertrait method on `T: Quux`; from inheritance/visibility), generic-bound-two-impls-tr2 (value-param dispatch, two primitive impls u32/f32; from issue-23825), default-method-bound-subst4-tr2 (generic trait `A<T>` default-method subst under `f<T, V: A<T>>` + turbofish; from default-method/bound-subst4), region-pointer-cast-dyn-tr2 (`(&a) as &dyn Foo` then dyn dispatch; from region-pointer-simple), multidispatch-conditional-impl-tr2 (inherent method shadows conditional blanket `impl<T: Clone> Foo for T` when receiver isn't Clone; from multidispatch-conditional-impl-not-considered #17594), assoc-const-in-trait-tr2 (associated const read via concrete receiver `Alpha::ID`), generic-trait-method-tr2 (method-level type param `fn pick<U>(&self,...)`), where-clause-method-tr2 (`where T: Speak` bound), operator-neg-impl-tr2 (unary `Neg` operator-trait impl `-p`), marker-trait-bound-tr2 (method-less marker trait as a bound), generic-impl-for-wrapper-tr2 (`impl<A: Clone> Repeat<A> for Holder<A>`; from kindck-owned-contains-1, Box→stack), assoc-type-method-tr2 (assoc type as method return `Self::Output`, two bindings), multi-typaram-trait-tr2 (`Pair<T,U>` returning `(T,U)`; from parameterized-with-bounds), dyn-multi-method-tr2 (`&dyn` with multi-method vtable), default-method-overridden-tr2 (override in one impl, default in other), two-typaram-fn-bounds-tr2 (`combine<A: Val, B: Val>`), trait-method-returns-self-tr2 (`fn bump(self)->Self` chained), generic-struct-trait-field-tr2 (`Box1<T: Val>` bound-field method delegation), enum-impls-trait-tr2 (enum impls a trait, matches own variants via `match *self`), two-traits-same-type-tr2 (one struct, two unrelated traits, `T: Width + Height`), trait-assoc-fn-no-self-tr2 (assoc fn no `self`: `S::make()`; from anon-static-method), default-method-with-args-tr2 (default method taking an arg × required-method result), blanket-three-supertraits-tr2 (`impl<T: Foo+Bar+Baz> Quux for T {}`; from inheritance/auto-xc-2, aux-crate folded), generic-calls-assoc-fn-tr2 (`T::from_int(i)` assoc fn through bound; from inheritance/num3), dyn-stored-in-field-tr2 (`Holder { item: &dyn A }` field dispatch; from mut-trait-in-struct-8249 #8249), ufcs-trait-method-tr2 (`Speak::say(&d)` UFCS; from ufcs-object), conditional-impl-on-wrapper-tr2 (`impl<T: Val> Val for Wrap<T>` recursion), trait-object-arg-two-impls-tr2 (`&dyn` calling a vtable DEFAULT method `describe`→dyn `kind`; from dyn-trait). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; assert/assert_eq→return-code checks; `&self`→`self:&T`; Box<dyn>→`&dyn`, Box<T>→stack; `match self`→`match *self` for `&Enum`; CamelCase variants; `format!`/println! payloads reduced to i64 tags; `unsafe`/aux-crate/`#[feature]` dropped. Gaps surfaced (ALL §B catch-up; NO new §A): **(G121-1)** associated-CONST projection through a generic bound or `Self` fails — `T::ID`→`unknown enum 'T'`, `Self::ID`→`unknown enum 'Self'` (concrete `Alpha::ID` works; assoc-TYPE projection `Self::Output` works — gap is assoc-const value reads via type-param/`Self` path); **(G121-2)** `impl Trait for fn(...)->...` (trait impl on a function-pointer TYPE) is a parse error `syntax error near 'impl'` (grammar impl-target doesn't accept a fn-ptr type; src fn-type-trait-impl-15444 left unimported); **(G121-3)** `match self` over `self: &Enum` (no explicit deref) → mlir-gen `'arith.cmpi' op operand #0 must be signless-integer-like, but got '!llvm.ptr'` (the discriminant compare gets the ref ptr; `match *self` works — missing `&Enum` auto-deref/match-ergonomics). assoc-const-in-trait-tr2 + enum-impls-trait-tr2 imported with the supported subset (concrete read / explicit deref). Re-confirmed known-open (NOT re-reported): inherent-vs-single-trait same-name; payload-binding nested variant. (see notes/B121-ui-surfaced-gaps.md). |
| B120 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/for-loop-while + tests/ui/enum run-pass — 30 NEW tests maximizing DISTINCT loop / control-flow + enum features (checked against existing pass/{for-loop-while,enum,iterators,option-result} to avoid dupes; both upstream corpora are heavily mined by prior batches so this batch DISTILLS distinct features). Suffix `-it` (for-loop-while, 15) / `-en` (enum, 15). Prelude-aware: Option/Some/None/Vec resolve implicitly. **for-loop-while/** (15): range-sum-it (for-over-`0..6` range + for-over-`&Vec` accumulation; from linear-for-loop), loop-break-cont-counter-it (unconditional `loop` with counter-break, then `loop` using `continue` to skip odds; from loop-break-cont), while-cont-decrement-it (`while` with explicit tail `continue`; from while-cont), break-in-arg-position-it (`break` in argument position `v.push(break)` diverges before the call; from issue-51345), while-with-break-it (early `break` out of `while` body leaving a known local state), nested-loop-continue-it (two nested range `for` loops writing a fixed array via a flat index; from foreach-nested), loop-scope-it (fresh `let` inside loop body accumulating into outer mut; from loop-scope), long-while-it (long-running `while` with a nested `while`; from long-while, bound lowered), labeled-continue-outer-it (`continue 'outer` from an inner range loop; from label_break_value), labeled-break-inner-it (`break 'label` no-value from inner→outer labeled loop; from labeled-break), while-flow-mut-it (`while` whose condition reads a mutated local; from while-flow-graph), move-in-loop-reinit-it (value `let`-bound in loop, moved into call, re-bound next iteration; from liveness-move-in-loop, Drop/diverge dropped), while-let-nested-it (nested `while let Some(x)=v.pop()` over Vecs draining LIFO; from while-let, BinaryHeap→Vec), loop-diverges-then-break-it (`loop` whose only exit is a counter-guarded interior `break`; from loop-diverges, made terminating), for-destruct-range-it (`let (a,b)=*pair` destructuring a tuple element inside for-over-`&Vec`; from for-destruct). **enum/** (15): struct-like-variant-construct-en (construct both struct-like (named-field) variants; from struct-like-variant-construct), struct-like-variant-match-en (match a struct-like variant binding named fields; from struct-like-variant-match), clike-as-int-en (explicit+implicit C-like discriminants cast via `as`; from enum-univariant-repr, repr/transmute dropped), clike-discr-from-enum-en (C-like discriminant `Y::A = 42` referencing another enum's cast; from issue-23304-2), generic-either-en (two-param generic enum `Either<T,U>` construct+match w/ wildcard; from match-either-enum-variants-6117, Box dropped), enum-method-area-en (inherent method matching on `*self` computing from bound payload), uninhabited-variant-en (construct one inhabited variant of a multi-variant enum; from enum-with-uninhabited-variant, empty-enum payload variant dropped), u8-variant-wildcard-en (single payload-carrying variant matched via `_`; from enum_u8_variant, repr dropped), generic-enum-in-fn-en (generic enum `E<T>` used through a generic fn; from enum-with-generic-parameter-5997, fn-local items hoisted to module), many-data-variants-en (enum mixing multi-field tuple variant + single-field + nullary, matched in a fn; from enum-size-variance), or-pattern-variants-en (or-pattern `E::A | E::B` collapsing variants), enum-method-returns-self-en (inherent method returning a fresh value of the same enum), generic-enum-method-en (`impl<T> Opt<T>` method matching `*self`, Option-like w/o prelude), negative-discriminant-en (explicit negative `Imaginary = -1` + auto-incremented discriminants cast to i64; from enum-disr-val-pretty), clike-eq-compare-en (compare two enum values through their `as i32` discriminants; from simple-enum-usage-8506). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; assert_eq/assert→return-code checks; `&self`→`self:&T` (+`match *self`); tuple-variant + struct-variant enums both used; CamelCase variant names; vec!→vec_new::<T>+push, `.len()`→`.length()` (Vec, i64); `#[repr]`/`mem::size_of`/`transmute`/`println!`/`Box`/`Drop`/`String`/`BinaryHeap`/`macro_rules!`/`static` dropped per §A or simplified. WORKING (confirmed this batch): range-for `for i in 0..n`; for-over-`&Vec` (deref `*x`); `while let Some(x)=v.pop()`; nested while-let; struct-like (named-field) enum variant construct+match; C-like enum `as i32`/`as i64` (explicit, implicit, negative, cross-enum-referencing discriminants); or-patterns over variants; generic enums + generic-impl methods; enum methods matching `*self` and returning Self; `continue 'outer` and `break 'outer` (no value) inner→outer labeled loops; `break` in argument position; `let (a,b)=*pair` in for body. Gaps surfaced (ALL §B catch-up; NO new §A): **(G120-1)** value-carrying labeled break `break 'label <value>` COMPILES but produces the WRONG value (silent miscompile — `let v = 'outer: loop { break 'outer 13i64 }` yields v≠13; the unlabeled `break 13i64` is correct), so the prior loop-labeled-break-value import correctly dropped value-carrying labeled breaks; **(G120-2)** labeled `break 'a`/`continue 'a` out of a `while let` loop → `'break 'a': label not in scope` (the same labels work on plain `loop`/`while`/`for` — the while-let lowering doesn't register its loop label); **(G120-3)** `.iter()` on a fixed-size array `[T;N]` → `method call: receiver is not a struct (got [i64; N])` (for-over-`&array` works directly; only the explicit `.iter()` adapter on an array is missing — `.iter().enumerate()` on a Vec additionally lacks `enumerate` on `VecIter`). Re-confirmed known-open (NOT re-reported): tuple-struct/struct-field literal arms needing `_` catch-all; nested-binding payload variants. Skipped (feature/surface, not new gaps): enum-discrim-width-stuff/issue-42747/while-let-2 (`macro_rules!` §A), enum-univariant-repr/enum-size-variance-introspection (`#[repr]`/size_of/transmute §A), issue-19340-1 (extern-crate aux), cleanup-rvalue-during-if-and-while (Drop-counting via `static mut`), foreach-external-iterators-loop full form (`array.iter().enumerate()` — G120-3). (see notes/B120-ui-surfaced-gaps.md). |
| B119 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/closures + tests/ui/typeck run-pass — 28 NEW tests maximizing DISTINCT closure / type-checking features (checked against existing pass/{closures,closures-extra,typeck} to avoid dupes; the two corpora are heavily mined by prior batches + the closures HRTB/fn-family sweep, so this batch DISTILLS distinct features rather than copying macro/Box-dyn/thread-driven files). Suffix `-cl2` (closures, 19) / `-tc` (typeck, 9). Prelude-aware: Option/Result/Vec/Fn/FnMut/FnOnce/Copy resolve implicitly. **closures/** (19): fnmut-return-move-cl2 (`apply<T,F:FnMut(i64)->T>` driving inner generic `g`, closure's final expr a move-out; from closure-last-use-move), fnptr-array-cl2 (`[fn(i32,i32)->i32;2]` indexed+called; from closure_no_cap_coerce_many), in-match-arm-cl2 (`match` arms = fn items unifying to one `fn`-ptr binding then invoked), nested-move-owned-cl2 (nested `move||{ move||{ work(a) }}` two FnOnce layers over same owned capture; from issue-10682), struct-field-call-cl2 (closure stored in a generic struct field `Checked<T,F:Fn(i64)->bool>`, called via `(c.check)(...)`; from issue-42463), capture-clone-then-move-cl2 (earlier non-final use must not be last-use when a later closure captures; from closure-capture-after-clone), returns-boxed-dyn-cl2 (fn builds+returns a closure value consumed by caller; from closure-upvar-last-use-analysis, Box<dyn> axis dropped), move-mutate-no-read-cl2 (`move||` mutating its own copy of a captured upvar, never reads; from moved-upvar-mut-rebind-11958), fnmut-accumulate-cl2 (`apply_each<F:FnMut(i64)>` over `[i64;3]`, capturing FnMut `+=` into outer mut), returns-struct-cl2 (closure returning a struct aggregate), higher-order-returns-fnptr-cl2 (HOF selecting+returning one of two fn items as `fn`-ptr; CoerceMany at return), fn-bound-twice-cl2 (`Fn` bound called twice — composition), generic-over-fn-and-closure-cl2 (one `Fn`-bound generic monomorphized at a fn-ptr AND a closure), recursive-via-fnptr-cl2 (fn-ptr param invoked alongside recursion), immediately-invoked-cl2 (IIFE `(||{...})()` in let-init), semistatement-body-cl2 (multi-statement closure body: let+while+if; from semistatement-in-lambda), vec-of-fnptr-cl2 (`Vec<fn(i64)->i64>` push+iterate+apply), capture-two-disjoint-mut-cl2 (one FnMut `+=` into two disjoint captured muts), passed-to-method-cl2 (closure into an inherent method's `F:Fn` generic param), fnonce-consume-string-cl2 NOT imported — see G119-2. **typeck/** (9): collect-into-vec-tc (`_`-style Vec element-type inference from usage; from typeck_type_placeholder_1), leak-slice-bound-tc (generic trait `Leakable<T> for Bag<U:Copy>` returns stored elem by value — Copy bound makes it valid; from issue-18937-1, the unsafe/slice tail dropped), int-inference-from-use-tc (literal int type fixed by later typed use), shadowing-rebind-type-tc (same name rebound to a different type per `let`), unify-if-branches-tc (if-expr arm-type unification as let value), method-chain-result-tc (type-arg inference across a Result combinator chain; from inference-method-chain-diverging-fallback, panic!→sentinel), generic-fn-twice-distinct-tc (same trait method resolved twice on one receiver — no projection-cache poison; from project-cache-issue-37154), nested-tuple-build-tc (3-deep nested tuple construct + destructure; from nested-generic-traits-performance), fn-item-as-value-tc (safe fn-item→`fn`-ptr value coercion at let; companion to typeck-fn-to-unsafe-fn-ptr). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; closures need explicit param/return types + `return`; `pub fn main()`→`fn main()->i32`; assert_eq→return-code; `&self`→`self:&T`, by-value self→`self:Self`; `Box<dyn>`/threads/`println!`/`format!`/`macro_rules!`/`#[derive]`/transmute/forget dropped per §A; vec!→vec_new::<T>+push, `.len()`→`.length()` (Vec, i64) / `.len()` (String, i64); `v[i]`→`v.get(i)` (Vec). Gaps surfaced (ALL §B catch-up; NO new §A): **(G119-1)** plain `=` assignment to a captured outer `mut` local inside a closure → `mlir_gen: assign to undefined` + silently dropped, while the **compound** `+=` form works (capture-by-mut-ref exists; only the plain-store path fails to resolve the upvar); **(G119-2)** capturing a non-Copy `String` into an `F:FnOnce`/`FnMut` closure passed to a generic bound + calling a method on it → codegen `'llvm.return' op expected 1 operand` (the i64-capture analogue once-move-out-on-heap works; `move`-independent); **(G119-3)** a `where`-bound on an **impl-level** type-param does NOT provide that param's trait methods inside the impl's method bodies (`type parameter 'A' has no trait bound providing method 'up'`), though the same bound on a **free generic fn** resolves — blocks the recursive `Upcast<(T1,T2)> for (S1,S2) where S1:Upcast<T1>,…` shape; **(G119-4)** raw-ptr cast with placeholder pointee `as *const _` → `unknown type '_'` (explicit `as *const u64` works). Re-confirmed known-open (NOT re-reported): closure→fn-ptr coercion at a let with explicit `fn(..)`-typed binding (`expected fn(i64)->i64, got |i64|->i64`); `Vec::get` on a `Vec<U>` of unbound generic elem inside a trait-impl method (`unresolved TypeVar 'U'`); `_` placeholder type-arg in turbofish (`Foo::<i32,_>`). Soundness wobble left UNIMPORTED (would be a silent miscompile, see notes): nested FnMut closures both capturing the same `&Vec` + a mut accumulator miscompute (sum=0) once the vec has ≥4 elements (correct at 3) — focused-baghunt candidate. Skipped (feature/surface, not new gaps): many-closures/closure-type-inference-9129 (`macro_rules!`/`int3!`/Box<dyn bomb> §A), issue-22864-2 (`std::thread::spawn`), boxed-closure-lifetime-13808 (`Box<dyn FnMut()+'a>` B3), unused-closure-ice-16256 (unused-closure-must-be-used lint, no runtime), osstring-str-equality (OsString), typeck_type_placeholder_1 statics (`static`+`unsafe impl Sync`+`&413` const-ref §A1). (see notes/B119-ui-surfaced-gaps.md). |
| B118 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/structs-enums + tests/ui/generics run-pass — 20 NEW tests maximizing DISTINCT generic-struct/enum + monomorphization features (checked against existing pass/{generics,structs-enums,struct,enum} to avoid dupes; the upstream corpora are heavily mined by B110–B117 so this batch DISTILLS distinct features rather than copying layout/macro-driven files). Suffix `-g2` (generics, 14) / `-se` (structs-enums, 6). Prelude-aware: Option/Some/None/Vec/Copy/FnOnce/FnMut resolve implicitly. **generics/** (14): autobind-g2 (generic `f<T>(Vec<T>)->T` passed as a value into a `F: FnOnce(Vec<i64>)->i64` bound; the `let f1=f` local-fn-item half dropped — G-FNLOCAL: a generic fn-item bound to a local loses its type-arg binding `expected Vec<T>, got Vec<i64>`), newtype-with-generics-g2 (generic newtype `MyVec<T>` destructured `let MyVec{f0:inner}=w`; payload reduced from `Vec<T>` to plain `T` — G-NVMOVE: destructuring a generic-newtype-of-Vec double-frees), generic-fn-multi-bound-g2 (`where T: Foo + Bar`, calls both), generic-fn-ptr-field-g2 (`Apply<T>{f:fn(T)->T,v:T}` called through the field, 2 widths), generic-fn-returns-generic-struct-g2 (`g::<Pair<T>>` identity on a constructed generic, from generic-derived-type), generic-method-infer-g2 (method-generic `pair_with<U>(self,U)->U` infers U at call, distinct from struct T), generic-trait-impl-for-generic-struct-g2 (`impl<T:Copy> GetVal<T> for MyBox<T>` dispatched as method; from generic-object, the `as Box<dyn>` cast dropped), impl-where-clause-g2 (`impl<T> Wrap<T> where T:Copy`), multi-type-param-pair-g2 (`Pair<A,B>` + `swap(self)->Pair<B,A>` reorders type params), nested-generic-struct-g2 (`L<L<L<i64>>>` constructed+read, from issue-32498), phantom-marker-param-g2 (generic param NOT stored in any field; 2 instantiations no-ICE), type-param-array-element-g2 (`Triple<T>{items:[T;3]}` indexed in a generic method), type-params-in-for-each-g2 (unused type param + nested `F:FnMut(u64)` closure helper no-ICE), where-clause-return-bound-g2 (`fn foo<T,U>(b:U)->T where U:GetBar<T>`, from issue-2936). **structs-enums/** (6): class-many-methods-se (generic `Cat<T>` with a spread of `&self`/`&mut self` methods — mutating counter, bool query, length, ctor, `find->Option<T>`; from class-impl-very-parameterized-trait), generic-enum-in-vec-se (generic enum `Shape<T>` as `Vec<Shape<i64>>` element, read+matched), generic-enum-option-like-se (user `MyOpt<T>` Some/None + inherent `unwrap_or(self,T)`, 2 widths), generic-enum-two-params-se (`Either<T,U>` Left/Right construct+match+extract, from small-enums-with-fields), generic-recursive-list-se (recursive generic cons-list via `&List<T>` link, recursively summed), mixed-generic-concrete-fields-se (generic field + concrete u8/bool/u16 fields, interior offsets, from class-typarams/issue-1112). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; assert_eq→return-code; `&self`/`&mut self`→`self:&T`/`self:&mut T`, by-value self→`self:Self`; `Box`→stack struct (renamed `MyBox` to avoid the prelude `Box` whose `get` is an unsafe raw-ptr accessor); tuple/unit structs→named-field; tuple-variant→struct-variant; CamelCase names; vec!→vec_new+push, `.len()`/`v[i]`→`.length()`/`.get(i)` (Vec `length()` returns i64, `get(i64)`); `println!`/`format!`/`#[derive]`/`#[repr]`/`size_of` dropped per §A. Gaps surfaced (ALL §B catch-up; NO new §A): **(G118-1)** functional-struct-update `..base` on a GENERIC struct → `field 'y' not initialized` (works on non-generic; rest-field expansion not done against the substituted field list); **(G118-2)** empty type-param list `<>` entirely unparsed (decl + turbofish `bar::<>()`); **(G118-3)** a trait DEFAULT method calling the trait's REQUIRED method on `self` miscompiles when its result is used directly as an operand (`if c.get_twice()==13`) — non-generic trait returns the wrong int, generic trait `Container<T>` returns garbage even let-bound (overlaps the known `generic_trait_method` mono gap). Re-confirmed known-open (NOT re-reported): nested/refutable patterns in enum-variant payloads (`record-pat` shape). Skipped (feature/surface, not new gaps): align-struct/enum-discrim-autosizing/multiple-reprs/type-sizes/issue-32498 (`#[repr]`/size_of §A), small-enums-with-fields/generic-derived-type/generic-tup (`format!`/`{:?}`/`macro_rules!` §A), empty-struct-braces/enum-export-inheritance/generic-fn-twice (`mod{}`/extern-crate), ivec-tag/resource-in-struct (thread/channel runtime / Drop-in-enum B110), tuple-struct-constructor-pointer (tuple-struct ctor as fn value, B111), generic-object (`as Box<dyn>` unsize B3), generic-static-methods (generic static trait method + `where F:FnMut` via UFCS → mono `unresolved TypeVar 'F'`, method-generic mono area), generic-default-type-params/default-type-params-well-formedness (default generic params `<A=T>` unparsed, B111), generic-associated-type-deref-target-56237 (`<P as Deref>::Target` qualified path, B115-G1). (see notes/B118-ui-surfaced-gaps.md). |
| B117 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/methods + tests/ui/coercion run-pass — 34 NEW tests maximizing DISTINCT method-resolution / coercion features (checked against existing pass/methods + pass/coercion + pass/autoref-autoderef to avoid dupes). Suffix `-m` (methods, 16) / `-co` (coercion, 18; 2 of which under pass/autoref-autoderef). Prelude-aware: Option/Some/None/Result/Vec resolve implicitly. **methods/** (16): method-two-trait-defer-resolution-2-m (single-trait blanket `impl<T:MyCopy> Foo for Vec<T>` resolves `x.foo()` w/o knowing T), method-self-arg-ufcs-m (`Type::bar(&self)`/`Type::baz(self)` UFCS from inside a method), method-chain-builder-m (by-value-self builder chain `.set_a().set_b().sum()`), method-generic-receiver-m (`Wrap<T>::get` two instantiations), method-default-overridden-m (default method calling another, override per impl), method-generic-arg-infer-m (`echo<U>(self,U)->U` infers U at call), method-trait-generic-bound-dispatch-m (`t.area()` in `fn<T:Area>`), method-supertrait-call-m (subtrait default calls supertrait method via `B:A`), static-method-on-generic-m (`Pair::<i64>::make` no-self assoc fn), method-on-primitive-trait-m (user trait `&self` default impl on `i64`), method-returns-ref-self-field-m (`&self`→`&Field` then chained method), method-on-concrete-option-m (trait impl on `Option<i32>` called via method notation; the `where Option<T>:Foo` generic indirection dropped — G3), method-dyn-trait-object-dispatch-m (`&dyn Shape` dynamic dispatch), method-multi-arg-m, method-generic-bound-returns-value-m (`fn<T:Area>(&T,&T)` combines), method-call-in-match-arm-m (method result as match scrutinee). **coercion/** (16 in pass/coercion): coerce-reborrow-imm-ptr-rcvr-co (`&self` method on `&Struct`), coerce-reborrow-mut-ptr-rcvr-co (`&mut self` repeated on `&mut Struct`), coerce-reborrow-mut-ptr-arg-co (re-pass `&mut Struct` arg), coerce-reborrow-mut-vec-rcvr-co (`&mut Vec`→slice `.reverse()` rcvr), coerce-mut-vec-to-slice-reverse-co (same via helper), coerce-vec-to-slice-arg-co (`&Vec`→`&[T]` arg), unsafe-coercion-co (fn-item→fn-ptr through a fn param; Logos has no unsafe-fn subtype distinction), coerce-many-with-ty-var-co (fn-item LUB in if-else/match arms), coerce-fn-if-else-unify-co (fn-items unify to fn-ptr in if-else, returned+called), coerce-fn-to-fnptr-array-co (`[foo,bar]:[fn()->i32;2]`), coerce-fnptr-struct-field-co (fn-item→fn-ptr struct field), coerce-unify-return-co (polymorphic call return-type unification `Foo.foo(target)`), trait-object-coercion-distribution-9951-co (`&dyn Bar` redistributed through bindings; orig impl-on-primitive `&u8 as &dyn` reworked to a 1-field struct — G1), coerce-block-tail-to-trait-object-co (method-result coerced to `&dyn A`), coerce-int-literal-suffix-infer-co (typed integer arithmetic across widths via `as`), coerce-ref-to-ref-arg-co (shared `&T` reborrowed into multiple `&T` args). **autoref-autoderef/** (2): autoderef-nested-field-method-co (auto-ref nested field as receiver), autoref-mut-method-on-place-co (`&mut self` method auto-refs a `mut` place + mutates; distilled from method-mut-self-modifies-mut-slice-lvalue). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; assert_eq→return-code; `&self`/`&mut self`→`self:&T`/`self:&mut T`, by-value self→`self:Self`; `Box<T>`→stack, `Box<dyn Tr>`→`&dyn Tr`; vec!→vec_new+push, `v[i]`→`v.get(i)` (Vec) / `v[i]` (slice); macros (`format!`/`println!`/`assert!`/the `coerce-many` permutation macros) dropped per §A; static-mut COUNT threaded as a return value; `#[derive]` dropped. Gaps surfaced (ALL §B catch-up; NO new §A): **(G1)** `&Primitive as &dyn Trait` — coercion of a NON-struct (`&u8`) to a trait object fails at both let-binding and arg position (`expected &dyn Bar, got &u8`), while `&Struct → &dyn` works at both → vtable construction is keyed to struct receivers only; **(G2)** deref-coercion at ARGUMENT position — `&Newtype → &Target` via a user `Deref` impl does NOT coerce as a plain `&Target` fn argument (`expected &Target, got &Newtype`), although the same Deref drives method-receiver autoderef fine (deref-newtype-method-call) → arg-position deref-coercion not wired; **(G3)** `where TypeExpr: Trait` — a where-clause bounding a CONSTRUCTED type (`where Option<T>: Foo`) still does not parse (`syntax error near 'fn'`), forcing the concrete-instantiation workaround (matches the long-standing note in the existing method-where-clause.logos); **(G4)** never-type coercion — a diverging `return` inside an `if`/`else` branch does not coerce to the other branch's type (`if-expression branches have incompatible types: i64 vs void`); **(G5)** inherent-vs-trait same-name collision — an inherent method and a (single) trait method of the same name+sig on the same type both mangle to `Type__name` (`duplicate function`), so inherent-shadows-trait priority can't be exercised; **(G6)** trait-qualified UFCS — `Trait::method(recv)` (vs the working type-qualified `Type::method(recv)`) → `call to undefined static method 'Trait::method'`. Skipped (feature/surface, not new gaps): coerce-overloaded-autoderef (`&&&&&` deref chains + Rc), coerce-unsize-subtype / issue-26905 / no_local_for_coerced_const (CoerceUnsized/Unsize custom-DST = B2/B3), method-projection (RB115-G3 projection-receiver SIGSEGV), method-lookup-order (cfg/macro 31-permutation matrix = §A), supertrait-shadowing (unstable `supertrait_item_shadowing`), method-normalize-bounds (assoc-projection winnowing, RB115-G3 area), trait-object-arrays-11205 / coerce-trait-object-removes-send-bound (Box<[dyn]>/Arc + Send-bound erasure = B3), issue-39823 (aux-build cross-crate). (see notes/B117-ui-surfaced-gaps.md). |
| B116 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/pattern run-pass — 27 NEW tests under `pass/pattern/` (suffix `-pat`), maximizing DISTINCT pattern-feature coverage (checked against existing pass/patterns + pass/binding to avoid dupes). Prelude-aware: Option/Some/None/Result/Ok/Err/Vec resolve implicitly. Files: unit-pattern-fn-arg, struct-wildcard-pattern, integer-range-binding, char-range, range-exclusive, negative-literal, match-bool, match-guard, guard-two-bindings, enum-tuple-payload, enum-struct-variant, match-result, or-pattern-literals, or-pattern-shared-binding, at-binding-range, bindings-after-at-struct, bindings-after-at-enum, array-fixed-pattern, struct-destructure-let, tuple-destructure-let, nested-struct-destructure, tuple-wildcard-mid, ref-binding-in-struct, if-let-binding, while-let-pop, mut-binding, wildcard-let. Confirmed-working: match guards (single + two-binding), enum tuple/struct-variant destructure, Result Ok/Err destructure, or-patterns (literal `1|2|3` + shared-binding `A(n)|B(n)`), inclusive/exclusive/char/negative ranges, `@`-binding (over range, over struct w/ field subbindings `x @ A{a,b}`, over enum payload `bar @ Some(_)`), fixed-size array pattern `[x,y,z]`, struct/tuple/nested-struct destructure-let, `_`-in-tuple, `ref` field binding, `if let`/`while let`, `mut` binding-then-mutate, wildcard/underscore let, unit-param pattern. Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; assert→return-code; `&self`/ref-bindings dropped where by-value works; tuple/unit structs→named-field; `crate::` paths + `macro_rules!`/`derive` wrappers dropped (pattern feature kept); vec!→vec_new+push; `()`→i64. Gaps surfaced (ALL §B catch-up — pattern grammar/sema/mlir-gen; NO §A): (G1) slice named-rest `[h, rest @ ..]` unparsed; (G2) dynamic `&[T]` slice patterns producing a value → mlir-gen verify fail; (G3) nested patterns inside enum-variant payload `Some(Color::Red)` unsupported (clean diag); (G4) refutable struct-field sub-pattern `A{v:1}` unsupported; (G5) match over `&Struct` w/ field bindings → llvm.load verify fail; (G6) nested-tuple pattern in let/match arm doesn't bind inner names. Skipped (feature/surface, not new gaps): box-patterns (B3), turbofish-in-pattern (overlaps RB115-G1/G2), macro_rules wrappers (§A), Into<[T;N]>+try_into stdlib surface, Arc/move-ref ownership. (see notes/B116-ui-surfaced-gaps.md). |
| RB115 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | **Re-import** of 4 B115-trimmed/skipped associated-types tests, now FAITHFUL thanks to the 4 just-fixed gaps (qualified path, ref-projection, projection-method-bound-carry, equality-bound). Under `pass/associated-types/` (`-at`). Files: associated-types-in-fn-at (`&<T as Get>::Value` ref-return + qualified path in a generic free fn — gaps 1+2), associated-types-in-inherent-method-at (same in an inherent generic method), associated-types-simple-ref-at (faithful `&Self::Value` ref-return; B115 had this by-value — gap 2), associated-types-binding-in-where-clause-at (equality bound `Foo<A=u64>`/`Foo<A=Bar>` normalizes `T::A` — gap 4 — + `<Self as Foo>::A` qualified return). Adaptations: isize→i64, usize→u64; `&self`→`self:&T`; #[derive(PartialEq)] + unused unit-struct dropped; literals suffixed; assert_eq→return-code. STILL-FAILING (residual §B gaps, see notes/B116-ui-surfaced-gaps.md RB115 section): associated-types-basic (RB115-G2: `<i64 as Foo>::T` on a CONCRETE self-type parses but does not normalize — `expected i64::T got i64`); associated-types-projection-from-known-type-in-impl (RB115-G1: qualified path in `impl ... for <i32 as Int>::T` self-type position unparsed — `syntax error near 'impl'`); associated-types-bound (RB115-G3: `.to_i32()` on a value of projection type `<G as GetToI32>::R` — sema NOW accepts the `type R: ToI32` bound, but mlir-gen crashes `unsupported receiver kind for struct/class access` → SIGSEGV). All 3 residuals are §B catch-up (assoc-type normalization for concrete-self + impl-target grammar + projection-receiver codegen). |
| B115 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-22 | Victor Smirnov | tests/ui/associated-types run-pass — 18 tests, all under `pass/associated-types/` (suffix `-at` to avoid global ctest-name clashes). Prelude-aware: zero `use` lines (Option/Some/None/Clone resolve implicitly). Files: associated-types-simple, -sugar-path, -return, -struct-field-named, -struct-field-numbered, -enum-field-named, -in-default-method, -in-impl-generics, -constant-type, assoc-type-method-call-via-blanket-impl, method-on-impl-with-assoc-type-projection, associated-types-method, -stream, -projection-in-supertrait, -project-from-type-param-via-bound-in-where, assoc-type-in-struct-constructor, associated-types-normalize-unifield-struct, same-name-assoc-type-and-method. Confirmed-working assoc-type features: `type Value;` decl + `type Value = …` impl binding; the **sugar projection** `Self::Value`/`T::A` in trait-method return, generic-fn return, generic-fn param, struct field (`K::Value`), enum struct/tuple-variant field, and a struct type-arg position (`impl Foo<D, D::Resources>`); projection through a **blanket impl** (`impl<T:Get> Grab for T { type U = T::Value; }`); projection in a **supertrait bound** (`trait Cc<TC:Aa>: Bb<TC::TA>`); a method reached through a blanket impl on a value of projected type `T::D`; two assoc types on one trait with mixed `&self`/by-value `self` (Stream car/cdr); same-name assoc type + method; assoc-type-bound `type R: ToI32` (bound carried). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; assert→return-code; `&self`→`self:&T`, by-value `self`→`self:Self`/`self:Type`; unit/tuple structs→named-field `{}`; turbofish `::<T>` kept. Forced trims (each = a §B gap, see below): `&Self::Value` ref-returns ported by-value; `Foo<A=Bar>`-bounded halves dropped; `.clone()`/arithmetic on a projected value dropped (param passed unused / direct field read). Gaps surfaced (ALL §B catch-up — assoc-type/projection normalization & grammar, NO §A const-fn/macro/async): (1) qualified path `<T as Trait>::Assoc` unparsed (kills associated-types-basic, projection-from-known-type-in-impl, add-output-via-operator-syntax); (2) ref-to-non-generic-projection `&Self::Value`/`&'a T::Value` unparsed (only the GAT `&'a T::Item<'a>` form parses); (3) projection `T::Assoc` not normalized for method/operator dispatch (`receiver is not a struct (got G::R)`, `operator '+' … got T::Item`); (4) assoc-type **equality bound** `Foo<A=Bar>` parses but does not normalize the projection (`return type mismatch — expected i64, got I::A`). Skipped for stdlib-surface reasons (not parse gaps): iterator-binding/issue-20220/binding-in-trait (DoubleEndedIterator/vec::IntoIter + assoc-binding), cc (aux-build cross-crate), conditional-dispatch & object/nested-projection family (`&dyn`/Deref-heavy), default-associated-types (`associated_type_defaults`). (see notes/B115-ui-surfaced-gaps.md). |
| RB114 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | **Re-import** of 2 B114-skipped tests unblocked by follow-up compiler fixes: `functions-closures/closure-reform` (closure-literal through `where F: FnOnce(T)->U`, fixed by closure-arg type-hint from Fn-family bound, commit 6c28d185) and `binding/match-vec-rvalue` (match owned Vec to whole-value binding then use it — was SIGSEGV double-free, fixed by sema move-mark + mlir-gen struct-alias, commit 181aba3f). Both verified passing. Closes the loop on the two highest-value B114 gaps per close-gap→re-import discipline. |
| B114 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | tests/ui run-pass — 25 tests across binding/match/closures/for-loop-while/functions-closures/structs-enums. Prelude-aware: ALL 25 files carry ZERO `use` lines — Option/Some/None/Result/Ok/Err/Vec/PartialEq/Clone/FnMut/FnOnce resolved implicitly via the now-default-on `logos.std.prelude`, matching the Rust originals (no prelude-resolution surprises). binding (10: pat-tuple-7, nested-matchs, match-in-macro, match-join, match-implicit-copy-unique, mut-in-ident-patterns, range-inclusive-pattern-precedence, func-arg-incomplete-pattern, let-assignability, if-let, shadow, underscore-prefixed-function-argument), match (2: tuple-usize-pattern-14393, match-nested-enum-box-3121), closures (2: closure-mut-argument-6153, local-enums-in-closure-2074), for-loop-while (3: for-loop-panic, for-loop-goofiness, for-loop-into-iterator), functions-closures (1: fn-item-type-cast), structs-enums (5: rec-extend, small-enum-range-edge, class-poly-methods, empty-tag, issue-38002). Confirmed-working: struct functional-update (`{x:.., ..origin}`), `for x in &arr`/`&Vec`/`Vec` (IntoIterator), nested-enum + guard match, generic struct + method-generic method (`speak<T>`), hand-written PartialEq impl called via closure, `ref mut` field bindings + mutation, `mut x @ lit` pattern, trait default method, inclusive-range match patterns, fn-item→fn-pointer coercion + fn-ptr `if/else` unification, FnMut/FnOnce bound called with a bare fn item, struct destructure with `..` in arg, variable shadowing in for-in-match, enum discriminant edges (u8/i8). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; assert→return-code checks; `&self`/`&mut self`→`self: &T`/`self: &mut T`; Box/Box-dyn→stack values; tuple-variant payloads→named fields; CamelCase names; vec!→vec_new+push; `.len()`/`v[i]`→`.length()`/`.get(i)`; turbofish-with-`_` dropped. Gaps surfaced: generic `where F: FnOnce/FnMut(T)->U` called with a CLOSURE LITERAL returns garbage (compiler-bug, no diagnostic — bare-fn works), match-bound owned Vec then used → SIGSEGV (compiler-bug), reference-typed arg in Fn sugar `FnMut(&T)` + empty-paren `FnMut()` unparsed, nullary closure mutable-capture `assign to undefined`, refutable inner/field patterns in struct-shape variants unsupported (worked around via bind+inner-match), `ref _y @ Pat` unparsed, destructuring-let tuple-of-refs struct-only, bare-rest `(..)`/1-tuple `(z,)` unparsed, empty struct-variant `A {}` unparsed, `&[T;N]`≠`&[T]`, `-128i8` literal range, `wrapping_add` not on u8/i8 (see notes/B114-ui-surfaced-gaps.md). |
| B113 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | tests/ui run-pass — 30 tests across fresh trait-family subtrees + small dirs. traits/default-method (7: trivial, self, bound, supervtable, bound-subst, bound-subst2, bound-subst3), traits/inheritance (7: basic, call-bound-inherited, call-bound-inherited2, cross-trait-call, multiple-inheritors, num2, overloading-simple), traits other (8: with-bounds-default, cycle-type-trait, trait-implementation-for-primitive-type-5280, trait-implementation-for-usize-5321, bug-7295, issue-6334, trait-implementation-restriction-5988, composition-trivial), coercion (2: issue-3794, coerce-reborrow-imm-vec-arg), regions (3: regions-self-impls, regions-infer-call-2, regions-dependent-autofn), ptr (2: raw-pointer-type-basic, ptr-swap-basic), methods (1: method-recursive-blanket-impl). Verified: primitive trait impls (`impl Trait for i64`), trait default methods (incl. chained + method-generic defaults + supervtable calls), supertrait dispatch through generic bounds (Foo::f via T:Bar/Baz), blanket impls reachable via `&dyn` object, method-generic methods with reordered bounds, lifetime-parametric traits/structs, raw-pointer types, safe `swap_ref`. Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; assert→return-code checks; by-value/`&self`→`self: Self`/`self: &T`; Box/Box-dyn→stack values/`&dyn`; unit structs→`{}`; `()` payloads→i64; tuple compared field-wise; user Add/PartialEq→Logos two-param shape / Eq; UFCS + extern-crate halves dropped (kept method-call/dispatch core). Gaps surfaced: method-generic trait method on a generic struct → mono invalid-callee verify fail (compiler-bug), tuple-by-value params through `&dyn` virtual call corrupted (compiler-bug; got 47 want 303), String through nested FnMut closures double-free (compiler-bug), struct `{ .. }` rest-pattern in match → cmpi-on-ptr verify fail (compiler-bug), `format!` across two primitive Display impls → moved-`__buf`/func.call-mismatch (compiler-bug), destructuring-assignment to tuple/struct/slice patterns unparsed, tuple `<`/`<=` ordering verify fail (`==` works), where-clause on a constructed type unparsed, `impl Trait for fn(..)->_` unparsed, `&mut a[..]` array full-slice unparsed, `mut` closure param unparsed (re B110), `(&s).bar()` blanket-method-on-`&S` resolution gap, `()` unit-type value/pattern (re B109), static-trait-method-via-bound (UFCS-static), multi-type-param supertrait bound substitution, supertrait method on `&dyn Sub`, `T:Copy` not auto-copy (divergence). (see notes/B113-ui-surfaced-gaps.md). |
| B112 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | tests/ui run-pass — 26 tests across enum-discriminant (2: issue-90038, issue-104519), issues (10: issue-3895, issue-41604, issue-25279, issue-4228, issue-3847, issue-18845, issue-4759, issue-4875, issue-26484, issue-3149), autoref-autoderef (1: autoderef-method), binding (2: expr-match-unique, match-value-binding-in-guard-3291), functions-closures (1: closure-returning-closure), return (2: return-nil, early-return-with-unreachable-code-24353), regions (1: owned-implies-static), parser (2: issue-17718-parse-const, unicode-multibyte-chars-no-ice), reachable (1: boolean-negation-in-unreachable-code-7344), recursion (1: instantiable), fmt (1: println-float), let-else (1: issue-99975), or-patterns (1: lazy-and-or). Note: the long-mined simple categories (numbers-arithmetic/expr/match/basic binding) were almost entirely already imported in B1–B111; new yield concentrates in scattered `issues/*`. Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; `assert_eq!`/`assert!`/`panic!`→return-code checks (a few `println!` tests kept with logos.std.fmt+mem.string+lang.str); `Box<T>`→plain values; `#[repr]`+explicit discriminants dropped (discriminant = variant index); `()` payloads→dummy-field struct / suffixed int; unit/tuple structs→named-field structs; type-qualified UFCS (`<Self>::bar`)→direct method call; by-value `self`→`self: Self`; in-file `mod{}` + in-fn item decls hoisted to top level; 1-char type names→multi-char; `Option<String>` let-else payload retyped to `Option<i64>` (heap-String let-else mis-runs). Gaps surfaced: `Option<String>` through let-else SIGSEGVs/wrong-value (compiler-bug; `Option<i64>` works), `{:?}` Debug of a tuple-of-str SIGSEGVs (compiler-bug), array-typed param indexed in a generic `F: Fn([T;N])` closure → MLIR verify fail (compiler-bug), `\u{..}` escape with underscores unparsed, float exponent literal without a decimal point unparsed, `f(return)` (return as call arg) unparsed (note: `break`-as-arg works), tuple-struct name as ctor value (re B111), string-literal + tuple-with-literal + const-name range match patterns unparsed (re B109), nested/or patterns inside enum-variant payloads unparsed (whole or-patterns dir except lazy-and-or; re B109), `_y @ Some(_)` over-variant exhaustiveness gap (re B109), `if (return){}` unparsed (re B108), deferred-init `let` unparsed, `&T`→`&dyn Trait` array-element coercion unsupported, no `Iterator::cloned`. (see notes/B112-ui-surfaced-gaps.md). |
| B111 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | tests/ui run-pass — 36 tests across structs-enums (19: struct-field-shorthand, tuple-struct-destructuring, functional-struct-upd, enum-discr, rec, rec-tup, borrow-tuple-fields, expr-if-struct, expr-match-struct, issue-1701, class-typarams, struct-aliases, tag-variant-disr-val, simple-match-generic-tag, discrim-explicit-23030, numeric-fields, tag, nonzero-enum, compare-generic-enums), self (7: by-value-self-in-mut-slot, move-self, explicit-self-generic, string-self-append, ufcs-explicit-self, objects-owned-object-owned-method, explicit-self), where-clauses (3: where-clauses, where-clauses-method, where-clause-region-outlives), privacy (2: private-class-field, private-method-rpass), for-loop-while (2: break-value, for-destruct), consts (1: consts-in-patterns), unboxed-closures (1: unboxed-closures-simple), string (1: byte-string-literal-index). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; `assert_eq!`/`assert!`/`panic!` → return-code checks (a few assert!/format! tests kept with logos.std.fmt+mem.string+lang.str imports); tuple structs `Foo(a,b)`→named `f0`/`f1` fields; CamelCase enum/variant names; 1-char type names→multi-char; hand-written `PartialEq` for the `tag` enum (derived `!=`); explicit-self receivers `self`→`self: Self`/`self: T`, `mut self`→rebind into a local; `Box`/`Rc` receivers + values→stack values / `&dyn`; `#[repr]` + primitive assoc-consts dropped (discrim-explicit collapsed to concrete discriminants; auto-increment-after-explicit kept); shift-in-discriminant variants dropped; type-alias name spelled as base in patterns; const-fn/transmute/ZST/size_of/`Option ==` arms dropped; generic-enum literals bound to typed locals before passing as args (call-arg retype bug); the `&mut tuple.N` disjoint-borrow demo rerouted through a struct (tuple-field writeback bug). Gaps surfaced: `&mut tuple.N` writeback no-op (compiler-bug; struct-field works), multi-field struct-payload extracted from enum variant misreads later fields (compiler-bug), `Option<customEnum>` Some(..) mis-codegens func.return (compiler-bug), bare generic-enum literal as direct call-arg mis-codegens (compiler-bug; let/assign works), in-file `mod{}` unparsed, shift-in-discriminant unparsed, non-literal-const array length unparsed, `static`/`static mut` items unparsed, type-alias in struct pattern rejected, tuple-struct ctor as fn value, intra-impl forward method ref order-dependent, nested-payload patterns + single-field `Variant(..)` rest (re B109), method-turbofish + `where F:FnMut` (re B110). (see notes/B111-ui-surfaced-gaps.md). |
| B110 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | tests/ui run-pass — 21 tests across drop (3: issue-979, destructor-run-for-let-ignore-6892, conditional-drop-10734), structs (5: struct-order-of-eval-3, struct-order-of-eval-4, struct-partial-move-1, struct-partial-move-2, mutable-unit-struct-borrow-11267), generics (3: mid-path-type-params, issue-94923, generic-recursive-tag), box (6: unique-generic-assign, unique-in-tag, self-assignment, basic-operations, unique-pat-2, unique-object-move), closures (1: nested-closure-call), borrowck (2: ref-mut-rebind-does-not-affect-outer, fsu-moves-and-copies), moves (1: move-3-unique). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; `assert_eq!`/`assert!`/`panic!` → return-code checks; `Box<T>`→plain values, `Box<dyn T>`→`&dyn T`; observable Drop via the local-`trait Drop { fn drop(self: Self) }` by-value idiom over a `*mut i64` counter (stdlib `&mut self` Drop does not fire drop-glue); `static mut`/`AtomicUsize` event logs → `*mut i64` shift-or counters; FRU `..p` partial-move kept; generic `Partial<T>`+FnMut bound specialized to concrete i64 (generic+FRU+FnMut type-param resolution gap); `mut F`/method-turbofish dropped; nested in-fn items hoisted to top level; 1-char type names→multi-char; recursive enum `Box` link→`&` reference. Gaps surfaced: stdlib `&mut self` Drop doesn't fire drop-glue (compiler-bug), enum Drop-glue doesn't fire + no recursive payload drop (compiler-bug), reassign generic-enum payload→no-payload-variant then match SIGSEGV (compiler-bug), closure capturing a `&dyn` param + method-call-through-it SIGSEGV (compiler-bug), method-call turbofish unparsed, `mut`-closure-param/`where FnMut` generic-fn-param unparsed, generic default type params unparsed, in-fn item decls unparsed, `&mut [N]`→`&mut [T]` coercion (re B108 #10). (see notes/B110-ui-surfaced-gaps.md). |
| B109 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | tests/ui run-pass — 11 tests across pattern (3: issue-6449, size-and-align, const-pattern-str-match-lifetime), binding (1: match-pipe-binding), deref (1: dereferenceable-type-behavior-22992 — the now-unblocked transitive method-autoderef-through-user-Deref subset), lifetimes (2: trait-object-constructor-14821, matcher-trait-equality-13323), tuple (1: tuple-index-fat-types), type-alias (1: type-param), typeck (2: issue-2063, infer-struct-with-trait-object). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; `panic!()` arms→return codes; `()`/unit payloads→i32 flags; single-letter type names→multi-char (stdlib generic-param collision); `crate::` path prefixes dropped; nested enum-variant patterns→bind+re-match; `Box`/`Rc`→references; `Box<dyn X>` fields→`&dyn X`; user Deref uses `Deref<Target>` trait-type-param shape; const-str-pattern→`==`. Gaps surfaced: tuple-of-two-enums match SIGSEGV (compiler-bug), `if let _`/`while let _` malformed-MLIR (compiler-bug), slice-pattern-in-tuple codegen type-mismatch (compiler-bug), 1-letter-type-name vs stdlib-generic-param poison (compiler-bug), plain-struct refutable-field-subpattern, `Foo::Bar(..)` single-field rest, nested variant patterns, 1-tuple destructure pattern, `@`-over-variant exhaustiveness, `mut` in tuple/param/`@` pattern, string-literal match patterns, slice rest-binding `xs @ ..`, range/const-name let+patterns, `(pat)` paren binder, `return` scrutinee, `mod{}` blocks, `for &ref x`, unit params. (see notes/B109-ui-surfaced-gaps.md). |
| B108 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | tests/ui run-pass — 16 tests across cast (3: cast-rfc0401, coercion-as-explicit-cast, supported-cast), coercion (3: coerce-bare-fn-returning-zst-to-closure, coerce-many-with-ty-var, coerce-unify-return), methods (3: method-self-arg, method-self-arg-trait, method-where-clause), overloaded (7: overloaded-index, overloaded-index-in-field, overloaded-deref, overloaded-deref-count, overloaded_deref_with_ref_pattern, subtyping-both-lhs-and-rhs-in-add-impl, overloaded-autoderef-order). Mechanical adaptations: isize/usize→i64/u64; literals suffixed; `pub fn main()`→`fn main()->i32`; `static mut COUNT` product-accumulators→returned-factor locals; `Box<Self>` self-receivers + Rc/RefCell/Cell rows dropped; user Index/IndexMut/Deref/DerefMut use the Logos `Index<Idx,Out>` / `Deref<Target>` trait-type-param shape; `struct Foo;`→`struct Foo {}`; by-value `self`→`self: Self`. Gaps surfaced: method-autoderef-through-user-Deref, trait-name UFCS, where-clause on type-expr, associated types, dup-mangling for two generic-instantiation impls, raw-ptr-receiver methods, `&mut [N]`→`&mut [T]` coercion, `if (return)`, trait-object/Box-dyn coercions+casts, `extern "rust-call"` Fn impls, supertrait shadowing, slice reverse/to_vec, Vec ref-element-access, Option::iter, `&mut x[i]` place-borrow writeback (compiler-bug). (see notes/B108-ui-surfaced-gaps.md). |
| B107.1 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | gap-closure re-import (1 test): `issues/issue-35423.rs` — was skipped in B107 (#17, compiler SIGSEGV). Both the `ref r if *r < 0` deref-guard and the `e @ 1..=100` at-binding-over-range now compile + run correctly in match position (fixed in 43bcc3d1). `println!` arm bodies → record-which-arm-fired + assert; literals suffixed; `pub fn main()` → `fn main()->i32`. |
| B1 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | parser top-level (11 tests) |
| B2 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | borrowck — move/copy/reference (11 tests) |
| B3 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | generics — generic fn / struct / enum / type-alias / fn-ptr (12 tests) |
| B4 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | patterns + match (5 tests; many features deferred — see pattern-match-gaps.md) |
| B5 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | closures (3 tests; bulk blocked on Fn/FnMut/FnOnce — see closures-gaps.md) |
| B6 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | coercion + cast (5 tests) |
| B7 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | methods (3 tests) |
| B8 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | structs + enums + binding (6 tests) |
| B9 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | traits — basic (2 tests; bulk blocked on Fn-trait + default-bodies + dyn-trait) |
| B10 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | consts + inference + typeck (3 tests; end of Tier-1 wide sweep) |

To kick off a batch:

1. Pick a stable, recent rustc commit
   (`git -C ~/src/rust rev-parse HEAD` on a fresh `master` clone, or
   a tagged release like `1.83.0`).
2. Add a row above with the SHA, date, and importer's git
   `user.name`.
3. Land all files in that batch in one commit (or a small commit
   series); each per-file row below references the SHA.

## Per-file manifest

Columns:

* **Our path** — path under `tests/imported/ui/`.
* **rustc path** — original path under `tests/ui/` in rust-lang/rust.
* **Commit** — the batch SHA from the table above (or `(direct)` if
  the import was a one-off outside a numbered batch).
* **Modifications** — one-line summary; matches the per-file
  provenance header in the test itself.

| Our path | rustc path | Commit | Modifications |
|---|---|---|---|
| `pass/parser/as-precedence.logos` | `tests/ui/parser/as-precedence.rs` | B1 | suffixed integer literals; ports as-is via `assert_eq!` |
| `pass/parser/doc-comment-parsing.logos` | `tests/ui/parser/doc-comment-parsing.rs` | B1 | `pub fn main()` → `fn main() -> i32`; bare `5;` → `let _: i64 = 5;` |
| `pass/parser/generics-rangle-eq-15043.logos` | `tests/ui/parser/generics-rangle-eq-15043.rs` | B1 | tuple-struct `S<T>(T)` → named-field `S<T> { v: T }` (Logos has no tuple structs) |
| `pass/parser/integer-literal-method-call-underscore.logos` | `tests/ui/parser/integer-literal-method-call-underscore.rs` | B1 | trait method signature carries explicit `self: Self` |
| `pass/parser/multiline-comments-basic.logos` | `tests/ui/parser/multiline-comments-basic.rs` | B1 | `pub fn main()` → `fn main() -> i32` |
| `pass/parser/nested-block-comments.logos` | `tests/ui/parser/nested-block-comments.rs` | B1 | `pub fn main()` → `fn main() -> i32` |
| `pass/parser/operator-associativity.logos` | `tests/ui/parser/operator-associativity.rs` | B1 | suffixed literals; uses `assert_eq!` |
| `pass/parser/operator-precedence-braces-exprs.logos` | `tests/ui/parser/operator-precedence-braces-exprs.rs` | B1 | suffixed literals; relies on Logos block-as-expression |
| `pass/parser/parse-panic.logos` | `tests/ui/parser/parse-panic.rs` | B1 | `panic!()` / `println!()` → `panic("")` / `let _ = 1;` (function is never called) |
| `pass/parser/reference-whitespace-parsing.logos` | `tests/ui/parser/reference-whitespace-parsing.rs` | B1 | trimmed to `&T` depth 1 (Logos `&&T` / whitespace-tolerant `&` stacking at type position is a tracked grammar gap, see `docs/track3-gaps/parser-gaps.md`) |
| `pass/parser/super-fast-paren-parsing.logos` | `tests/ui/parser/super-fast-paren-parsing.rs` | B1 | `static a: isize = (...)` → `const A: isize = (...)` |
| `pass/borrowck/borrowck-assign-to-subfield.logos` | `tests/ui/borrowck/borrowck-assign-to-subfield.rs` | B2 | nested struct decls hoisted to top level (Logos doesn't permit struct decls inside fn bodies) |
| `pass/borrowck/borrowck-borrow-of-mut-base-ptr-safe.logos` | `tests/ui/borrowck/borrowck-borrow-of-mut-base-ptr-safe.rs` | B2 | trimmed: `let t2: &&mut isize = &t0;` step crashes mlir-gen at runtime (`&&mut T` codegen partial) — tracked gap |
| `pass/borrowck/borrowck-closures-two-imm.logos` | `tests/ui/borrowck/borrowck-closures-two-imm.rs` | B2 | trimmed to fn `a()`: cases `b/c` use `&x` inside closure body (capture-by-ref / addr-of-captured-local) — mlir-gen "& undefined 'x'", tracked gap |
| `pass/borrowck/borrowck-fixed-length-vecs.logos` | `tests/ui/borrowck/borrowck-fixed-length-vecs.rs` | B2 | explicit array type `[i64; 1]` |
| `pass/borrowck/borrowck-mut-vec-as-imm-slice.logos` | `tests/ui/borrowck/borrowck-mut-vec-as-imm-slice.rs` | B2 | `&[isize]` slice arg → `&Vec<i64>` (no implicit Vec→slice coercion); for-loop unrolled to index loop (tracked gap on borrowed for-iter pattern) |
| `pass/borrowck/borrowck-pat-reassign-no-binding.logos` | `tests/ui/borrowck/borrowck-pat-reassign-no-binding.rs` | B2 | `Option::Some` / `Option::None` qualified |
| `pass/borrowck/borrowck-rvalues-mutable.logos` | `tests/ui/borrowck/borrowck-rvalues-mutable.rs` | B2 | explicit lifetime `<'a>` dropped from `inc` — Logos elides |
| `pass/borrowck/borrowck-scope-of-deref-issue-4666.logos` | `tests/ui/borrowck/borrowck-scope-of-deref-issue-4666.rs` | B2 | rust user-struct `Box` renamed `MyBox`; `fun1` (declare-then-init for immut binding) dropped — grammar gap |
| `pass/borrowck/lazy-init.logos` | `tests/ui/borrowck/lazy-init.rs` | B2 | declare-without-init `let mut x: isize;` not accepted — pre-init to 0 (grammar gap tracked) |
| `pass/borrowck/pointer-reassignment-after-deref-78192.logos` | `tests/ui/borrowck/pointer-reassignment-after-deref-78192.rs` | B2 | explicit `as *const u32` cast on `c = d` (no implicit `&T → *const T` coercion) |
| `pass/borrowck/two-phase-baseline.logos` | `tests/ui/borrowck/two-phase-baseline.rs` | B2 | `Vec` via `vec_new::<i64>` + `push`; `assert_eq!` per-element since Logos `Vec` doesn't implement equality vs array literal |
| `pass/generics/generic-fn.logos` | `tests/ui/generics/generic-fn.rs` | B3 | `char` literals + `Triple` Copy semantics dropped (Logos: no auto-Copy for scalar structs); pre-read `p.z` before move |
| `pass/generics/generic-fn-infer.logos` | `tests/ui/generics/generic-fn-infer.rs` | B3 | unchanged |
| `pass/generics/generic-ivec-leak.logos` | `tests/ui/generics/generic-ivec-leak.rs` | B3 | `vec![…]` → `vec_from_arr([…])` (no `vec!` macro yet) |
| `pass/generics/generic-tag-local.logos` | `tests/ui/generics/generic-tag-local.rs` | B3 | turbofish on variant ctor dropped (gap G3-tg-01) |
| `pass/generics/generic-tag-match.logos` | `tests/ui/generics/generic-tag-match.rs` | B3 | turbofish on variant ctor in match-pat dropped (same gap) |
| `pass/generics/generic-tag-values.logos` | `tests/ui/generics/generic-tag-values.rs` | B3 | same — turbofish dropped on variants |
| `pass/generics/generic-temporary.logos` | `tests/ui/generics/generic-temporary.rs` | B3 | fn-pointer type requires explicit `-> ()` (no bare `fn(T)` form) |
| `pass/generics/generic-type.logos` | `tests/ui/generics/generic-type.rs` | B3 | unchanged |
| `pass/generics/generic-type-synonym.logos` | `tests/ui/generics/generic-type-synonym.rs` | B3 | call site exercises the alias |
| `pass/generics/issue-1112.logos` | `tests/ui/generics/issue-1112.rs` | B3 | explicit literal suffixes |
| `pass/generics/issue-2936.logos` | `tests/ui/generics/issue-2936.rs` | B3 | rename `fn cbar(...)` → `cbar_new` to avoid name-shadow of struct |
| `pass/generics/issue-333.logos` | `tests/ui/generics/issue-333.rs` | B3 | explicit `fn(T) -> T` type on the binding for `id::<T>` as fn-ptr |
| `pass/match/guards.logos` | `tests/ui/match/guards.rs` | B4 | trimmed to integer-guard half; struct-pattern + guard half deferred to a later batch |
| `pass/match/match-large-array.logos` | `tests/ui/match/match-large-array.rs` | B4 | trimmed — array-prefix patterns `[1, ..]` not yet supported (gap P4-pm-04) |
| `pass/match/match-on-negative-integer-ranges.logos` | `tests/ui/match/match-on-negative-integer-ranges.rs` | B4 | `if let` form rewritten as `match` (no `if let` sugar in Logos — gap P4-pm-05) |
| `pass/pattern/issue-10392.logos` | `tests/ui/pattern/issue-10392.rs` | B4 | nested struct pattern inside Option payload rewritten as bind-then-destructure (gap P4-pm-02); separate mlir-gen GEP crash on struct-by-value destructure (gap P4-pm-08) workaround |
| `pass/pattern/match-ref-option-pattern.logos` | `tests/ui/match/match-ref-option-pattern.rs` | B4 | `&Some(_)` / `&None` rewritten via auto-deref + qualified path |
| `pass/closures/issue-5239-2.logos` | `tests/ui/closures/issue-5239-2.rs` | B5 | `ref x` in closure param dropped (gap C5-cl-03); explicit param + ret type |
| `pass/closures/no-capture-closure-call.logos` | `tests/ui/closures/no-capture-closure-call.rs` | B5 | unrelated `Box::new(1)` dropped; explicit `-> ()` and `return` |
| `pass/closures/simple-capture-and-call.logos` | `tests/ui/closures/simple-capture-and-call.rs` | B5 | explicit return-type / `return` on the closure |
| `pass/cast/cast-does-fallback.logos` | `tests/ui/cast/cast-does-fallback.rs` | B6 | `(&u8 >> 4)` → `(u8 >> 4)` (no auto-deref of `&u8` at shift-op site, tracked gap) |
| `pass/cast/cast-enum-const.logos` | `tests/ui/cast/cast-enum-const.rs` | B6 | top-level `const` as enum discriminant rejected — inlined literal |
| `pass/cast/cast-region-to-uint.logos` | `tests/ui/cast/cast-region-to-uint.rs` | B6 | `println!("…{:x}…")` dropped (no `{:x}` dispatch on usize yet); cast retained |
| `pass/cast/constant-expression-cast-9942.logos` | `tests/ui/cast/constant-expression-cast-9942.rs` | B6 | `[0; S]` rejected — top-level `const` not yet a valid array length; inlined literal |
| `pass/coercion/basic-ptr-coercions.logos` | `tests/ui/coercion/basic-ptr-coercions.rs` | B6 | explicit reborrow + `as`-casts (Logos has no implicit `&mut→&` / `&→*const` / `*mut→*const` at binding sites); `&<literal>` rejected by temp-lifetime check |
| `pass/methods/inherent-methods-same-name.logos` | `tests/ui/methods/inherent-methods-same-name.rs` | B7 | tuple struct `Foo<T>(T)` rewritten with named field `v` |
| `pass/methods/method-two-trait-defer-resolution-1.logos` | `tests/ui/methods/method-two-trait-defer-resolution-1.rs` | B7 | `Vec::new()` → `vec_new::<T>()` |
| `pass/methods/trait-method-resolution-7575.logos` | `tests/ui/methods/trait-method-resolution-7575.rs` | B7 | trait default bodies dropped (no defaults in Logos traits); method name `new` renamed `mk` — `new` rejected by trait-body grammar (gap M7-mt-01) |
| `pass/structs/large-records.logos` | `tests/ui/structs/large-records.rs` | B8 | unchanged shape — 12-field struct |
| `pass/structs/struct-new-as-field-name.logos` | `tests/ui/structs/struct-new-as-field-name.rs` | B8 | `new` field renamed `new_field` — KW_NEW reserved (gap S8-st-01) |
| `pass/structs/struct-pattern-matching.logos` | `tests/ui/structs/struct-pattern-matching.rs` | B8 | `println!` dropped; match shapes preserved |
| `pass/enum/enum-disr-val.logos` | `tests/ui/enum/enum-disr-val-pretty.rs` | B8 | `imaginary = -1` dropped — negative discriminants not parsed (gap S8-en-01); `String::to_string()` not exercised |
| `pass/binding/exhaustive-bool-match-sanity.logos` | `tests/ui/binding/exhaustive-bool-match-sanity.rs` | B8 | tuple scrutinee `match (x, y)` not supported (gap P4-pm-03) — rewritten as if-chain preserving the truth table |
| `pass/binding/match-bot.logos` | `tests/ui/binding/match-bot.rs` | B8 | turbofish on variant ctor dropped (gap G3-tg-01); `Option::Some(3)` direct |
| `pass/traits/anon-static-method.logos` | `tests/ui/traits/anon-static-method.rs` | B9 | `pub fn new()` → `pub static fn new()` (Logos explicit `static fn` for associated constructors; see logos.peg:780+) |
| `pass/traits/impl-implicit-trait.logos` | `tests/ui/traits/impl-implicit-trait.rs` | B9 | explicit `self: &T` |
| `pass/consts/arithmetic-expr-in-array-len.logos` | `tests/ui/consts/arithmetic-expr-in-array-len.rs` | B10 | arithmetic at array-length position rejected (gap K10-co-01); literal inlined |
| `pass/inference/auto-instantiate.logos` | `tests/ui/inference/auto-instantiate.rs` | B10 | `println!` dropped; type-param inference at call site |
| `pass/typeck/unify-return-ty.logos` | `tests/ui/typeck/unify-return-ty.rs` | B10 | `null` → `null_p` (KW_NULL keyword, gap K10-co-02); `mem::transmute` → `as *const T` cast (K10-co-03) |
| `pass/unsized/unsized.logos` | `tests/ui/unsized/unsized.rs` | B21 | PhantomData removed (Logos has no PhantomData and doesn't need it here); `type TT<T:?Sized> = T;` dropped (Logos type-aliases don't allow bare unsized on RHS yet) |
| `pass/unsized/maybe-bounds-where-cpass.logos` | `tests/ui/unsized/maybe-bounds-where-cpass.rs` | B22 | tuple-struct → named-field (Logos uses named fields throughout); `vec![1,2,3]` → fixed-size array (no vec! macro); `&u[..]` slice-from-array → explicit cast `&buf as *const [u8]`; `where T:?Sized` placed in-bracket as `<T:?Sized>` (Logos accepts both forms; in-bracket is idiomatic) |
| `pass/bool/test_harness_coretest_bool.logos` | `library/coretests/tests/bool.rs` (test_bool only) | B23 | first port from `library/coretests/`; `use core::cmp::Ordering::{Equal, Greater, Less}` → `use std.lang.ord;` (path syntax) + `Ordering::Equal` etc. (no bare-variant re-export — gap CP-cm-02); `use core::ops::{BitAnd, BitOr, BitXor}` → `use std.lang.ops;`; sibling tests `test_bool_not`/`test_bool_to_option`/`test_bool_to_result` deferred (gaps SL-sl-06, SL-sl-08, CP-cm-03, CP-cm-04, CP-cm-05). Full gap catalog: docs/core-port/coretests-port-gaps.md |
| `pass/binding/bind-field-short-with-modifiers.logos` | `tests/ui/binding/bind-field-short-with-modifiers.rs` | B107 | isize → i64; the struct is hoisted to module scope (Logos does not declare structs inside fn bodies); shorthand field patterns (`ref mut x`, `ref x`) are written in explicit `field: pattern` form (Logos requires the field name in struct patterns); literals suffixed. Tests struct field-pattern modifiers: `ref mut`, `ref`, and `mut` bindings. |
| `pass/binding/expr-match-generic.logos` | `tests/ui/binding/expr-match-generic.rs` | B107 | isize → i64; the `compare<T>` fn-pointer type alias is monomorphized to `fn(i64, i64) -> bool`; `.clone()` is replaced by a plain copy of the (Copy) i64 value; the bool/Pair generic instantiations collapse to a single i64 case. Tests a generic match-as-expression whose result is fed to a fn-pointer comparison. |
| `pass/binding/match-ref-binding-mut.logos` | `tests/ui/binding/match-ref-binding-mut.rs` | B107 | isize → i64; `match *x` over `&mut Rec` → `match x` (Logos auto-derefs the reference); literals suffixed. Tests a `ref mut` field binding in a struct pattern, mutating through it. |
| `pass/binop/operator-multidispatch.logos` | `tests/ui/binop/operator-multidispatch.rs` | B107 | isize → i64; only the same-type `Point + Point` overload is kept (Logos's Add trait is single-type `Self + Self`, with no `Add<Rhs>` for a distinct right-hand side, so the `Point + isize` half is dropped); equality is checked field-wise (no derived PartialEq on the struct). Tests overloading the `+` operator for a user struct. |
| `pass/closures/issue-22864-1.logos` | `tests/ui/closures/issue-22864-1.rs` | B107 | the generic tuple-struct `Fun<F>(F)` holding a closure becomes a named-field generic struct (Logos reads/writes named struct fields); the `let Fun(g) = f` destructure is written as a field access; `println!` → assert on the closure result. Tests storing a closure in a generic struct field and invoking it after extraction. |
| `pass/closures/old-closure-explicit-types.logos` | `tests/ui/closures/old-closure-explicit-types.rs` | B107 | the nested `fn as_buf` is hoisted to module scope; `println!` dropped; the generic return type `T` is specialized to `()` (Logos couldn't resolve `T` when it appears only in the FnOnce return position). The closure has an explicit `String` parameter type. Tests a FnOnce-bounded helper invoked with an explicitly-typed closure consuming a String. |
| `pass/drop/drop-trait-generic.logos` | `tests/ui/drop/drop-trait-generic.rs` | B107 | the `println!` in drop is dropped; literals suffixed. Tests a Drop impl on a generic struct `S<T>` running its destructor at scope exit. |
| `pass/drop/enum-drop-impl-15063.logos` | `tests/ui/drop/enum-drop-impl-15063.rs` | B107 | the `println!` in drop is dropped. Regression for rustc #15063. Tests that a Drop impl on a unit-variant enum compiles and runs the destructor when the value goes out of scope. |
| `pass/enum/enum-size-variance.logos` | `tests/ui/enum/enum-size-variance.rs` | B107 | i64 substituted for the i64/isize payloads; lint attributes dropped. Tests that a range of enum shapes — empty, C-like, tuple payloads of varying arity, and a generic two-parameter enum — all declare cleanly. |
| `pass/enum/enum-with-uninhabited-variant.logos` | `tests/ui/enum/enum-with-uninhabited-variant.rs` | B107 | i32 kept; the uninhabited variant B(Void) holds an empty enum `Void {}`. Tests that an enum can contain a variant whose payload is an uninhabited (empty) enum, and that a normal variant still constructs. |
| `pass/enum/namespaced-enums-or-pattern.logos` | `tests/ui/enum/namespaced-enums-xcrate.rs` | B107 | the cross-crate `aux-build` enum is declared locally (Logos has no aux-crate harness); the `_foo` matcher with an or-pattern spanning a unit variant, a tuple variant, and a struct variant is preserved, and `main` exercises each variant. Tests an or-pattern combining the three enum variant kinds in a single match arm. |
| `pass/enum/simple-enum-usage-8506.logos` | `tests/ui/enum/simple-enum-usage-8506.rs` | B107 | String fields via logos.mem.string; the module-level `static` is rewritten as a local binding inside `main` (Logos has no String-payload enum at module-static position), preserving the value. Regression for rustc #8506. Tests declaring an enum with a unit variant and a two-String tuple variant, and binding the unit variant. |
| `pass/enum/struct-like-variant-construct.logos` | `tests/ui/enum/struct-like-variant-construct.rs` | B107 | isize → i64, f64 literals suffixed; constructs a struct-style enum variant. Tests building a struct-like enum variant with named fields. |
| `pass/enum/struct-like-variant-match.logos` | `tests/ui/enum/struct-like-variant-match.rs` | B107 | isize → i64; f64 literals suffixed; the `match *x` over a `&Foo` is rewritten to `match x` (Logos auto-derefs the enum reference). Tests matching struct-style enum variants binding the named fields, with fields in a different order than declared in the Bar arm. |
| `pass/fn/nested-function-names-issue-8587.logos` | `tests/ui/fn/nested-function-names-issue-8587.rs` | B107 | isize → i64; `pub struct X;` zero-field struct → `struct X {}`; #[inline(never)] attrs dropped; n.<method> calls kept. Regression for rustc #8587 — nested functions with the same name in different methods stay distinct. Two methods (f, h) each declare a local `fn inner()`. |
| `pass/for-loop-while/issue-1257.logos` | `tests/ui/for-loop-while/issue-1257.rs` | B107 | the `String != String` comparison is written via `!line.eq_str("exit")` (Logos has no String inequality operator); the loop body assigns one of two literal Strings. Tests a while loop whose condition is a String comparison, terminating after a fixed number of iterations. |
| `pass/for-loop-while/liveness-assign-imm-local-after-loop.logos` | `tests/ui/for-loop-while/liveness-assign-imm-local-after-loop.rs` | B107 | isize → i64; lint attrs dropped; `let v` → `let mut v` (the local is assigned twice, which Logos requires `mut` for — Rust allowed it because the second assignment is after a non-terminating loop). As upstream, `test` is compiled but never called (its `loop {}` never terminates). Tests that an assignment after a non-terminating `loop {}` lowers cleanly. |
| `pass/for-loop-while/liveness-move-in-loop.logos` | `tests/ui/for-loop-while/liveness-move-in-loop.rs` | B107 | isize → i64; literals suffixed; `Vec::new()` → `vec_new::<i64>()` (Logos's Vec constructor). As upstream, `the_loop` is compiled but never called from main (its `loop` would not terminate). Tests that moving a value into a Vec::push inside a `loop` (with a `break` arm) type-checks and lowers. |
| `pass/for-loop-while/loop-labeled-break-value.logos` | `tests/ui/for-loop-while/loop-labeled-break-value.rs` | B107 | the `let _: i32 = loop { break 'outer }` bindings are dropped (in Logos a loop that only breaks an *outer* label is void-typed, so it can't initialize an i32 local) — the inner loops are kept as plain statements that break the outer label. Tests labeled `loop` blocks broken out of from nested inner loops via `break 'label`. |
| `pass/functions-closures/copy-closure.logos` | `tests/ui/functions-closures/copy-closure.rs` | B107 | `println!` and the explicit `.clone()` are dropped; the closure captures a Copy local `a` and returns it; it is passed to a generic `call<T, F: FnOnce() -> T>` helper. Tests that a closure capturing a Copy value can be invoked via a FnOnce bound and yields the captured value. |
| `pass/functions-closures/fn-coerce-field.logos` | `tests/ui/functions-closures/fn-coerce-field.rs` | B107 | the generic `r<F: FnOnce()>` is specialized to a concrete fn-pointer field type `field: fn()` (Logos has no FnOnce-bounded generic struct ports here); the `f as fn()` coercion writes a top-level fn into the field. Tests storing a function item into a fn-pointer struct field. |
| `pass/functions-closures/fn-item-type-coerce.logos` | `tests/ui/functions-closures/fn-item-type-coerce.rs` | B107 | isize → i64; `type IntMap = fn(i64) -> i64;` kept; the generic `eq::<IntMap>(foo, bar)` is rewritten to direct calls through the fn-pointer-typed locals (verifies both functions coerce to the same fn pointer type and produce the expected results). Tests fn-item → fn-pointer coercion via a type alias. |
| `pass/generics/generic-unique.logos` | `tests/ui/generics/generic-unique.rs` | B107 | isize → i64; the `Box<Triple<T>>` boxing is dropped (the `box_it` helper returns the value directly — Logos has no Box wrapper needed here); literals suffixed. Tests a generic struct passed through a generic function and a field read on the result. |
| `pass/issues/issue-27054-primitive-binary-ops.logos` | `tests/ui/issues/issue-27054-primitive-binary-ops.rs` | B107 | literals suffixed; `&mut 1` is bound to a mutable local and `x` is taken by `&mut` (Logos has no `&mut <literal>` rvalue ref). Tests evaluation order of `*x + { *x = 2; 1 }` — the left `*x` is read before the block mutates it. |
| `pass/issues/issue-41479.logos` | `tests/ui/issues/issue-41479.rs` | B107 | the unit-typed `()` payloads are replaced with i64 values (Logos tuple elements are concrete values); the nested-tuple shape and the generic field projection (pair.0 / pair.1) are preserved. Tests a generic function destructuring a tuple via field access. |
| `pass/issues/issue-4759-1.logos` | `tests/ui/issues/issue-4759-1.rs` | B107 | isize → i64; the receiver `4` is suffixed `4i64`. Tests defining a trait and implementing it for a primitive integer type, then calling the method on an integer literal. |
| `pass/mir/issue-76803-branches-not-same.logos` | `tests/ui/mir/issue-76803-branches-not-same.rs` | B107 | derives dropped; the result is checked via a match (no enum equality assert). Tests an `encode` match that maps Type::A to Type::B and passes everything else through unchanged. |
| `pass/mir/issue-77002.logos` | `tests/ui/mir/issue-77002.rs` | B107 | type alias M = [i64;2] kept; `M::default()` (no assoc-fn on array aliases) replaced with an explicit zero-initialized array literal. Tests array-typed local mutation through an indexed write and array return. |
| `pass/mir/issue-89485.logos` | `tests/ui/mir/issue-89485.rs` | B107 | literals suffixed; derives dropped. Regression for rustc #89485 (SimplifyArmIdentity). Tests matching a custom-discriminant C-like enum (A=1, B=2) directly with one named arm and a wildcard. |
| `pass/mir/mir_cast_fn_ret.logos` | `tests/ui/mir/mir_cast_fn_ret.rs` | B107 | the `extern "C"` ABI markers are dropped (Logos fns use the default ABI); literals suffixed. The returned tuple is bound to a local before projecting (`let t = tuple2(); t.1`) — projecting a field directly off a call result (`tuple2().1`) currently fails codegen in Logos. Tests functions returning tuples and projecting single elements out of them. |
| `pass/mir/mir_codegen_switch.logos` | `tests/ui/mir/mir_codegen_switch.rs` | B107 | literals suffixed; lint attributes dropped. Tests match codegen over a four-variant enum (two tuple-payload, two unit) returning an integer, plus a second match returning bool with a wildcard arm. |
| `pass/mir/mir_constval_adts.logos` | `tests/ui/mir/mir_constval_adts.rs` | B107 | the `const` items become plain locals (Logos routes compile-time values through metacall, not const at type position); the `&'static str` tuple half and the derived-PartialEq tuple equality asserts are dropped (str-tuple consts + struct tuple equality are out of scope); equality is checked field-wise. Tests constructing struct / tuple / generic newtype aggregates and reading their fields back. |
| `pass/mir/mir_small_agg_arg.logos` | `tests/ui/mir/mir_small_agg_arg.rs` | B107 | literals suffixed; the destructuring tuple parameter `(x, y): (i8, i8)` is rewritten to a named tuple parameter that is destructured in the body (Logos does not bind patterns in fn parameter position). Tests passing a small aggregate (a 2-tuple) by value. |
| `pass/mir/mir_void_return_2.logos` | `tests/ui/mir/mir_void_return_2.rs` | B107 | faithful; `pub fn main()` → `fn main() -> i32`. Tests that a void-returning function calling another void-returning function lowers and runs cleanly. |
| `pass/mir/no-trait-method-issue-40473.logos` | `tests/ui/mir/mir-inlining/no-trait-method-issue-40473.rs` | B107 | literals suffixed; the `impl Foo for ()` is replaced with an `impl Foo for Unit` on a zero-field struct (Logos has no method surface on the bare `()` type). Regression for rustc #40473. Tests that an overriding trait method is selected over the trait's default body. |
| `pass/mir/simplify-branch-same.logos` | `tests/ui/mir/simplify-branch-same.rs` | B107 | literals suffixed; the `m!` macro is inlined into `f`'s body (the macro expanded to a match with a value-only or-pattern arm and a side effect). Regression for the SimplifyBranchSame miscompilation. Tests an or-pattern match arm (`Fat \| Thin`) sharing a body with side effects. |
| `pass/moves/move-out-of-field.logos` | `tests/ui/moves/move-out-of-field.rs` | B107 | String via logos.mem.string; the final `assert_eq!(str, "...")` uses String::eq_str (no String-vs-&str ==). Tests building a String field via `&mut self` method calls, then moving the field out of the struct by value. |
| `pass/structs/struct-literal-dtor.logos` | `tests/ui/structs/struct-literal-dtor.rs` | B107 | String field uses logos.mem.string; the `println!("{}", self.x)` in drop is dropped (println! is not ported). Tests that a struct literal with a String field and a Drop impl constructs and drops cleanly. |
| `pass/structs/struct-order-of-eval-2.logos` | `tests/ui/structs/struct-order-of-eval-2.rs` | B107 | String fields use logos.mem.string. Upstream moved `s` into f0 while f1 borrowed it; in Logos `to_string()` consumes its receiver, so each field gets its own freshly-built String from a string literal. Tests out-of-order struct-literal field initialization with String values. The final equality check uses String::eq_str (no String-vs-&str ==). |
| `pass/traits/bound-multiple.logos` | `tests/ui/traits/bound/multiple.rs` | B107 | literals suffixed; trait names mapped to Logos's Eq/Ord (PartialEq/PartialOrd). Tests a generic function with two trait bounds on a single type parameter (`T: Eq + Ord`). |
| `pass/traits/conditional-dispatch.logos` | `tests/ui/traits/conditional-dispatch.rs` | B107 | the `Box<i32>` impl and `Copy`/Option blanket are dropped (Box is out of scope here); the core conditional dispatch — a blanket `impl<T: MyCopy> Get for T` over per-type `MyCopy` impls, reached through a generic `get_it<T: Get>` — is preserved on integer types. Tests resolving a blanket trait impl bounded on another trait. |
| `pass/traits/inheritance-auto.logos` | `tests/ui/traits/inheritance/auto.rs` | B107 | isize → i64; literals suffixed. Tests that a blanket impl `impl<T: Foo + Bar + Baz> Quux for T` makes any type implementing all three supertraits automatically a `Quux`, then dispatches Foo/Bar/Baz methods through a `T: Quux` bound. |
| `pass/traits/inheritance-diamond.logos` | `tests/ui/traits/inheritance/diamond.rs` | B107 | isize → i64; the unit `bogus: ()` field becomes `bogus: i64`; literals suffixed. Tests diamond trait inheritance — `D: B + C`, with both `B: A` and `C: A` — implemented on a struct and dispatched through a `T: D` bound. |
| `pass/traits/inheritance-multiple-params.logos` | `tests/ui/traits/inheritance/multiple-params.rs` | B107 | isize → i64; the unit `bogus: ()` field becomes `bogus: i64`; literals suffixed. Tests two sibling supertraits (`B: A`, `C: A`) implemented on one struct, dispatched through a generic fn with three separately-bounded type parameters. |
| `pass/traits/inheritance-num.logos` | `tests/ui/traits/inheritance/num.rs` | B107 | trait supertrait bounds mapped to Logos's Eq/Ord; the two bound-checking functions keep their `loop {}` bodies and are never called (matching upstream). Tests declaring a trait hierarchy with supertrait bounds (`NumExt: Eq + Ord`, `FloatExt: NumExt`) and bounding generic fns on them. |
| `pass/traits/inheritance-self.logos` | `tests/ui/traits/inheritance/self.rs` | B107 | isize → i64; `println!` dropped; the `Sized` bound on the supertrait is dropped (implicit in Logos). Tests a supertrait that mentions `Self` as a type argument (`Bar: Foo<Self>`), with the subtrait method calling the supertrait method via `self.f(self)`. |
| `pass/traits/inheritance-simple.logos` | `tests/ui/traits/inheritance/simple.rs` | B107 | isize → i64; literals suffixed. Tests a supertrait relationship (`Bar: Foo`) with both traits implemented on a struct, and generic functions bounded on the sub- and super-trait dispatching the right method. |
| `pass/traits/issue-3683.logos` | `tests/ui/traits/issue-3683.rs` | B107 | isize → i64; literals suffixed; the receiver `3` is suffixed `3i64`. Tests a trait with a default method `b` that calls the required method `a`, implemented on a primitive integer type. |
| `pass/typeck/ufcs-type-params.logos` | `tests/ui/typeck/ufcs-type-params.rs` | B107 | literals suffixed; the UFCS `Foo::<i32>::get(&x)` call is written in method form `x.get()` (Logos does not support the `Trait::<T>::method(recv)` static-call form). Tests invoking a generic trait method implemented on a primitive integer type. |

## When upstream changes

The manifest pins to a commit, not to `master`. If rust-lang/rust
later changes a test we imported, our copy diverges silently. A
periodic reconciliation sweep:

1. Run a diff tool (TBD) comparing each imported file against the
   current upstream version at the same path.
2. For each non-trivial drift, decide:
   * **Re-import** the new version (replace, bump the manifest row's
     commit).
   * **Keep** our version (rustc tightened a Rust-specific check
     that doesn't apply to Logos).
   * **Retire** our copy if upstream removed the test.

This file is the source of truth for what came from where; keep it
honest.
| B11 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | Phase 3 second-pass — new imports (binop + array-slice-vec, 3 tests) |
| B12 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | expr — block-as-expr / fn-ptr / if-generic / panic / early-return (6 tests) |
| B13 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | array-slice-vec — fixed-length copy / index / repeat / static-array (4 tests) |
| B14 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | array-slice-vec — &[1,2,3] slice from array literal (B-as-01 close); fn — expr-fn variants + fn-ptr-in-struct-field; for-loop-while — break/continue (4 tests) |
| B15 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | binding — match-as-expression (basic / panic / panic-all / fat-arrow over enum) (4 tests) |
| B16 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | expr — return-in-block-tuple (K10-co-04 partial close); match — issue-33498 (tuple pattern arms); functions-closures — fn-bare-{item,assign} (4 tests) |
| B17 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | structs — drop+destructure (dtor-6344, newtype-struct-with-dtor); match — guard with closure / guard with parenthesised AND / char-range guard arms (5 tests) |
| B18 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | generics — derived-type (Pair<T> auto-Copy); regions — escape-into-other-fn / nullary-variant (3 tests) |
| B19 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | coercion — 3 reborrow tests (mut-ptr-arg, imm-ptr-arg, imm-ptr-rcvr); enum — issue-23304-2 (disc literal), match-either-enum-variants-6117 (S8-en-03 close: `_` shorthand for unit-payload variant) (5 tests) |
| B20 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-11 | Victor Smirnov | grab-bag — enum/issue-23304-1 (cross-enum disc), closures/old-closure-fn-coerce, regions/regions-reassign-match-bound-pointer (P4-pm-12 close `mut z`), binding/let-destruct-ref (P4-pm-14 close `let ref y`), binding/fn-pattern-expected-type (C5-cl-06 close closure-ret-infer), binding × 5 (borrowed-ptr-pattern P4-pm-17 close, match-tag, nullary-or-pattern, match-naked-record-expr, nested-pattern, use-uninit-match2, match-phi, match-range, inconsistent-lifetime-mismatch), binding/func-arg-wild-pattern (12 tests) |
| B21 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-13 | Victor Smirnov | unsized — `?Sized` syntax-coverage smoke (Phase 1B-6 validation of trait / struct / enum / impl / fn declaration sites with `?Sized`) (1 test) |
| B22 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-13 | Victor Smirnov | unsized — maybe-bounds-where-cpass (Phase 1B-8 validation: generic struct with `?Sized` field of `*const T` instantiated with bare `[u8]`) (1 test) |
| B23 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-13 | Victor Smirnov | coretests — first port from `library/coretests/tests/`: `bool.rs::test_bool` (PartialEq method form, BitAnd/BitOr/BitXor/Not methods+operators, ordering, `.cmp()` returning Ordering). Phase 4a kickoff. Gap catalog: `docs/core-port/coretests-port-gaps.md`. |
| B24 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-14 | Victor Smirnov | coretests — `option.rs` subset (test_unwrap, test_unwrap_panic1 with `#[should_panic]`, test_unwrap_or, test_is_some_is_none, test_or_typed). Validates Option method surface (SL-sl-03) + prelude shorthand (CP-cm-03). Box/Rc/RefCell/Drop-impl forms deferred; full `.and()` / `.or_else()` / `.or(Some(x))` shapes deferred (None-receiver T-inference gap). |
| B25 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-14 | Victor Smirnov | coretests — `result.rs` subset (test_unwrap_or, test_is_ok_is_err, test_unwrap_err with isize payload, test_expect_panic / test_expect_err_panic / test_unwrap_panic via `#[should_panic]`). Validates Result method surface (SL-sl-04). Surfaced new gap CP-cm-11 (`str == str` returns false even for equal values). Closures / `.and()`/`.or()` / variant-ctor turbofish deferred. |
| B26 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-14 | Victor Smirnov | coretests — `bool.rs::test_bool_to_option` / `test_bool_to_result` (closes SL-sl-06). Validates the four bool conversion methods (`then` / `then_some` / `ok_or` / `ok_or_else`) end-to-end after CP-cm-10 closure (sema's unify_types now handles `Kind::FnPtr` / `Kind::Closure` so type-params inside fn-ptr arg signatures infer correctly). Closures (`\|\| 0`) lowered to named fn refs; `const A: …` const-context uses deferred (no `const fn` — metacall channel instead). |
| B27 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-14 | Victor Smirnov | coretests — `cmp.rs::test_int_totalord` / `test_bool_totalord` + `test_isize_totalord` (added beyond upstream). Validates Ord on i32/bool/isize. Required adding `impl Ord for isize` in stdlib (was missing). test_mut_int_totalord / test_ord_max_min / min_by/max_by/minmax_by tests deferred — see gap catalog F section. |
| B28 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter adapters batch (4 files / 8 #[test] fns) validating the SL-sl-05 closures of the previous batches: `iter/traits/double_ended.rs::test_rev` + `test_iterator_rev_nth` (DoubleEndedIterator + RevIter + `.nth`), `iter/adapters/cycle.rs::test_cycle` simplified to Counter-source (CycleIter + `I: Clone` bound; hand-rolled Clone impl because the metaprog `#[derive_clone]` handler doesn't fire under `logosc --test` synthesis — follow-up gap), `iter/adapters/peekable.rs::test_iterator_peekable` (front subset — PeekableIter `peek`/`next` interleaving; len/next_back parts deferred), `iter/adapters/mod.rs` shape for `.collect::<Vec<T>>()` (CP-cm-14 method-tparam inference for trait-static dispatch). Tests live under `pass/iterators/test_harness_coretest_iter_*.logos`. |
| B100 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | `?` operator with full From-coerce semantics (Rust-parity Try trait shape). **Compiler change**: sema_expr.cpp's TRY_EXPR handler, when inner Result's E differs from outer fn's E, now desugars `expr?` to: `match expr { Ok(v) => v, Err(e) => return Err(<E_outer>::from(e)) }`. Reuses existing match + return + EnumLitData + static-call lowering — sidesteps ETry's byte-copy path for heterogeneous E. Synthesises bindings `__try_ok_<n>` / `__try_err_<n>`. Finds the `From` impl via `find_func_candidates(<E_outer>__from)` matching by arg type to `E_inner`. **Tests**: 7 #[test] fns covering Result ok-propagation, err-propagation, chained `?`, Option Some-propagation, Option None-short-circuit, **plus heterogeneous-E ok-arm and err-arm with From-coerce** (`impl From<i32> for OuterErr` producing `OuterErr{code: val+1000}` — verified e.code == 1007 when inner returns Err(7)). Full ctest: 3191/3191. |
| B99 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | From/Into/TryFrom/TryInto traits + primitive impls. **Compiler fix**: coherence-key now includes the trait's type-arguments (separate `coherence_keys_` StrSet keyed by `Trait[arg1,arg2,...]::Target`). Pre-fix, `impl From<i8> for i32` and `impl From<i16> for i32` would conflict because `impls_` was keyed by bare `From::i32`. `impls_` stays bare (so existing bound-check / has_impl / find_impl callers without trait_args still hit a registered impl); duplicate-detection uses the new set. **Stdlib**: std.lang.convert rewritten with `pub`, ConvertError marker struct, ~25 widening `From` impls (i8→i16/i32/i64, etc.), bool→i32/i64/u32/u64/u8, char→u32, and ~12 `TryFrom` impls including u32→char (Unicode scalar-value check). 11 #[test] fns in tests/imported/pass/casts/. 3190/3190. |
| B98 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | Numeric surface extension — wrapping/saturating/checked/overflowing for u32/u64/i64 + tests (1 file / 14 #[test] fns). Stdlib: `std/lang/ord/ord.logos` gets the full surface mirror of the i32 family, ~250 LOC. Implementation uses `std.lang.arith::wrapping_{add,sub,mul}::<T>` intrinsics for the wrapping primitives; overflow detection without u128/i128 uses post-op sign comparison for signed (i64) and post-op magnitude comparison for unsigned (u32/u64). i64::MIN literal built from `0x8000000000000000u64 as i64` (parser pre-negation guard). 3189/3189. |
| B97 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — bool.rs to_string subset (1 file / 4 #[test] fns): `test_bool.to_string`/`format!("{}", true/false)` shape exercises Display + ToString impls on bool. String == &str needs `.as_str()` (Logos's `==` on String is by-Eq-trait; there's no implicit String→&[u8] coercion). |
| B96 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | Iterator::nth shape fixed to `&mut self` (Rust parity, full-parity policy) + coretests — Iterator::nth / nth_back on ranges (1 file / 6 #[test] fns): `iter/range.rs::test_range_nth` (Some+None arms), `test_range_nth_back` (Some+None — uses DoubleEndedIterator nth_back trait-default), `test_range_nth_consumes` (faithful upstream chain — same r advanced by successive nth calls), plus i64-Range nth at 0 / middle / end positions. Stdlib change in std/lang/iter/iter.logos: `fn nth(self: Self, n)` → `fn nth(self: &mut Self, n)`. 3187/3187 after change. |
| B95 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | char::to_digit → `Option<u32>` (Rust shape) + char.rs test imports. Stdlib change: `char::to_digit(radix) -> u32` (sentinel u32::MAX) upgraded to `-> Option<u32>` to match Rust. Adopts full-Rust-parity policy. Pre-existing `test_harness_coretest_char_ascii.logos::test_to_digit` patched to use `.unwrap()` / `.is_none()`. New test_harness_coretest_char_to_digit_option.logos (4 #[test] fns): test_to_digit_option (Some arms via match), test_to_digit_none (None arms), test_is_whitespace_ascii, test_is_whitespace_u2007 (FIGURE SPACE + NBSP probes). |
| B94 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — cmp.rs Ordering methods + min/max by fn (1 file / 6 #[test] fns): `test_ordering_reverse` / `test_ordering_then` / `test_ordering_then_with` (latter validates lazy non-Equal branch — Less/Greater paths don't call the fallback fn-ptr); `test_ord_min_by` / `test_ord_max_by` / `test_ord_min_max_by_key` exercise the `cmp_min_by` / `cmp_max_by` / `cmp_min_by_key` / `cmp_max_by_key` free fns from std.lang.ord. Closures `\|x, y\| x.abs().cmp(&y.abs())` lowered to named fn refs (no closure-as-fn-ptr coercion required). Ordering doesn't impl Display/Debug, so test_ordering_reverse/then/then_with destructure via match. |
| B93 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — u32 + i64 num batches paired with B92 (2 files / 18 #[test] fns). u32 (8 fns): same surface as B92's u64 — count_ones/zeros, rotate, swap_bytes, leading_trailing_zeros, pow, max/min/clamp. i64 (10 fns): from `num/int_macros.rs` — same bit ops + abs / signum / is_positive / is_negative; i64::MIN literal `-0x8000000000000000i64` triggers a parse-time overflow (abs-value pre-negation > MAX), so the const is built from u64::MSB via `as i64` cast. |
| B92 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — u64 bit ops + pow + max/min/clamp (1 file / 7 #[test] fns): subset of `num/uint_macros.rs` instantiated at u64 — `test_overflows` / `test_count_ones` / `test_count_zeros` / `test_rotate` / `test_swap_bytes` / `test_leading_trailing_zeros` / `test_pow` / `test_max_min_clamp`. Missing-surface tests skipped: `trailing_ones` / `leading_ones` / `bit_width` / `funnel_shift` / `carryless_mul` / `extract_bits` / `deposit_bits` / `reverse_bits` (none yet implemented in std.lang.ord). Const-context shape (`assert_eq_const_safe!`) lowers to runtime assert — Logos has no const-eval; metacall is the compile-time channel. First non-i32 num test in the suite. |
| B91 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — Option::ok_or / ok_or_else (1 file / 5 #[test] fns): `option.rs::test_ok_or` (Some+None), `test_ok_or_else` (Some+None), + ok_or-chained-with-map smoke. Stdlib additions: `Option::ok_or<E>(self, err)` and `Option::ok_or_else<E>(self, err: fn() -> E)` in std/lang/option/option.logos. Required adding `use std.lang.result;` to option.logos (Result was previously not imported); cross-pkg back-ref works fine since result→option dep is one-way (Result methods returning Option only). |
| B90 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — map_or / map_or_else on Option + Result (1 file / 8 #[test] fns): `option.rs::test_map_or` (Some+None), `option.rs::test_map_or_else` (Some+None), `result.rs::test_map_or` (Ok+Err), `result.rs::test_map_or_else` (Ok+Err). Stdlib additions in same batch: `Option::map_or<U>(self, default, f)` + `Option::map_or_else<U>(self, dfl, f)` in std/lang/option/option.logos; `Result::map_or<U>` + `Result::map_or_else<U>` in std/lang/result/result.logos. Both map_or_else flavors take a closure-less `fn() -> U` (Option) / `fn(E) -> U` (Result) — no closure-as-fn-ptr coercion at call sites. |
| B89 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — Option filter/zip/flatten + tail combinators (1 file / 14 #[test] fns): `option.rs::test_filter` (+ chained), `option.rs::test_zip` (some/some, some/none, none/some via `option_zip` free fn — U tparam not on impl block), `option.rs::test_flatten` (3 cases via `option_flatten` free fn), `option.rs::test_xor`, `test_or_else` (some / none-recover / none-still-none), `test_and_chain`. Stdlib additions in same batch: `Option::filter(self, pred: fn(T) -> bool) -> Option<T>` method (uses raw-ptr copy idiom to dodge move-of-moved-value on payload `v` before pred call), `option_zip<T, U>(a, b) -> Option<(T, U)>` free fn. Payload Display/Debug not yet on Option, so assertions destructure via `match`. |
| B88 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter lex-comparison helpers (1 file / 5 #[test] fns): `iter/traits/iterator.rs::test_lt` / `test_cmp_by` shape — equal, lex-greater, lex-less, length-difference, empty-vs-nonempty. Stdlib companions: `iter_cmp` / `iter_lt` / `iter_le` / `iter_gt` / `iter_ge` / `iter_ne` free fns in std/lang/iter/iter.logos (T: Ord for cmp/lt/le/gt/ge; T: Eq for ne which delegates to !iter_eq). |
| B87 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter helpers batch (1 file / 9 #[test] fns): `iter/traits/iterator.rs::test_eq` / `test_iterator_max_by_key` / `test_iterator_min_by_key` / `test_partition`. Stdlib companions in one batch: `iter_eq<I, J, T: Eq>(a, b)`, `iter_max_by_key<I, T: Copy, K: Ord>(iter, key)`, `iter_min_by_key<I, T: Copy, K: Ord>(iter, key)` in std/lang/iter/iter.logos; `iter_partition_vec<I, T>(iter, pred) -> ZipPair<Vec<T>, Vec<T>>` in std/collections/vec/vec.logos (cyclic-import-avoided placement). |
| B86 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter_eq_by (1 file / 3 #[test] fns): `iter/traits/iterator.rs::test_eq_by` — pairwise equality of two iters under a custom equivalence. Stdlib companion: `iter_eq_by<I, J, T, U>(a, b, eq)` free fn in std/lang/iter/iter.logos. 3176/3176. |
| B85 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter_rposition (1 file / 5 #[test] fns): `iter/traits/double_ended.rs::test_rposition / test_rev_rposition` — first hit, no match, index 0, even-predicate, empty. Stdlib companion: `iter_rposition<I: DEI<T> + ESI<T>, T>(iter, pred) -> Option<i64>` free fn. Walks from back, counts position down from len-1. 3175/3175. |
| B84 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — ScanIter adapter (1 file / 2 #[test] fns): `iter/adapters/scan.rs::test_scan` shape — running-sum + early-stop. Stdlib companion: new `ScanIter<I, T, St, B>` struct + `iter_scan` free fn in std/lang/iter/iter.logos. Closure with captured state replaced by explicit `St` struct + named fn-ptr `fn(&mut St, T) -> Option<B>`. 3174/3174. |
| B83 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — FuseIter adapter (1 file / 3 #[test] fns): `iter/adapters/fuse.rs::test_fuse` shape — pin-to-None after exhaustion, DEI variant, empty source. Stdlib companion: new `FuseIter<I, T>` struct + `iter_fuse` free fn in std/lang/iter/iter.logos. Once inner yields None, future .next() / .next_back() return None directly. 3173/3173. |
| B82 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — FilterMapIter adapter (1 file / 3 #[test] fns): `iter/adapters/filter_map.rs::test_filter_map` + DEI variant + empty case. Stdlib companion: new `FilterMapIter<I, T, B>` struct + free fn `iter_filter_map<I, T, B>(iter, f: fn(T) -> Option<B>)` in std/lang/iter/iter.logos. Iterator impl skips None results from f; DEI impl symmetric over the back. 3172/3172. |
| B81 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — .find_map / .reduce trait-defaults (1 file / 7 #[test] fns): `iter/traits/iterator.rs::test_find_map` + reduce smoke (sum, product, empty, single). Stdlib companion: `.find_map<B>` (method-generic) and `.reduce` trait-default methods on Iterator. find_map returns first Some(f(x)); reduce seeds fold from first item, None on empty. 3171/3171. |
| B80 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — Ord-bounded iter_max / iter_min (1 file / 4 #[test] fns): `iter/traits/iterator.rs::test_iterator_max / test_iterator_min` analogues. Stdlib companion: free fns `iter_max<I: Iterator<T>, T: Ord>(iter)` / `iter_min<I, T: Ord>(iter)` in std/lang/iter/iter.logos using raw-ptr-copy idiom on non-Copy T. 3170/3170. |
| B79 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter/range.rs::test_range (1 file / 6 #[test] fns) using `(0..N)` range syntax as expression. Logos's sema range-expr handler accepts `0..N` outside for-loops too (evaluates to `RangeI32` / `RangeI64`), unblocking faithful upstream ports. Tests: test_range_collect / rev_collect / negative bounds / reversed bounds / sum / exhaustion. 3169/3169. |
| B78 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — Sum / Product traits + .sum<S>() / .product<S>() (1 file / 5 #[test] fns): `iter/traits/accum.rs::test_iterator_sum_*` for i32 + i64 + range source; product equivalents. Stdlib companion: new `pub trait Sum<A>` / `pub trait Product<A>` in std/lang/iter/iter.logos with primitive impls (impl Sum<i32> for i32 etc.); `.sum<S>()` / `.product<S>()` trait-default methods on Iterator dispatching via `S::sum(self)`. **Compiler change (CP-cm-14 extension)**: mono_clone's prefix-rewrite + mono_scan's struct-method-template fallback both now also walk `templates_` for `[pkg.]<base>__<method>__g__*` when `struct_method_templates_` doesn't have the entry (primitive-receiver impl methods live in templates_, not struct_method_templates_). 3168/3168. |
| B77 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — Default for Option (1 file / 3 #[test] fns): Option<T>::default() = None for i32/i64; option_unwrap_or_default(default) = T::default(). Stdlib companion: `impl<T> Default for Option<T>` in std/lang/option/option.logos returning `Option::<T>::None`. 3167/3167. |
| B76 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — RangeI32 DEI + ESI (1 file / 6 #[test] fns): range forward / back interleave / len / `.rev()` trait-default → RevIter / `.count()` / `.is_empty()` via len. Stdlib companion: `impl DoubleEndedIterator<i32>` + `impl ExactSizeIterator<i32>` for RangeI32 (and RangeI64) in std/lang/range/range.logos. Inherent `RangeI32::rev` renamed to `into_rev_range` to avoid clashing with the DEI trait-default's `.rev()` (which now returns `RevIter<RangeI32, i32>`). 3166/3166. |
| B75 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — i32 overflowing + wrapping arithmetic (1 file / 5 #[test] fns): overflowing_add / sub / mul / neg / abs (returning `(i32, bool)`) and wrapping_add / sub / mul / neg / abs. Stdlib companion: matching inherent methods on i32 in std.lang.ord. wrapping_* implemented via i64 intermediate + truncation (Logos's plain `+` / `-` / `*` trap on signed overflow with SIGILL); overflowing_* returns the wrapping result plus an explicit overflow flag. 3165/3165. |
| B74 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — i32 checked arithmetic (1 file / 5 #[test] fns): `int_macros.rs::test_signed_checked_div` + a wider cross-section of checked_add / sub / mul / div / rem / neg / abs. Stdlib companion: matching inherent methods on i32 in std.lang.ord, returning `Option<i32>`. i64 intermediates detect overflow; explicit `i32::MIN / -1` and `-i32::MIN` special-cases yield None. 3164/3164. |
| B73 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — i32 bit rotation (1 file / 6 #[test] fns): `int_macros.rs::test_rotate` shape — rotate_roundtrip, byte_aligned_u32, zero, overlong (rotation by BITS = identity; by N*BITS+k = by k), i32 (delegates to u32), u64. Stdlib companion: rotate_left / rotate_right inherent methods on u32 / i32 / u64 / i64 in std.lang.ord — modular shift amount with k==0 corner case explicit (shifting by BITS is UB on x86). 3163/3163. |
| B72 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — i32 swap_bytes / to_le / to_be (1 file / 5 #[test] fns): `int_macros.rs::{test_swap_bytes, test_le, test_be}` flattened for i32 + u64. Stdlib companion: swap_bytes / to_le / from_le / to_be / from_be inherent methods on u32 / i32 / u64 / i64 in std.lang.ord, wrapping the `bswap_u64` intrinsic with width shift; to_le/from_le identity on Logos's LE x86_64 target. 3162/3162. |
| B71 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — i32 saturating arithmetic + ExactSize.is_empty (1 file / 5 #[test] fns): `int_macros.rs::{test_saturating_add, test_saturating_sub, test_saturating_mul, test_saturating_abs, test_saturating_neg}`. Stdlib companions: saturating_add/sub/mul/abs/neg inherent methods on i32 (i64 intermediates dodge i32 overflow, clamp to [MIN, MAX]); plus `is_empty` trait-default on ExactSizeIterator (`len() == 0`). 3161/3161. |
| B70 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — integer pow (1 file / 4 #[test] fns): `int_macros.rs::test_pow` subset for i32/i64/u32 — pow_basic, pow_negative_base, pow_i64 (powers of 10/2 up to 1099511627776), pow_u32. Stdlib companion: `pub fn pow(self, exp: u32) -> Self` inherent methods on i32/i64/u32/u64 via repeated squaring (wrapping arithmetic — Rust's `wrapping_pow` shape; `checked_pow` / `overflowing_pow` variants deferred). 3160/3160. |
| B69 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — integer bit-counting methods (1 file / 5 #[test] fns): `int_macros.rs::{test_count_ones, test_count_zeros, test_leading_zeros, test_trailing_zeros}` for i32 + i64 cross-check. Stdlib companion: `count_ones`/`count_zeros`/`leading_zeros`/`trailing_zeros` inherent methods on u32/i32/u64/i64 in std.lang.ord — wrap `popcount_u64`/`leading_zeros_u64`/`trailing_zeros_u64` compiler intrinsics with width-appropriate adjustments (e.g. u32 ctlz = u64_ctlz - 32 after zero-extension; ctlz/cttz on 0 returns BITS to match Rust semantics). 3159/3159. |
| B68 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — DoubleEndedIterator::nth_back (1 file / 2 #[test] fns): `iter/traits/double_ended.rs::test_iterator_rev_nth_back` + a direct nth_back smoke. Stdlib companion: trait-default `fn nth_back(self: &mut Self, n: i64) -> Option<Item>` on DoubleEndedIterator — skips n items from the back via repeated next_back, returns the next one. 3158/3158. |
| B65 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — Option IntoIterator (1 file / 3 #[test] fns): `option.rs::test_iter` subset — Some/None iteration via explicit `.into_iter().next()` + DEI back-iteration. Stdlib companion: new `OptionIter<T>` struct + `impl IntoIterator<T> for Option<T>` + `impl DoubleEndedIterator<T> for OptionIter<T>` in std/lang/iter/iter.logos. Rust's `for x in opt` syntactic sugar over IntoIterator isn't yet wired in Logos's for-loop lowering (only arrays/slices and direct-`next` types accepted); explicit `.into_iter()` is the working form. 3155/3155. |
| B64 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — option_unwrap_or_default (1 file / 3 #[test] fns): `option.rs::test_unwrap_or_default` for i32/i64/bool payloads. Stdlib companion: new free fn `pub fn option_unwrap_or_default<T: Default>(opt: Option<T>) -> T` — lifted out of `impl<T> Option<T>` because `where T: Default` on an inherent impl method doesn't yet bind T to Default for `T::default()` dispatch (compiler gap, free-fn workaround documented inline). 3154/3154. |
| B63 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — SkipIter ExactSizeIterator (1 file / 2 #[test] fns): `skip.rs::{test_iterator_skip_len_basic, test_iterator_skip_len_over}`. Stdlib companion: `impl<I: ESI<T>, T> ExactSizeIterator<T> for SkipIter<I, T>` — len = max(0, inner.len() - remaining). 3153/3153. |
| B62 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — ZipIter DEI + ESI (1 file / 3 #[test] fns): `zip.rs::{test_double_ended_zip_balanced, test_double_ended_zip_left_longer, test_zip_len_smoke}`. Stdlib companion: `impl<A: DEI+ESI<T>, B: DEI+ESI<U>, T, U> DoubleEndedIterator<ZipPair<T,U>>` (drops `max-min` items from the longer side's back to align lengths, then pulls next_back from both) + `impl ExactSizeIterator<ZipPair<T,U>>` (returns min of inner lens). 3152/3152. |
| B61 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — EnumIter DEI (1 file / 1 #[test] fn): `enumerate.rs::test_double_ended_enumerate` — interleaved front/back consumption yields correct positions. Stdlib companion: `impl<I: DEI + ESI<T>, T> DoubleEndedIterator<EnumPair<T>> for EnumIter<I, T>` — position computed as `self.idx + inner.len()` after each back-pull (clean closed form, no `back_count` field). `impl<I: ESI<T>, T> ExactSizeIterator<EnumPair<T>> for EnumIter<I, T>` delegates len. 3151/3151. |
| B60 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — ExactSizeIterator infra + TakeIter DEI (1 file / 3 #[test] fns). Stdlib companion: new `pub trait ExactSizeIterator<Item>: Iterator<Item> { fn len(&self) -> i64 }` with impls on SliceIter / VecIter / TakeIter, and `impl<I: DEI + ESI<T>, T> DoubleEndedIterator<T> for TakeIter` — drops `inner.len() - cap` items off the back to align with the take cap, then pulls inner.next_back. Tests: `take.rs::test_double_ended_take`, `take.rs::test_iterator_take_len_smoke`, `iterator.rs::test_slice_iter_len`. 3150/3150. |
| B53 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — char.rs ASCII surface (1 file / 12 #[test] fns): `char.rs::{test_is_whitespace, test_is_lowercase, test_is_uppercase, test_is_alphabetic, test_is_alphanumeric, test_is_numeric, test_is_control, test_to_ascii_uppercase, test_to_ascii_lowercase, test_to_digit, test_len_utf8, test_is_ascii_hexdigit, test_is_ascii_punctuation, test_is_ascii_graphic}` — Unicode-property halves (`'ö'.is_lowercase()` etc.) dropped pending Unicode tables. `\u{XXXX}` char-escape isn't on Logos's grammar; codepoints constructed via `(0xNN as u32) as char`. `assert_eq!` over char cast both sides to u32 (no Display/Debug for char yet). Stdlib companion: new `std/lang/char/char.logos` with `impl char { is_ascii*, is_whitespace, is_alphabetic, ..., to_ascii_lower/upper, to_digit, len_utf8 }`. 3143/3143. |
| B52 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter/sources.rs + Peekable DEI (1 file / 6 #[test] fns): `iter/sources.rs::{test_once, test_once_dei, test_empty, test_repeat_take, test_repeat_take_collect}` (Logos stdlib gained `OnceIter` / `EmptyIter` / `RepeatIter` + free fns `iter_once` / `iter_empty` / `iter_repeat`; DEI on the bounded ones), `peekable.rs::test_peekable_dei_basic` (Peekable.next_back returns cached front-peek when inner is exhausted from the back, matching Rust's `Peekable<I: DoubleEndedIterator>` semantics). Stdlib companion: SkipIter DEI + PeekableIter DEI + OnceIter/EmptyIter/RepeatIter. 3142/3142. |
| B51 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — DEI on iter adapters (1 file / 5 #[test] fns): `map.rs::test_double_ended_map`, `filter.rs::test_double_ended_filter`, `chain.rs::test_double_ended_chain` + `test_iterator_chain_nth_back` (simulated via repeated next_back — `.nth_back` isn't a trait-default yet), `inspect.rs::test_double_ended_inspect`. Stdlib companion: `impl DoubleEndedIterator<…>` for MapIter / FilterIter / InspectIter / ChainIter added to `std/lang/iter/iter.logos`. ZipIter / TakeIter / EnumIter DEI deferred — those need `ExactSizeIterator` infra. |
| B50 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — multi-source mixed batch (1 file / 18 #[test] fns) covering option.rs::{replace, take, is_some_and}, result.rs::{is_ok_is_err, ok, err}, cmp.rs::{int_totalord_cmp, ordering_eq}, num/i32.rs::{neg_op, min_max_consts, arith_shifts, div_rem, bitwise_ops, comparison_ops}, macros.rs::{assert_with_msg}, tuple.rs::{partial_eq_pairs, partial_eq_quads}, bool.rs::then_some. `INT_MIN` literal computed as `-INT_MAX - 1` (Logos lexer rejects `-2147483648i32` since `2147483648` overflows i32 before negation). |
| B49 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter adapter terminal-method batch (1 file / 21 #[test] fns) consolidating B44–B48 + new terminals into one harness: `enumerate.rs::{count,nth}`, `chain.rs::{nth,find,any,all}`, `skip.rs::{nth,find}`, `peekable.rs::{nth,last,find}`, `filter.rs::nth`, `map.rs::count`, `take.rs::{count,find}`, `take_while.rs::count`, `skip_while.rs::count`, `step_by.rs::count`, `inspect.rs::count`, `zip.rs::{count,last}`. All routed through free-fn adapter constructors with named fn-ptr predicates; one `--test` synthesised main runs all 21 in ~4s. |
| B48 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter_peekable count (1 file / 1 #[test] fn): `peekable.rs::test_iterator_peekable_count`. .peekable().count() over 6-elem and 1-elem sources; empty-array case dropped (Logos warns). |
| B47 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — user-defined PartialEq (1 file / 1 #[test] fn): `cmp.rs::test_user_defined_eq`. Custom `eq` for `SketchyNum` based on abs-difference threshold; struct moved to top level (Logos can't nest struct decls in fn body the way Rust does). |
| B46 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter_chain terminals (1 file / 2 #[test] fns): `chain.rs::test_iterator_chain_count` (10) / `test_iterator_chain_last` (60). Empty-array `zs = []` halves dropped (Logos warns on empty array literal at let-binding). |
| B45 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter_skip count/last (1 file / 2 #[test] fns): `iter/adapters/skip.rs::test_iterator_skip_count` / `test_iterator_skip_last`. .skip(N).count() across N = 0/1/5/11/12/100; .skip(N).last() ditto. |
| B44 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter_take short (1 file / 1 #[test] fn): `iter/adapters/take.rs::test_iterator_take_short`. .take(N) on a shorter source yields exactly the source length. |
| B43 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — ordering.reverse (1 file / 1 #[test] fn): `cmp.rs::test_ordering_reverse`. `Less`/`Equal`/`Greater` → `Ordering::Less`/etc. (no bare-variant re-export in Logos's std.lang.ord). |
| B42 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — i32 arith ops (1 file / 1 #[test] fn): `num/i32.rs::test_arith_operation`. `isize` → `i32`. Bitwise+shift precedence differs from Rust (`<<` doesn't bind tighter than `&`/`\|` in Logos); explicit parens added to preserve upstream intent. |
| B41 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — assert macros trailing comma (1 file / 2 #[test] fns): `macros.rs::assert_eq_trailing_comma` / `assert_ne_trailing_comma`. matches!() / cfg_select! / r#"..."# raw-string variants of the upstream file deferred (not on the Logos surface yet). |
| B40 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter zip.nth (1 file / 1 #[test] fn): `iter/adapters/zip.rs::test_zip_nth`. Logos's `.nth(n)` takes self by value (consumes the iter); upstream's chained `it.nth(0); it.nth(1); it.nth(0)` rebuilds the iter for each call. Same pair sequence asserted. |
| B39 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — array iterator (1 file / 2 #[test] fns): `array.rs::iterator_collect` / `iterator_rev_collect`. Adapted from `IntoIterator::into_iter(arr.clone())` to `iter_over_slice(&arr)` since Logos doesn't blanket-Clone fixed arrays; `.collect::<Vec<i32>>()` routes through CP-cm-14. |
| B38 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter_sum (1 file / 1 #[test] fn): `iter/traits/accum.rs::test_iterator_sum`. Routed through typed free fn `iter_sum_i32` (no generic `Sum` trait surface yet); Result/Option fold variants deferred. |
| B37 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter_len port (1 file / 1 #[test] fn): `iter/traits/iterator.rs::test_iterator_len`. Slice-prefix `v[..N]` modeled via iter_take. |
| B36 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter trait min_by/max_by with abs-comparator (1 file / 2 #[test] fns): `iter/traits/iterator.rs::test_max_by` / `test_min_by`. Hand-written `cmp_abs(x, y) -> i32` since Logos's by-comparator forms expect i32 verdict (neg/zero/pos), not Ordering. |
| B35 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter trait predicates (1 file / 5 #[test] fns): `iter/traits/iterator.rs::test_all` / `test_any` / `test_find` / `test_position` / `test_count`. All use named fn-ptr predicates instead of upstream's closures (`\|&&x\| x < 10` → `fn lt10`). |
| B34 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter trait terminals (2 files / 4 #[test] fns): `iter/traits/iterator.rs::test_iterator_nth` / `test_iterator_last` (the trait-default forms on SliceIter — yield T by value, no `.cloned()` needed), `test_iterator_max` / `test_iterator_min` adapted to `.max_by(cmp)` / `.min_by(cmp)` with an explicit i32 comparator (Logos's stdlib provides the by-comparator forms; bare-Ord `.max()` / `.min()` deferred). |
| B107 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | tests/ui run-pass — **49 tests** across mir (10), traits (9), enum (6), for-loop-while (4), issues (3), functions-closures (3), binding (3), structs (2), drop (2), closures (2), typeck/moves/generics/fn/binop (1 each). Mechanical run-pass ports: `pub fn main()` → `fn main()->i32{…;return 0;}`, isize/usize→i64/u64, int literals suffixed, `extern "C"` ABI markers dropped, String comparisons via `String::eq_str`, `Vec::new()`→`vec_new::<T>()`, `Box`/`vec!`/`println!`/`size_of`/`Default`/derives dropped where incidental. Confirmed-working features exercised: impl-on-primitive (`impl U for i64`) + trait-default-method on primitive; supertrait inheritance (simple/diamond/multiple-params/self-param); blanket impls `impl<T:Bound> Trait for T` (conditional-dispatch, inheritance-auto); struct-like enum variants (construct/match); empty enum payload; FRU; closures-in-generic-struct; FnOnce-bounded helpers; labeled-loop break; custom-discriminant C-like enum matched directly. **NEW gaps surfaced** (see notes/B107-ui-surfaced-gaps.md): struct pattern with refutable field sub-pattern (`Foo{f:0}`) unsupported; string-literal match arms (`"foo"=>`) syntax error; `(pat)` parenthesized pattern syntax error; tuple/ref patterns in `let` (`let (&x,&y)=…`) unsupported; `\u{}` char escape with `_` rejected; tuple-struct name as ctor value (`let f=A; f(true)`) unknown; field-projection off a call result (`f().1`) codegen GEP-on-struct error (works via local bind); uninitialized-local conditional-assign + `loop{}` → `arith.constant` LLVM translate fail; multi-arm guarded+unguarded `Some` over `Option<i8>` MLIR fail; tuple relational `<`/`<=`/`>`/`>=` compares the tuple pointer (cmpi-on-ptr); `e @ 1..=100` at-binding over inclusive range SEGFAULTS logosc; for-loop over `&array`-of-structs runtime segfault; nested/loop-driven FnMut closures capturing mut locals abort at runtime; `static` item inside fn body unsupported; UFCS `Trait::<T>::method(recv)` static-call form; supertrait-item shadowing reports ambiguity. Shorthand struct field patterns (`{ref mut x}`) rejected — use `{x: ref mut x}`. |
| B106 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-21 | Victor Smirnov | tests/ui run-pass — **49 tests** across binding (13), array-slice-vec (8), match (7), numbers-arithmetic (5), enum (4), for-loop-while (3), structs (2), generics (2), cast/inference/recursion/str/tuple (1 each). Mechanical run-pass ports: `pub fn main()` → `fn main()->i32{…;return 0;}`, isize/usize→i64/u64, all int literals suffixed, MIN as `-MAX-1` (no assoc-consts on primitives), `i64`/`f64` substituted for `i128`/`f16`/`f128`. `vec![]`/`println!`/`size_of`/`derive`/`Box`/`String` sub-tests dropped where they were incidental to the test's point. Skips recorded for tests hitting 5 surfaced compiler gaps (see notes/B106-ui-surfaced-gaps.md): str relational `<`/`>` codegen (pointer cmpi), tuple or-pattern cross-rebind MLIR fail, multi-arm `Some(_) if g` over `Option<i64>` abort, custom-discriminant C-like enum matched in a tuple abort, plus unsupported syntax (unit `struct S;`, 1-tuple `(y,)`, patterns in fn params, leading `\|` arm, open-ended `0..` pattern). `cast/cast.rs` imported as `ui-cast.logos` (ctest names are global — avoids clash with the existing `cast` suite test). |
| B105 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-18 | Victor Smirnov | coretests — `mem.rs::assume_init_good` (1 file / 1 #[test] fn). Drove introduction of `logos.mem.uninit::MaybeUninit<T>` (mem-tier; lang-tier can't depend on the allocator). Surface: `new`/`uninit`/`zeroed`/`write`/`as_ptr`/`as_mut_ptr`/`assume_init`/`assume_init_read`/`assume_init_ref`/`assume_init_mut`. Storage is `value: T` (not Rust's `union { uninit, T }`); `uninit()` zero-fills via alloc+memset workaround — contract identical. Drop-T caveat documented in module header. Slice/array surface (`transpose`/`as_bytes`/`write_copy_of_slice`/etc.) deferred. Imported test deferred `uninit_array_assume_init` upstream sibling because it needs `[MaybeUninit; N].transpose()` (separate addition). |
| B104 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-18 | Victor Smirnov | coretests — `num/int_macros.rs` + `num/uint_macros.rs` battery, expanded by hand for i32/i64/u32/u64 (1 file / **52 #[test] fns**). Per-type coverage: `test_overflows`, `test_bitwise_operators`, `test_count_ones`, `test_count_zeros`, `test_rotate`, `test_swap_bytes`, `test_to_from_le_be`, `test_pow`, `test_checked_div`, `test_wrapping_ops`. Signed types add: `test_abs`, `test_signum`, `test_is_positive`, `test_is_negative`, `test_saturating_abs`, `test_saturating_neg`. **Macro expansion in lieu of macro_rules!** — upstream uses `int_module!(i32, u32);` which expands per type; with MC-mc-01 (macro_rules!) still Open the file enumerates each type-method combo by hand and suffixes the fn name with `_<type>` for failure-report clarity. **Constants**: `$T::MAX` / `$T::MIN` literal-replaced because Logos has no associated-const surface on primitives yet; MIN computed as `-MAX - 1` because `-2147483648i32` parses as `-(2147483648i32)` and the inner token overflows. **`.bitor()` / `.bitand()` / `.shl()` etc. trait-method form** dropped in favour of operator form — the operator path is what drivers use; trait-method form is Rust-specific test surface. **`test_overflows_uXX`**: upstream `assert_eq!(MAX + 1 + 0, MAX + 1)` rewritten as `assert_eq!(MAX - 1 + 1, MAX)` because Logos's unsigned-add has a runtime overflow trap (use `wrapping_add` for wrap semantics; the original test relies on `as $T` cast to silence the overflow). |
| B103 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-18 | Victor Smirnov | coretests — `num/i32.rs::test_arith_operation` (1 file / 1 #[test] fn). Pure arithmetic over `isize`. Modifications: every integer literal carries its `isize` suffix (Logos's let-with-annotation doesn't infer literal type from the binding annotation); `&` / `|` over a `<<` operand parenthesised explicitly because Logos's bitwise precedence (`&` / `|` bind tighter than `<<`) differs from Rust's (Rust binds `<<` tighter). The bit-shifted constant `(2 + i32_a as usize)` is annotated as `((2isize + i32_a as isize) as isize)` to keep all operands `isize` (no implicit usize↔isize coercion). |
| B102 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-18 | Victor Smirnov | coretests — `char.rs` subset (1 file / 4 #[test] fns): `test_is_whitespace` / `test_is_control` / `test_is_numeric` / `test_to_digit`. ASCII-only assertions kept faithful; upstream Unicode-only cases (`'\u{92}'` C1 control in is_control, `'¾'` U+00BE in is_numeric) dropped — Logos's char predicates for `is_control` / `is_numeric` are ASCII-only. Non-ASCII codepoints (U+2007 figure space in is_whitespace + control-char codepoints in test_is_control) constructed via `(0xNNu32 as char)` since Logos's char-literal lexer only accepts single-char `\X` escapes (no `\u{HEX}` / `\xNN`). `assert_eq!` on `Option<u32>` replaced with match-with-inner-assert since Option<u32> doesn't impl Eq yet. `assert!(!c.is_X())` → `assert!(!(c.is_X()))` because `!` directly before a char-literal in assert! macro args trips the arg parser. |
| B101 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-18 | Victor Smirnov | coretests — `mem.rs` subset (1 file / 6 #[test] fns): `size_of_basic` / `size_of_64` / `align_of_basic` / `align_of_64` / `test_swap` / `test_replace`. Drove introduction of safe `swap_ref` / `replace_ref` / `take_ref` in `logos.lang.mem` (suffixed to coexist with the existing unsafe pointer-based forms). Compiler fix coupled to the slice: `align_of::<usize>()` / `align_of::<isize>()` were returning 4 (mlir-gen primitive-align table missed Usize/Isize → fell through to MLIR DataLayout's default for `i64`); added to the 8-byte entry. Pointer-width-conditional tests scoped to 64-bit (no `target_pointer_width` cfg query yet). `test_replace` substitutes a struct payload for upstream's `Option<String>` because `replace_ref::<Option<T>>` triggers a separate mono cascade ([[baghunt-replace-ref-option-cascade]]). |
| B100 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-18 | Victor Smirnov | coretests — `cell.rs::smoketest_unsafe_cell` + `cell.rs::unsafe_cell_raw_get` (1 file / 2 #[test] fns). Drove introduction of `std.lang.cell` stdlib module wrapping `UnsafeCell<T>` (`#[repr(transparent)]` + `new` / `into_inner` / `get(&self) -> *mut T` / `get_mut(&mut self) -> &mut T` / `raw_get(*const Self) -> *mut T`). No compiler change needed: Logos's existing `(&val) as *const T as *mut T` cast under `unsafe` already permits the heart of UnsafeCell. NLL note: smoketest scopes `&mut x` in an explicit block so the borrow ends before `x.into_inner()` (Rust's NLL infers the same drop point automatically). Imported test: `tests/imported/pass/cell/test_harness_coretest_unsafe_cell.logos`. |
| B33 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — small ports (2 files / 2 #[test] fns): `tuple.rs::test_partial_eq` (validates SL-sl-08 / CP-cm-08b tuple Eq blanket on primitive fields), `clone.rs::test_borrowed_clone` (primitive Clone via `(*y).clone()` — Logos doesn't blanket-impl Clone for `&T`, so the upstream `(&y).clone()` form is adapted to a deref). |
| B32 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — `result.rs` deferred combinators (1 file / 4 #[test] fns): `test_and` / `test_and_then` / `test_or` / `test_or_else`. Closes the B25 deferred bits unlocked by CP-cm-12 (method-generic on generic enum) + CP-cm-09 (multi-param generic enum mono). Closures `\|i\| Ok(i+1)` lowered to named fn refs (closure-as-fn-ptr partial); `Err::<T, E>(...)` turbofish-in-arg simplified to typed helper fns. |
| B31 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — `option.rs` deferred combinators (1 file / 4 #[test] fns): `test_or` / `test_and_then` / `test_or_else` / `test_unwrap_or_else`. Closes the B24 deferred bits unlocked by CP-cm-12 (method-generic on generic enum) + CP-cm-13 (None-receiver T-inference). Faithful upstream shape using bare `None` with `let`-pinned T; the explicit `Option::<i32>::None` literal in arg position triggers a separate mono bug that clones Option methods with `<error>` type-param (sibling of CP-cm-12 surfaced 2026-05-15, deferred). |
| B30 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter adapters third batch (4 files / 5 #[test] fns): `skip.rs::test_iterator_skip` (front-only), `take_while.rs::test_iterator_take_while`, `skip_while.rs::test_iterator_skip_while`, `step_by.rs::test_iterator_step_by_identity` + `test_iterator_step_by_3`. All use the free-fn adapter forms with explicit fn-ptr predicates (no closure→fn-ptr coercion at trait-default args). DEI halves deferred (no DEI on Skip/TakeWhile/SkipWhile/StepBy yet). |
| B29 | `4b0c9d76ae7d387229caea55cfa73c280b08b8a7` | 2026-05-15 | Victor Smirnov | coretests — iter adapters second batch (6 files / 6 #[test] fns): `filter.rs::test_iterator_filter_count` + `test_iterator_filter_fold`, `take.rs::test_iterator_take` (front-only), `enumerate.rs::test_iterator_enumerate`, `chain.rs::test_iterator_chain`, `zip.rs::test_iterator_zip` (basic), `map.rs::test_iterator_map`. Free-fn adapter forms (`iter_filter` / `iter_take` / `iter_enumerate` / `iter_chain` / `iter_zip` / `iter_map`) used as the canonical entry point — Logos closures don't auto-coerce to fn-ptrs at trait-default-method args without explicit signatures; the free-fn form sidesteps that. DoubleEnded follow-ups (`next_back` on MapIter / FilterIter / TakeIter) deferred — those adapters don't currently impl DEI. `try_fold` / `try_rfold` family deferred — no `Try` trait surface yet. The B28 cycle test was simultaneously rewritten to use `#[derive_clone]` after the under-`--test` metaprog dispatch fix landed in 3710cae0. |
