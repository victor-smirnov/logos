// rustc regions/regions-addr-of-upvar-self.rs — upstream lifetime may not live long enough / E0597
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root lifereg.NEW-R20
package regions_addr_of_upvar_self_upvar_self;
struct Dog { food: u64 }
impl Dog {
  fn chase_cat(self: &mut Dog) {
    let f = || { let p: &'static mut u64 = &mut self.food; *p = 3u64; };
  }
}
fn main() -> i32 { return 0i32; }
