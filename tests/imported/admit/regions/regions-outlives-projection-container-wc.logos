// rustc regions/regions-outlives-projection-container-wc.rs — upstream lifetime may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root lifereg.NEW-L3
// port of rust ui/regions/regions-outlives-projection-container-wc.rs
package regions_outlives_projection_container_wc;

trait TheTrait { type TheAssocType; }

struct TheType<'b> { m: [fn(&'b ()); 0] }

impl<'b> TheTrait for TheType<'b> { type TheAssocType = &'b (); }

struct WithAssoc<T> where T: TheTrait { m: [T; 0] }

fn with_assoc<'a, 'b>(x: &'a WithAssoc<TheType<'b>>) { }

fn main() -> i32 { return 0i32; }
