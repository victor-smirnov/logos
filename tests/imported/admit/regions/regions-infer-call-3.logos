// rustc regions/regions-infer-call-3.rs — upstream lifetime may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root lifereg.C
package regions_infer_call_3;
fn select<'r>(x: &'r i64, y: &'r i64) -> &'r i64 { return x; }
fn with<'q>(f: &dyn Fn(&'q i64) -> &'q i64) -> i64 { let t: i64 = 20i64; return *f(&t); }
fn manip<'a>(x: &'a i64) -> i64 {
  let c = |y: &i64| { return select(x, y); };
  return with(&c);
}
fn main() -> i32 { let v: i64 = 1i64; if manip(&v) != 1i64 { return 1i32; } return 0i32; }
