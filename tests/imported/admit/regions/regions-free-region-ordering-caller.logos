// rustc regions/regions-free-region-ordering-caller.rs — upstream lifetime may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root lifereg.NEW-L3
// port of rust ui/regions/regions-free-region-ordering-caller.rs
package regions_free_region_ordering_caller;

fn call2<'a, 'b>(a: &'a u64, b: &'b u64) {
    let z: Option<&'b &'a u64> = Option::None;
}

fn call4<'a, 'b>(a: &'a u64, b: &'b u64) {
    let z: Option<&'a &'b u64> = Option::None;
}

fn main() -> i32 { return 0i32; }
