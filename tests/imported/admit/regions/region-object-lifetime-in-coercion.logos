// rustc regions/region-object-lifetime-in-coercion.rs — upstream lifetime may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root lifereg.A
// port of rust ui/regions/region-object-lifetime-in-coercion.rs
package region_object_lifetime_in_coercion;

trait Foo { fn f(self: &Self) -> i64; }
struct W<'a> { s: &'a [u8] }
impl<'a> Foo for W<'a> { fn f(self: &Self) -> i64 { return 1i64; } }

fn b<'v>(v: &'v [u8]) -> Box<dyn Foo + 'static> {
    return Box::new(W { s: v });
}

fn main() -> i32 { return 0i32; }
