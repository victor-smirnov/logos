// rustc regions/regions-in-enums-anon.rs — upstream E0106 missing lifetime specifier
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root lifereg.R17
// ── STAYS BLOCKED, OWNER-CORPUS — THE PORT IS FAITHFUL AND THE WALL IS REAL. 2026-09-07.
// Original path: tests/ui/regions/regions-in-enums-anon.rs
// upstream commit: 4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// upstream construct: line 4 — `Bar(&isize)` //~ ERROR missing lifetime specifier (E0106)
// Modifications: isize -> i64; assert -> exit code (`fn main() -> i32`). NOTHING ELSE —
//   the construct (an elided `&` in an enum PAYLOAD declaration) is verbatim, so this
//   is a re-port as-is that reaches the same shelf it was already on.
// ⚠ THIS IS NOT A BLESSED DIVERGENCE AND MUST NOT BE FILED AS ONE: no clause in
//   docs/DIVERGENCES.md or docs/spec/ canonises an elided `&` in a declaration —
//   searched 2026-09-07 (`item.struct.*`, ownership.md's elision block, types.md's
//   trait-object clause). What blocks it is a CORPUS decision with an owner.
// ⚠ THE WALL, RE-MEASURED BY DIRECT LISTING 2026-09-07 (it was recorded as "134 legal
//   declaration sites in 101 files"; it has GROWN): 6784 pass/stdlib sources scanned,
//   195 bare-`&` field/payload declaration sites in 124 files — 174 struct fields,
//   21 enum payloads. And the exemption is PINNED, not incidental:
//   tests/logos/pass/bc_enumpldlt_placeholder_payload_lifetime `enum E1 { V(&i64) }`
//   asserts that exactly this compiles, with a header saying so.
package regions_in_enums_anon;
enum Foo { Bar(&i64) }
fn main() -> i32 { return 0i32; }
