// rustc nll/issue-27282-move-ref-mut-into-guard.rs — upstream E0507 cannot move out of `foo` in pattern guard
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root nllmoves.B
// t14 reduced: a `ref mut` pattern binding deposits no loan on the matched place
package issue_27282_move_ref_mut_into_guard;
fn main() -> i32 {
    let mut v: Option<i64> = Option::Some(4i64);
    match v {
        Option::None => { return 0i32; }
        ref mut foo => { let n: Option<i64> = v; return 1i32; }
    }
}
