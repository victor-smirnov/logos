// rustc nll/user-annotations/closure-substs.rs — upstream (region)
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root nllmoves.C
package closure_substs;
// nll/user-annotations/closure-substs.rs — a closure signature annotated `-> &'static i64`
// returning a borrow with a shorter region.
fn foo() { let _c = |x: &i64| -> &'static i64 { return x; }; }
fn main() -> i32 { foo(); return 0i32; }
