// rustc nll/issue-51191.rs — upstream E0596 cannot borrow `self` as mutable
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root nllmoves.A
// control for t17: the same &mut of a non-mut by-value binding, but an ordinary
// parameter instead of `self`.
package issue_51191;
struct Struct { n: i64 }
impl Struct { fn bar(self: &mut Struct) { } }
fn imm(me: Struct) { let r: &mut Struct = &mut me; r.bar(); }
fn main() -> i32 { return 0i32; }
