// rustc lifetimes/lifetime-errors/ex3-both-anon-regions-latebound-regions.rs — upstream lifetime may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root lifereg.A
package ex3_both_anon_regions_latebound_regions;
fn foo<'a, 'b>(x: &mut Vec<&'a i64>, y: &'b i64) {
  x.push(y);
}
fn main() -> i32 {
  let n = 5i64;
  let mut v: Vec<&i64> = Vec::new();
  foo(&mut v, &n);
  return 0i32;
}
