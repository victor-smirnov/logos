// rustc borrowck/two-phase-nonrecv-autoref.rs — upstream E0499 / E0502 ×3
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root bck.D
// upstream: borrowck/two-phase-nonrecv-autoref.rs twice_ten_sm (E0499 cannot borrow `*f`
// as mutable more than once at a time)
// RE-PORTED AS-IS 2026-09-07 (stage 5) — verified against the upstream worksheet:
// the construct was already carried verbatim; only the audit trail was missing.
// Original path: tests/ui/borrowck/two-phase-nonrecv-autoref.rs
// upstream commit: 4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// upstream construct: lines 50-53 —
//     fn twice_ten_sm<F: FnMut(i32) -> i32>(f: &mut F) {
//         f(f(10));
//         //~^ ERROR cannot borrow `*f` as mutable more than once at a time
//     }
// Modifications:
//   · i32 -> i64
//   · the body `f(f(10));` is a statement upstream and a `return` here (Logos has no
//     tail expressions in this position); the nested call is unchanged
//   · the caller passes a named `let mut c` instead of an inline `&mut |x| x + 1`
//   · assert -> exit code: `fn main() -> i32`
package two_phase_nonrecv_autoref_a_fnmut_twice;
fn twice_ten_sm<F: FnMut(i64) -> i64>(f: &mut F) -> i64 {
    return f(f(10i64));
}
fn main() -> i32 { let mut c = |x: i64| { return x + 1i64; }; let _ = twice_ten_sm(&mut c); return 0i32; }
