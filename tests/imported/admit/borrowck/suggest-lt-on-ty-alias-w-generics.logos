// rustc borrowck/suggest-lt-on-ty-alias-w-generics.rs — upstream (lifetime may not live long enough)
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root bck.A
// upstream: suggest-lt-on-ty-alias-w-generics.rs — closure may outlive the fn
package suggest_lt_on_ty_alias_w_generics;
struct Boxed { f: i64 }
fn test(x: &i64) -> Box<dyn Fn() -> i64> {
    return Box::new(|| { return *x; });
}
fn main() -> i32 { let n = 1i64; let b = test(&n); return b() as i32; }
