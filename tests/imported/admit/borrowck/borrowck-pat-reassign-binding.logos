// rustc borrowck/borrowck-pat-reassign-binding.rs — upstream E0506
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root bck.B
package borrowck_pat_reassign_binding;
fn main() -> i32 {
    let mut x: Option<i64> = Option::None;
    match x {
        Option::None => { x = Option::Some(0i64); }
        Option::Some(ref i) => {
            x = Option::Some(*i + 1i64);
            let _ = *i;
        }
    }
    return 0i32;
}
