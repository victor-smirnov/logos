// rustc borrowck/anonymous-region-in-apit.rs — upstream escapes-closure
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root bck.C
// CONTROL for t01: differs in ONE thing — the closure parameter's borrow leaves by
// RETURN instead of by store into the `&mut Sink` out-parameter. Refused.
package anonymous_region_in_apit_ctl_return_channel;
fn qux() -> &i64 {
    let c = |baz: &i64| -> &i64 { return baz; };
    let local: i64 = 5i64;
    return c(&local);
}
fn main() -> i32 { let p = qux(); let _ = *p; return 0i32; }
