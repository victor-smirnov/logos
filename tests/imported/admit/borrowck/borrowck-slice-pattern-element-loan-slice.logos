// rustc borrowck/borrowck-slice-pattern-element-loan-slice.rs — upstream E0502
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root bck.B
package borrowck_slice_pattern_element_loan_slice;
fn nop(a: &i64, b: &mut i64) {}
fn const_index_err(s: &mut [i64]) {
    if let [ref first, ref second, ..] = *s {
        if let [_, ref mut second2, ..] = *s {
            nop(first, second2);
        }
    }
}
fn main() -> i32 { let mut v: [i64; 4] = [1i64,2i64,3i64,4i64]; const_index_err(&mut v); return 0i32; }
