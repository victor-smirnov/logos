// rustc lifetimes/static-typos.rs — upstream use of undeclared lifetime name `'stati`
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail · was lifereg.R17, CLOSED 2026-09-01 — the reader every
// declaration uses dropped `T: 'a` on the floor, so the undeclared-lifetime
// check at sema_decl.cpp had an empty list to walk and had never printed.
package static_typos;
fn stati<T: 'stati>(x: T) -> i32 { return 0i32; }
fn statoc<T: 'statoc>(x: T) -> i32 { return 0i32; }
fn main() -> i32 { return 0i32; }
