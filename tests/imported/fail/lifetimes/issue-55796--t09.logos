// rustc lifetimes/issue-55796.rs — upstream lifetime may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02y: the OBJECT-LIFETIME BOUND is checked at the unsize coercion (`Src: 'r` for `dyn Tr + 'r`, 'static for an unbounded owned object). Was lifereg.N1.
package issue_55796_t09;
trait It { fn get(self: &Self) -> i64; }
struct Holder<'a> { r: &'a i64 }
impl<'a> It for Holder<'a> { fn get(self: &Self) -> i64 { return *self.r; } }
struct G { v: i64 }
impl G {
  fn out_neighbors<'a>(self: &'a Self) -> Box<dyn It> { return Box::new(Holder { r: &self.v }); }
}
fn main() -> i32 { return 0i32; }
