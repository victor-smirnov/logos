// rustc lifetimes/lifetime-errors/ex2c-push-inference-variable.rs — upstream lifetime may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-09: the struct literal's lifetime args are
// read off its VALUES and a binder the values did not spell stays EMPTY-BUT-PRESENT;
// an ELIDED `let` annotation region takes the initializer's. Was lifereg.NEW-R19.
package ex2c_push_inference_variable;
struct Ref<'a> { data: &'a i64 }
fn foo<'a, 'b, 'c>(x: &'a mut Vec<Ref<'b>>, y: Ref<'c>) {
    let z: Ref = Ref { data: y.data };
    x.push(z);
}
fn main() -> i32 { return 0i32; }
