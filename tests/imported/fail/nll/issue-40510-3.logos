// rustc nll/issue-40510-3.rs — upstream E0521
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root nllmoves.NEW-CESC — RE-PORTED AS-IS 2026-09-07 (stage 4)
// ── BUCKET 2: re-ported as-is, still admitted, still a defect. The old port bound both
//    closures to `let`s and CALLED them (`let e = c(); let f = c(); e(); f();`), adding
//    uses upstream does not have; with upstream's two bare statement-expression closures
//    and nothing called at all, it is still admitted.
// ── ROOT RE-CHECKED: same as issue-40510-1 — an escaping closure region is constrained
//    only by a `dyn` coercion (pair measured 2026-09-07: boxed -> REFUSED, plain generic
//    -> ADMITS), and this port passes through neither.
// Original path: tests/ui/nll/issue-40510-3.rs
// upstream commit: 4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// upstream construct: lines 4-11 —
//     let mut x: Vec<()> = Vec::new();
//     || {
//         || { x.push(()) }
//         //~^^^ ERROR captured variable cannot escape `FnMut` closure body
//     };
// Modifications:
//   · `Vec<()>` + `x.push(())` -> `i64` + `x = x + 1`. Logos has no unit type; the
//     subject is the INNER closure that mutates the OUTER closure's capture and then
//     escapes the outer body, which is unchanged.
//   · assert -> exit code: `fn main() -> i32`
package issue_40510_3;
fn f() {
    let mut x: i64 = 0i64;
    || { return || { x = x + 1i64; }; };
}
fn main() -> i32 { return 0i32; }
