// B78: `&mut x` rejected on non-mut binding even inside conditional.
package mut_binding_missing_in_cond;

fn main() -> i32 {
    let x: i32 = 0i32;
    if x == 0i32 {
        let r: &mut i32 = &mut x;
        *r = 1i32;
    }
    return 0i32;
}
