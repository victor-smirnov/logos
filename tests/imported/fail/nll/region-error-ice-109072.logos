// rustc nll/user-annotations/region-error-ice-109072.rs — upstream E0261 undeclared lifetime
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: FAIL (was admit) — closed 2026-08-28: `'missing` is written in a
// PARAMETER type, and until today only outlives clauses and type-param bounds
// were checked for undeclared names. Upstream E0261, same reason.
// port of nll/user-annotations/region-error-ice-109072.rs: undeclared lifetime name
package region_error_ice_109072;
struct A { n: i64 }
fn f(x: &'missing A) -> i64 { return x.n; }
fn main() -> i32 { let a: A = A { n: 1i64 }; return f(&a) as i32; }
