error [fn uninit]: let annotation: type `&'static &'a i64` is not well-formed — the reference under `'static` requires `'a: 'static` and no such bound is declared (reference has a longer lifetime than the data it references)
