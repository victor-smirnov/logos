// rustc nll/user-annotations/adt-tuple-enums.rs — upstream E0597
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02: a local's borrow into a `&'static` let through an enum payload; an EMPTY region is not 'static. Was nllmoves.NEW-L1.
package adt_tuple_enums_ctl;
fn f() -> i32 {
  let c: i64 = 66i64;
  let z: &'static i64 = &c;
  return 0i32;
}
fn main() -> i32 { return f(); }
