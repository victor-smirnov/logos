// B83: field mut blocks whole-value access.
package df_field_then_whole;
struct P { a: i32, b: i32 }
fn main() -> i32 {
    let mut p: P = P { a: 0i32, b: 0i32 };
    let f: &mut i32 = &mut p.a;
    let r: &mut P = &mut p;
    *f = 1i32; (*r).a = 2i32;
    return 0i32;
}
