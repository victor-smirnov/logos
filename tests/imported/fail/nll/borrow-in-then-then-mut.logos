// Borrow in then-arm extends beyond if; mut after rejected.
package borrow_in_then_then_mut;
fn main() -> i32 {
    let mut x: i32 = 0i32;
    let cond: bool = true;
    let r: &i32 = if cond { &x } else { &x };
    x = 5i32;
    return *r;
}
