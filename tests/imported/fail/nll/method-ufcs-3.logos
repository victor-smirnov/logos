// rustc nll/user-annotations/method-ufcs-3.rs — upstream borrow of local under `::method::<&'static u32>`
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02: a local's borrow into a `&'static` method argument; an EMPTY region is not 'static. Was nllmoves.NEW-N2.
// port of nll/user-annotations/method-ufcs-3.rs annot_reference_static_lifetime
package method_ufcs_3;
trait Bazoom<T> { fn method<U>(self: &Self, arg: T, arg2: U) {} }
struct A { n: i64 }
impl Bazoom<i64> for A { }
fn f() {
    let a: A = A { n: 22i64 };
    let c: u32 = 66u32;
    a.method::<&'static u32>(44i64, &c);   // ERROR
}
fn main() -> i32 { f(); return 0i32; }
