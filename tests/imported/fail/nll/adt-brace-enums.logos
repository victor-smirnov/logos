// rustc nll/user-annotations/adt-brace-enums.rs — upstream E0597
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02: a local's borrow into an enum literal's `&'static` payload; an EMPTY region is not 'static. Was nllmoves.B.
package adt_brace_enums;
// nll/user-annotations/adt-brace-enums.rs — an explicit `&'static` type argument on an
// enum-variant literal must reject a borrow of a local.
enum SomeEnum<T> { SomeVariant { t: T } }
fn annot_reference_static_lifetime() {
    let c: u32 = 66u32;
    let _e: SomeEnum<&'static u32> = SomeEnum::SomeVariant { t: &c };
}
fn main() -> i32 { annot_reference_static_lifetime(); return 0i32; }
