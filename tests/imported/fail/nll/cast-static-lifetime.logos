// rustc nll/user-annotations/cast_static_lifetime.rs — upstream E0597 `x` does not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02: a local's borrow into a `&'static` let; an EMPTY region is not 'static. Was nllmoves.R17.
// port of rust tests/ui/nll/user-annotations/cast_static_lifetime.rs
// rustc: E0597 `x` does not live long enough  (the `as &'static u32` cast
// forces the borrow of the local `x` to be 'static).
package cast_static_lifetime;
fn main() -> i32 {
    let x: u32 = 22u32;
    let y: &'static u32 = &x;
    return *y as i32;
}
