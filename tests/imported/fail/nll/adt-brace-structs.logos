// rustc nll/user-annotations/adt-brace-structs.rs — upstream E0597/E0716 (borrow of local annotated `&'static`)
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02: a local's borrow into a struct literal's `&'static` field; an EMPTY region is not 'static. Was nllmoves.NEW-N2.
// port of nll/user-annotations/adt-brace-structs.rs annot_reference_static_lifetime
package adt_brace_structs;
struct SomeStruct<T> { t: T }
fn f() {
    let c: u32 = 66u32;
    let _s: SomeStruct<&'static u32> = SomeStruct::<&'static u32> { t: &c };  // ERROR
}
fn main() -> i32 { f(); return 0i32; }
