// Shared borrow used in cond, mut borrow tries to overlap.
package nll_mut_borrow_during_shared_in_cond;
fn main() -> i32 {
    let mut x: i32 = 0i32;
    let r: &i32 = &x;
    if *r > 0i32 {
        let s: &mut i32 = &mut x;
        *s = 1i32;
        let _a = *r;
    }
    return 0i32;
}
