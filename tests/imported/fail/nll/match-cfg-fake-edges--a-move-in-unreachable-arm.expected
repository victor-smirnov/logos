# Logos refuses an arm after an unguarded catch-all (spec expr.match.arm-after-catchall-unreachable);
# rustc only warns and reports the fake-edge move (E0382).
unreachable match arm: a previous '_' arm matches all values
