// rustc nll/user-annotations/fns.rs — upstream borrow of local under `some_fn::<&'static u32>`
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02: a local's borrow into a `&'static` call argument; an EMPTY region is not 'static. Was nllmoves.NEW-N2.
// port of nll/user-annotations/fns.rs annot_reference_static_lifetime
package fns;
fn some_fn<T>(_arg: T) {}
fn f() {
    let c: u32 = 66u32;
    some_fn::<&'static u32>(&c);   // ERROR
}
fn main() -> i32 { f(); return 0i32; }
