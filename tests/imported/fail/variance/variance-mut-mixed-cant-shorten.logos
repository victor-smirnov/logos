// B84: Inv via *mut wins even when *const co-exists in same struct.
package variance_mut_mixed_cant_shorten;
struct M<T> { c: *const T, m: *mut T }
fn bad<'short, 'long>(v: M<&'long i32>) -> M<&'short i32>
    where 'long: 'short
{ return v; }
fn main() -> i32 { return 0i32; }
