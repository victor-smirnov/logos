// B84: `*const T` Co — cannot lengthen.
package variance_const_ptr_cant_lengthen;
struct W<T> { p: *const T }
fn bad<'short, 'long>(v: W<&'short i32>) -> W<&'long i32>
    where 'long: 'short
{ return v; }
fn main() -> i32 { return 0i32; }
