// B76: at least one Inv field forces struct Inv; shorten rejected.
package variance_three_field_inv;

struct Three<T> { a: T, b: *mut T, c: T }

fn bad<'short, 'long>(v: Three<&'long i32>) -> Three<&'short i32>
    where 'long: 'short
{ return v; }

fn main() -> i32 { return 0i32; }
