// B76: enum carrying &'a T → Co. Cannot lengthen.
package variance_co_enum_cant_lengthen;

enum Opt<T> { Some(T), None }

fn bad<'short, 'long>(v: Opt<&'short i32>) -> Opt<&'long i32>
    where 'long: 'short
{ return v; }

fn main() -> i32 { return 0i32; }
