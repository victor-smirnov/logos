// B76: struct holding both Co (T) and Contra (fn(T)) — meet = Inv.
package variance_co_plus_contra_is_inv;

struct Both<T> { v: T, f: fn(T) -> i32 }

fn bad<'short, 'long>(v: Both<&'long i32>) -> Both<&'short i32>
    where 'long: 'short
{ return v; }

fn main() -> i32 { return 0i32; }
