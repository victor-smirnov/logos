// B76: Cell<Cell<&'a T>> — Inv chain; no relaxation.
package variance_cell_of_cell;

struct Cell<T> { p: *mut T }

fn bad<'short, 'long>(v: Cell<Cell<&'long i32>>) -> Cell<Cell<&'short i32>>
    where 'long: 'short
{ return v; }

fn main() -> i32 { return 0i32; }
