// rustc moves/moves-based-on-type-tuple.rs — upstream E0382
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: pin
package moves_based_on_type_tuple;
struct D { x: i64 }
impl Drop for D { fn drop(self: &mut Self) { } }
fn dup(x: D) -> (D, D) { return (x, x); }
fn main() -> i32 {
  let p: (D, D) = dup(D { x: 3i64 });
  return 0i32;
}
