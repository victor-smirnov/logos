// rustc regions/regions-addr-of-upvar-self.rs — upstream lifetime may not live long enough / E0597
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02: a local's `&mut` into a `&'static mut` let; an EMPTY region is not 'static. Was lifereg.NEW-R20.
package regions_addr_of_upvar_self_d_static_local;
fn main() -> i32 {
    let mut x: u64 = 1u64;
    let p: &'static mut u64 = &mut x;
    *p = 3u64;
    return 0i32;
}
