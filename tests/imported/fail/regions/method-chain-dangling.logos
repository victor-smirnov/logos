// Method returning ref to local — dangling.
package method_chain_dangling;

struct C { n: i32 }
impl C {
    fn make_ref(self: &C) -> &i32 {
        let v: i32 = 5i32;
        return &v;
    }
}

fn main() -> i32 { return 0i32; }
