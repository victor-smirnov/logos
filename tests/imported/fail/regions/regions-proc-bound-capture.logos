// rustc regions/regions-proc-bound-capture.rs — upstream lifetime may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02y: the OBJECT-LIFETIME BOUND is checked at the unsize coercion (`Src: 'r` for `dyn Tr + 'r`, 'static for an unbounded owned object). Was lifereg.N1.
package regions_proc_bound_capture;
fn borrowed_proc<'a>(x: &'a i64) -> Box<dyn FnMut() -> i64 + 'a> { return Box::new(move || { return *x; }); }
fn static_proc(x: &i64) -> Box<dyn FnMut() -> i64 + 'static> { return Box::new(move || { return *x; }); }
fn main() -> i32 { return 0i32; }
