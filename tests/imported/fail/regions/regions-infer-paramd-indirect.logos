// rustc regions/regions-infer-paramd-indirect.rs — upstream lifetime may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-08-31n, when the ELISION ENGINE + THE
// REGION SLOT landed (src/compiler/PROBES.md). The row this file used to
// carry in tests/logos/bc_admits.ledger is deleted in the same change.
package regions_infer_paramd_indirect;
struct B<'a> { v: &'a i64 }
struct C<'a> { f: B<'a> }
trait SetF<'a> {
  fn set_f_ok(self: &mut Self, b: B<'a>);
  fn set_f_bad(self: &mut Self, b: B);
}
impl<'a> SetF<'a> for C<'a> {
  fn set_f_ok(self: &mut Self, b: B<'a>) { self.f = b; }
  fn set_f_bad(self: &mut Self, b: B) { self.f = b; }
}
fn main() -> i32 { return 0i32; }
