error [fn call2]: let annotation: type `Option<&'b &'a u64>` is not well-formed — the reference under `'b` requires `'a: 'b` and no such bound is declared (reference has a longer lifetime than the data it references)
