// rustc regions/region-borrow-params-issue-29793-big.rs — upstream E0373 closure may outlive the current function
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02y: the OBJECT-LIFETIME BOUND is checked at the unsize coercion (`Src: 'r` for `dyn Tr + 'r`, 'static for an unbounded owned object). Was lifereg.C.
package region_borrow_params_issue_29793_big;
fn outer() -> Box<dyn Fn() -> i64> {
  let x: i64 = 1i64;
  let f = || { return x; };
  return Box::new(f);
}
fn main() -> i32 { let g = outer(); if g() != 1i64 { return 1i32; } return 0i32; }
