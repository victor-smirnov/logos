// rustc regions/region-borrow-params-issue-29793-small.rs — upstream E0373 closure may outlive fn, borrows params
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02y: the OBJECT-LIFETIME BOUND is checked at the unsize coercion (`Src: 'r` for `dyn Tr + 'r`, 'static for an unbounded owned object). Was lifereg.C.
// port of rust tests/ui/regions/region-borrow-params-issue-29793-small.rs — E0373 closure may outlive fn, borrows params
package region_borrow_params_issue_29793_small;
fn g(x: u64, y: u64) -> Box<dyn Fn(bool) -> u64> {
    let f = |t: bool| -> u64 { if t { return x; } else { return y; } };
    return Box::new(f);
}
fn main() -> i32 { let h = g(1u64, 2u64); if h(true) != 1u64 { return 1i32; } return 0i32; }
