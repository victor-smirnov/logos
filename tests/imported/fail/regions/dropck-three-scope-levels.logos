// B87: outer binding, middle scope source — still detected at middle pop.
package dropck_three_scope_levels;
struct W<'a> { r: &'a i32 }
impl<'a> Drop for W<'a> { fn drop(self: &mut W<'a>) { } }
fn main() -> i32 {
    let mut w: W;
    {
        let mid: i32 = 5i32;
        {
            w = W { r: &mid };
        }
    }
    return 0i32;
}
