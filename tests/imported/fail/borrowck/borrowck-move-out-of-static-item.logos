// rustc borrowck/borrowck-move-out-of-static-item.rs — upstream E0507
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-08-27 (E0507 is asked where `consuming` is already computed, at the deref itself, instead of at five hand-listed positions over a `VarRef`-spelled operand)
// upstream: borrowck-move-out-of-static-item.rs E0507
package borrowck_move_out_of_static_item;
struct Foo { foo: i64 }
impl Drop for Foo { fn drop(self: &mut Foo) {} }
static BAR: Foo = Foo { foo: 5i64 };
fn test(f: Foo) -> i64 { return f.foo; }
fn main() -> i32 {
    return test(BAR) as i32;   // E0507 cannot move out of static item
}
