// rustc borrowck/derive-clone-implicit-bound.rs — upstream E0382
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: pin
// upstream: derive-clone-implicit-bound.rs — the GENERIC spelling (T not Copy)
package derive_clone_implicit_bound_b_generic_param_moved_twice_generic;
struct S { n: i64 }
impl Drop for S { fn drop(self: &mut S) {} }
fn consume<T>(v: T) -> i64 { return 0i64; }
fn twice<T>(offset: T) -> i64 {
    let a = consume::<T>(offset);
    let b = consume::<T>(offset);
    return a + b;
}
fn main() -> i32 { return twice::<S>(S { n: 1i64 }) as i32; }
