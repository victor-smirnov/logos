// rustc borrowck/issue-81365-4.rs — upstream E0506
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-08-29 (was root bck.B: a place reached
//   through a reference-returning CALL recorded no loan, because the walker
//   stopped at the call and `bp.root` stayed empty)
package issue_81365_4_rd2;
struct T { tf: i64 }
struct C { t: T, cf: i64 }
impl Deref for C { type Target = T; fn deref(self: &C) -> &T { return &self.t; } }
fn main() -> i32 {
    let mut c = C { t: T { tf: 5i64 }, cf: 0i64 };
    let first: &i64 = &c.tf;
    c.cf = 9i64;
    return (*first + c.cf) as i32;
}
