// rustc borrowck/borrowck-move-out-from-array-use-match.rs — upstream E0382
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-08-30 (an array pattern's element binding
// now carries the element TYPE and an INDEX place; the whole-value reader in
// `consume` asks `report_partial_move` first; `slicearr`)
package borrowck_move_out_from_array_use_match_b;
use logos.mem.string;
fn arr() -> [String; 3] { return [String::from("a"), String::from("b"), String::from("c")]; }
fn main() -> i32 {
    let mut a: [String; 3] = arr();
    match a { [_, _, x] => { let _n = x.len(); } }
    a[2u64] = String::from("z");    // ERROR E0382: assign to moved-out element
    return 0i32;
}
