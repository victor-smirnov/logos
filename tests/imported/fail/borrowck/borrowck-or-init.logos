// rustc borrowck/borrowck-or-init.rs — upstream E0381
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-08-30: the RHS of `&&`/`||` is a CONDITIONAL path for definite-init: the
// names uninitialised before it are RESTORED after it (sema_expr.cpp,
// lower_binop). Was bck.NEW.
package borrowck_or_init;
fn main() -> i32 {
    let i: i64;
    let b: bool = false || { i = 5i64; true };
    let z: i64 = i;
    return 0i32;
}
