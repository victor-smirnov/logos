// rustc borrowck/borrowck-issue-14498.rs — upstream E0506/E0594
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-08-27; re-pinned 2026-09-08a to the E0506 it is upstream (`let mut y` restored: upstream borrow_in_var_from_var has it, the port had dropped it; PROBES.md 2026-09-08a)
package borrowck_issue_14498_box_mut_ref;
use logos.mem.boxed;
fn main() -> i32 {
    let mut x: i64 = 1i64;
    let mut y: Box<&mut i64> = Box::new(&mut x);
    let p: &Box<&mut i64> = &y;
    let q: &i64 = &***p;
    **y = 2i64;
    let _ = *q;
    return 0i32;
}
