// rustc borrowck/issue-93093.rs — upstream E0594
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: pin · WAS admit/bck.B until lower_place_compound_assign asked
// check_place_writable — the call its plain-assign sibling already made.
// upstream: borrowck/issue-93093.rs (E0594 `self.foo += 1` through `&self`)
package issue_93093;
struct S { foo: i64 }
impl S { fn bar(self: &S) { self.foo += 1i64; } }
fn main() -> i32 { let s = S { foo: 1i64 }; s.bar(); return 0i32; }
