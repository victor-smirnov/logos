// rustc borrowck/suggest-local-var-imm-and-mut.rs — upstream E0502
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: pin · WAS admit until the MethodCall arm RECORDED the bare-place
// receiver's loan across argument evaluation (B94's missing producer).
package suggest_local_var_imm_and_mut;
struct Foo { n: i64 }
impl Foo {
    fn foo(self: &Self, q: i64) -> i64 { return 1i64; }
    fn bar(self: &mut Self) -> i64 { return 2i64; }
    fn baz(self: &mut Self) -> i64 { return self.foo(self.bar()); }
}
fn main() -> i32 { let mut f: Foo = Foo { n: 0i64 }; return f.baz() as i32; }
