// rustc borrowck/issue-47215-ice-from-drop-elab.rs — upstream E0507
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-08-27 (E0507 is asked where `consuming` is already computed, at the deref itself, instead of at five hand-listed positions over a `VarRef`-spelled operand)
// upstream: issue-47215 — the static moved TWICE (a local moved twice is refused)
package issue_47215_ice_from_drop_elab_b_move_out_of_static_twice;
struct S { n: i64 }
impl Drop for S { fn drop(self: &mut S) {} }
pub static X: S = S { n: 0i64 };
fn main() -> i32 {
    let a = X;
    let b = X;
    return (a.n + b.n) as i32;
}
