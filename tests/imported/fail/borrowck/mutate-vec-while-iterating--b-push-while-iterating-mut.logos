// rustc borrowck/mutate-vec-while-iterating.rs — upstream E0502 / E0499
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail · was bck.D, CLOSED 2026-08-31r — `Code::ForEach` visited its
// iterable with a plain visit() and recorded NO loan; the iterable's borrow is
// now taken under a statement-scoped synthetic holder (`__foreach_it_N`), the
// same machinery `retain_temp_scrut_loan` uses for a temporary scrutinee.
// upstream: borrowck/mutate-vec-while-iterating.rs second fn (E0499)
package mutate_vec_while_iterating_b_push_while_iterating_mut;
fn main() -> i32 {
    let mut values: Vec<i64> = Vec::new();
    values.push(1i64); values.push(2i64);
    for value in &mut values {
        if *value == 2i64 { values.push(4i64); }
    }
    return 0i32;
}
