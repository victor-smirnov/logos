// rustc borrowck/move-in-pattern-mut-in-loop.rs — upstream E0382 use of moved value
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: FAIL (was admit) — closed 2026-08-28: the partial-move record now
// crosses the LOOP EDGE, so the field is moved on entry to iteration 2.
package move_in_pattern_mut_in_loop;
use logos.lang.option;
fn main() -> i32 {
    let mut x: i64 = 42i64;
    let opt: Option<&mut i64> = Option::Some(&mut x);
    let mut i: i64 = 0i64;
    while i < 5i64 {
        match opt { Option::Some(mut y) => {} Option::None => {} }   // ERROR: use of moved value
        i = i + 1i64;
    }
    return 0i32;
}
