// rustc borrowck/move-error-in-promoted-2.rs — upstream E0508
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: pin
// upstream: move-error-in-promoted-2.rs E0508 cannot move out of type `[S;1]`
package move_error_in_promoted_2;
struct S { n: i64 }
impl Drop for S { fn drop(self: &mut S) {} }
fn foo() -> i64 {
    let arr = [S { n: 1i64 }];
    let t = (arr[0],);     // E0508: cannot move out of an array index
    return t.0.n;
}
fn main() -> i32 { return foo() as i32; }
