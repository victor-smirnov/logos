// rustc borrowck/unconstrained-closure-lifetime-generic.rs — upstream (param may not live long enough / `f` does not live long enough)
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02y: the OBJECT-LIFETIME BOUND is checked at the unsize coercion (`Src: 'r` for `dyn Tr + 'r`, 'static for an unbounded owned object). Was bck.C.
// t38 REDUCED: a borrow of a fn-local is captured by a closure that is stored
// into a field behind `&mut self`; the closure outlives the local.
package unconstrained_closure_lifetime_generic_min_capture_escapes_to_field;
struct Foo { bar: Box<dyn Fn() -> u64> }
impl Foo {
    pub fn ack(self: &mut Self) {
        let f: u64 = 7u64;
        let r: &u64 = &f;
        self.bar = Box::new(|| -> u64 { return *r; });
    }
}
fn main() -> i32 { return 0i32; }
