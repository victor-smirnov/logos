// rustc borrowck/move-in-pattern.rs — upstream E0382
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: pin
// upstream: borrowck/move-in-pattern.rs first site  E0382 use of partially moved value
package move_in_pattern_a_partial_move_then_use_whole;
struct S { n: i64 }
impl Drop for S { fn drop(self: &mut S) {} }
fn foo(v: Option<S>) -> i32 { return 0i32; }
fn main() -> i32 {
    let s: Option<S> = Option::Some(S { n: 1i64 });
    match s {
        Option::Some(x) => { let _ = x.n; }
        Option::None => {}
    }
    return foo(s);
}
