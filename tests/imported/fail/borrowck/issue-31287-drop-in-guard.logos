// rustc borrowck/issue-31287-drop-in-guard.rs — upstream E0382 use of moved value in guard
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: pin
package issue_31287_drop_in_guard;
struct Own { v: i64 }
impl Drop for Own { fn drop(self: &mut Own) { } }
fn eat(o: Own) -> bool { return false; }
fn main() -> i32 {
    let a: Option<Own> = Option::Some(Own { v: 1i64 });
    let b: Option<Own> = match a {
        Option::Some(_) if eat(a.unwrap()) => Option::None,
        x => x,
    };
    return 0i32;
}
