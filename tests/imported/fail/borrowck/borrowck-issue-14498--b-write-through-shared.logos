// rustc borrowck/borrowck-issue-14498.rs — upstream E0506/E0594
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-08-27 (check_place_mut_use inherited the shared-`&` question record_borrow already asks: a write through a shared reference is E0594)
package borrowck_issue_14498_b_write_through_shared;
use logos.mem.boxed;
fn main() -> i32 {
    let mut x: i64 = 1i64;
    let y: Box<&mut i64> = Box::new(&mut x);
    let p: &Box<&mut i64> = &y;
    ***p = 2i64;
    let _ = p;
    return 0i32;
}
