// rustc borrowck/borrowck-struct-update-with-dtor.rs — upstream E0509 cannot move out of Drop type
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root bck.E
// ⚠ CORRECTION 2026-09-08 — THE BLESSING IS WITHDRAWN, THIS ROW IS A DEFECT AGAIN.
//   Everything above about `intrinsic.drop.skip-moved-out-paths` licensing this
//   admission was true when it was written and is FALSE NOW. The owner decided on
//   2026-09-08 that Logos's drop semantics are Rust-canonical: `Drop::drop` takes
//   `&mut self`, and moving a sub-value out of a value that implements `Drop` is
//   E0509 and REFUSED. The clause was rewritten the same day and now covers only
//   NON-`Drop` owners; the rule for this program is the new
//   `intrinsic.drop.move-out-of-drop-type-refused` (docs/spec/expressions.md).
//   The "RE-VERIFIED BY RUNNING IT" evidence above is not withdrawn — it is
//   accurate about what the compiler DOES; it is no longer an argument that what
//   the compiler does is CORRECT. The row moved back to tests/logos/bc_admits.ledger.
// ── BUCKET 3, BLESSED DIVERGENCE — NOT RE-PORTED, AND NOT A DEFECT. 2026-09-07.
// Original path: tests/ui/borrowck/borrowck-struct-update-with-dtor.rs
// upstream commit: 4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// upstream construct: lines 36-39 — `let _s2 = T { a: 2, ..s0 };` (functional struct update) where `impl Drop for T`; //~ ERROR [E0509]
// Modifications: none — NOT PORTED AS-IS AND NOT PORTABLE AS-IS. The construct
//   cannot be made illegal here: Logos deliberately ADMITS a move out of a field
//   of a value with a user `Drop`, and defines what its destructor then does —
//   `intrinsic.drop.skip-moved-out-paths` (docs/spec/expressions.md), "Drop of a
//   value suppresses sub-values that were moved out: a dotted path whose segment
//   exactly matches a child skips that child's drop entirely". E0509 exists in
//   Rust precisely because Rust has no such rule.
// ⚠ THE CLAUSE WAS RE-VERIFIED BY RUNNING IT, 2026-09-07, not by reading it: a
//   program with `struct S { f: Inner }`, `impl Drop` on BOTH, `let g = s.f;`
//   and a static drop counter per type prints INNER_DROPS=1 S_DROPS=1 —
//   `S::drop` runs once, the moved-out field is dropped once through `g`, and
//   the owner skips it. No leak, no double free. The divergence is implemented,
//   not merely declared.
// The file stays on the admit shelf and keeps its row in
// tests/logos/bc_admits_blocked.ledger; it is not a hole and never was.
package borrowck_struct_update_with_dtor_t17;
use logos.mem.string;
struct T { a: i64, b: String }
impl Drop for T { fn drop(self: &mut T) {} }
fn f(s0: T) -> i32 { let s2 = T { a: 2i64, ..s0 }; return 0i32; }
fn main() -> i32 { return 0i32; }
