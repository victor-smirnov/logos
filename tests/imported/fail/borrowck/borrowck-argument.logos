// rustc borrowck/borrowck-argument.rs — upstream E0596
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail · was bck.A, CLOSED 2026-08-31p — the `not declared as mut`
// param hatch is for REFERENCE params, and the reborrow exemption at the
// field / AddrOfTemp sites now keys on the reference actually DEREFERENCED
// rather than on the root binding.
// upstream: borrowck/borrowck-argument.rs  E0596
package borrowck_argument;
struct S { v: i64 }
impl S { fn mutate(self: &mut S) { self.v = 1i64; } }
fn func(arg: S) -> i64 { arg.mutate(); return arg.v; }
fn main() -> i32 { return func(S { v: 0i64 }) as i32; }
