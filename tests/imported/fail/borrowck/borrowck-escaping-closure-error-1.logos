// rustc borrowck/borrowck-escaping-closure-error-1.rs — upstream E0373
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-09-02y: the OBJECT-LIFETIME BOUND is checked at the unsize coercion (`Src: 'r` for `dyn Tr + 'r`, 'static for an unbounded owned object). Was bck.C.
package borrowck_escaping_closure_error_1;
use logos.mem.boxed;
fn spawn(f: Box<dyn FnMut()>) -> Box<dyn FnMut()> { return f; }
fn mk() -> Box<dyn FnMut()> {
    let mut books: Vec<i64> = Vec::<i64>::new();
    return spawn(Box::new(|| { books.push(4i64); }));
}
fn main() -> i32 { let mut f = mk(); f(); return 0i32; }
