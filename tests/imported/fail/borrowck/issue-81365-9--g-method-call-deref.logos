// rustc borrowck/issue-81365-9.rs — upstream E0506
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: fail — REFUSED since 2026-08-29 (was root bck.B: a place reached
//   through a reference-returning CALL recorded no loan, because the walker
//   stopped at the call and `bp.root` stayed empty)
// VARIANT of t29: the same Deref impl invoked as a METHOD (self.deref())
package issue_81365_9_g_method_call_deref;
use logos.lang.ops;
struct DerefTarget { target_field: bool }
struct Container { target: DerefTarget, container_field: bool }
impl Deref for Container {
    type Target = DerefTarget;
    fn deref(self: &Container) -> &DerefTarget { return &self.target; }
}
impl Container {
    fn bad_borrow(self: &mut Container) -> i32 {
        let first = &self.deref().target_field;
        self.container_field = true;
        let _ = *first;
        return 0i32;
    }
}
fn main() -> i32 {
    let mut c = Container { target: DerefTarget { target_field: false }, container_field: false };
    return c.bad_borrow();
}
