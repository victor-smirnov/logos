package g13;
fn apply<T, F: Fn(T) -> T>(f: F, a: T) -> T { return f(a); }
fn main() -> i32 {
    // Rust allows partial turbofish with `_`; Logos rejects '_' as a type.
    return apply::<i64, _>(|x: i64| -> i64 { return x * 2i64; }, 5i64) as i32;  // error: unknown type '_'
}
