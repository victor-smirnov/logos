// rustc lifetimes/suggest-introducing-and-adding-missing-lifetime.rs — upstream E0310 the parameter type `T` may not live long enough
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: pass. RETIRED from tests/logos/bc_admits.ledger (was root lifereg.L2)
// on 2026-09-01: the row was wrong. The upstream E0310 is about a DIFFERENT
// function in the original file; this reduction kept the version that WRITES
// the `T: 'b` bound, so it is legal Rust and must compile. What it now pins is
// the correct acceptance of a written type-outlives bound: verified rc 0 with
// the bound, under alpha-rename (`<'q, U: 'q>`), and at a call site.
package suggest_introducing_and_adding_missing_lifetime_c22b_outlives_bound_ignored;
struct W<T> { v: T }
fn keep<'b, T: 'b>(x: &'b T) -> &'b T { return x; }
fn main() -> i32 { return 0i32; }
