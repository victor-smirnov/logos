// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/traits/default_method_simple.rs
// Modifications: println! dropped; default method `f` returns `self.g()` (an
// observable i64) rather than printing; `&self`->`self:&Self`/`self:&A`; unit
// struct `A`->`A {}` (Logos requires a brace body). Tests
// a trait DEFAULT method `f` that calls another (required) method `g` on self.
package default_method_simple_tr4;

trait Foo {
    fn f(self: &Self) -> i64 {
        return self.g() + 1i64;
    }
    fn g(self: &Self) -> i64;
}

struct A {}

impl Foo for A {
    fn g(self: &A) -> i64 {
        return 41i64;
    }
}

fn main() -> i32 {
    let a = A {};
    if a.f() != 42i64 { return 1i32; }
    return 0i32;
}
