// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/traits/issue-40085.rs
// Modifications: static UNIT/`()` Output→an i64 field; `bar` returns i64; the `S.index(bar)`
// UFCS form dropped (kept `S[bar]` indexing); assert→ret-guard.
// Tests an `impl Index<fn()->i64> for S` — overloaded indexing keyed on a fn-POINTER index type,
// invoked as `s[bar]` where `bar` is a fn item.
package issue_40085_b143;

fn bar() -> i64 { return 7i64; }

struct S { v: i64 }

impl Index<fn() -> i64> for S {
    type Output = i64;
    fn index(self: &S, f: fn() -> i64) -> &i64 { return &self.v; }
}

fn main() -> i32 {
    let s: S = S { v: 9i64 };
    if s[bar] != 9i64 { return 1i32; }
    return 0i32;
}
