// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/traits/cycle-type-trait.rs
// Modifications: `pub fn main()`→`fn main()->i32`; receiver `self: &i32`;
// 1-char generic param C kept (it's a where-bound param, not a stdlib clash).
// A supertrait references a type (Struct<i32>) that references the original
// trait (via the Chromosome bound) — a self-referential supertrait cycle.
package cycle_type_trait;

trait Chromosome: Get<Struct<i32>> {
}

trait Get<A> {
    fn get(self: &Self) -> A;
}

struct Struct<C: Chromosome> { c: C }

impl Chromosome for i32 { }

impl Get<Struct<i32>> for i32 {
    fn get(self: &i32) -> Struct<i32> {
        return Struct { c: *self };
    }
}

fn main() -> i32 {
    return 0;
}
