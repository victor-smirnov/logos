// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/traits/inheritance/multiple-inheritors.rs
// Modifications: isize→i64; `&self`→`self: &S`; unit field `bogus: ()` → `bogus: i64` (unit-field
//   rejected); assert_eq!→distinct nonzero return; f returns the failure code.
// Tests a diamond supertrait bound: `B: A`, `C: A`, and a generic fn `f<T: B + C>` that calls all of
//   `a()` (from A), `b()` (from B), `c()` (from C) on the same `&T`.
package inheritance_multiple_inheritors_b148;

trait A { fn a(self: &Self) -> i64; }
trait B: A { fn b(self: &Self) -> i64; }
trait C: A { fn c(self: &Self) -> i64; }

struct S { bogus: i64 }

impl A for S { fn a(self: &S) -> i64 { 10i64 } }
impl B for S { fn b(self: &S) -> i64 { 20i64 } }
impl C for S { fn c(self: &S) -> i64 { 30i64 } }

fn f<T: B + C>(x: &T) -> i32 {
    if x.a() != 10i64 { return 1i32; }
    if x.b() != 20i64 { return 2i32; }
    if x.c() != 30i64 { return 3i32; }
    return 0i32;
}

fn main() -> i32 {
    let s = S { bogus: 0i64 };
    return f(&s);
}
