// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/traits/composition-trivial.rs
// Modifications: `pub fn main()`→`fn main()->i32`; receivers `self: &Self`.
// (Upstream is `//@ check-pass`; ported as a run test with an exit-0 main.)
// A trivial supertrait composition (`Bar: Foo`) compiles.
package composition_trivial;

trait Foo {
    fn foo(self: &Self);
}

trait Bar: Foo {
    fn bar(self: &Self);
}

fn main() -> i32 {
    return 0;
}
