// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/mir/mir_temp_promotions.rs
// Temporaries (results of `f.is_nan()` / `f.is_infinite()`) promoted to allocas
// when combined under `||` and negated. Tests f32 classification methods.
// Modifications: assert_eq! → early-return sentinel; literals suffixed.
package mir_temp_promotions_b156;

fn test1(f: f32) -> bool {
    return !(f.is_nan() || f.is_infinite());
}

fn main() -> i32 {
    if test1(0.0f32) != true { return 1i32; }
    return 0i32;
}
