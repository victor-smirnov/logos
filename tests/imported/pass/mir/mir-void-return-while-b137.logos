// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/mir/mir_void_return.rs
// Modifications: literals suffixed; the unit-returning fn body (a while loop
//   with no tail value) kept verbatim; called from main for effect.
// Tests a unit-returning fn whose entire body is a `while y < x { y += 1 }`
//   loop (no tail expression, implicit unit return).
package mir_void_return_while_b137;

fn run() {
    let x: i64 = 1i64;
    let mut y: i64 = 0i64;
    while y < x {
        y += 1i64;
    }
}

fn main() -> i32 {
    run();
    return 0i32;
}
