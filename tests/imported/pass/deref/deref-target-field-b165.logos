// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/deref/ shapes (distilled).
// Modifications: isize->i64; assert->if-return sentinels; &self->self:&Self;
//   user Deref via `impl Deref<Target> for T` (house idiom) + `use logos.lang.ops;`.
// Tests a wrapper whose Deref target is an i64; `*w` reaches the value.
package deref_target_field_b165;

use logos.lang.ops;

struct Counter { n: i64 }
impl Deref<i64> for Counter {
    fn deref(self: &Self) -> &i64 { return &self.n; }
}
fn main() -> i32 {
    let c: Counter = Counter { n: 41i64 };
    let v: i64 = *c;
    if v != 41i64 { return 1i32; }
    if *c + 1i64 != 42i64 { return 2i32; }
    return 0i32;
}
