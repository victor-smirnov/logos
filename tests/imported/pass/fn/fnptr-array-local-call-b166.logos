// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/ adversarial intersection (B166 depth probe, distilled).
// Modifications: isize->i64; assert!->if-return sentinels; &self->self:&Self.
//   array of fn pointers, element bound to a local then called (workaround idiom).
package t11;
fn inc(x: i64) -> i64 { return x + 1i64; }
fn dec(x: i64) -> i64 { return x - 1i64; }
fn main() -> i32 {
    let ops = [inc, dec];
    let f = ops[0i64];
    let g = ops[1i64];
    if f(5i64) != 6i64 { return 1i32; }
    if g(5i64) != 4i64 { return 2i32; }
    return 0i32;
}
