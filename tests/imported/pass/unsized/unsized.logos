// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/unsized/unsized.rs
// Original copyright: The Rust Project Developers (Apache 2.0 / MIT).
// Modifications: PhantomData removed (Logos has no PhantomData and
// doesn't need it for this test — the unsized type parameter just has
// no field that uses it). `use std::marker::PhantomData` dropped.
// `type TT<T: ?Sized> = T;` retained — Logos type-aliases now accept
// the RHS as the `?Sized` typevar (Phase 1B-11 sanity-check). Test
// surface focuses on `?Sized` syntax acceptance at trait / struct /
// enum / impl / fn / type-alias declaration sites.

package imported_unsized;

trait T1  { }
pub trait T2  { }
trait T3<X: T1> : T2 { }
trait T4<X: ?Sized> { }
trait T5<X: ?Sized, Y> { }
trait T6<Y, X: ?Sized> { }
trait T7<X: ?Sized, Y: ?Sized> { }
trait T8<X: ?Sized + T2> { }
trait T9<X: T2 + ?Sized> { }

struct S1<X: ?Sized> { _u: u8 }
enum E<X: ?Sized> { E1 }
impl<X: ?Sized> T1 for S1<X> {}

fn f<X: ?Sized>() {}

type TT<T: ?Sized> = T;

unsafe fn main() -> i32 {
    return 0;
}
