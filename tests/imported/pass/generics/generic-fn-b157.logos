// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/generics/generic-fn.rs
// Identity generic fn `id<T>` instantiated with isize/char/struct via turbofish.
// Modifications: isize->i64; #[derive(Copy,Clone)] dropped; println!->dropped;
// assert_eq! -> sentinels.
package generic_fn_b157;

fn id<T>(x: T) -> T { return x; }

struct Triple { x: i64, y: i64, z: i64 }

fn main() -> i32 {
    let x: i64 = 62i64;
    let mut y: i64 = 63i64;
    let a: char = 'a';
    let mut b: char = 'b';
    let p: Triple = Triple { x: 65i64, y: 66i64, z: 67i64 };
    let mut q: Triple;
    y = id::<i64>(x);
    if x != y { return 1i32; }
    b = id::<char>(a);
    if a != b { return 2i32; }
    q = id::<Triple>(p);
    let xz: i64 = p.z;
    let yz: i64 = q.z;
    if xz != yz { return 3i32; }
    return 0i32;
}
