// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/generics/generic-fn-infer.rs
// Modifications: explicit type-arg-less call relies on inference from
// the argument type.
package generic_fn_infer;

use logos.std.fmt;
use logos.mem.string;
use logos.lang.str;

fn id<T>(x: T) -> T { return x; }

fn main() -> i32 {
    let x: isize = 42isize;
    let y: isize = id(x);
    assert_eq!(x, y);
    return 0;
}
