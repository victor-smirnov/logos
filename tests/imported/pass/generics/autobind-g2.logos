// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/generics/autobind.rs
// Feature: a generic fn `f<T>(Vec<T>)->T` passed as a value to a `where F: FnOnce(Vec<i64>)->i64` bound.
// assert_eq->return-code; isize->i64; vec!->vec_new+push; String half dropped (kept the i64 numeric
// half, which exercises the same fn-item->FnOnce feature). The `let f1 = f;` half (binding a generic
// fn-item to a local then calling it with a concrete arg) dropped — the local loses the type-arg
// binding (`expected Vec<T>, got Vec<i64>`); G-FNLOCAL gap.
package autobind_g2;
fn f<T>(x: Vec<T>) -> T { return x.into_iter().next().unwrap(); }
fn g<F: FnOnce(Vec<i64>) -> i64>(act: F) -> i64 {
    let mut v: Vec<i64> = vec_new::<i64>();
    v.push(1i64);
    v.push(2i64);
    v.push(3i64);
    return act(v);
}
fn main() -> i32 {
    if g(f) != 1i64 { return 1i32; }
    return 0i32;
}
