// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/tuple/ (tuple structural equality facet).
// Modifications: isize->i64; assert->if-return. Tests structural `==`
// over fixed-arity tuples: equal-valued tuples compare equal and a
// tuple differing in one element compares not-equal (the variadic-tuple
// Eq impl, per the prelude).
package tuple_eq_compare_tp2;

fn main() -> i32 {
    let x: (i64, i64) = (1i64, 2i64);
    let y: (i64, i64) = (1i64, 2i64);
    let z: (i64, i64) = (1i64, 3i64);
    if !(x == y) { return 1i32; }
    if x == z { return 2i32; }
    if x != z { } else { return 3i32; }
    let a: (i64, i64, i64) = (5i64, 6i64, 7i64);
    let b: (i64, i64, i64) = (5i64, 6i64, 7i64);
    if !(a == b) { return 4i32; }
    return 0i32;
}
