// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/tuple/ (unit `()` value facet).
// Modifications: isize->i64. Tests the unit type `()` as a first-class
// value: a fn whose return type is `()` returning `()`, and a `let u: ()
// = ();` binding.
package unit_tuple_value_tp2;

fn noop() -> () { return (); }

fn main() -> i32 {
    noop();
    let u: () = ();
    return 0i32;
}
