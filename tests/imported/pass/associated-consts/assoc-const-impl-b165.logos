// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/associated-consts/ shapes (distilled).
// Modifications: isize->i64; assert->if-return sentinels; &self->self:&Self.
// Tests an associated const declared in an inherent impl, read in a method body
//   and via the `Type::CONST` path.
package assoc_const_impl_b165;

struct Circle { r: i64 }
impl Circle {
    const PI3: i64 = 3i64;
    fn area(self: &Self) -> i64 { return Circle::PI3 * self.r * self.r; }
}
fn main() -> i32 {
    let c = Circle { r: 2i64 };
    if c.area() != 12i64 { return 1i32; }
    if Circle::PI3 != 3i64 { return 2i32; }
    return 0i32;
}
