// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/shadowed/ shapes (distilled).
// Modifications: isize->i64; assert->if-return sentinels.
// Tests `let` shadowing where each rebind is computed from the PRIOR binding of
//   the same name in the same scope (a chain of `let x = <expr over x>;`), plus a
//   shadow inside a nested block that does not leak out.
package shadow_same_scope_arith_b163;

fn main() -> i32 {
    let x: i64 = 1i64;
    let x: i64 = x + 10i64;
    let x: i64 = x * 2i64;
    if x != 22i64 { return 1i32; }
    let y: i64 = 5i64;
    {
        let y: i64 = y + 100i64;
        if y != 105i64 { return 2i32; }
    }
    if y != 5i64 { return 3i32; }
    return 0i32;
}
