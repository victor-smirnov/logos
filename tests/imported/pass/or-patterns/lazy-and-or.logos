// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/or-patterns/lazy-and-or.rs
// Modifications: the side-effecting `would_panic_if_called` RHS (which relies on
// short-circuit to never run) → return-code checks on the computed boolean
// (Logos `||`/`&&` short-circuit; the panic-on-eval probe is replaced by a value
// check); literals suffixed; `println!`/`assert*` → return codes; `pub fn
// main()`→`fn main()->i32`. Exercises `||`/`&&` short-circuit evaluation.
package op_lazy_and_or;

fn main() -> i32 {
    let x = 1i32 == 2i32 || 3i32 == 3i32;
    if !x { return 1; }

    if true && x {
        // ok
    } else {
        return 2;
    }
    return 0;
}
