// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/methods/method-two-trait-defer-resolution-2.rs
// `x.foo()` resolves to `Foo::foo` from a single trait even though the receiver's element type is not
// yet fully known; here we keep the distinguishing structure: a blanket `impl<T:MyCopy> Foo for Vec<T>`.
// isize->i64; assert_eq->return-code; Box<i32> variant dropped (overlapping-impl picks would need Box).
package method_two_trait_defer_resolution_2_m;
trait MyCopy { fn tag(self: &Self) {} }
impl MyCopy for i32 {}
trait Foo { fn foo(self: &Self) -> i64; }
impl<T: MyCopy> Foo for Vec<T> { fn foo(self: &Vec<T>) -> i64 { return 1i64; } }
fn call_foo_copy() -> i64 {
    let mut x: Vec<i32> = vec_new();
    let y: i64 = x.foo();
    x.push(0i32);
    return y;
}
fn main() -> i32 {
    if call_foo_copy() == 1i64 { return 0i32; }
    return 1i32;
}
