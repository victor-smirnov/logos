// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/array-slice-vec/fixed_length_copy.rs
// Modifications: integer literals suffixed (i64); assert_eq! → early-return sentinels.
//   Faithful: a fixed-size array is bound, then copied by value into another binding;
//   both are indexed afterward (the original remains usable, i.e. Copy semantics).
// Tests: a fixed-size array of a Copy element type copies by value on `let arr2 = arr`,
//   leaving the original array usable.
package fixed_length_copy_b155;

fn main() -> i32 {
    let arr = [1i64, 2i64, 3i64];
    let arr2 = arr;
    if arr[1] != 2i64 { return 1i32; }
    if arr2[2] != 3i64 { return 2i32; }
    return 0i32;
}
