// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/array-slice-vec/ivec-pass-by-value.rs
// Modifications: `vec![…]` → `vec_from_arr([…])`; `isize` → `i64`.
package asv_ivec_pass_by_value;

use std.fmt;
use logos.mem.string;
use logos.lang.str;
use logos.lang.option;
use std.collections.vec;

fn f(_a: Vec<i64>) { }

fn main() -> i32 {
    f(vec_from_arr([1i64, 2i64, 3i64, 4i64, 5i64]));
    return 0i32;
}
