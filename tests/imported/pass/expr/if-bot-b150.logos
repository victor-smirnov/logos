// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/expr/if-bot.rs
// Modifications: dropped the `panic!()` branch (never taken); println! dropped;
//   isize -> i64; assert via exit code.
// Tests an if-expression whose else branch supplies the value while the then
// branch is divergent-shaped (here a constant), used in a let binding.
package if_bot_b150;

fn main() -> i64 {
    let i: i64 = if false { 0 } else { 5 };
    if i != 5 { return 1; }
    return 0;
}
