// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/borrowck/lazy-init.rs
// Modifications: `println!` → side-effect-only consumer. The lazy
// `let mut x: isize;` form (declare-without-init) is now admitted
// (B3-bg-01 / B3-bg-02 closed 2026-05-11).
package lazy_init;

fn foo(x: isize) -> isize { return x; }

fn main() -> i32 {
    let mut x: isize;
    if 1isize > 2isize { x = 12isize; } else { x = 10isize; }
    let _r: isize = foo(x);
    return 0;
}
