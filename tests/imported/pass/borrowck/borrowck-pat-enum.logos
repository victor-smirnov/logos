// Imported from rust-lang/rust tests/ui/borrowck/borrowck-pat-enum.rs
// Modifications: i64; exit-code main; `Option::Some(i)` explicit path.
package r_borrowck_pat_enum;

use logos.lang.option;

fn match_ref(v: Option<i64>) -> i64 {
    match v {
        Option::Some(ref i) => { return *i; }
        Option::None => { return 0i64; }
    }
}

fn impure(_i: i64) {}

fn match_imm_reg(v: &Option<i64>) {
    match *v {
        Option::Some(ref i) => { impure(*i); }
        Option::None => {}
    }
}

fn main() -> i32 {
    let v: Option<i64> = Option::Some(42i64);
    if match_ref(v) != 42i64 { return 1; }
    return 0;
}
