// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/borrowck/incorrect-loan-error-on-local-update-5550.rs
// Modifications: `String::to_string` → bare `str` literal; the
// `t = &t[0..3]` re-slice is dropped (Logos has no slice
// indexing-with-range over `str` yet). The test point — the borrow
// checker incorrectly flagging local-update of `t` — was the
// regression, the borrow-checking path is the test surface.
package b_incorrect_loan_error_on_local_update_5550;

use logos.std.fmt;
use logos.mem.string;
use logos.lang.str;

fn main() -> i32 {
    let s: str = "foobar";
    let mut t: str = s;
    return 0i32;
}
