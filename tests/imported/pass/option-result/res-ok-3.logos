package res_ok_3;
enum Res<T, E> { Ok(T), Err(E) }
fn main() -> i32 {
    let r: Res<i32, i32> = Res::Ok(3i32);
    let v: i32 = match r {
        Res::Ok(x)  => x,
        Res::Err(e) => e,
    };
    return v - 3i32;
}
