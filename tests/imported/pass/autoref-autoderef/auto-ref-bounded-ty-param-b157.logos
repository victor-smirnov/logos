// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/autoref-autoderef/auto-ref-bounded-ty-param.rs
// Blanket `impl<T: Baz> Foo for T` whose default method body calls a Baz method
// on self; auto-ref of the value receiver to the &self Baz method.
// Modifications: isize->i64; `&self`->`self:&Self`; println! -> sentinel via a
// returned value; assert through a counter.
package auto_ref_bounded_ty_param_b157;

trait Foo {
    fn f(self: &Self) -> i64;
}

struct Bar { x: i64 }

trait Baz {
    fn g(self: &Self) -> i64;
}

impl<T: Baz> Foo for T {
    fn f(self: &Self) -> i64 {
        return self.g();
    }
}

impl Baz for Bar {
    fn g(self: &Self) -> i64 {
        return self.x;
    }
}

fn main() -> i32 {
    let y: Bar = Bar { x: 42i64 };
    if y.f() != 42i64 { return 1i32; }
    return 0i32;
}
