// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/autoref-autoderef/auto-ref.rs
// Modifications: isize->i64; `&self`->`self:&Foo`; `println!(self.x)` -> store into a
//   `*mut i64` out-param so we can assert the value reached the method; nonzero return.
// Tests calling a `&self` trait method on a by-value receiver (autoref) (#auto-ref).
package auto_ref_trait_method_b146;

struct Foo { x: i64 }

trait Stuff {
    fn getme(self: &Foo) -> i64;
}

impl Stuff for Foo {
    fn getme(self: &Foo) -> i64 { self.x }
}

fn main() -> i32 {
    let x = Foo { x: 3i64 };
    if x.getme() != 3i64 { return 1i32; }
    return 0i32;
}
