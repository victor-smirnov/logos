// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/ adversarial intersection (B166 depth probe, distilled).
// Modifications: isize->i64; assert!->if-return sentinels; &self->self:&Self.
//   let-else binding an Option with diverging else.
package t08;
fn get(n: i64) -> Option<i64> { if n > 0i64 { return Option::Some(n); } return Option::None; }
fn run(n: i64) -> i64 {
    let Option::Some(v) = get(n) else { return -1i64; };
    return v * 2i64;
}
fn main() -> i32 {
    if run(5i64) != 10i64 { return 1i32; }
    if run(-1i64) != -1i64 { return 2i32; }
    return 0i32;
}
