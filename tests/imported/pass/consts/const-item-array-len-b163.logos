// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/consts/ shapes (distilled).
// Modifications: isize->i64; assert->if-return. A `const N` used as a fixed-array
//   length and in arithmetic; const folding in the array type position.
// Tests a `const` integer used as a `[i64; N]` array length and as an operand in
//   ordinary arithmetic.
package const_item_array_len_b163;

const N: i64 = 4i64;

fn main() -> i32 {
    let arr: [i64; 4] = [1i64, 2i64, 3i64, 4i64];
    let mut sum: i64 = 0i64;
    let mut i: i64 = 0i64;
    while i < N {
        sum = sum + arr[i];
        i = i + 1i64;
    }
    if sum != 10i64 { return 1i32; }
    if N * 3i64 != 12i64 { return 2i32; }
    return 0i32;
}
