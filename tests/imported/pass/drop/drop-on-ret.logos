// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/drop/drop-on-ret.rs
// Modifications: `String::to_string` → `str` literal. `isize` →
// `i64`. The test point is that early-return correctly drops
// locally-owned data; with `str` the question reduces to "does
// `let _s: str = "…"; return 1;` compile + run".
package d_drop_on_ret;

fn f() -> i64 {
    if true {
        let _s: str = "should not leak";
        return 1i64;
    }
    return 0i64;
}

fn main() -> i32 {
    f();
    return 0i32;
}
