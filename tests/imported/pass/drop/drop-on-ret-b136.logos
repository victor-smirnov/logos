// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/drop/drop-on-ret.rs
// Modifications: isize→i64; String field via to_string(); pub fn main()→i32.
// Tests that an early `return` inside an `if` block correctly runs the
// destructor of a String local before returning (no leak / clean drop).
package drop_on_ret_b136;

fn f() -> i64 {
    if true {
        let _s: String = "should not leak".to_string();
        return 1i64;
    }
    return 0i64;
}

fn main() -> i32 {
    let r: i64 = f();
    if r != 1i64 { return 1i32; }
    return 0i32;
}
