// B76: enum Res<T,E> with Co in both — shortenable independently.
package variance_co_result_enum;

enum Res<T, E> { Ok(T), Err(E) }

fn shorten_ok<'short, 'long>(v: Res<&'long i32, i64>) -> Res<&'short i32, i64>
    where 'long: 'short
{ return v; }

fn main() -> i32 { return 0i32; }
