// B76: enum variant carrying &'a T → enum Co in T.
package variance_co_enum;

enum Opt<T> { Some(T), None }

fn shorten<'short, 'long>(v: Opt<&'long i32>) -> Opt<&'short i32>
    where 'long: 'short
{ return v; }

fn main() -> i32 { return 0i32; }
