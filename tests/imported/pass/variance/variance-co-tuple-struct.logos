// B76: Co tuple-struct wrapper shortens through.
package variance_co_tuple_struct;

struct Wrap<T> { x: T }

fn wco<'short, 'long>(v: Wrap<&'long i32>) -> Wrap<&'short i32>
    where 'long: 'short
{ return v; }

fn main() -> i32 { return 0i32; }
