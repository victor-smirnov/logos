// B84: user-defined Contra marker via fn(T) — auto-Contra.
package variance_user_contra_marker;
struct Contra<T> { f: fn(T) -> i32 }
fn lengthen<'short, 'long>(v: Contra<&'short i32>) -> Contra<&'long i32>
    where 'long: 'short
{ return v; }
fn main() -> i32 { return 0i32; }
