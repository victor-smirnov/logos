// B84: `*const T` is Co in T — wrapper shortens.
package variance_const_ptr_co;
struct W<T> { p: *const T }
fn shorten<'short, 'long>(v: W<&'long i32>) -> W<&'short i32>
    where 'long: 'short
{ return v; }
fn main() -> i32 { return 0i32; }
