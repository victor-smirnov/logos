// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/issues/issue-25279.rs
// Modifications: lifetime-parameterized `S<'a>(&'a ())` → a plain `Sref { v: i64 }`
// (the test's point is an impl method calling a sibling method on `self`); the
// UFCS form `<Self>::bar(self)` → the direct method call `self.bar()` (Logos has
// no type-qualified UFCS); by-value `self`→`self: Self`; `pub fn main()`→`fn
// main()->i32`.
package iss_issue_25279;

struct Sref { v: i64 }

impl Sref {
    fn foo(self: Self) -> i64 { return self.bar(); }
    fn bar(self: Self) -> i64 { return self.v; }
}

fn main() -> i32 {
    let s = Sref { v: 7i64 };
    if s.foo() != 7i64 { return 1; }
    return 0;
}
