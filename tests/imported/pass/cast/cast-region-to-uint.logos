// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/cast/cast-region-to-uint.rs
// Modifications: `isize → 3isize` suffix; the address print is back —
// LowerHex/UpperHex/Octal/Binary impls extended to all primitive
// integer widths (was: only u64/u32/i64/i32) so `{:x}` on `usize`
// works as in the original. New gap row: F-fm-01.
package cast_region_to_uint;

use logos.std.fmt;
use logos.mem.string;
use logos.lang.str;

fn main() -> i32 {
    let x: isize = 3isize;
    println!("&x={:x}", (&x as *const isize as usize));
    return 0;
}
