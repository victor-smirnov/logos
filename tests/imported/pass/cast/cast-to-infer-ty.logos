// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/cast/cast-to-infer-ty.rs
// Modifications: Logos has no `0 as _` cast-to-inferred-type form
// (no Hindley-Milner). The `*const i32` annotation that drives the
// Rust inference is folded directly into the cast expression.
// `i.is_null()` becomes an explicit `i == 0i64 as *const i32`.
package c_cast_to_infer_ty;

fn main() -> i32 {
    let i: *const i32 = 0i64 as *const i32;
    if i != 0i64 as *const i32 { return 1i32; }
    return 0i32;
}
