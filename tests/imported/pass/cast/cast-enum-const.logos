// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/cast/cast-enum-const.rs
// Modifications: `const QUUX: isize = 5; Bar = QUUX` → `Bar =
// metacall { 5 }`. Logos has no Rust-style const-eval; metacall is
// the compile-time computation channel. C6-cc-03 closed via MP-mc-01.
package cast_enum_const;

use logos.std.fmt;
use logos.mem.string;
use logos.lang.str;

enum Stuff { Bar = metacall { 5i64 } }

fn main() -> i32 {
    assert_eq!(Stuff::Bar as isize, 5isize);
    return 0;
}
