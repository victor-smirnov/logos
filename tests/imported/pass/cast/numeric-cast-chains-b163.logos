// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/cast/ shapes (distilled).
// Modifications: isize->i64; assert->if-return sentinels. `as`-casts across int
//   widths and signedness: widen, truncate, sign-handling, and a float<->int hop.
// Tests `as` value semantics: u8->i64 widen, i64->u8 truncate (300->44), i64->f64
//   and f64->i64 truncation, and a negative i64->u32 wrap.
package numeric_cast_chains_b163;

fn main() -> i32 {
    let a: u8 = 200u8;
    if (a as i64) != 200i64 { return 1i32; }
    if (300i64 as u8) != 44u8 { return 2i32; }
    let f: f64 = 3i64 as f64;
    if f != 3.0f64 { return 3i32; }
    if (3.9f64 as i64) != 3i64 { return 4i32; }
    if (-1i64 as u32) != 4294967295u32 { return 5i32; }
    return 0i32;
}
