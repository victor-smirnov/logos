// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/cast/ shapes (distilled).
// Modifications: isize->i64; assert->if-return sentinels; literals suffixed.
// Tests numeric `as` casts: i64->u8 truncation, int->f64 widening, f64->i64 trunc.
package cast_numeric_chain_b165;

fn main() -> i32 {
    let a: i64 = 300i64;
    let b: u8 = a as u8;
    if b != 44u8 { return 1i32; }          // 300 mod 256
    let c: f64 = 3i64 as f64;
    if c != 3.0f64 { return 2i32; }
    let d: i64 = 3.9f64 as i64;
    if d != 3i64 { return 3i32; }
    let e: i32 = (b as i32) + 1i32;
    if e != 45i32 { return 4i32; }
    return 0i32;
}
