// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/pattern/bindings-after-at/bind-by-copy.rs (mut-binding shape)
// `mut` binding in a match arm pattern then mutating it: `mut x => { x = x + 1 }`.
// isize->i64; assert->return-code.
package mut_binding_pat;
fn f(n: i64) -> i64 { match n { mut x => { x = x + 1i64; x } } }
fn main() -> i32 {
    if f(5i64) == 6i64 { return 0i32; }
    return 1i32;
}
