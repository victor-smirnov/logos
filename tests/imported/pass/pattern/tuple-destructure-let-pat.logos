// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/pattern/ignore-all-the-things.rs (tuple destructure-let shape)
// Tuple destructuring in a `let` binding: `let (a, b, c) = (..)`. (Flat tuple;
// nested-tuple-in-let is a known gap — see notes.) isize->i64; assert->return-code.
package tuple_destructure_let_pat;
fn main() -> i32 {
    let (a, b, c) = (1i64, 2i64, 3i64);
    if a + b + c == 6i64 { return 0i32; }
    return 1i32;
}
