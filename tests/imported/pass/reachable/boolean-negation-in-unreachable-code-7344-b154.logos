// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/reachable/boolean-negation-in-unreachable-code-7344.rs
// Modifications: `""=="" `→ a bool literal comparison; statements after a `return` are
//   dead but must still type-check. `pub fn main()`→`fn main()->i32`.
// Tests: dead statements after `return` (a `!foo()` expression-statement and an `if`
//   over a comparison) are accepted and the fns run cleanly.
package boolean_negation_in_unreachable_code_7344_b154;

fn foo() -> bool { false }

fn bar() {
    return;
    let _ = !foo();
}

fn baz() {
    return;
    if 1i64 == 1i64 {}
}

fn main() -> i32 {
    bar();
    baz();
    return 0i32;
}
