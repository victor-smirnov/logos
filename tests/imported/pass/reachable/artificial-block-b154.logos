// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/reachable/artificial-block.rs
// Modifications: isize→i64; assert_eq!→return-code. Upstream's fn body is a BARE
//   trailing block `{ return 3; }` whose value is the fn result; Logos's return-checker
//   does not see that trailing-block-with-`return` as diverging ("not all paths return"
//   — see B154 notes G154-2), so the block is wrapped in an explicit `return { ... };`
//   which IS accepted. The artificial-block + nested-return essence is preserved.
// Tests: a `return` from inside an artificial `{ }` block reaches the fn result (3).
package artificial_block_b154;

fn f() -> i64 {
    return {
        return 3i64;
    };
}

fn main() -> i32 {
    if f() != 3i64 { return 1i32; }
    return 0i32;
}
