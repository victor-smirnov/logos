// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/reachable/artificial-block.rs (distilled)
// Modifications: `assert_eq!` -> explicit nonzero return; isize -> i64. The
//   nested `{ return 3; }` block-tail form is replaced by a direct `return`
//   followed by (unreachable) code, which preserves the original test's point:
//   typechecking + codegen must not choke on code after a divergence.
//   (Logos gap: a bare block `{ return X; }` used as a fn body tail is not yet
//   recognized as diverging — see Bnever notes.)
// Tests that code after a `return` (unreachable) typechecks and codegens.
package artificial_block_return_bnever;

fn f() -> i64 {
    return 3i64;
    let _unreached: i64 = 99i64;
}

fn main() -> i32 {
    if f() != 3i64 { return 1i32; }
    return 0i32;
}
