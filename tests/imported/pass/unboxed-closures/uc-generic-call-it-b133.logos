// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/unboxed-closures/unboxed-closures-generic.rs
// Modifications: F:FnMut(i32,i32)->i32 bound -> fn-ptr param; closure literal
// coerces to the fn pointer; println! dropped; assert->if-return. Tests a
// higher-order fn taking a two-arg callable and a closure passed into it.
package uc_generic_call_it_b133;

fn call_it(y: i32, f: fn(i32, i32) -> i32) -> i32 {
    return f(2i32, y);
}

fn add(x: i32, y: i32) -> i32 { return x + y; }

fn main() -> i32 {
    let z: i32 = call_it(3i32, add);
    if z != 5i32 { return 1i32; }
    return 0i32;
}
