// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/unboxed-closures/unboxed-closures-zero-args.rs
// Modifications: the original binds `let () = zero();` to destructure the unit
// result; Logos uses a unit-typed closure called for its effect, observed via a
// captured-mutation side effect through a FnMut. Tests both a zero-argument
// closure returning unit (called for effect, twice) and a zero-argument closure
// returning a value bound to a local.
package uc_zero_args_b133;

fn main() -> i32 {
    let mut hits: i64 = 0i64;
    let mut zero = || { hits = hits + 1i64; };
    zero();
    zero();
    if hits != 2i64 { return 1i32; }
    let answer = || -> i64 { return 42i64; };
    if answer() != 42i64 { return 2i32; }
    return 0i32;
}
