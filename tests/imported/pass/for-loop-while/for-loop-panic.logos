// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/for-loop-while/for-loop-panic.rs
// Modifications: `Vec::new()`→`vec_new` (free fn); `isize`→`i64`; the panic body
// is never reached (empty vec) so it's a no-op `{}`; `pub fn main()`→`fn main()->i32`.
// Prelude provides Vec without `use`.
package fl_for_loop_panic;

fn main() -> i32 {
    let x: Vec<i64> = vec_new::<i64>();
    for _ in &x { return 1i32; }
    return 0i32;
}
