// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/for-loop-while/while-loop-constraints-2.rs
// Modifications: `isize` → `i64`; `let mut x: isize` (uninit)
// initialized to `0i64` (Logos rejects uninit `let`). `assert!`
// → if-return.
package fl_while_loop_constraints_2;

fn main() -> i32 {
    let mut y: i64 = 42i64;
    let mut z: i64 = 42i64;
    let mut x: i64 = 0i64;
    while z < 50i64 {
        z = z + 1i64;
        while false { x = y; y = z; }
    }
    if !(y == 42i64 && z == 50i64) { return 1i32; }
    return 0i32;
}
