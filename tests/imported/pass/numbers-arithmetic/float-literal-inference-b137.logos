// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/numbers-arithmetic/float-literal-inference.rs
// Modifications: literals suffixed; println! dropped; the f32 local, f64 local,
//   and an f64 struct field are constructed and checked via nonzero ret.
// Tests float-literal inference into f32 / f64 locals and an f64 struct field.
package float_literal_inference_b137;

struct S {
    z: f64,
}

fn main() -> i32 {
    let x: f32 = 4.0f32;
    if x != 4.0f32 { return 1i32; }
    let y: f64 = 64.0f64;
    if y != 64.0f64 { return 2i32; }
    let z: S = S { z: 1.0f64 };
    if z.z != 1.0f64 { return 3i32; }
    return 0i32;
}
