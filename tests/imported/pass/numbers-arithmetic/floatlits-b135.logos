// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/numbers-arithmetic/floatlits.rs
// Modifications: assert! → return-code checks; exponent-literal mantissas given
// a decimal point (`5e-11`→`5.0e-11`) — Logos requires `<digits>.<digits>` before `e`.
// Tests f64 literal parsing incl. a scientific-notation literal and ordering.
package floatlits_b135;

fn main() -> i32 {
    let f = 4.999999999999f64;
    if !(f > 4.90f64) { return 1i32; }
    if !(f < 5.0f64) { return 2i32; }
    let g = 4.90000000001e-10f64;
    if !(g > 5.0e-11f64) { return 3i32; }
    if !(g < 5.0e-9f64) { return 4i32; }
    return 0i32;
}
