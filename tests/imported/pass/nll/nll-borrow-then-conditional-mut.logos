// Borrow then conditional mut (borrow already dead).
package nll_borrow_then_conditional_mut;
fn main() -> i32 {
    let mut x: i32 = 0i32;
    let r: &i32 = &x;
    let _v: i32 = *r;
    if x == 0i32 { x = 5i32; }
    return x - 5i32;
}
