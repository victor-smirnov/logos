// Match scrutinee borrow ends at arm body for non-binding patterns.
package match_borrow_scrutinee;

fn main() -> i32 {
    let mut x: i32 = 5i32;
    match x {
        0i32 => { x = 1i32; }
        _    => { x = 2i32; }
    }
    return x - 2i32;
}
