package label_continue_with_mut;
fn main() -> i32 {
    let mut x: i32 = 0i32;
    let mut i: i32 = 0i32;
    'outer: while i < 3i32 {
        i = i + 1i32;
        if i == 2i32 { continue 'outer; }
        let m: &mut i32 = &mut x;
        *m = *m + 1i32;
    }
    return x - 2i32;
}
