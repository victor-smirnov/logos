// 2026-09-22: THIS PORT IS LEGAL RUST and lands under pass/. MEASURED with rustc
// 1.98.1 --edition 2024 by path: it compiles clean — under edition 2024 a return-position `impl Trait` captures every in-scope lifetime, including `x`'s.
// The row's upstream error belongs to an older edition or rustc.
// rustc nll/ty-outlives/impl-trait-captures.rs — upstream E0700
// imported batch B167 @ da5114692c9ebe46b869488c5f34f92eb10b98c1 (rust 1.100.0)
// shelf: admit · root nllmoves.NEW-N1
// port of tests/ui/nll/ty-outlives/impl-trait-captures.rs
// the opaque return type captures the (elided) lifetime of `x`, which does not
// appear in its bounds -> rustc E0700
package impl_trait_captures;
trait Foo<'a> { fn go(self: &Self) -> i64; }
struct W<'a> { r: &'a i64 }
impl<'b, 'a> Foo<'a> for W<'b> { fn go(self: &W) -> i64 { return *self.r; } }
fn foo<'a>(x: &i64) -> impl Foo<'a> { return W { r: x }; }
fn main() -> i32 { let v = 1i64; return foo(&v).go() as i32; }
