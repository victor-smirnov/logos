// Imported from rust-lang/rust tests/ui/nll/issue-50461-used-mut-from-moves.rs
// Modifications: dropped println; exit-code main.
package r_nll_issue_50461_used_mut_from_moves;

struct Foo {
    value: i32,
}

fn use_foo_mut(foo_in: Foo) -> i32 {
    let mut foo = foo_in;
    foo = foo;
    return foo.value;
}

fn main() -> i32 {
    let v = use_foo_mut(Foo { value: 413i32 });
    if v != 413i32 { return 1; }
    return 0;
}
