// B83: two disjoint mut field borrows on flat struct.
package df_disjoint_flat;
struct P { a: i32, b: i32 }
fn main() -> i32 {
    let mut p: P = P { a: 0i32, b: 0i32 };
    let ra: &mut i32 = &mut p.a;
    let rb: &mut i32 = &mut p.b;
    *ra = 3i32; *rb = 4i32;
    return p.a + p.b - 7i32;
}
