// B72: shared borrow of x dead; move other variable y is fine.
package nll_borrow_then_move_other;

struct S { v: i64 }

fn take(s: S) -> i64 { return s.v; }

fn main() -> i32 {
    let x: i32 = 1i32;
    let y: S = S { v: 5i64 };
    let _r: &i32 = &x;
    return (take(y) - 5i64) as i32;
}
