// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/regions/regions-infer-borrow-scope-view.rs
// Modifications: isize→i64; Vec/vec! → a fixed `[i64;3]` array (B111); a `&[T;3]`
//   coerces to `&[T]`; assert! → ret.
// Tests a slice-identity fn `view<T>(&[T])->&[T]` applied twice (view of a view); the
//   returned borrows share the same backing storage.
package regions_infer_borrow_scope_view_b147;

fn view(x: &[i64]) -> &[i64] { return x; }

fn main() -> i32 {
    let v: [i64; 3] = [1i64, 2i64, 3i64];
    let x = view(&v);
    let y = view(x);
    if v[0] != x[0] { return 1i32; }
    if v[0] != y[0] { return 2i32; }
    return 0i32;
}
