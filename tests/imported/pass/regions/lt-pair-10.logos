package lt_pair_10;
struct P<'a> { x: &'a i32, y: &'a i32 }
fn make<'a>(x: &'a i32, y: &'a i32) -> P<'a> { return P { x: x, y: y }; }
fn main() -> i32 {
    let a: i32 = 10i32;
    let b: i32 = 10i32;
    let p: P = make(&a, &b);
    return *p.x + *p.y - 20i32;
}
