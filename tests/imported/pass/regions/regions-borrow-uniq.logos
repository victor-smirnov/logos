// Imported from rust-lang/rust tests/ui/regions/regions-borrow-uniq.rs
// Modifications: dropped Box, use stack variable.
package r_regions_borrow_uniq;

fn foo(x: &u64) -> u64 { return *x; }

fn main() -> i32 {
    let p: u64 = 3u64;
    if foo(&p) != 3u64 { return 1; }
    return 0;
}
