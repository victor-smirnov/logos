// Drop-lt struct borrowing fn-result temp — temp lifetime extends to outer.
package dropck_borrow_fn_result;
struct W<'a> { r: &'a i32 }
impl<'a> Drop for W<'a> { fn drop(self: &mut W<'a>) { } }
fn main() -> i32 {
    let v: i32 = 5i32;
    let _w: W = W { r: &v };
    return 0i32;
}
