// B79 GAP: enum-lifetime-substitution check does not currently fire.
// Pinned as gap-regression; Rust rejects this case.
// B65: enum with two lt params; return type with mismatched lt.
package enum_lt_mix;

enum E<'a, 'b> { L(&'a i32), R(&'b i32) }

fn bad<'a, 'b>(x: &'a i32) -> E<'b, 'a> { return E::L(x); }

fn main() -> i32 { return 0i32; }
