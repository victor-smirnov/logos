// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/overloaded/overloaded-calls-object-two-args.rs
// Modifications: isize→i64; closure block body; assert_eq!→ret-guard.
// Tests calling a 2-arg `&mut dyn FnMut(i64,i64) -> i64` trait-object through a fn param.
package overloaded_calls_object_two_args_b142;

fn foo(f: &mut dyn FnMut(i64, i64) -> i64) -> i64 {
    return f(1i64, 2i64);
}

fn main() -> i32 {
    let mut c = |x: i64, y: i64| { return x * 10i64 + y; };
    let z: i64 = foo(&mut c);
    if z != 12i64 { return 1i32; }
    return 0i32;
}
