// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/iterators/ shapes (distilled).
// Modifications: isize->i64; assert->if-return sentinels; iterator-adapter chain
//   reshaped to a `for x in &arr` loop yielding `&i64` (deref to read).
// Tests for-by-reference iteration over a fixed array, dereferencing each element.
package for_ref_array_doubled_b165;

fn main() -> i32 {
    let xs: [i64; 5] = [1i64, 2i64, 3i64, 4i64, 5i64];
    let mut s: i64 = 0i64;
    for x in &xs {
        s = s + *x * 2i64;
    }
    if s != 30i64 { return 1i32; }
    return 0i32;
}
