// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/iterators/ shapes (distilled).
// Modifications: isize->i64; assert->if-return sentinels; adapter chain reshaped to
//   an indexed loop computing a filtered-then-squared sum and a max.
// Tests a filter-then-map-then-sum and a running max over a fixed array.
package indexed_filter_sum_b165;

fn main() -> i32 {
    let xs: [i64; 6] = [1i64, 2i64, 3i64, 4i64, 5i64, 6i64];
    let mut acc: i64 = 0i64;
    let mut mx: i64 = 0i64;
    let mut i: i64 = 0i64;
    while i < 6i64 {
        if xs[i] % 2i64 == 0i64 { acc = acc + xs[i] * xs[i]; }
        if xs[i] > mx { mx = xs[i]; }
        i = i + 1i64;
    }
    if acc != 56i64 { return 1i32; }    // 4 + 16 + 36
    if mx != 6i64 { return 2i32; }
    return 0i32;
}
