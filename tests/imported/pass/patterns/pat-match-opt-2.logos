package pat_match_opt_2;
enum Opt<T> { Some(T), None }
fn main() -> i32 {
    let o: Opt<i32> = Opt::Some(2i32);
    let r: i32 = match o {
        Opt::Some(x) => x,
        Opt::None    => 0i32,
    };
    return r - 2i32;
}
