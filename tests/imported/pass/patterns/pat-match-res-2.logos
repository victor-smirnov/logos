package pat_match_res_2;
enum Res<T, E> { Ok(T), Err(E) }
fn main() -> i32 {
    let r: Res<i32, i32> = Res::Ok(2i32);
    let v: i32 = match r {
        Res::Ok(x)  => x,
        Res::Err(_) => -1i32,
    };
    return v - 2i32;
}
