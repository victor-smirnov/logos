package pat_match_opt_4;
enum Opt<T> { Some(T), None }
fn main() -> i32 {
    let o: Opt<i32> = Opt::Some(4i32);
    let r: i32 = match o {
        Opt::Some(x) => x,
        Opt::None    => 0i32,
    };
    return r - 4i32;
}
