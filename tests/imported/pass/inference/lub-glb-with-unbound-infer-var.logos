// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/inference/lub-glb-with-unbound-infer-var.rs
// Modifications: closure param explicitly `_x: i32` and return
// `-> ()` (Logos requires explicit closure types). fn-ptr type
// `fn(_)` → `fn(i32) -> ()` (no Logos `_` infer slot).
package inf_lub_glb_with_unbound_infer_var;

fn main() -> i32 {
    let a_f: fn(i32) -> () = |_x: i32| -> () { };
    let b_f: fn(i32) -> () = |_x: i32| -> () { };
    let c_f: fn(i32) -> () = match 22i32 {
        0i32 => a_f,
        _ => b_f,
    };
    c_f(4i32);
    return 0i32;
}
