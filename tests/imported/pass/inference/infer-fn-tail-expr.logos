// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/inference/infer-fn-tail-expr.rs
// Modifications: `Vec::new()` → `vec_new::<i64>()`. `isize` → `i64`.
package inf_infer_fn_tail_expr;

use std.fmt;
use logos.mem.string;
use logos.lang.str;
use logos.lang.option;
use std.collections.vec;

fn f() -> Vec<i64> { return vec_new::<i64>(); }

fn main() -> i32 {
    let _ = f();
    return 0i32;
}
