// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/inference/ shapes (distilled).
// Modifications: isize->i64; literals suffixed; assert->if-return. Type of a
//   generic identity result is fixed by the call's turbofish / the surrounding
//   annotation; an `if`-expression's arms unify to the annotated type.
// Tests type propagation through a generic identity fn and an `if`-expression whose
//   branches unify to the let-annotated type.
package infer_from_return_b163;

fn id<T>(x: T) -> T { return x; }

fn main() -> i32 {
    let a: i64 = id::<i64>(42i64);
    if a != 42i64 { return 1i32; }
    let b: bool = id::<bool>(true);
    if !b { return 2i32; }
    let c: i64 = if a > 0i64 { 1i64 } else { -1i64 };
    if c != 1i64 { return 3i32; }
    return 0i32;
}
