// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/inference/generic-type-inference-10436.rs
// Modifications: `vec![x]` → `vec_from_arr([x])`. `works` uses
// `T: Copy` for vec_from_arr's input. `also_works` arm dropped
// (Clone bound; Logos's Copy bound here is sufficient). Explicit
// turbofish on `works::<u64>` since Logos can't infer T just from
// the `let _: Vec<u64>` annotation.
package inf_generic_type_inference_10436;

use std.fmt;
use logos.mem.string;
use logos.lang.str;
use logos.lang.option;
use std.collections.vec;

fn works<T: Copy>(x: T) -> Vec<T> { return vec_from_arr([x]); }

fn main() -> i32 {
    let _: Vec<u64> = works::<u64>(0u64);
    return 0i32;
}
