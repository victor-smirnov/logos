// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/enum/issue-23304-2.rs
// Modifications: isize→i64; `X::A as isize` discriminant initializer pre-evaluated
// to a literal (Logos enum discriminants take a literal, not a const expression);
// assert_eq! → return-code checks. Tests an explicit discriminant value read via `as`.
package enum_discr_derived_b135;

enum X { A = 42 }

enum Y { A = 42 }

fn main() -> i32 {
    let x = X::A;
    let x2 = x as i64;
    if x2 != 42i64 { return 1i32; }
    if (Y::A as i64) != 42i64 { return 2i32; }
    return 0i32;
}
