package enum_gen_3;
enum Box<T> { Filled(T), Empty }
fn main() -> i32 {
    let b: Box<i32> = Box::Filled(3i32);
    let v: i32 = match b {
        Box::Filled(x) => x,
        Box::Empty     => 0i32,
    };
    return v - 3i32;
}
