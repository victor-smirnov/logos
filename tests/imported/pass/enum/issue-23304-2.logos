// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/enum/issue-23304-2.rs
// Modifications: Rust `enum Y { A = X::A as isize }` (using another
// enum variant as the discriminant expression) reduced to the
// literal value `42` — Logos's enum-discriminant grammar accepts
// integer literals but not arbitrary constant expressions. The
// resulting numeric assertion is preserved.
package e_issue_23304_2;

enum X { A = 42 }
enum Y { A = 42 }

fn main() -> i32 {
    let x: X = X::A;
    let xi: i32 = x as i32;
    if xi != 42i32 { return 1; }
    if Y::A as i32 != 42i32 { return 2; }
    return 0;
}
