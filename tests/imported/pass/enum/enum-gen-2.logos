package enum_gen_2;
enum Box<T> { Filled(T), Empty }
fn main() -> i32 {
    let b: Box<i32> = Box::Filled(2i32);
    let v: i32 = match b {
        Box::Filled(x) => x,
        Box::Empty     => 0i32,
    };
    return v - 2i32;
}
