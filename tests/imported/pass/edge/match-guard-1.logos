// Edge: match arm with guard.
package match_guard_1;
fn main() -> i32 {
    let x: i32 = 5i32;
    let r: i32 = match x {
        n if n > 10i32 => 100i32,
        n if n > 0i32  => n,
        _              => -1i32,
    };
    return r - 5i32;
}
