package two_mut_args_2;
fn swap(a: &mut i32, b: &mut i32) {
    let t: i32 = *a;
    *a = *b;
    *b = t;
}
fn main() -> i32 {
    let mut x: i32 = 2i32;
    let mut y: i32 = 4i32;
    swap(&mut x, &mut y);
    return x - 4i32;
}
