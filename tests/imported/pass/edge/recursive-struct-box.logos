package recursive_struct_box;
use logos.mem.boxed;
struct Node { v: i32, next: Box<Option_> }
enum Option_ { Some(Node), None }
fn main() -> i32 {
    let n: Node = Node { v: 7i32, next: box_new(Option_::None) };
    return n.v - 7i32;
}
