package while_cond_method;
use std.collections.vec;
fn main() -> i32 {
    let mut v: Vec<i32> = vec_new::<i32>();
    while v.length() < 5i64 { v.push(0i32); }
    return v.length() as i32 - 5i32;
}
