// GAP: nested struct destructure pattern `let Outer { i: Inner { v: x }, .. } = o;`
// is not yet supported. Sema rejects: "nested patterns not yet supported; bind
// to a name". Pinned regression — needs pattern lowering enhancement.
package nested_struct_destr_gap;
struct Inner { v: i32 }
struct Outer { i: Inner, k: i32 }
fn main() -> i32 {
    let _o: Outer = Outer { i: Inner { v: 5i32 }, k: 3i32 };
    return 0i32;
}
