package vec_push_2;
use logos.mem.collections.vec;
fn main() -> i32 {
    let mut v: Vec<i32> = vec_new::<i32>();
    let mut i: i32 = 0i32;
    while i < 2i32 {
        v.push(i);
        i = i + 1i32;
    }
    return v.length() as i32 - 2i32;
}
