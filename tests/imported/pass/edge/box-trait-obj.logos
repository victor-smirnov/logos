package box_trait_obj;
use logos.mem.boxed;
trait Speak { fn val(self: &Self) -> i32; }
struct S { v: i32 }
impl Speak for S { fn val(self: &S) -> i32 { return self.v; } }
fn main() -> i32 {
    let s: S = S { v: 7i32 };
    let b: Box<dyn Speak> = box_new(s);
    return b.val() - 7i32;
}
