package cl_mut_no_leak;
fn main() -> i32 {
    let mut x: i32 = 0i32;
    {
        let r: &mut i32 = &mut x;
        let f = || -> i32 { *r = 5i32; return *r; };
        let _v: i32 = f();
    }
    return x - 5i32;
}
