package vec_push_1;
use std.collections.vec;
fn main() -> i32 {
    let mut v: Vec<i32> = vec_new::<i32>();
    let mut i: i32 = 0i32;
    while i < 1i32 {
        v.push(i);
        i = i + 1i32;
    }
    return v.length() as i32 - 1i32;
}
