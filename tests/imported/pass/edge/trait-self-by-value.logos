package trait_self_by_value;
trait Consume { fn val(self: Self) -> i32; }
struct S { v: i32 }
impl Consume for S { fn val(self: S) -> i32 { return self.v; } }
fn main() -> i32 {
    let s: S = S { v: 7i32 };
    return s.val() - 7i32;
}
