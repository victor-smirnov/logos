package trait_with_drop;
trait Speak { fn speak(self: &Self) -> i32; }
struct D { v: i32 }
impl Drop for D { fn drop(self: &mut D) { } }
impl Speak for D { fn speak(self: &D) -> i32 { return self.v; } }
fn main() -> i32 {
    let d: D = D { v: 5i32 };
    return d.speak() - 5i32;
}
