package cl_deep_capture;
fn apply<F: FnOnce() -> i32>(f: F) -> i32 { return f(); }
fn main() -> i32 {
    let mut x: i32 = 0i32;
    let r: &mut i32 = &mut x;
    let f = || -> i32 { *r = 7i32; return *r; };
    return apply(f) - 7i32;
}
