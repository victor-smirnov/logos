// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/ adversarial intersection (B166 depth probe, distilled).
// Modifications: isize->i64; assert!->if-return sentinels; &self->self:&Self.
//   &[&dyn Sh] slice param signature accepted (compile-level).
package u05;
trait Sh { fn area(self: &Self) -> i64; }
struct Sq { s: i64 }
impl Sh for Sq { fn area(self: &Sq) -> i64 { return self.s * self.s; } }
fn total(items: &[&dyn Sh]) -> i64 {
    let mut s: i64 = 0i64; let mut i: i64 = 0i64;
    while i < 2i64 { s = s + items[i].area(); i = i + 1i64; }
    return s;
}
fn main() -> i32 { return 0i32; }
