// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/traits/object/ shapes (distilled).
// Modifications: isize->i64; assert->if-return sentinels; &self->self:&Self;
//   unit structs given a dummy field (`struct X;` form is unsupported).
// Tests dynamic dispatch through a `&dyn Trait` function parameter, two impls.
package dyn_arg_dispatch_b165;

trait Speak { fn val(self: &Self) -> i64; }
struct Dog { tag: i64 }
struct Cat { tag: i64 }
impl Speak for Dog { fn val(self: &Dog) -> i64 { return 1i64; } }
impl Speak for Cat { fn val(self: &Cat) -> i64 { return 2i64; } }
fn ask(s: &dyn Speak) -> i64 { return s.val(); }
fn main() -> i32 {
    let d = Dog { tag: 0i64 };
    let c = Cat { tag: 0i64 };
    if ask(&d) != 1i64 { return 1i32; }
    if ask(&c) != 2i64 { return 2i32; }
    return 0i32;
}
