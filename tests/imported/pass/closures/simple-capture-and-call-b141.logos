// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/closures/simple-capture-and-call.rs
// Modifications: closure given a block body (`|| { x }`); usize→u64; the
//   captured value is checked via a ret guard.
// Tests a zero-arg closure capturing an enclosing `let` by value and returning
//   it when called.
package simple_capture_and_call_b141;

fn main() -> i32 {
    let x: u64 = 1u64;
    let y = || { return x; };
    let z: u64 = y();
    if z != 1u64 { return 1i32; }
    return 0i32;
}
