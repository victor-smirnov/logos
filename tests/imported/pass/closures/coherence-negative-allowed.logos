// B91: negative impl + positive impl coexist.
package coherence_negative_allowed;
trait T { fn x(self: &Self) -> i32; }
struct S { _x: i32 }
impl T for S { fn x(self: &S) -> i32 { return 1i32; } }
fn main() -> i32 {
    let s: S = S { _x: 0i32 };
    return s.x() - 1i32;
}
