// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/binding/fn-pattern-expected-type.rs
// Modifications: faithful — tuple-destructure in closure parameter
// (`|(x, y): (isize, isize)| { … }`) now supported (C5-cl-07 closed
// 2026-05-11). Closure-return-type inference (C5-cl-06) makes the
// annotation-less unit-returning body work.
package b_fn_pattern_expected_type;

use logos.std.fmt;
use logos.mem.string;
use logos.lang.str;

fn main() -> i32 {
    let f = |(x, y): (isize, isize)| {
        assert_eq!(x, 1isize);
        assert_eq!(y, 2isize);
    };
    f((1isize, 2isize));
    return 0;
}
