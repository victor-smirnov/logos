// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/binding/match-join.rs
// Modifications: `isize`→`i64`; `Vec::new()`→`vec_new`; turbofish on None/Some
// dropped (inferred); the dead `if true {} else {..}` branch kept (regression
// point); `println!` dropped; `pub fn main()`→`fn main()->i32`. Option/Vec from the
// implicit prelude (no `use`).
package binding_match_join;

fn foo<T>(y: Option<T>) {
    let mut x: i64 = 0i64;
    let mut rs: Vec<i64> = vec_new::<i64>();
    if true {
    } else {
        match y {
            None => { x = 17i64; }
            _ => { x = 42i64; }
        }
        rs.push(x);
    }
    return;
}

fn main() -> i32 {
    foo::<i64>(Some(5i64));
    return 0i32;
}
