// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/binding/func-arg-wild-pattern.rs
// Modifications: faithful — `fn foo((x, _): (isize, isize)) -> isize`
// tuple-destructure with wildcard in fn-parameter position now
// supported (P4-pm-19 closed 2026-05-11; uses synth tuple-typed
// param + body-prologue `let user_k = synth.k;` for each binding).
package b_func_arg_wild_pattern;

use std.fmt;
use std.lang.text;

fn foo((x, _): (isize, isize)) -> isize {
    return x;
}

fn main() -> i32 {
    assert_eq!(foo((22isize, 23isize)), 22isize);
    return 0;
}
