// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/binding/match-bot.rs
// Modifications: `println!` dropped; assert via direct comparison.
// Turbofish on enum variant ctor (G3-tg-01) un-trimmed 2026-05-11.
package match_bot;

use logos.std.fmt;
use logos.mem.string;
use logos.lang.str;
use logos.lang.option;

fn main() -> i32 {
    let i: isize = match Option::Some::<isize>(3isize) {
        Option::None => { return 99; }
        Option::Some(_) => 5isize,
    };
    assert_eq!(i, 5isize);
    return 0;
}
