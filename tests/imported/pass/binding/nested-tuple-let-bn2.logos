// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/binding/pat-tuple-*.rs (nested tuple let).
// Modifications: isize->i64; assert->if-return. Tests a nested
// tuple-destructuring `let`: `let (a, (b, c)) = (1, (2, 3))` binds all
// three names through one pattern with an inner tuple sub-pattern.
package nested_tuple_let_bn2;

fn main() -> i32 {
    let (a, (b, c)) = (1i64, (2i64, 3i64));
    if a != 1i64 { return 1i32; }
    if b != 2i64 { return 2i32; }
    if c != 3i64 { return 3i32; }
    let ((p, q), r) = ((10i64, 20i64), 30i64);
    if p + q + r != 60i64 { return 4i32; }
    return 0i32;
}
