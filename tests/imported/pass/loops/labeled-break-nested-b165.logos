// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/loops/ shapes (distilled).
// Modifications: isize->i64; assert->if-return sentinels.
// Tests a labeled `break 'outer` escaping two nested while loops.
package labeled_break_nested_b165;

fn main() -> i32 {
    let mut found: i64 = -1i64;
    let mut i: i64 = 0i64;
    'outer: while i < 5i64 {
        let mut j: i64 = 0i64;
        while j < 5i64 {
            if i * j == 6i64 { found = i * 10i64 + j; break 'outer; }
            j = j + 1i64;
        }
        i = i + 1i64;
    }
    if found != 23i64 { return 1i32; }
    return 0i32;
}
