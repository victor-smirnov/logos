// Imported from rust-lang/rust@4b0c9d76ae7d387229caea55cfa73c280b08b8a7
// Original path: tests/ui/numeric/ref-int.rs
// Modifications: original `assert_eq!(&x, &y)` requires Display-for-&T
// blanket impl (Logos doesn't have it). Comparison form preserved:
// auto-deref at `==` matches Rust's PartialEq-for-&T blanket and is
// implemented in mlir_gen_expr for &T → &<primitive>.
package ref_int;

use std.fmt;
use logos.mem.string;
use logos.lang.str;

fn main() -> i32 {
    let x: i32 = 1i32;
    let y: i32 = 1i32;
    let xr: &i32 = &x;
    let yr: &i32 = &y;
    let eq: bool = xr == yr;
    assert!(eq);
    return 0;
}
