// @rule pat.variant.binding-arity-check
package pat_diag_3_variant_binding_arity;

enum E { A(i32, i32), B }

fn main() -> i32 {
    let e: E = E::A(1i32, 2i32);
    match e {
        E::A(x) => { return x; }
        _ => { return 0i32; }
    }
}
