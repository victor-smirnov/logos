// @rule pat.or.same-binding-set
package pat_diag_1_or_binding_set;

enum E { A(i32), B(i32) }

fn main() -> i32 {
    let e: E = E::A(1i32);
    match e {
        E::A(x) | E::B(y) => { return x; }
    }
}
