// @rule stmt.let-else.diverging-else
package stmt_diag_2_let_else_nondiverging;

enum Opt { Some(i32), None }

fn main() -> i32 {
    let o: Opt = Opt::Some(5i32);
    let Opt::Some(x) = o else { let _y: i32 = 0i32; };
    return x;
}
