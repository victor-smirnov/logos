// @rule pat.union.one-field-unsafe
package pat_diag_2_union_unsafe;

union U { a: i32, b: f32 }

fn main() -> i32 {
    let u: U = U { a: 5i32 };
    match u {
        U { a } => { return a; }
    }
}
