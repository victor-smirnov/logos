// @rule metaprog.fn-macro.args-are-expr-list
package metaprog_diag_1_fnmacro_args_not_expr;

use logos.std.compiler.metaprog;

#[fn_macro]
pub fn ident(e: ExprBlob) -> ExprBlob { return e; }

fn main() -> i32 {
    let v: i32 = ident!(struct Z);
    return v;
}
