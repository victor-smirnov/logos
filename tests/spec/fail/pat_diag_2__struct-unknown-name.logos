// @rule pat.struct.unknown-name
package pat_diag_2_struct_unknown;

struct P { x: i32, y: i32 }

fn main() -> i32 {
    let p: P = P { x: 1i32, y: 2i32 };
    match p {
        Nope { x, y } => { return x; }
    }
}
