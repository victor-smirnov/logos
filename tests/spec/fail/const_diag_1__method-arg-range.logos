// @rule const.intlit.method-arg-range-check
package const_diag_1_method_arg_range;

struct S { v: i32 }

impl S {
    fn take(&self, x: u8) -> i32 { return self.v; }
}

fn main() -> i32 {
    let s: S = S { v: 0i32 };
    return s.take(99999);
}
