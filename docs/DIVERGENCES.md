# Logos ↔ Rust divergences — the single source of truth

This is the canonical, language-wide register of where Logos behaves
differently from Rust. It governs the gap-grind triage: **on every failing
imported test you must classify the cause as either a blessed divergence
(§A) or a catch-up TODO (§B) — never leave a silent green or a silent
"that's just how Logos is".**

## The rule (read first)

A difference from Rust is a **blessed divergence** *only* if it is one of:

- **(a) Explicitly replaced** — Rust has a mechanism; Logos provides a
  *different, named* mechanism that covers the capability (e.g. `const fn`
  → `metacall`). The Rust mechanism will not be added.
- **(b) Deliberate design model** — Logos chose a different model on purpose,
  documented, with the capability preserved (e.g. async *colour* → green
  fibres).
- **(c) Logos addition** — Logos has it, Rust doesn't (variadics, Writ,
  metaprog). Not a parity concern; listed for completeness.

**Everything else that currently differs from Rust is NOT a divergence — it
is a catch-up TODO marked "НЕ ОТКЛАДЫВАТЬ" (§B), with a convergence plan.**
"Edge case rare — defer" / "deferred indefinitely" is a banned classification
(see `feedback_full_rust_parity_now`). The only legitimate non-blessed delay
is an item explicitly gated on a *named* prerequisite roadmap step.

Discipline (`project_gap_grind_strategy`): **no green without an explicit
reason** — a red imported test is either a real gap (→ fix now), a §B catch-up
TODO (→ scheduled, not deferred), or a §A blessed divergence (→ re-import via
the replacement mechanism, e.g. `metacall`-splice; do NOT leave it trimmed).

---

## §A — Blessed divergences (intentional; won't converge to the Rust form)

| # | Area | Rust | Logos | Kind | Notes |
|---|---|---|---|---|---|
| A1 | const-eval | compile-time const folding (miri) | `metacall { … }` (full Logos on JIT, splices a literal) | replaced | const at array-length / enum-discriminant / type position is this. **Re-import the test via metacall**, don't trim. If metacall is rejected in that position → that's a *metacall gap* to fix, NOT a divergence. Const-**generics** `<const N: i64>` are NOT this (they work). See `project_no_const_eval`. |
| A2 | `const fn` | const-evaluable fn | plain `fn`; const-context call sites rewrite to `metacall` | replaced | `feedback_const_fn_via_metacall`. Any inability = a metacall gap, not a const-fn gap. |
| A3 | `macro_rules!` / proc-macros / `#[derive]` | declarative + procedural macros | `metaprog` handlers + `quote_*!` / fn-macros over `metacall` | replaced | The expressive macro layer. `:tt` "any token tree" fragment → Logos requires an explicit fragment choice. |
| A4 | async / await / `Future` / `Pin` | stackless coroutines, function "colour" | green fibres + reactor; every fn is implicitly suspendable, no colour | design model | The *capability* exists (write the sync form; the fibre runtime makes it non-blocking). The colour mechanism itself is intentionally absent. `tests/ui/async-await/*` permanently skipped (`WHY-WE-SKIP`). |
| A5 | rustc-internal attrs: `#[lang]`, `#[rustc_*]`, `#[stable]`/`#[unstable]`, `#[diagnostic::*]` | compiler-internal hooks / stability / diagnostics | distilled to a Logos mechanism (`is_anyval`, `#[writ_eidos]`, …) or stripped | replaced/N-A | No stability-tracking concept; lang-item roles are wired compiler-side. |
| A6 | (additions) — variadics (`A...`), Writ datatype/view fabric, metaprog/metacall, fibres | n/a | Logos-only features | addition | Not parity items; here so "Logos has X, Rust doesn't" isn't mistaken for a gap. |
| A7 | panic strategy | `-C panic=unwind` (default) or `=abort`; stack-unwinding runs `Drop` frame-by-frame; `catch_unwind` returns `Result<R, Box<dyn Any + Send>>`; `UnwindSafe`/`RefUnwindSafe` enforced; `extern "C-unwind"` ABI for unwind across FFI | **abort, only.** `panic!()` → `abort()` (`stdlib/lang/panic/panic.logos:29-39`). NO stack-unwinding; `Drop` does NOT run on the panic path. App-level recovery is **supervisor-process** style: a worker that panics dies hard; a monitor process detects the death, cleans up out-of-process resources the OS doesn't reclaim (named shared memory, on-disk locks, registered services, external-system state), then restarts or escalates. The model is Erlang/OTP "let it crash", not in-process `Drop`-on-unwind. `panic = "unwind"` is not a mode and never will be. | design model | Capability "fail fast on unrecoverable error" + "recover via external supervision" is preserved — fallible in-process APIs use `Result<T, E>` (Rust-idiomatic anyway); cross-process recovery uses the supervisor pattern. Implications: (a) `catch_unwind` cannot be a real unwind catcher — the current `stdlib/lang/panic/panic.logos:88` setjmp-based form is a **test-harness shortcut** for `#[should_panic]` that intentionally skips destructors; not part of the language model. Outside the harness it should be considered absent (the supervisor pattern replaces it for production recovery). (b) `UnwindSafe`/`RefUnwindSafe`/`AssertUnwindSafe` exist as empty marker traits so Rust source ports parse (`panic.logos:116-124`); they have no enforcement to do. (c) `extern "C-unwind"` ABI is absent (a panic never leaves a Logos frame, so the FFI-unwind question is moot). (d) `#[cfg(panic = "abort")]` is not a meaningful gate (strategy is fixed); recognising it would be cosmetic. (e) Resources that survive worker death require **explicit unregister hooks the supervisor can run** (named shm, lockfiles, etc.); idiom is to register with the supervisor at creation, NOT to rely on `Drop` for them — `Drop` is for in-process resources only. (f) Imported tests that rely on unwinding through `Drop` for cleanup ordering, on observing post-panic state, or on `catch_unwind` recovering a real value → **permanently skipped** (extend `tests/imported/WHY-WE-SKIP.md`). See `docs/language/feature-audit/O-other-panic-divergence.md` for the surface inventory. |

---

| A8 | `Pin`/`Unpin`/`PhantomPinned` | pinning is a property of the **pointer** (`Pin<P>`): a type is freely movable until pinned; `PhantomPinned` opts out of `Unpin`; the unsafe `Pin` contract guards the "movable until anchored" phase (exists chiefly for stackless coroutines/futures) | **Both models now coexist (2026-06-12).** `#[pinned]` remains the native model — a property of the **type**: no value-form at all (`is_non_movable_type`); arena residents are born in the never-move arena and never exist as a stack value. AND the Rust-side API is implemented for ports/public APIs: `logos.lang.pin.Pin<P>` (`new`/`into_inner`/`get_ref`/`get_mut`/`as_ref`/`as_mut`/unsafe `new_unchecked`/`get_unchecked_mut`, `box_pin`, `Deref`/cond-`DerefMut where T: Unpin`), with `Unpin` as a real auto trait: structural by default, `&T`/`&mut T`/`*T` always `Unpin`, `PhantomPinned` and `#[pinned]` types are `!Unpin`, negative impls honored. | design model | The *reason* the divergence stays: (1) the zone/Writ memory model anchors at *birth*, so the movable-until-pinned phase doesn't arise for `#[pinned]` types (they have no safe `Pin::new` path — only `box_pin`/`new_unchecked`); (2) per A4, async = **stackful** fibres, so the self-referential-state-machine pressure that forced `Pin` into Rust's API surface never appears. Tests: pin_basic, pin_box, pin_unpin_auto, fail/pin_unpin_phantom, fail/pin_get_mut_not_unpin. |

---

| A9 | module-path syntax | all-`::`: `crate::mod::Item`; `use … as Alias` rename | `.`-separated PACKAGE + `::` into the item: `my.cool.pkg::Item`, `my.pkg.Type::method`, no-paren `my.pkg.Enum::Variant` / `my.pkg.Type::CONST`. `use pkg.path` binds the short name; **`use … as Alias` is design-gated** — the rename has no role in the dotted-package model (`use` already introduces the short name). | design model | `ref_logos_path_model`. Fully-qualified dotted+`::` in expr/type position parses (T2-28, `5e9203ad`+); residual `pkg.Type<A>` generic-qualified type-position uses `use`+short-name. Imported tests using `as`-rename → rewrite to `use`+short-name (T2-20). |
| A10 | `dyn Fn` / `FnMut` / `FnOnce` | a distinct trait object per Fn-trait (fat `&dyn Fn(…)` with its own vtable) | sema resolves `dyn Fn*(…)` to the bare **Closure** type — `dyn Fn*` is already a fat `{fn-ptr, env}` pair, so there is no separate Fn-trait-object layer (`sema.cpp:1872-1873`); `&dyn FnMut(…)` annotations are accepted and collapse to Closure. | replaced | A bare `dyn Fn` without `+ Send` is conservatively `!Send` (sema_auto_trait.cpp:244), matching Rust's `dyn Fn()`. No `dyn Fn` vtable distinct from the closure pair. |
| A11 | integer widths | `i{8,16,32,64,128}` / `u…` / `i/usize` | the same set PLUS Writ-fabric widths `I24/U24/I56/U56` (`sema.hpp:46-138`) for packed datatype/view storage. | addition | Logos-only widths serving the Writ datatype fabric — not parity items (a Rust port never names them); here so "Logos has extra int kinds" isn't read as a gap. |
| A12 | "no value" surfaces | unit `()` is the single no-value type (both "a fn returns nothing" and "a zero-size value") | TWO internal surfaces: unit `()` = empty `Tuple` (a real zero-size *value* you can bind/move), and `Kind::Void` = "no return value" (the *absence* of a return, `sema.hpp:47`). | addition | A Rust port maps both to `()`; the split is internal to typing (return-position nothing vs unit value) and never needs naming in ported source. |
| A13 | integer arithmetic overflow | `+`/`-`/`*` overflow **panics in debug, wraps (two's-complement) in release** (profile-dependent); `wrapping_*`/`checked_*`/`overflowing_*`/`saturating_*` for explicit control | **always traps (aborts), regardless of build profile** — `+`/`-`/`*` are always checked (signed vs unsigned selected by the LHS type's signedness); intentional wrapping must use `wrapping_*`. Surfaced by `expr.binop.integer-overflow-trap`. | design model | Consistent fail-fast, aligned with A7 (`panic = abort`): no silent-wrap-in-release footgun and no debug/release behavioral split. The *capability* (wrap/checked/saturating) is preserved via the explicit `wrapping_*`/`checked_*`/`saturating_*` methods. |

## §B — Catch-up TODOs ("НЕ ОТКЛАДЫВАТЬ" — must reach Rust parity)

These currently differ from Rust but are **not** blessed — each needs a fix
that converges to Rust behaviour. They are NOT to be parked as "divergence".

| # | Behaviour today | Rust behaviour (target) | Plan / gating | Priority |
|---|---|---|---|---|
| ~~B1~~ | ~~generic `T: Copy` is treated as move~~ **DONE 2026-05-22** | a `T: Copy` bound makes `T` `Copy` — used-after-pass stays valid | FIXED: `is_move_type(TypeVar)` now returns false when the type-param carries an explicit `Copy` bound (via `current_type_bounds_`). Copy⊥Drop makes this sound; only explicit `Copy` counts (Rust parity); non-Copy TypeVars stay conservative-move. See "Recently caught up". | ✅ done |
| ~~B2~~ | **custom-DST tail-slice — DONE (2026-05-29, `1088b703` read + `ac85cb0e` owning).** `struct Foo { hdr: H, tail: [T] }`: decl, fat `&Foo` w/ tail length, prefix field access, tail-as-slice (`f.tail`→`&[T]`), `.len()`, `f.tail[i]`, `dst_from_raw_parts::<Foo>(raw,n)`, `&Foo↔*const u8` casts (under `unsafe`); AND **owning `Box<Foo>`** — collapse to owning DstRef (OwningKind::Box in const_val), construct via `dst_from_raw_parts(..) as Box<Foo>`, `&bb` deref-coerce to `&Foo`, drop = prefix-field + tail-element drops (runtime loop) + free block. Verified valgrind 0/0 (custom_dst_tail_roundtrip 110, box_dst_owning 60, box_dst_owning_drop 2). | full custom-DST layout + construction | DONE (fat model). By-value construction stays impossible (unsized — Rust agrees). WritString stays Zone-hand-rolled (thin-ptr/length-in-content/variable-prefix — a different model). | ✅ done |
| ~~B3~~ | **`Box<[T]>` — DONE (2026-05-29, `8c49d59b`).** `Box<T: ?Sized>` accepts a slice elem: `Box<[T]>` = heap-owned move-only fat slice `{data,len}`, collapsed to a Slice carrying `OwningKind::Box` (mirrors Box<dyn>). `box_new::<[T;N]>(..) as Box<[T]>`; index/`.len()`/iterate; `&b` deref-coerces to `&[T]`; per-element Drop + buffer free (valgrind 0/0). `Box<dyn>` already worked. | `Box<dyn Trait>` / `Box<[T]>` by value | DONE for `Box<[T]>`+`Box<dyn>`; owning `Box<CustomDstStruct>` tracked under B2. | ✅ done |
| B5 | **Dynamic-slice patterns — mostly DONE.** Remaining: *named* element bindings in a slice pattern **nested inside a tuple pattern** (`match x { (2, [a, b]) => a+b }`). | Rust slice patterns | DONE (2026-05-22): top-level `match s { [a, b] => …, [h, ..] => … }` over a `&[T]` scrutinee — refutable on length, binds elements through the data ptr; AND nested-in-tuple length **discrimination** (`(2, [_, _])` checks the slice length). Both in the match-AS-EXPRESSION codegen (`gen_expr_kind(EMatchExprView)`) — note statement-position matches lower to `EMatchExpr` too, so that one site covers both. match-tuple-slice now imported + passing. **Remaining gap**: nested-in-tuple slice patterns with *named* elements (`(2, [a, b])`) — sema's tuple-element handler (sema_stmt.cpp:2740-2804) builds the slice sub via the PAT_OR fallback (so length dispatch works) but declares only a top-level `_` binding, never the nested names → clean "undefined variable 'a'" (NOT a miscompile). Fix = declare nested-slice prefix names in scope (bind_pattern_ref recursion into the Slice sub) + the corresponding nested-slice binding GEP in the tuple binding extractor. The `&[_, _]` ref-slice surface stays a Logos `[_, _]` (in Logos `&[T]` is one Slice type — no separate deref in the pattern). | low (only the named-nested-in-tuple case left) |
| ~~B6~~ | ~~slice mutability is not tracked at the type level~~ — **CLOSED 2026-05-30.** `Kind::Slice` carries `mut_ptr`; `make_slice_type` accepts the bit; the write site at `sema_stmt.cpp:6273` rejects `s[0] = …` through a `&[T]` with "cannot write through a shared `&[T]` slice (need `&mut [T]`)". `&mut [T] → &[T]` downgrade at fn-call args works; flow through let-bindings of `&[T]` rejects writes correctly (verified `tests/imported/pass/coercion/` + targeted shapes). Residual now also CLOSED: the type-pool UID hashes the slice `mut_ptr` bit (B6/P2-11, `sema.cpp:972`), so `&mut [T]` and `&[T]` intern to distinct TypeRefs and a fresh `&mut [T]` is never deduped into an existing `&[T]`. | done | done |
| B7 | **enum heap-representation — ✅ RESOLVED (2026-05-26, commit 51d2e29e).** Tagged enums are now VALUE-representation: ptr-to-inline-**stack** storage `{i32 disc,[N x i8]}` (like Struct), `alloca` not `malloc`, inline-embedded in struct fields / tuple / array / nested enum payloads / Vec buffers (sizeof+memcpy), one-level `&Enum`, returned-by-value-via-load. Killed the A1/A2 heap leak + the heap-promotion hack. Move/drop suppression for inline enums extended to PLACE scrutinees (`match s.o`) — the issue-19367 double-free. 5235/5235 green; valgrind-clean on json_parse (recursive droppable). Sema/Mono unchanged (only `mark_match_scrutinee_moved` widened). | Rust: enums are by-value inline — now MATCHED. | DONE. Residual Vec-element-drop since RESOLVED (`52c24b28`). | resolved |
| B8 | **assignment drop-before-replace — ✅ RESOLVED (2026-05-28).** `x = y` for a live owning `x` now drops `x`'s prior contents first (Rust order: eval RHS, drop old, store). Path: `b9cf9d81` (static drop-before-replace for definitely-init vars) → `33d12014` (dynamic drop flags for declared-uninit) → `3e7bb5df` (full **drop elaboration** — a fn-body pre-scan emits a runtime drop flag ONLY for vars with conditional/loop assignment; straight-line vars use static drop placement, exactly Rust's MIR model). Exact for `if c {x=a;} x=b;` (drops `a` iff c ran), no garbage-drop on early-return-before-init. Test `b8_uninit_drop_matrix` (exact drop counts), valgrind 0/0, 5255/5255. | Rust `expr.assign.drop-target`: drop the old value first, *unless the place is uninitialized*. | DONE — matches Rust incl. conditional-init via drop elaboration. | resolved |
| ~~B9~~ | **Box ergonomics — DONE.** `Deref`/`DerefMut` for `Box<T>` (`*b`, auto-deref `b.field`/`b.method()`) landed; `Box::into_raw`/`from_raw`/`leak` added (`f3176745`); `Box<dyn>` + `Box<[T]>` representable (B3 done). | Rust: `*b`, `b.field`/`b.method()` via Deref/DerefMut; `Box<T: ?Sized>` fat pointer. | DONE. Owning `Box<CustomDstStruct>` tracked under B2. | ✅ done |
| B4 | accumulated unsupported-syntax / unimplemented features surfaced by imports B106–B114 (pattern features: refutable field/inner sub-patterns, `ref _y @ Pat`, tuple-of-refs `let`, 1-tuple `(z,)`, empty struct-variant `A {}`; `&[T;N]`→`&[T]` coercion; `-128i8` literal range; nullary-closure mutable capture; …) | as Rust | These are plain GAPS, tracked per-batch in `tests/imported/notes/B1NN-ui-surfaced-gaps.md` (one file per import batch, e.g. `B106`…`B115`) and the baghunt catalog (`docs/baghunt/README.md`). Work them in the grind; this row is the pointer, not the list. | rolling |
| B-mod | **Module-linkage mangling — PARTIAL (2026-06-20).** The owning-module id (C++ module-linkage model: module-ID baked into the symbol) is now injected into **free functions + module-level symbols** via `function_symbol_name` (rides the package encoding: `<module_id>.<pkg>$base__…`, parser-transparent). The pkg→module map (`LProgram::pkg_module_ids`, filled by sema, `Mono::mq()`) is in place. **METHODS are still EXEMPT** (`!is_method` gate in `function_symbol_name`): their call sites synthesise the callee ad-hoc as bare `<pkg>.<Struct>__<method>` in sema (`finish_generic_call`) + mono (`mono_clone.cpp` type-derived-pkg sites), so qualifying only the definition would desync def from call. | every module-owned symbol (incl. methods) carries its module-id so same-named packages from different modules/versions coexist (the C++ `W<module>` model). | NEXT: un-exempt methods in `function_symbol_name`; route the sema method-callee source (`finish_generic_call` callee construction) + the ~5 mono `pkg = TypeRef(t).pkg_name()` synthesis sources through `mq()`; mind lookup-key vs emitted-symbol sites. Gate L2/L4. | high (module-system follow-up) |
| B-assoc | **Ambiguous associated-type projection across two `Trait<T>` impls at distinct `T` (G156-1).** When one type has two impls of a parameterized trait `Trait<T>` at distinct `T`, each declaring the same associated type, Logos resolves a *bare* projection `X::Assoc` by erasing the ambiguous key (assoc types are indexed by the impl's concrete trait type-args — `mono.cpp:251`, `sema_impl.hpp:4282-4294`), and the trait-qualified method-mangling for the two impls can still collide. | Rust rejects the bare projection and requires fully-qualified `<X as Trait<T>>::Assoc`; the two impls get distinct, non-colliding mangled symbols. | Converge: require disambiguation instead of erasing the bare key, and finish the trait-type-arg suffix in method-mangling so the two impls never collide. See `baghunt_trait_typearg_mangling`, `docs/baghunt/README.md` (G156-1). | low (narrow, known) |

| ~~B6~~ | ~~**Borrow checker: no scope-based lifetime (NLL / E0597).**~~ **CLOSED 2026-06-19** (`borrow_check.cpp`, generalising the B87 dropck machinery). A reference / borrow-carrying binding now records the LOCAL vars it borrows (`record_ref_sources`/`collect_ref_sources`: direct `&x`, `&x.f`/`&x[i]`, struct/tuple/array/cast wrapping, borrow-returning method calls → receiver/ref-arg locals; field/tuple writes `s.f=&x` via `add_ref_sources` on the `SDerefWrite` place chain). At `pop_scope`, a binding in an OUTER scope borrowing a local dying there is marked `dangling_` (NOT reported — NLL: a stored borrow never used after its referent dies is sound); the FIRST subsequent use (`check_live`) reports **E0597 "x does not live long enough"**. Rebind/reassign re-owns (clears dangling). Coverage is broad: stored `&x`, struct/tuple/array-wrapped `&x`, casts, nested scopes, method-returns (`b.borrow()`), field/tuple writes (`s.f=&x`), ref-to-ref chaining (`o=r`), enum payloads (`Some(&x)`), if-expressions / block tails, free-call returns (`pick(&x)`), match-arm extraction (`match o {Some(r)=>…}`, with branch-merge union of sources/danglers across arms), AND **container-element stores** — `v.push(&x)` into a `Vec<&T>` extends the capture-flow: a by-value arg whose type IS the receiver's element type (distinguishing `push(&i64)` from `contains(&&i64)` by element-type match) records the receiver as borrowing the local. return-of-local (E0515) still via the existing path. Full suite 5733/5733 + ~20 hand-checked valid patterns, NO false positives. Tests under `tests/logos/{fail,pass}/nll_*`. Remaining precision gap (pre-existing, orthogonal): NLL release is LINE-granular, so multiple borrow-conflicting statements on ONE physical line can under-report (real code is multi-line; statement-granular release would need region_infer's per-point model). | Rust NLL E0597. | done | done |

### Recently caught up (kept for the record — no longer divergences)
- bool bitwise `&` / `|` / `^` — now works (was rejected as "left/right must be integer"). ✅ verified 2026-05-21.
- `&T == &T` — now value-equality (was pointer-equality). ✅ verified 2026-05-21.
- `&[T; N]` → `&[T]` **unsized coercion** — `&named_arr`, `&[lit, …]`, and structurally inside tuple/struct fields (`(i64, &[i64;2])` unifies with `(i64, &[i64])`) all coerce at let/arg/return. ✅ verified 2026-05-21. (Was the headline of B5; only dynamic-slice *patterns* remain — see B5.)
- array pattern `[x, y]` in **match-as-expression** position — was a silent miscompile (bindings read garbage; the expr-form match codegen had no Slice arm-binding case, only the stmt form). Fixed 47413e65. ✅ 2026-05-21. (Fixed-size arrays; dynamic Slice scrutinees are B5.)
- **dynamic-slice patterns** `match s { [a, b] => …, [h, ..] => … }` over a `&[T]` scrutinee + nested-in-tuple length discrimination (`(2, [_, _])`) — ✅ 2026-05-22. Only named-nested-in-tuple bindings remain (see B5).
- **nested variant pattern in enum-variant payload** `Some(Color::Red)` / `Ok(Status::Done)` — ✅ 2026-05-22 (bindingless inner variant gates the arm via a synthesized `match payload { inner => true, _ => false }` guard). Recurring across B107/B112/B114/B116. Needs a catch-all for exhaustiveness (Logos doesn't yet prove finite-enum coverage of guarded arms). Payload-binding inners (`Some(Inner(x))`) still pending.
- **trait-qualified UFCS** `Trait::method(recv, …)` — ✅ 2026-05-22 (dispatch on the first arg's concrete type).
- **bitwise/shift operator overloading on structs** (`& | ^ << >>` → BitAnd/BitOr/BitXor/Shl/Shr) — ✅ 2026-05-22 (arithmetic ops already worked; added the parallel mappings).
- **nested tuple destructure-let** `let (a, (b, c)) = t` (and deeper) — ✅ 2026-05-22 (grammar `pat_binding` += nested tuple; `lower_let_destruct` recurses). Was "supports struct patterns only".
- **generic `T: Copy` auto-copy** (was §B1) — a `T: Copy` bound now makes `T` Copy; by-value use of `x: T` no longer moves it. ✅ 2026-05-22 (`is_move_type` checks the type-param's Copy bound). Non-Copy generics stay move-checked.
- **closure `|mut x|` / `|mut x: T|`** mutable param bindings — ✅ 2026-05-22 (grammar + closure-lowering synth-param/prologue, mirrors fn `mut` params). Nullary capturing closures mutating an outer `mut` local (`|| { hit += 1; }`) also work now.
- **`for x in &coll` yields `&T`** (not `T` by value) — was a soundness divergence (moving out of a borrow; only sound for Copy types). Now Rust-correct for slices, arrays (via slice coercion), and `&Vec` (via new `Vec::as_slice`). ✅ 2026-05-22. By-value `for x in coll` (owned array / `Vec` via VecIter) still consumes/yields `T` — also Rust-correct. `&mut` iteration yielding `&mut T` not yet wired (for-each `&mut` path is separate).
- **`Rc<dyn Tr>` / `Arc<dyn Tr>` as the real struct repr + implicit unsize coercion** — ✅ 2026-06-02 (B3 stage-2b, commits 30f1aafc/f1c65cc7/6202da30/ea92f72f). Was: `Rc/Arc<dyn>` collapsed to an owning trait object `{data,vtable}`, layout-incompatible with the `Rc<T>` `{inner}` struct, so generic inherent methods (`clone_ref`/`strong_count`/…) couldn't run and a repr-aware `.clone()` marker was needed; implicit `Rc<A>→Rc<dyn>` coercion was absent (explicit `as` required — "GAP-C"). Now `Rc<dyn Tr>` resolves to the real `Rc` STRUCT `{inner: *mut RcInner<dyn Tr>}` (inner = fat custom-DST `DstRef`); all generic methods run directly (per-instance mono re-lowering of the prefix-field read + the unsized-dyn-tail projection + a vtable drop-in-place for the moved dyn tail), and CoerceUnsized fires implicitly at let/arg/return (no `as`). Repr-aware specials removed (`__smartptr_dyn_clone__` marker + handler). Arc's inherent methods correctly relaxed to `impl<T: ?Sized>` (Send+Sync gates only `unsafe impl Send/Sync for Arc`, per Rust). `Box<dyn>` intentionally stays the owning trait object — it IS Rust's canonical owned-dyn fat pointer (no refcount machinery to access). Foundation: custom-DST struct with a `dyn`-trait tail (a generic struct `<T:?Sized>` whose tail field is bound to `dyn`).
- **unit struct `struct Foo;` (Rust parity)** — ✅ 2026-05-25 (G172-14). Was repurposed as the `struct_inst` explicit-instantiation form, so a bare `struct Foo;` errored "'Foo' is not defined". A grammar rule now parses `struct Foo;` / `pub struct Foo;` as a zero-field struct decl, and a bare unit-struct name in value position constructs it. The dedicated explicit-instantiation syntax is **`instantiate Foo<T>;`** (KW_INSTANTIATE, ADR-0009); the legacy generic `struct Foo<Args>;` shape still parses as `struct_inst` (kept for the unbound-typevar diagnostic). Logos `#[derive]` still diverges (per-trait `#[derive_<trait>]` + metaprog handler — see A3).

---

## Process (per imported batch)

1. A failing test needs a feature Logos lacks → **gap**: implement now (or, if it
   maps to §A, re-import via the replacement — e.g. metacall-splice for const).
2. A failing test relies on a §A blessed divergence → re-write the test through
   the Logos mechanism; record it; never leave it trimmed.
3. A behaviour difference that "works but not as Rust intends" → it is §B, not
   silent acceptance. Add a row (or reference an existing one) + a plan.
4. Never silently change semantics to make a port compile. Document in the same
   batch.

## Registers & cross-refs
- `stdlib/imported/DIVERGENCES.md` — coretest/alloc/std per-module divergence
  ledger (this doc is the language-wide superset; keep the architectural rows in
  sync).
- `tests/imported/WHY-WE-SKIP.md` — permanently-skipped categories (async,
  proc-macro, rustc-internal) with rationale.
- `tests/imported/notes/B1xx-ui-surfaced-gaps.md` — per-batch surfaced gaps.
- `docs/baghunt/README.md` — bug/gap catalog (the `Divergence` tag must mean §A).
- Memory: `feedback_full_rust_parity_now`, `project_no_const_eval`,
  `feedback_const_fn_via_metacall`, `project_gap_grind_strategy`.
