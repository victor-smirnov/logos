# Consts / typeck / inference gaps surfaced by Track 3 imports

Status legend: `Open` — not started; `Partial` — partial fix landed,
notes inline; `✅ Closed` — gap closed (commit, date).

| ID | Surface | Status | Gap | Surfaced by | Repro |
|---|---|---|---|---|---|
| K10-co-01 | Arithmetic expression at array-length position | ✅ Closed (2026-05-11) — via MP-mc-01 partial: `arr_fill_lit` SIZE now admits `metacall { … }`, ctfe-evaluated to integer at sema. | `[T; 2*4]` rejected at parser; the Logos idiom is `[0i64; metacall { 2 * 4 }]`. | `arithmetic-expr-in-array-len` (un-trimmed via metacall) | `[0i64; metacall { 2i64 * 4i64 }]` |
| K10-co-02 | `null` as identifier (fn name / etc.) | ✅ Closed (`77316df`, Sprint 1.1) | `null` was KW_NULL in logos.peg; now admitted as identifier in fn/field/call positions. | `unify-return-ty` (renamed `null` → `null_p`) | `fn null<T>() -> *const T { … }` |
| K10-co-03 | `mem::transmute` | Divergence — Logos has explicit `as`/deref equivalents; no transmute planned. | Multiple typeck tests rely on transmute. | `unify-return-ty` (transmute → cast) | `mem::transmute(0_usize)` |
| K10-co-04 | Divergent (`return`-only / `panic`-only) expression usable at non-void type position | Partial (2026-05-11) — `lower_block_expr` now peeks at the tail-RETURN's value-type and adopts it as the block's expression-level type when no fall-through result exists. Unblocks `({ return 0 },)`-style patterns in tuple / struct literals. **`panic`-only tail** still needs the same treatment (today `panic(msg)` is a regular void-returning fn so the block stays void; full fix would tag `panic` with a `!`-marker attribute or hand-recognise it). Real long-term answer is a Bot/Never type kind propagated through expression positions. | A block whose tail diverges keeps `void` type instead of unifying with the required type. | `expr/return-in-block-tuple` (un-imported); `expr/if-panic-all` (still uses dummy-after-panic workaround) | `let _: (i32,) = ({ return 0 },);` works |
| K10-co-05 | `&<int-lit-with-suffix>` temp-materialisation drops the suffix | Open — `&3usize` (or `&3u64`, `&3i64`, …) as a fn-call argument lowers the literal at type `i32` (the IntLit default after suffix is dropped) and stores it into an alloca sized at the suffix-named type. Result: `alloca x i64` filled with `c3_i32`, only low 4 bytes set, high 4 bytes garbage — any subsequent `load i64` from that alloca returns garbage. Workaround: bind through a let-typed local (`let three: usize = 3usize; foo(&three);`). Closing it = thread the suffix-resolved type through the `&<lit>` temp-mat path in `lower_unary` (sema_expr.cpp's `op == "&"` `&<expr>` branch), similar to C6-cc-04's hint propagation for `let p = &<lit>;`. | `binding/borrowed-ptr-pattern-3` (workaround used: let-bound local instead of `&3usize` directly). | `binding/borrowed-ptr-pattern-3` (imported with workaround) | `foo(&3usize)` stores i32 in i64 alloca, returns garbage |
