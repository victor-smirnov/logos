# Struct / enum / binding gaps surfaced by Track 3 imports

Status legend: `Open` — not started; `Partial` — partial fix landed,
notes inline; `✅ Closed` — gap closed (commit, date).

| ID | Surface | Status | Gap | Surfaced by | Repro |
|---|---|---|---|---|---|
| S8-st-01 | Keyword-shaped identifier as struct field name | ✅ Closed (`77316df`, Sprint 1.1) | `new` (KW_NEW in `tools/peg_gen/grammars/logos.peg`) is reserved. The grammar permits it as a method name (logos.peg:788) but not as a struct field. Cross-link to M7-mt-01 (same root cause in trait-body context). | `struct-new-as-field-name` | `struct Foo { new: isize }` ⇒ "syntax error near 'struct'" |
| S8-en-01 | Negative integer literal as enum discriminant | ✅ Closed (`1ab8826`, Sprint 1.4) | `enum E { … X = -1 }` — Logos's enum-discriminant production didn't accept `-1`; only non-negative integer literals. | `enum-disr-val` (one variant dropped) | `enum color { …, imaginary = -1 }` |
| B-ts-01 | Tuple-struct syntax `struct Foo(T1, T2);` | ✅ Closed (2026-05-11) — non-pub + pub forms both supported. Grammar accepts `struct Foo(T1, T2);` and `pub struct Foo(T1, T2);`; sema interns synth field names "0", "1", … in a long-lived pool (SemaFieldInfo::name is a string_view) and marks `is_tuple_struct = true` on SemaStructInfo. Positional constructor `Foo(a, b)` routes through `struct_lit`. Tuple-index `foo.0` recognises tuple-struct receivers. Pattern `Foo(a, b)` accepted in match arms and `let` destructure. **Lesson:** several earlier imports (newtype-struct-with-dtor, basic-newtype-pattern, …) had been trimmed to named-field shape; should have been logged as a gap immediately, not worked around silently. | Many rustc tests use tuple-struct (newtype pattern, etc.). | `structs/newtype-struct-with-dtor` (un-trimmed, `pub struct Fd(u32);`), `structs/basic-newtype-pattern` | `pub struct Pair(i32, i32);` + `Pair(1, 2)` + `p.0` + `let Pair(a, b) = p;` |
| B-pg-01 | peg_gen segfault: MultiChunk arena breaks RelativePtr | ✅ Closed (2026-05-11) — root cause: peg_gen used `make_doc_multi` (MultiChunk arena) for its parsed-grammar IR; once the IR grew past the initial 512KB chunk it spilled into a separate chunk whose virtual-address delta to the head chunk overflowed `arena_offset_t` (uint32_t). `TinyObjectMap::data_.get(head)` then returned a wild pointer (`base + sign-truncated delta`) and the next `put()` SIGSEGV'd inside the values array. Switched peg_gen to `make_doc` (GrowableSingleChunk) — buffer doubles on overflow within one contiguous chunk; `RelativePtr` offsets stay valid through realloc because `grow()` saves `self_off` before allocating and recomputes pointers from the new base. This was the blocker preventing the pub-tuple-struct alt from being added — it crashed peg_gen by tipping the IR just over the 512KB threshold. | Adding a single additional grammar alt could deterministically segfault peg_gen if it tipped the IR past the initial chunk. | Surfaced while wiring B-ts-01's `pub_struct_def` alt. | n/a — bug in peg_gen itself |
