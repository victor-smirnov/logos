# Pattern / match gaps surfaced by Track 3 imports

Logos has tuple-only enum variants and a fairly narrow pattern
surface. Most rustc match/pattern tests sit deep in features Logos
hasn't built yet — Batch 4 is therefore small (5 tests) and the table
below is correspondingly large.

Status legend: `Open` — not started; `Partial` — partial fix landed,
notes inline; `✅ Closed` — gap closed (commit, date).

| ID | Surface | Status | Gap | Surfaced by | Repro |
|---|---|---|---|---|---|
| P4-pm-01 | Struct-shape enum variants `enum E { V { field: T } }` | ✅ Closed (2026-05-12) — declaration, construction, both irrefutable and refutable match patterns, AND single-variant `let`-destructure all land end-to-end. **Declaration**: new `variant_def` alt `IDENT LBRACE variant_struct_field_list RBRACE` carries an `IS_STRUCT_SHAPE` flag in the new `variant` key group (slot 47, scoped to VARIANT_DEF / ENUM_LIT_DATA / PAT_VARIANT_DATA which never carry a loop label). Sema's `collect_enum` reads field name+type per FIELD_DEF entry and populates `SemaVariantInfo.payload_field_names` parallel to `payload_types`. **Construction `E::V { x: e, y: e }`**: new `enum_lit` alt; sema resolves names → variant indices, type-checks each field, fills a positional payload. Shorthand `E::V { x }` accepted. **Match pattern `E::V { x, y: pat, .. }`**: new `pat_single_base` alt; sema resolves names → positions, supports shorthand (bind = field name), `field: _` skip, `field: ident` rebind, and `..` rest. **Refutable inner literal patterns** (`E::V { f: 1 }`, `E::V { b: true }`, `E::V { c: 'a' }`, and the matching tuple-shape `Option::Some(1)`): sema synthesizes a fresh `__refut_*` binding at that position and emits a `__refut_* == <value>` predicate into a per-arm refutable-guard side channel; lower_match / lower_match_expr AND-combine those predicates into the arm's guard (atop any user-written guard). **Single-variant `let E::V { f } = e;`**: detected at lower_let_pat dispatch (one variant only ⇒ irrefutable); lowered as a temp + per-binding synthetic match-as-expression `match __dst { E::V { f: __syn, .. } => __syn }` (one match per user binding, each returning its field). mlir-gen unchanged throughout — sema resolves names → positions, downstream LIR stays positional. **Still open**: (1) multi-variant `let E::V { … } = e;` would need `_ => panic!()` fall-through (refutable let); (2) range patterns / nested variant patterns inside the payload (separate gap); (3) tuple-pattern element accepting `PAT_VARIANT_DATA` (`match (e1, e2) { (E::Foo {..}, …) }` from issue-5530 needs a parallel PAT_TUPLE elem-kind extension); (4) or-patterns in arm LHS (`A {…} \| B` from issue-114691). | n/a (refutable inner + single-variant let now land). | `tests/imported/pass/pattern/issue-8351-1`, `issue-8351-2`, `issue-11577` un-trimmed (faithful Rust ports modulo `panic!()` → return-code substitution); `tests/logos/pass/enum_struct_shape` regression covers ctor + irrefutable match + `..` rest + rename. | `enum E { Foo { f: i64, b: bool } } match v { E::Foo { f: 1, b: true } => …, E::Foo { f, .. } => …, … }` works; `let E::Foo { f } = v;` works for single-variant enums |
| P4-pm-02 | Nested patterns inside enum-variant payloads | ✅ Closed (2026-05-11) — side channel `current_pat_nested_subs_` in SemaChecker collects nested struct/tuple sub-pats during variant-pat build; arm-body builder emits field-by-field SLet destructure stmts as a body prologue. Refutable sub-pats (nested variant, range) still rejected — would need nested-guard scheme. | `Option::Some(A { foo: _foo, })` rejected. | `issue-10392` (un-trimmed) | `Option::Some(A { foo: _foo, })` works |
| P4-pm-03 | Tuple types as match scrutinees | ✅ Closed (2026-05-12) — earlier work landed basic tuple types in patterns + tuple-arm AND-chain (`ead8f39`/`8c51edc`) + tail-position match (`80bef0c`); B53 added P4-pm-24 (variant pattern as tuple element). Final B-ZZ piece: or-pattern as tuple element (`(true \| false, false, …)`, `("a" \| "b", n)`). Grammar's `pat_tuple_elem` now matches `pat_single (PIPE pat_single)*` emitting PAT_OR (single-alt PAT_OR is unwrapped inline so existing wildcard / binding / scalar shapes still parse as before). Sema's PAT_TUPLE element handler routes PAT_OR through `build_pattern(sub, elem_ty)` (multi-alt) and unwraps single-alt PAT_OR at the field level so binding names survive. mlir-gen tuple-arm dispatch (both `gen_match` and `gen_expr_kind(EMatchExprView)`) adds an `Or`-sub branch: OR-chain each alt's eq/disc test, AND into the row's cond. Scalar Int/Bool alts supported (range and variant inside or-pat-at-tuple-element still future work; not blocking imports). Untouched 1-tuple `(z,)` syntax (Logos has no 1-tuple type) — drop the `h` fn from upstream issue-72680. | n/a (was tuple sub-shape gap). | `tests/imported/pass/match/issue-72680-g` (subset port: fn `g` only — or-pat inside 4-element bool tuple) un-trimmed. `issue-33498` / `issue-18060` already imported earlier. | `match (x, y, z) { (true \| false, false, false) => …, … }` works |
| P4-pm-04 | Array-prefix patterns `[1, ..]` | ✅ Closed (2026-05-11) — refutable PatSlice now lowers in both `gen_match` (stmt) and `gen_expr_kind(EMatchExprView)` (expr): GEP each scalar prefix/suffix sub-element, AND-chain equality tests, CondBranch into arm body. Match-as-expr path spills scrut to alloca when scrut isn't already a pointer. Trailing `..` absorbs the rest. Dynamic-slice scrutinees still deferred (would need runtime length check). | Array-prefix pattern in match. | `match-large-array` (un-trimmed), `borrowck-slice-pattern-element-loan-rpass` | `[1, ..] => …` works |
| P4-pm-05 | `if let` form | ✅ Closed (`e380aab`, 2026-05-11) — `if let` works (Sprint 1.3 rescind). | Logos has `match` but no `if let` sugar. | `match-on-negative-integer-ranges`, many others | `if let -128i8..=-101i8 = x { ... }` |
| P4-pm-06 | Const-pattern (bare ident referencing a module-const) | ✅ Closed (2026-05-12) — earlier work lowered scalar (int/bool/char) consts via ctfe. Final step closes str-typed consts: when `module_consts_[name]` resolves to a `str` (Slice<u8>) const and ctfe returns `Kind::Slice`, sema synthesizes a `__str_<n>` binding + pushes `str_eq(__str_<n>, CONST)` into the refutable-guard side channel (P4-pm-01 plumbing). Lookup uses `find_func_candidates("str_eq")` so the mangled stdlib symbol is picked correctly; a parallel pre-existing private `str_eq` in `std.sys.args` was deleted to avoid the duplicate-mangled-name collision. Stdlib gains a `pub fn str_eq(a: str, b: str) -> bool` in `std.lang.text.string` (compiles to a length check + byte-loop). | n/a (was sema-rejected). | `tests/imported/pass/match/issue-11940` un-trimmed (faithful Rust port modulo `&'static str` → `str` and `unreachable!()` → conditional return). | `const TEST_STR: str = "abcd"; match s { TEST_STR => … }` works |
| P4-pm-07 | Byte-string literals `b"..."` / byte-string patterns `b"foo"` | ✅ Closed (2026-05-12) — earlier work landed PAT_BYTES → PatSlice/PatInt lowering on `[u8; N]` array scrutinees. Final step adds `b"..."` at **expression position**: new `LIT_BYTES` AST code (slot 234); grammar admits BYTE_STRING in primary_expr / primary_expr_ns; sema's LIT_BYTES handler decodes escapes (parity with PAT_BYTES decoder — `\n`/`\t`/`\r`/`\0`/`\\`/`\"`/`\x..`) and emits an `[u8; N]` ArrLit. Const initializers `const X: [u8; N] = b"…";` work via `is_const_evaluable`'s extended literal allow-list. Remaining future work (parallel slice, not blocking imports): (1) `&[u8]` dynamic scrut byte-string match (length check + memcmp); (2) `b"…"` typed as `&[u8; N]` ref-to-array (rather than the by-value `[u8; N]` form). `issue-46920-byte-array-patterns` exercises both `&[u8]` and `&[u8; N]` shapes, so its un-trim still depends on (1)/(2). | n/a (was sema-rejected at expression position). | `tests/logos/pass/byte_string_expr` regression for `let arr: [u8; N] = b"…"` + `match arr { b"…" => … }`. | `let arr: [u8; 5] = b"hello"; match arr { b"hello" => … }` works |
| P4-pm-08 | `mka()` returning struct-by-value + destructure inside `test()` body (mlir-gen GEP crash) | ✅ Closed (2026-05-11) — mlir-gen `gen_pat`'s PatStruct case spills the scrut to an alloca when it arrives as a value (not pointer) before GEP. | n/a | `issue-10392` (un-trimmed; only nested-variant-pat slice remains out for P4-pm-02). | `match mka() { A { foo: _foo, } => {} }` works |
| P4-pm-09 | Char-range patterns `'a' ..= 'z'` | ✅ Closed (`ca17040`, 2026-05-11) — PAT_CHAR / PAT_CHAR_RANGE end-to-end. | char-range pattern arms in match. | `inc-range-pat` | `match c { 'a'..='z' => … }` |
| P4-pm-10 | Match-as-expression Range arm codegen | ✅ Closed (`ca17040`, 2026-05-11) — Range-arm AND-chain in EMatchExpr path; signed/unsigned. | `let b = match c { 0..=10 => true, _ => false };` returned wrong value (Range disc=0). | `inc-range-pat` (un-trim) | as above |
| P4-pm-11 | Match-as-tail-expression in non-void fn | ✅ Closed (`80bef0c`, 2026-05-11) — gen_function_body emits `llvm.unreachable` instead of bare `func.return` on non-terminated fallthrough. | Tail-position `match (x,y) {...}` in `fn -> T` produced "func.return op has 0 operands". | `match-tuple-tail` | `fn foo() -> u32 { match (x,y) { ... } }` |
| P4-pm-12 | `mut` binding pattern (`match x { mut z => ... }`) | ✅ Closed (2026-05-11) — new pat_single alt `KW_MUT IDENT` emits PAT_WILD with IS_MUT=true; sema's `build_pattern_impl` registers the name in a `current_pat_mut_names_` side-channel and `bind_pattern_ref` redefines the binding as mutable via `define(name, ty, /*is_mut=*/true)`. Wired through both match-stmt (`lower_match`) and match-as-expression (`lower_match_expr`). | rustc test `regions/regions-reassign-match-bound-pointer`. | `regions/regions-reassign-match-bound-pointer` (un-trimmed) | `match x { mut z => { z = &a; } }` works |
| P4-pm-13 | Tuple destructure in `let` with type annotation (`let (a, b): (T1, T2) = …;`) | ✅ Closed (2026-05-11) — new let_stmt alt admits `COLON type_ref` between the pat-binding list and ASSIGN. Grammar produces LET_DESTRUCT with TYPE+VALUE+NAMES; sema's lower_let_destruct uses the rhs_type to drive binding types (annotation accepted, not strict-checked — matches the bare form's behaviour). Sub-patterns inside the tuple (`let (&x, &y) = …`) still need a deeper extension. | `binding/borrowed-ptr-pattern-infallible` `let (&x, &y) = (&3, &'a');` (sub-pattern part still open). | `tests/logos/pass/let_typed_tuple_destructure` regression. | `let (x, y): (i32, char) = (1i32, 'a');` works |
| P4-pm-14 | `let ref y = x;` (ref-binding at let position) | ✅ Closed (2026-05-11) — new let_stmt alts `KW_LET KW_REF IDENT (COLON type_ref)? ASSIGN expr SEMI` emit LET with IS_REF=true. Sema's `lower_let` detects the flag and wraps the lowered RHS in `addr_of_temp` so the binding takes `&T` instead of `T`. Behaves identically to `let y = &x;`. | `binding/let-destruct-ref` (un-imported). | `binding/let-destruct-ref` (newly imported) | `let ref y: usize = x;` works |
| P4-pm-15 | Array destructure in `let` (`let [a, b, c] = arr;`) | ✅ Closed (2026-05-12) — Drop case closed end-to-end. Two-part fix: (1) sema `lower_let_pat` (array branch) marks the `__dst_*` temp moved AND marks the RHS expression moved via `mark_moved_expr` — suppresses both the temp's and the source array's tail `[T;N]` drop sweep. (2) mlir-gen `gen_expr_kind(ESliceIndexView)` adds a struct-element fast path: for `Struct`/`ZonedStruct` element types it GEPs with the aggregate's LLVM struct type as stride (sizeof(struct)) and returns the element ptr directly — the prior path used `logos_to_mlir(Struct) == ptr_type()` for the GEP stride, which strided 8B per element and then `load ptr` re-dereferenced into garbage. Scalar shapes unchanged. Refutable rest shapes (`let [..]`, `let [a, ..]`) still rejected at let-position — slice-shape refutable patterns need a separate flow (would also need a runtime length check). | n/a (was: SIGSEGV double-drop on Drop-impl elements). | `tests/logos/pass/let_array_destructure_drop` Drop-case regression; scalar shapes still in `let_array_destructure`. | `let [a, b, c] = arr_of_DropStruct;` runs (Res::drop fires exactly once per binding) |
| P4-pm-16 | Bare-IIFE closure call (`(\|x\| body)(arg)`) | ✅ Closed (2026-05-12) — new postfix in `atom`'s suffix chain admits `LPAREN call_arg_list? RPAREN` on any primary expression, emitting an INVOKE_EXPR node. Sema's `lower_invoke_expr` routes through the existing closure-call / fn-ptr-call paths against the receiver's type. Chained `(…)(a)(b)` works for the same reason. | `match/closure-in-match-guard`. | `match/closure-in-match-guard` (now faithful — IIFE shape `(\|\| body)()` restored). | `match x { e if (\|\| e == 0)() => {} … }` works |
| P4-pm-17 | `&ref a` pattern + `*a` deref returns the alloca address instead of the deref'd value | ✅ Closed (2026-05-11) — mlir-gen `extract_payload`'s `PatRefBind` case spilled the scrut value to an alloca first, then stored *that alloca's address* in the binding's slot. Result: an extra indirection level — `*a` would load `a` (got the spill-alloca address) and then read i32 from it, instead of reading through the original ref. Fix: when scrut is already a pointer (e.g. from an outer `PatRefPat` over `&T`), store the scrut value directly in the binding slot — no extra spill. Non-pointer scrut still spills (existing path). | `fn foo(x: &i32) -> i32 { match x { &ref a => { return *a; } } }` returned the low 32 bits of the spilled pointer, not 3. | `binding/borrowed-ptr-pattern` (newly imported) | `&ref a` over `&T` now works |
| P4-pm-18 | `&<int-lit>` pattern over `&T` scrut (mlir-gen verifier crash) | ✅ Closed (2026-05-11) — both `gen_match` (stmt) and `gen_expr_kind(EMatchExprView)` (expr) now recognise `PatRefPat` wrapping a scalar inner pattern: deref the scrut (load through the `&T`), then cmp the loaded value against the inner disc. Previously the dispatch fell through to the default int-disc path which cmpi'd the raw pointer against an i32 → MLIR verifier crash. | `binding/borrowed-ptr-pattern-3`. | `binding/borrowed-ptr-pattern-3` (newly imported) | `match s { &3 => true … }` works |
| P4-pm-19 | Tuple-destructure with wildcard in fn-parameter (`fn foo((x, _): (T1, T2))`) | ✅ Closed (2026-05-11) — closed alongside C5-cl-07 since both share the same `param` grammar. `lower_fn` (sema_decl) detects PARAM with NAMES, synths a tuple-typed `__tup_param_<fn>__<i>` parameter, registers user-visible binding names with element types, and prepends `let user_k = synth.k;` to the body. Wildcard `_` supported. | `binding/func-arg-wild-pattern`. | `binding/func-arg-wild-pattern` (un-trimmed). | `fn foo((x, _): (isize, isize)) -> isize` works |
| P4-pm-20 | Tuple pattern with `..` rest (`match x { (a, b, ..) => … }` / `(.., b, c)` / `(a, .., c)`) | ✅ Closed (2026-05-11) — new helper `pat_tuple_elem` admits either DOTDOT (PAT_REST) or a regular `pat_single`. PAT_TUPLE alt uses the helper. Sema's PAT_TUPLE handler expands the single PAT_REST position into PAT_WILD `_` skip entries so the underlying PatTuple LIR keeps its fixed-arity layout — no codegen change required. Multiple `..` rejected; explicit-element count exceeding tuple arity rejected. | `binding/pat-tuple-1`, `binding/pat-tuple-5`, `binding/pat-tuple-6`. | `binding/pat-tuple-1`, `binding/pat-tuple-6` (newly imported). | `match (1,2,3) { (a, b, ..) => … }` works |
| P4-pm-21 | `ref` binding inside variant payload pattern (`Some(ref v) => *v`) | ✅ Closed (2026-05-12) — closed end-to-end via the B3-bg-07 deref-as-identity shim: PAT_REF_BIND inside variant payload still lowers as a plain by-value binding (the inner `ref` doesn't change the binding's type, so `v: T` not `v: &T`), but the body's `*v` now resolves as the identity on a non-pointer value and returns `v` unchanged. Faithful Rust port runs with no source rewrites. | `binding/match-ref-binding` (`Some(ref v) => *v`). | `binding/match-ref-binding` (faithful — `*v` retained). | `match x { Some(ref v) => *v, … }` runs |
| P4-pm-23 | Range pattern as tuple-pattern element (`(0, 2..=5)` etc.) | ✅ Closed (2026-05-11) — sema's PAT_TUPLE element allow-list extended to include PAT_RANGE (was Int/NegInt/Bool only); mlir-gen tuple-arm dispatch in both stmt and expr forms extended to emit `lo <= elem <= hi` AND-chain test alongside existing Int/Bool eq tests. Signed/unsigned predicate selected per-element from binding type. Previously the range sub-pattern was silently dropped (Wild-equivalent), causing the arm to fire too eagerly. | `match/issue-18060`. | `match/issue-18060` (faithful). | `match (1,3) { (0, 2..=5) => 1, … }` works |
| P4-pm-22 | Exclusive range pattern `lo..hi` | ✅ Closed (2026-05-11) — new pattern alts admit `INTEGER DOTDOT INTEGER` (with optional `-` for either bound) emitting PAT_RANGE with `INCLUSIVE: false`; existing inclusive alts now explicitly set `INCLUSIVE: true`. Sema reads the flag and rewrites exclusive `lo..hi` to inclusive `lo..=(hi-1)` (the underlying PatRange mirror has no flag — single representation downstream). Empty exclusive ranges (`lo >= hi`) reported. No tuple-rest collision yet because tuple `..` rest stays Open (P4-pm-20). | `binding/match-range`. | `binding/match-range` (un-trimmed: both inclusive and exclusive arms preserved). | `match 1usize { 1usize..5usize => … }` works |
| P4-pm-24 | Variant pattern as tuple-pattern element (`match (e1, e2) { (E::Foo {..}, E::Bar { bar: _ }) => … }`) | ✅ Closed (2026-05-12) — sema's PAT_TUPLE element allow-list extended to include PAT_VARIANT_DATA; sub-pattern recursively built via `build_pattern(sub, elem_ty)` produces a PatVariantData LIR node. mlir-gen tuple-arm dispatch (both `gen_match` and `gen_expr_kind(EMatchExprView)`) extended to handle Variant subs: load enum-pointer from the tuple slot (Logos enum values are heap pointers per `logos_to_mlir(Enum) == ptr_type()`), GEP {0,0} into the enum's `llvm_type` to read the disc, compare to the variant's disc. `bind_pattern_ref`'s Tuple arm now recurses into VariantData sub-patterns so payload bindings (`(E::Foo { x }, _)`) reach the outer arm scope. Sister to P4-pm-23 (ranges as tuple elements). | n/a (was rejected at sema). | `tests/imported/pass/match/issue-5530` un-trimmed (faithful Rust port modulo `assert_eq!` → return-codes and `&Enum` borrows → by-value to fit Logos enum heap-ptr ABI). | `(Enum::Foo {..}, Enum::Bar {..}) => …` works |
| P4-pm-25 | Or-patterns in match arm LHS (`A {…} \| B => arm_body`) | ✅ Closed (2026-05-12) — grammar already admitted `pat_single (PIPE pat_single)*` emitting PAT_OR, and sema's build_pattern lowered to a PatOr LIR node. Two completion steps: (1) `collect_names` (the all-alts-must-bind-same-set check) now skips synth bindings (`__refut_*`, `__pat_pld_*`, `__sve_*`) introduced by P4-pm-01/02's side channels, since they're per-alt unique by construction. (2) `lower_match` fans an arm out into N effective arms when any alternative is `PAT_VARIANT`/`PAT_VARIANT_DATA` — the existing merged PatOr mlir-gen only does disc-eq and then extracts payload from `alts[0]`, which is wrong for mixed-shape alts (e.g. `Pass::Opaque {with_depth_pre_pass: true, ..} \| Pass::Transparent`: extracting `with_depth_pre_pass` from a Transparent value reads garbage). Fan-out re-uses the normal single-arm path per alt, so each alt's refutable-inner synth guard and payload extraction stay tied to the right variant. Scalar-only or-patterns (`1 \| 2 \| 3`) and same-variant alts skip fan-out. Body / user-guard AST is shared across the fanned arms — sema re-lowers it per arm so each gets fresh LIR (small code-size tax). Match-as-expression path unchanged for now: it doesn't fan out, so mixed-shape variant or-patterns in `let x = match … { A {…} \| B => v }` still hit the alts[0] extraction issue. | n/a (was rejected at sema or wrong at runtime). | `tests/imported/pass/match/issue-114691` un-trimmed (faithful Rust port modulo `Option<LoadOp>` → return-code check and dropped `#[inline(never)]` / `println!`). | `Pass::Opaque { with_depth_pre_pass: true, .. } \| Pass::Transparent => …` works |
| P4-pm-26 | Struct pattern with refutable field sub-pattern + match-AS-EXPRESSION struct scrutinee (G148-1 + sema root cause) | ✅ Closed (2026-05-22) — two layers. **(1) Sema root cause** (the "match-expr-struct mystery"): `bind_pattern_ref`'s Struct case looked up the struct via a BARE `structs_.find(sname)`, but `structs_`/`datatypes_` are keyed by package-qualified names — so it always missed, leaving every destructured field typed `Error`. The statement-form match masked this (the arm value type is unused), but match-as-EXPRESSION propagates the Error arm type to the whole match → `logos_to_mlir(Error)` is null → the function body emitted nothing (`{ llvm.unreachable }`). Fix: route through the package-aware `find_struct_by_name` / `find_datatype_by_name`. This alone unbroke ALL struct-scrutinee match-expressions (even fully-irrefutable `Wrap { x, y } => x + y`). **(2) Refutable field subs in codegen** (G148-1): `pat_test`/`pat_bind` (the recursive matcher) gained a `Struct` case (GEP each named field via `gep_field`, recurse for the test / the bind; logos field types from `all_struct_defs_`); both `gen_match` (stmt) and `gen_expr_kind(EMatchExprView)` (expr) gained a struct-arm dispatch branch that spills the struct ptr into an alloca-of-ptr and routes through `pat_test`. The expr `is_wild` test now uses a recursive `pat_irref` so only fully-irrefutable structs skip the dispatch. Sema's `build_pattern` struct-field allow-list widened to accept refutable subs (variant/tuple/range/or/struct). | n/a (sema mis-lookup → empty body; refutable struct-field subs unsupported in codegen). | `tests/logos/pass/match_struct_refutable_field` (stmt), `match_expr_struct_refutable_field` (expr), `match_expr_struct_irref_field` (expr irref). | `let r = match w { Wrap { x: Inner::A(v), y } => v + y, Wrap { x: Inner::B(v), y } => v - y };` works |
| B170-A | Bind a unit (`()`) enum payload field by NAME (`Err(err)` over `Result<(),()>`) | ✅ Closed (2026-05-25, `82931f72`) — an all-unit payload variant elides its field from the layout, so `binding_types` is empty; only a bare `_` was accepted, a named binding errored "expected 0 bindings, got 1". `lower_match`'s unit-payload special case now keeps each NAMED binding with a `()` (Void) binding type → `bind_pattern` defines it as a zero-sized unit local; re-wrapping `Err(err)` ignores the value. | rustc `issues/issue-41888` (`Err(err) => return Err(err)`). | `match/unit-payload-named-binding-b170`, `match/issue-41888-b170`. | `match r { Err(err) => return Err(err), Ok(u) => return Ok(u) }` over `Result<(),()>` works |
| B170-B | Assignment RHS gets the LHS enum type as a hint (`status = None`) | ✅ Closed (2026-05-25, `08bd77ac`) — sema gap not strictly pattern-match but surfaced via a re-match. `x = <enum-lit>` lowered the RHS without pinning the expected enum type (unlike `let x: T = …`); a bare `None` baked a C-style i32 discriminant into the pointer slot → a later `match x { Some(v) => … }` deref'd a bogus pointer → SIGSEGV. `lower_assign` now sets `hint_enum_type_ = var_type` while lowering the RHS. | rustc `issues/issue-41888` (`status = None` re-assigned in a loop). | `match/option-reassign-none-then-match-b170`, `match/issue-41888-b170`. | `status = None; match status { Some(x) => …, _ => … }` works |
| B170-D | Nested TUPLE sub-pattern in a variant payload (`Some((a, b))`, `Ok((a, (b, c)))`, OR of `Some(tuple)` alts) | ✅ Closed (2026-05-25) — only nested STRUCT / VARIANT_DATA payload subs were destructured by the arm-body builder; a nested PAT_TUPLE was registered but skipped, leaving its bindings undefined. Both `lower_match` and `lower_match_expr` now emit `let <name> = __synth.<i>` element reads for a PAT_TUPLE payload sub (recursing into nested tuples), unwrapping the grammar's single-alt PAT_OR wrapper around each tuple element. | rustc `or-patterns/issue-70413` (block 2: `Some((a,_)) \| Some((_,a))`). | `match/nested-tuple-in-variant-payload-b170`. | `match Some((3,42)) { Some((a, b)) => …, Some((a,_)) \| Some((_,a)) => … }` works |
| B170-E | Multi-alt OR-pattern INSIDE a variant payload, with guard (`Some((a,_) \| (_,a)) if a > 10`) | ✅ Closed (2026-05-25) — or-DISTRIBUTION without AST synthesis. A side channel `payload_or_alt_` tells `build_pattern_variant` which alternative of a multi-alt PAT_OR payload arg to select; `lower_match` / `lower_match_expr` detect a variant whose single payload arg is a multi-alt PAT_OR (after unwrapping the grammar's single-alt top-level PAT_OR) and fan the arm out one-per-alt (`Some(P\|Q)` → `Some(P) \| Some(Q)`), each re-evaluating the guard with its own bindings (Rust backtracks alts under a failing guard). ALSO fixed a B170-D guard-completeness bug this exposed: the nested-payload destructure (`let a = synth.i`) ran in the arm BODY, but gen_match evaluates the guard in a block that PRECEDES the body → a guard reading `a` crashed the compiler; the destructure is now factored into a lambda and a guarded arm wraps its guard in a block-expr running an independent copy first (the body keeps its own — a shared/leaked copy is unreliable, the block-expr shadow-restore reverts a binding already in scope from a sibling fanned arm). Single payload arg only (multi-arg ors would need a cartesian product). | `Some((a,b)) if a>0` crashed the compiler; `Some((a,_)\|(_,a)) if g` rejected at sema. | match/issue-70413-b170 (full faithful port), match/or-pattern-in-payload-guard-b170 (guard value checks + backtracking). | `match Some((3,42)) { Some((a,_) \| (_,a)) if a > 10 => … }` works (binds a=42) |
