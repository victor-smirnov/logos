package test;

#[tag_dispatch(TestTag)]
pub trait Printable {
    fn print(self: *const Self);
}

pub eidos TestTag { }

impl TestTag {
    pub unsafe fn read_tag(obj: *const u8) -> u64 { return 42; }
}

impl<T: Printable> Printable for *const T {
    pub fn print(self: *const *const T) {
        unsafe {
            // dummy
        }
    }
}

pub unsafe fn main() -> i32 {
    return 0;
}
