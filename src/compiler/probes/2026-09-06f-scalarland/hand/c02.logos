package c02;
struct P { x: i64, y: i64 }
fn main() -> i32 {
    let p: P = P { x: 3i64, y: 4i64 };
    let mut out: i64 = 0i64;
    match &p {
        P { x: a, y: b } => { out = a + b; }
    }
    if out != 7i64 { return 1i32; }
    return 0i32;
}
