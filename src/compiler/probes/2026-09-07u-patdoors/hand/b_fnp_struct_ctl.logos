package b_fnp_struct_ctl;
struct P { x: i64, y: i64 }
fn probe(P { x, y }: P) -> i32 {
    if x + y != 7i64 { return 1i32; }
    return 0i32;
}
fn main() -> i32 {
    let p: P = P { x: 3i64, y: 4i64 };
    return probe(p);
}
