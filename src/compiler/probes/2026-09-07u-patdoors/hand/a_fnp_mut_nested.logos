package a_fnp_mut_nested;
fn probe((mut a, (b, mut c)): (i64, (i64, i64))) -> i32 {
    a = a + 1i64;
    c = c + 2i64;
    if a + b + c != 12i64 { return 1i32; }
    return 0i32;
}
fn main() -> i32 {
    let t: (i64, (i64, i64)) = (3i64, (2i64, 4i64));
    return probe(t);
}
