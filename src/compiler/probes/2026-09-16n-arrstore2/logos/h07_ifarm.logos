package h07_ifarm;
fn main() -> i32 {
    let mut a: [i64; 2] = [1i64, 2i64];
    let c: bool = true;
    if c {
        let e: &i64 = &a[1u64];
        a[0u64] = *e + 1i64;
    }
    return (a[0u64] - 3i64) as i32;
}
