package a24_at_mut_body_addrof;
struct S { v: i64 }
fn bump(p: &mut S) { p.v = p.v + 1i64; }
fn main() -> i32 {
    let s: S = S { v: 1i64 };
    match s {
        n @ S { v: _ } => { bump(&mut n); }
    }
    return 0i32;
}
