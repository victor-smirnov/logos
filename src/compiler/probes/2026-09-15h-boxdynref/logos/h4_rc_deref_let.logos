package p;
use logos.lang.rc;
trait Sp { fn v(&self) -> i64; }
struct A { x: i64 }
impl Sp for A { fn v(&self) -> i64 { return self.x; } }
fn main() -> i32 {
    let r: Rc<dyn Sp> = rc_new(A { x: 42i64 });
    let q: &dyn Sp = &*r;
    if q.v() != 42i64 { return 3i32; }
    return 0i32;
}
