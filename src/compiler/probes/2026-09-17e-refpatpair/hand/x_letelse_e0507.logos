// hand battery: round 2026-09-17e, program x_letelse_e0507 — ABUSE twin of h_letelse.
// legality: rustc 1.98.1 E0507 "cannot move out of `r` as enum variant `Some` which is behind a shared reference". MUST STAY REFUSED.
package x_letelse_e0507;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
fn get(r: &Option<D>) -> i64 {
    let &Option::Some(x) = r else { return 77i64; };
    return x.v;
}
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    let o: Option<D> = Option::Some(D { v: 2i64, c: p });
    unsafe { printf("got=%ld\n".as_ptr(), get(&o)); }
    return 0i32;
}
