package hb_g03;
use logos.lang.cmp;

fn main() -> i32 {
    let mut a: i64 = 1i64;
    let b: i64 = 2i64;
    let ra: &mut i64 = &mut a;
    let rb: &i64 = &b;
    if !(ra < rb) { return 2i32; }
    return 0i32;
}
