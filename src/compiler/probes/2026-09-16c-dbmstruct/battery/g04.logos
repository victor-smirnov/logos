package hb_g04;
use logos.lang.cmp;

fn main() -> i32 {
    let mut a: [i64; 2] = [1i64, 2i64];
    let b: [i64; 2] = [1i64, 3i64];
    let ra: &mut [i64; 2] = &mut a;
    let rb: &[i64; 2] = &b;
    if !(ra < rb) { return 2i32; }
    return 0i32;
}
