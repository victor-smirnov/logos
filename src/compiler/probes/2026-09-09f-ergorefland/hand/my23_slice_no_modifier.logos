// LEGAL: array elements under `&` with NO modifier written.
package my23;
fn main() -> i32 {
    let arr: [i64; 2] = [7i64, 8i64];
    let mut out: i64 = 0i64;
    match &arr {
        [a, b] => { out = a + b; }
    }
    if out != 15i64 { return 1i32; }
    return 0i32;
}
