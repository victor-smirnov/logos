package d1;
struct TS(i64, i64);
fn main() -> i32 {
    let t: TS = TS(5i64, 6i64);
    let mut out: i64 = 0i64;
    match &t {
        TS(mut a, b) => { a = a + 1i64; out = a + *b; }
    }
    if out != 12i64 { return 1i32; }
    return 0i32;
}
