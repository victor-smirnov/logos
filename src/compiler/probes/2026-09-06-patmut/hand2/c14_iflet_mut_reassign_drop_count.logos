package c14_iflet_mut_reassign_drop_count;
use logos.std.fmt;
struct S { v: i64 }
impl Drop for S { fn drop(self: &mut S) { println!("DROP v={}", self.v); } }
fn main() -> i32 {
    let o: Option<S> = Option::Some(S { v: 1i64 });
    if let Option::Some(mut s) = o {
        s = S { v: 2i64 };
        s.v = s.v + 5i64;
    }
    println!("-- end");
    return 0i32;
}
