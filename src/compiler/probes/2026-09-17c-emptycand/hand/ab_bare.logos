// hand battery: round 2026-09-17c-emptycand, abuse ab_bare — rustc E0515, must STAY refused
package ab_bare;
static V: i64 = 5i64;
fn pick<'a>(x: &'a i64, y: &'a i64) -> &'a i64 { if *x > *y { return x; } return y; }
fn escape() -> &'static i64 { let n: i64 = 9i64; return pick(&V, &n); }
fn main() -> i32 { return (*escape()) as i32; }
