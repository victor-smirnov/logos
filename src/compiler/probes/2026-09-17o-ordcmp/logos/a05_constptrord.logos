package a05_constptrord;
extern fn printf(fmt: *const u8, ...) -> i32;
fn main() -> i32 {
    let a: [u8; 4] = [1u8, 2u8, 3u8, 4u8];
    let p: *const u8 = &a[0u64] as *const u8;
    let q: *const u8 = &a[3u64] as *const u8;
    let lt: bool = p < q;
    unsafe { printf("lt=%d\n".as_ptr(), lt as i32); }
    if !lt { return 1i32; }
    return 0i32;
}
