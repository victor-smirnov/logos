// CONTROL: `mut` (by value) tuple binding — landed earlier, must not move.
package h15;
fn main() -> i32 {
    let t: (i64, i64) = (3i64, 4i64);
    match t {
        (mut a, b) => { a = a + 1i64; if a + b != 8i64 { return 1i32; } }
    }
    return 0i32;
}
