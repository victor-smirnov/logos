// ILLEGAL: write through a SHARED `ref` binding. Must stay refused.
package h04;
fn main() -> i32 {
    let mut t: (i64, i64) = (3i64, 4i64);
    match t {
        (ref a, b) => { *a = *a + 1i64; if *a + b != 8i64 { return 1i32; } }
    }
    return 0i32;
}
