package t03_struct_under_ref_ref;
struct P { x: i64, y: i64 }
fn main() -> i32 {
    let p: P = P { x: 1i64, y: 2i64 };
    let mut out: i64 = 0i64;
    match &p {
        P { ref x, y } => { out = *x + 1i64; }
    }
    if out != 2i64 { return 1i32; }
    return 0i32;
}
