package b16v_struct_field_at_value;
struct P { x: i64, y: i64 }
fn main() -> i32 {
    let p: P = P { x: 1i64, y: 2i64 };
    let mut out: i64 = 0i64;
    match p {
        P { x: a @ _, y } => { out = a * 100i64 + y; }
    }
    return out as i32;
}
