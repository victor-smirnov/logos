package b02_let_struct_mut;
struct P { x: i64, y: i64 }
fn main() -> i32 {
    let p: P = P { x: 1i64, y: 2i64 };
    let P { mut x, y } = p;
    x = x + y;
    if x != 3i64 { return 1i32; }
    return 0i32;
}
