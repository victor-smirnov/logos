ceiling-probe: unarmed baseline (from the store where it is already there)
ceiling-probe: armed run — LOGOS_PROBE=loanbk2
fail-oracle: 1507 fail fixtures recorded (rc, stderr sha, .expected match) -> build/probe/failtext-dd621cba2c9a14e2-loanbk2.tsv
ceiling-probe: 'loanbk2'  builds 1072 (unarmed) -> 1074 (armed)
probe: fired 22915432 times
probe: CEILING = 0 rows
probe: COST    = 0 legal programs refused   [saw: pass(ledger+legal) fail(text) stdlib]
probe:           a LOWER bound — the corpus refuses only what it CONTAINS;
probe:           write the counter-examples)
probe: COST-fail = 1 of 1507 `-L bc -L fail` fixtures changed (rc 0, .expected-match 0, text-only 1)
probe:   TEXT logos_06_diagnostics_fail_issue-62007-assign-differing-fields--t22  stderr changed, .expected still matches
probe:   ⚠ the text-only rows are INVISIBLE TO ctest: run_test.sh matches
probe:     .expected as a grep -F SUBSTRING, so those fixtures stay GREEN.
probe: stdlib: all four layers compile under 'loanbk2'
probe: ⛔ it changes MORE pinned diagnostics (1)
probe:    than it closes rows (0). Read every one before funding it — a
probe:    branch that only re-words an already-red diagnostic buys nothing.
probe: ⚠ a CEILING bounds the COUNT, not the SET: diff these names against what
probe:   you predicted BY NAME. A matching count can be two errors cancelling.
