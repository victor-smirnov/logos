package g1_nongeneric;
struct D { v: i64 }
fn h(a: &&D) -> *const D {
    let x: &D = *a;
    return x as *const D;
}
fn g(x: &D) -> bool {
    return h(&x) == x as *const D;
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    if !g(&d) { return 1i32; }
    return 0i32;
}
