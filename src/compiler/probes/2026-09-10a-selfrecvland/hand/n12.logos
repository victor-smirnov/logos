// n12: REFUSE — stdlib `Drop` written by value. The corpus conversion's
// acceptance test: this is the shape 33 files carried.
package n12;
struct Foo { c: *mut i64 }
impl Drop for Foo {
    fn drop(self: Foo) { unsafe { *self.c = *self.c + 1i64; } }
}
fn main() -> i32 {
    let mut counter: i64 = 0i64;
    let c = &mut counter as *mut i64;
    { let f = Foo { c: c }; }
    return counter as i32;
}
