package p04_refref_pat_local;
struct P { x: i32 }
fn main() -> i32 {
    let p = P { x: 5i32 };
    let r: &P = &p;
    let pp: &&P = &r;
    match pp { &&P { x } => { if x != 5i32 { return 4i32; } } }
    return 0i32;
}
