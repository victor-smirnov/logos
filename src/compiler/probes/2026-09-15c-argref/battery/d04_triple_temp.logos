package d04_triple_temp;
struct D { v: i64 }
fn inner(a: &&D) -> *const D {
    let x: &D = *a;
    return x as *const D;
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    let rd: &D = &d;
    let p: &&&D = &&rd;
    if inner(*p) != rd as *const D { return 4i32; }
    return 0i32;
}
