package c03_deferred_init;
struct D { v: i64 }
fn inner(a: &&D) -> *const D {
    let x: &D = *a;
    return x as *const D;
}
fn main() -> i32 {
    let d: D = D { v: 6i64 };
    let rd: &D;
    rd = &d;
    if inner(&rd) != &d as *const D { return 3i32; }
    return 0i32;
}
