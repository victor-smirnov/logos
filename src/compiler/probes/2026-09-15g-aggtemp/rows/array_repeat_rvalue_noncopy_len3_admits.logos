// SOUNDNESS QUEUE row array_repeat_rvalue_noncopy_len3_admits (tier 2, admits). `[D{..}; 3]` WITH A NON-Copy OPERAND IS ADMITTED.
// rustc refuses: E0277 "the trait bound `D: Copy` is not satisfied" (the repeat operand must be Copy for a count above 1 unless it
// is a const). TODAY it compiles and runs, re-lowering the operand once per slot (three distinct D, n=3 at the end).
// The LOCAL operand `[a; 2]` is refused with the wrong sentence (row array_repeat_len2_noncopy_says_use_of_moved); this rvalue
// spelling is not refused at all. Found BESIDE round 2026-09-15g-aggtemp (hand c09). Not this class; unchanged by its arms.
package array_repeat_rvalue_noncopy_len3_admits;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c + self.v; } } }
fn main() -> i32 {
    let mut n: i64 = 0i64;
    let p: *mut i64 = &mut n;
    {
        let arr: [D; 3] = [D { v: 1i64, c: p }; 3];
        if arr[2].v != 1i64 { return 3i32; }
    }
    let got: i64 = unsafe { n };
    unsafe { printf("n=%ld\n".as_ptr(), got); }
    return 0i32;
}
// rustc 1.98.1 --edition 2024 on the twin src/compiler/probes/2026-09-15g-aggtemp/rust/rows/array_repeat_rvalue_noncopy_len3_admits.rs: rustc=1 run=- codes=error[E0277]
