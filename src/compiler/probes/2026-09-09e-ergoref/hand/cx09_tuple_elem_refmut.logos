// ILLEGAL under Rust 2024: written `ref mut` at the tuple-element door under a
// `&mut` scrutinee.
package cx09;
fn main() -> i32 {
    let mut t: (i64, i64) = (1i64, 2i64);
    match &mut t {
        (ref mut a, b) => { *a = *a + 1i64; }
    }
    return 0i32;
}
