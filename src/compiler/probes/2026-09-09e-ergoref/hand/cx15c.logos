// Minimal: top-level `ref w` over a NON-reference scrutinee (a plain i64).
package cx15c;
fn main() -> i32 {
    let n: i64 = 3i64;
    match n {
        ref w => { if *w != 3i64 { return 1i32; } return 0i32; }
    }
}
