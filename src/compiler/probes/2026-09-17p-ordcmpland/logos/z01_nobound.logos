// abuse: unbounded &T ordering. rustc E0369. MUST stay refused.
package z01_nobound;
fn less<T>(a: &T, b: &T) -> bool {
    return a < b;
}
fn main() -> i32 {
    let x: i64 = 3i64;
    let y: i64 = 8i64;
    if less::<i64>(&x, &y) { return 1i32; }
    return 0i32;
}
