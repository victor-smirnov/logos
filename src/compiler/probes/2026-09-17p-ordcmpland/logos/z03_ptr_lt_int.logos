// abuse: raw pointer ordered against integer literal. rustc: no impl. MUST stay refused.
package z03_ptr_lt_int;
fn main() -> i32 {
    let mut v: i64 = 1i64;
    let p: *mut i64 = &mut v as *mut i64;
    if p < 0 { return 1i32; }
    return 0i32;
}
