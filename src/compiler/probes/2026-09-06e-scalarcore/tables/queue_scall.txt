[soundness] canary: a clean program ran and exited 0, a wrong one exited 3, a refusal read as refused
FAIL: row 'range_pattern_under_ref_scrutinee' (tier 3) NO LONGER REPRODUCES — the program now compiles clean (cc=0 diag=0 run=0).
      That is a defect CLOSING, and it must not be silent: delete the row,
      decrement '# TOTAL', and land tests/soundness/open/range_pattern_under_ref_scrutinee.logos as a pass or fail fixture
      in the same commit — the row leaving IS the record of the fix. If
      nothing in this change was meant to touch it, it closed by accident
      or was never real; either way the row may not stand.
tests/logos/soundness_queue_gate.sh: line 88: 1047925 Aborted                 timeout 60 "$d/t" > "$d/stdout" 2> /dev/null
tests/logos/soundness_queue_gate.sh: line 88: 1062876 Segmentation fault      (core dumped) "$LOGOSC" "$src" -o "$d/t.o" > "$d/cc.out" 2> "$O_ERR"
FAIL: row 'literal_patterns_under_ref_scrutinee' (tier 3) NO LONGER REPRODUCES — the program now compiles clean (cc=0 diag=0 run=0).
      That is a defect CLOSING, and it must not be silent: delete the row,
      decrement '# TOTAL', and land tests/soundness/open/literal_patterns_under_ref_scrutinee.logos as a pass or fail fixture
      in the same commit — the row leaving IS the record of the fix. If
      nothing in this change was meant to touch it, it closed by accident
      or was never real; either way the row may not stand.
FAIL: row 'refpat_scalar_under_ref_mlir_abort' (tier 3) NO LONGER REPRODUCES — the program now compiles clean (cc=0 diag=0 run=0).
      That is a defect CLOSING, and it must not be silent: delete the row,
      decrement '# TOTAL', and land tests/soundness/open/refpat_scalar_under_ref_mlir_abort.logos as a pass or fail fixture
      in the same commit — the row leaving IS the record of the fix. If
      nothing in this change was meant to touch it, it closed by accident
      or was never real; either way the row may not stand.
tests/logos/soundness_queue_gate.sh: line 88: 1093541 Segmentation fault      timeout 60 "$d/t" > "$d/stdout" 2> /dev/null
GATE_RC=1
