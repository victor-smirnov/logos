package r3;
fn bar() -> impl Fn() -> i64 {
    let mut x: i64 = 0i64;
    return move || -> i64 { x += 1i64; return x; };
}
fn main() -> i32 {
    let f = bar();
    let a: i64 = f();
    let b: i64 = f();
    return (a * 10i64 + b) as i32;
}
