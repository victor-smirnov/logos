// hand battery: round 2026-09-16f-atbind, program b03 — tuple @ binder, both elements read, no Drop type involved
// legality: rustc 1.98.1 twin measured (see rust/b03.rs)
package b03;
extern fn printf(fmt: *const u8, ...) -> i32;
struct D { v: i64, c: *mut i64 }
impl Drop for D { fn drop(self: &mut D) { unsafe { *self.c = *self.c * 10i64 + self.v; } } }
fn rd(p: *mut i64) -> i64 { return unsafe { *p }; }
fn g() -> i64 {
    let t: (i64, i64) = (11i64, 22i64);
    match t {
        y @ (_, _) => { return y.0 * 100i64 + y.1; }
    }
}
fn main() -> i32 {
    let k: i64 = g();
    unsafe { printf("k=%ld\n".as_ptr(), k); }
    if k != 1122i64 { return 1i32; }
    return 0i32;
}
