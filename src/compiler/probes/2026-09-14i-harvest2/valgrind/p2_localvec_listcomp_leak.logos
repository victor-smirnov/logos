// p2 — the same producer, the OTHER door: the list comprehension at
// sema_expr.cpp:13086/13098 (`find_struct_by_name("Vec")` then
// `make_generic_struct("Vec", …)`).
package p2_localvec_listcomp;
use logos.mem.collections.vec;

pub struct Vec<T> { a: i64, b: i64 }

fn main() -> i32 {
    let src: [i64; 3] = [1i64, 2i64, 3i64];
    let v = [x * 2i64 for x in src];
    return 0i32;
}
