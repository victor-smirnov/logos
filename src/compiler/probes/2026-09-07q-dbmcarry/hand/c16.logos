package c16;
use logos.std.fmt;
struct S { pub n: i64 }
impl Drop for S { fn drop(self: &mut S) { println!("D{}", self.n); } }
fn main() -> i32 {
    let p: (S, i64) = (S { n: 5i64 }, 9i64);
    let (s, b) = &p;
    let out: i64 = s.n + *b;
    if out != 14i64 { return 1i32; }
    return 0i32;
}
