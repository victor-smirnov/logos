package f13_u64_mut;
fn main() -> i32 {
    let mut acc: u64 = 0u64;
    let n: u64 = 3u64;
    for mut i in 0u64..n {
        i = i + 1u64;
        acc = acc + i;
    }
    if acc != 6u64 { return 1i32; }
    return 0i32;
}
