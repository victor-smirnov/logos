// Generated by peg_gen — DO NOT EDIT.
// SPDX-License-Identifier: Apache-2.0

#include "logos_parser.hpp"
#include <logos/hermes/access.hpp>
#include <logos/verification/assert.hpp>
#include <charconv>
#include <cctype>

namespace logos::compiler {

using namespace logos_ast;
using logos::hermes::AnyVal;
using TK = TK_LOGOS;

// ── Lexer ─────────────────────────────────────────────────────────────────

LogosParser::LogosParser(std::string_view source) : source_(source) {}

LogosParser::Token LogosParser::next_token() {
    if (have_la_) { have_la_ = false; return la_; }
    return lex_one();
}

LogosParser::Token LogosParser::peek_token() {
    if (!have_la_) { la_ = lex_one(); have_la_ = true; }
    return la_;
}

bool LogosParser::try_token(TK kind) {
    if (peek_token().kind == kind) { next_token(); return true; }
    return false;
}

LogosParser::Token LogosParser::expect_token(TK kind, std::string_view what) {
    Token t = next_token();
    LOGOS_ASSERT(t.kind == kind, "LOGOS-LEX-001",
        "expected {}, got '{}'", what, t.text);
    return t;
}

LogosParser::Token LogosParser::lex_one() {
    // Skip whitespace and comments.
    restart:
    while (pos_ < source_.size()) {
        char c = source_[pos_];
        if (c == ' ' || c == '\t' || c == '\n' || c == '\r') { if (c == '\n') ++line_; ++pos_; continue; }
        if (c == '/' && pos_+1 < source_.size() && source_[pos_+1] == '/') {
            while (pos_ < source_.size() && source_[pos_] != '\n') ++pos_;
            continue; }
        if (c == '/' && pos_+1 < source_.size() && source_[pos_+1] == '*') {
            pos_ += 2;
            while (pos_+1 < source_.size() &&
                   !(source_[pos_] == '*' && source_[pos_+1] == '/')) ++pos_;
            if (pos_+1 < source_.size()) pos_ += 2;
            continue; }
        break;
    }
    if (pos_ >= source_.size()) return {TK::Eof, {}, line_};
    size_t   start      = pos_;
    uint32_t start_line_ = line_;
    char     c           = source_[pos_];
    (void)c;

    // Keyword / punctuation literals.
    if (source_.substr(pos_, 8).size() == 8 &&
        source_.substr(pos_, 8) == "continue" &&
        (pos_ + 8 >= source_.size() || (!std::isalnum(source_[pos_ + 8]) && source_[pos_ + 8] != '_'))) {
        pos_ += 8; return {TK::KW_CONTINUE, source_.substr(start, 8), start_line_}; }
    if (source_.substr(pos_, 7).size() == 7 &&
        source_.substr(pos_, 7) == "package" &&
        (pos_ + 7 >= source_.size() || (!std::isalnum(source_[pos_ + 7]) && source_[pos_ + 7] != '_'))) {
        pos_ += 7; return {TK::KW_PACKAGE, source_.substr(start, 7), start_line_}; }
    if (source_.substr(pos_, 6).size() == 6 &&
        source_.substr(pos_, 6) == "static" &&
        (pos_ + 6 >= source_.size() || (!std::isalnum(source_[pos_ + 6]) && source_[pos_ + 6] != '_'))) {
        pos_ += 6; return {TK::KW_STATIC, source_.substr(start, 6), start_line_}; }
    if (source_.substr(pos_, 6).size() == 6 &&
        source_.substr(pos_, 6) == "struct" &&
        (pos_ + 6 >= source_.size() || (!std::isalnum(source_[pos_ + 6]) && source_[pos_ + 6] != '_'))) {
        pos_ += 6; return {TK::KW_STRUCT, source_.substr(start, 6), start_line_}; }
    if (source_.substr(pos_, 6).size() == 6 &&
        source_.substr(pos_, 6) == "extern" &&
        (pos_ + 6 >= source_.size() || (!std::isalnum(source_[pos_ + 6]) && source_[pos_ + 6] != '_'))) {
        pos_ += 6; return {TK::KW_EXTERN, source_.substr(start, 6), start_line_}; }
    if (source_.substr(pos_, 6).size() == 6 &&
        source_.substr(pos_, 6) == "return" &&
        (pos_ + 6 >= source_.size() || (!std::isalnum(source_[pos_ + 6]) && source_[pos_ + 6] != '_'))) {
        pos_ += 6; return {TK::KW_RETURN, source_.substr(start, 6), start_line_}; }
    if (source_.substr(pos_, 5).size() == 5 &&
        source_.substr(pos_, 5) == "match" &&
        (pos_ + 5 >= source_.size() || (!std::isalnum(source_[pos_ + 5]) && source_[pos_ + 5] != '_'))) {
        pos_ += 5; return {TK::KW_MATCH, source_.substr(start, 5), start_line_}; }
    if (source_.substr(pos_, 5).size() == 5 &&
        source_.substr(pos_, 5) == "const" &&
        (pos_ + 5 >= source_.size() || (!std::isalnum(source_[pos_ + 5]) && source_[pos_ + 5] != '_'))) {
        pos_ += 5; return {TK::KW_CONST, source_.substr(start, 5), start_line_}; }
    if (source_.substr(pos_, 5).size() == 5 &&
        source_.substr(pos_, 5) == "break" &&
        (pos_ + 5 >= source_.size() || (!std::isalnum(source_[pos_ + 5]) && source_[pos_ + 5] != '_'))) {
        pos_ += 5; return {TK::KW_BREAK, source_.substr(start, 5), start_line_}; }
    if (source_.substr(pos_, 5).size() == 5 &&
        source_.substr(pos_, 5) == "while" &&
        (pos_ + 5 >= source_.size() || (!std::isalnum(source_[pos_ + 5]) && source_[pos_ + 5] != '_'))) {
        pos_ += 5; return {TK::KW_WHILE, source_.substr(start, 5), start_line_}; }
    if (source_.substr(pos_, 5).size() == 5 &&
        source_.substr(pos_, 5) == "false" &&
        (pos_ + 5 >= source_.size() || (!std::isalnum(source_[pos_ + 5]) && source_[pos_ + 5] != '_'))) {
        pos_ += 5; return {TK::KW_FALSE, source_.substr(start, 5), start_line_}; }
    if (source_.substr(pos_, 4).size() == 4 &&
        source_.substr(pos_, 4) == "true" &&
        (pos_ + 4 >= source_.size() || (!std::isalnum(source_[pos_ + 4]) && source_[pos_ + 4] != '_'))) {
        pos_ += 4; return {TK::KW_TRUE, source_.substr(start, 4), start_line_}; }
    if (source_.substr(pos_, 4).size() == 4 &&
        source_.substr(pos_, 4) == "else" &&
        (pos_ + 4 >= source_.size() || (!std::isalnum(source_[pos_ + 4]) && source_[pos_ + 4] != '_'))) {
        pos_ += 4; return {TK::KW_ELSE, source_.substr(start, 4), start_line_}; }
    if (source_.substr(pos_, 4).size() == 4 &&
        source_.substr(pos_, 4) == "loop" &&
        (pos_ + 4 >= source_.size() || (!std::isalnum(source_[pos_ + 4]) && source_[pos_ + 4] != '_'))) {
        pos_ += 4; return {TK::KW_LOOP, source_.substr(start, 4), start_line_}; }
    if (source_.substr(pos_, 4).size() == 4 &&
        source_.substr(pos_, 4) == "enum" &&
        (pos_ + 4 >= source_.size() || (!std::isalnum(source_[pos_ + 4]) && source_[pos_ + 4] != '_'))) {
        pos_ += 4; return {TK::KW_ENUM, source_.substr(start, 4), start_line_}; }
    if (source_.substr(pos_, 4).size() == 4 &&
        source_.substr(pos_, 4) == "type" &&
        (pos_ + 4 >= source_.size() || (!std::isalnum(source_[pos_ + 4]) && source_[pos_ + 4] != '_'))) {
        pos_ += 4; return {TK::KW_TYPE, source_.substr(start, 4), start_line_}; }
    if (source_.substr(pos_, 3).size() == 3 &&
        source_.substr(pos_, 3) == "for" &&
        (pos_ + 3 >= source_.size() || (!std::isalnum(source_[pos_ + 3]) && source_[pos_ + 3] != '_'))) {
        pos_ += 3; return {TK::KW_FOR, source_.substr(start, 3), start_line_}; }
    if (source_.substr(pos_, 3).size() == 3 &&
        source_.substr(pos_, 3) == "use" &&
        (pos_ + 3 >= source_.size() || (!std::isalnum(source_[pos_ + 3]) && source_[pos_ + 3] != '_'))) {
        pos_ += 3; return {TK::KW_USE, source_.substr(start, 3), start_line_}; }
    if (source_.substr(pos_, 3).size() == 3 &&
        source_.substr(pos_, 3) == "mut" &&
        (pos_ + 3 >= source_.size() || (!std::isalnum(source_[pos_ + 3]) && source_[pos_ + 3] != '_'))) {
        pos_ += 3; return {TK::KW_MUT, source_.substr(start, 3), start_line_}; }
    if (source_.substr(pos_, 3).size() == 3 &&
        source_.substr(pos_, 3) == "let" &&
        (pos_ + 3 >= source_.size() || (!std::isalnum(source_[pos_ + 3]) && source_[pos_ + 3] != '_'))) {
        pos_ += 3; return {TK::KW_LET, source_.substr(start, 3), start_line_}; }
    if (source_.substr(pos_, 3).size() == 3 &&
        source_.substr(pos_, 3) == "pub" &&
        (pos_ + 3 >= source_.size() || (!std::isalnum(source_[pos_ + 3]) && source_[pos_ + 3] != '_'))) {
        pos_ += 3; return {TK::KW_PUB, source_.substr(start, 3), start_line_}; }
    if (source_.substr(pos_, 3).size() == 3 &&
        source_.substr(pos_, 3) == "..=") {
        pos_ += 3; return {TK::DOTDOTEQ, source_.substr(start, 3), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "->") {
        pos_ += 2; return {TK::ARROW, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "&&") {
        pos_ += 2; return {TK::AND, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "||") {
        pos_ += 2; return {TK::OR, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "==") {
        pos_ += 2; return {TK::EQ, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "!=") {
        pos_ += 2; return {TK::NE, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "<=") {
        pos_ += 2; return {TK::LE, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == ">=") {
        pos_ += 2; return {TK::GE, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "..") {
        pos_ += 2; return {TK::DOTDOT, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "=>") {
        pos_ += 2; return {TK::FATARROW, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "::") {
        pos_ += 2; return {TK::COLONCOLON, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "as" &&
        (pos_ + 2 >= source_.size() || (!std::isalnum(source_[pos_ + 2]) && source_[pos_ + 2] != '_'))) {
        pos_ += 2; return {TK::KW_AS, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "in" &&
        (pos_ + 2 >= source_.size() || (!std::isalnum(source_[pos_ + 2]) && source_[pos_ + 2] != '_'))) {
        pos_ += 2; return {TK::KW_IN, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "fn" &&
        (pos_ + 2 >= source_.size() || (!std::isalnum(source_[pos_ + 2]) && source_[pos_ + 2] != '_'))) {
        pos_ += 2; return {TK::KW_FN, source_.substr(start, 2), start_line_}; }
    if (source_.substr(pos_, 2).size() == 2 &&
        source_.substr(pos_, 2) == "if" &&
        (pos_ + 2 >= source_.size() || (!std::isalnum(source_[pos_ + 2]) && source_[pos_ + 2] != '_'))) {
        pos_ += 2; return {TK::KW_IF, source_.substr(start, 2), start_line_}; }
    if (c == '<') { ++pos_; return {TK::LT, source_.substr(start, 1), start_line_}; }
    if (c == ';') { ++pos_; return {TK::SEMI, source_.substr(start, 1), start_line_}; }
    if (c == ':') { ++pos_; return {TK::COLON, source_.substr(start, 1), start_line_}; }
    if (c == '=') { ++pos_; return {TK::ASSIGN, source_.substr(start, 1), start_line_}; }
    if (c == '!') { ++pos_; return {TK::BANG, source_.substr(start, 1), start_line_}; }
    if (c == '%') { ++pos_; return {TK::PERCENT, source_.substr(start, 1), start_line_}; }
    if (c == '/') { ++pos_; return {TK::SLASH, source_.substr(start, 1), start_line_}; }
    if (c == '-') { ++pos_; return {TK::MINUS, source_.substr(start, 1), start_line_}; }
    if (c == '+') { ++pos_; return {TK::PLUS, source_.substr(start, 1), start_line_}; }
    if (c == '>') { ++pos_; return {TK::GT, source_.substr(start, 1), start_line_}; }
    if (c == '*') { ++pos_; return {TK::STAR, source_.substr(start, 1), start_line_}; }
    if (c == '.') { ++pos_; return {TK::DOT, source_.substr(start, 1), start_line_}; }
    if (c == ',') { ++pos_; return {TK::COMMA, source_.substr(start, 1), start_line_}; }
    if (c == '{') { ++pos_; return {TK::LBRACE, source_.substr(start, 1), start_line_}; }
    if (c == '}') { ++pos_; return {TK::RBRACE, source_.substr(start, 1), start_line_}; }
    if (c == '[') { ++pos_; return {TK::LBRACKET, source_.substr(start, 1), start_line_}; }
    if (c == ']') { ++pos_; return {TK::RBRACKET, source_.substr(start, 1), start_line_}; }
    if (c == '(') { ++pos_; return {TK::LPAREN, source_.substr(start, 1), start_line_}; }
    if (c == ')') { ++pos_; return {TK::RPAREN, source_.substr(start, 1), start_line_}; }
    if (c == '&') { ++pos_; return {TK::AMP, source_.substr(start, 1), start_line_}; }

    // STRING = /"([^"\\]|\\.)*"/
    if (c == '"') {
        ++pos_;
        while (pos_ < source_.size() && source_[pos_] != '"') {
            if (source_[pos_] == '\\') ++pos_;
            ++pos_;
        }
        if (pos_ < source_.size()) ++pos_;
        return {TK::STRING, source_.substr(start, pos_ - start), start_line_};
    }

    // INTEGER = /[-]?[0-9]+/
    if (std::isdigit(c) || (c == '-' && pos_+1 < source_.size() && std::isdigit(source_[pos_+1]))) {
        if (c == '-') ++pos_;
        while (pos_ < source_.size() && std::isdigit(source_[pos_])) ++pos_;
        return {TK::INTEGER, source_.substr(start, pos_ - start), start_line_};
    }

    // IDENT = /[a-zA-Z_][a-zA-Z0-9_]*/
    if (std::isalpha(c) || c == '_') {
        while (pos_ < source_.size() && (std::isalnum(source_[pos_]) || source_[pos_] == '_'))
            ++pos_;
        return {TK::IDENT, source_.substr(start, pos_ - start), start_line_};
    }

    ++pos_; // unknown character — skip
    goto restart;
}

// ── Public entry points ───────────────────────────────────────────────────

logos::hermes::HermesCtr LogosParser::parse_module() {
    doc_ = logos::hermes::make_doc(524288).get();
    AnyVal root = rule_module();
    LOGOS_ASSERT(!root.is_null(), "LOGOS-PARSE-001", "parse_module: expected module");
    logos::hermes::HermesCtrAccess::set_root_offset(doc_, root.to_offset());
    return std::move(doc_);
}

// ── Grammar rules ─────────────────────────────────────────────────────────

AnyVal LogosParser::rule_module() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_PACKAGE IDENT DOT IDENT SEMI use_decl* item* => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            auto rcap_0 = doc_.make_array(4).get();
            if (peek_token().kind != TK::KW_PACKAGE) goto bt_module_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_module_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::DOT) goto bt_module_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_module_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            if (peek_token().kind != TK::SEMI) goto bt_module_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            auto arr_1 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_1 = rule_use_decl();
                        if (rep_item_1.is_null()) goto rep_fail_1;
                        rcap_0.push_back(rep_item_1).get();
                        if (!rep_item_1.is_null()) arr_1.push_back(rep_item_1).get();
                        continue;
                    }
                    rep_fail_1: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap6 = arr_1.to_anyval();
            auto arr_2 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_2 = rule_item();
                        if (rep_item_2.is_null()) goto rep_fail_2;
                        rcap_0.push_back(rep_item_2).get();
                        if (!rep_item_2.is_null()) arr_2.push_back(rep_item_2).get();
                        continue;
                    }
                    rep_fail_2: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap7 = arr_2.to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 7).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::MODULE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::USES, cap6, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::PATH, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::ITEMS, rcap_0.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_module_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: KW_PACKAGE IDENT SEMI use_decl* item* => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            auto rcap_3 = doc_.make_array(4).get();
            if (peek_token().kind != TK::KW_PACKAGE) goto bt_module_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_module_1;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::SEMI) goto bt_module_1;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto arr_4 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_4 = rule_use_decl();
                        if (rep_item_4.is_null()) goto rep_fail_4;
                        rcap_3.push_back(rep_item_4).get();
                        if (!rep_item_4.is_null()) arr_4.push_back(rep_item_4).get();
                        continue;
                    }
                    rep_fail_4: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap4 = arr_4.to_anyval();
            auto arr_5 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_5 = rule_item();
                        if (rep_item_5.is_null()) goto rep_fail_5;
                        rcap_3.push_back(rep_item_5).get();
                        if (!rep_item_5.is_null()) arr_5.push_back(rep_item_5).get();
                        continue;
                    }
                    rep_fail_5: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap5 = arr_5.to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::MODULE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::USES, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::ITEMS, rcap_3.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_module_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_use_decl() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_USE IDENT DOT IDENT SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_USE) goto bt_use_decl_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_use_decl_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::DOT) goto bt_use_decl_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_use_decl_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            if (peek_token().kind != TK::SEMI) goto bt_use_decl_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::USE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::PATH, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_use_decl_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_item() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: const_def
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_const_def();
            if (cap1.is_null()) goto bt_item_0;
            return cap1;
        }
        [[maybe_unused]] bt_item_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: type_alias
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_type_alias();
            if (cap1.is_null()) goto bt_item_1;
            return cap1;
        }
        [[maybe_unused]] bt_item_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 3: enum_def
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_enum_def();
            if (cap1.is_null()) goto bt_item_2;
            return cap1;
        }
        [[maybe_unused]] bt_item_2:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 4: struct_def
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_struct_def();
            if (cap1.is_null()) goto bt_item_3;
            return cap1;
        }
        [[maybe_unused]] bt_item_3:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 5: extern_fn_def
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_extern_fn_def();
            if (cap1.is_null()) goto bt_item_4;
            return cap1;
        }
        [[maybe_unused]] bt_item_4:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 6: pub_fn_def
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_pub_fn_def();
            if (cap1.is_null()) goto bt_item_5;
            return cap1;
        }
        [[maybe_unused]] bt_item_5:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 7: fn_def
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_fn_def();
            if (cap1.is_null()) goto bt_item_6;
            return cap1;
        }
        [[maybe_unused]] bt_item_6:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_const_def() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_CONST IDENT COLON type_ref ASSIGN expr SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_CONST) goto bt_const_def_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_const_def_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::COLON) goto bt_const_def_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap4 = rule_type_ref();
            if (cap4.is_null()) goto bt_const_def_0;
            if (peek_token().kind != TK::ASSIGN) goto bt_const_def_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap6 = rule_expr();
            if (cap6.is_null()) goto bt_const_def_0;
            if (peek_token().kind != TK::SEMI) goto bt_const_def_0;
            Token tok_cap7_ = next_token();
            [[maybe_unused]] AnyVal cap7 = doc_.make_string(tok_cap7_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::VALUE, cap6, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::CONST_DEF), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::TYPE, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_const_def_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_type_alias() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_TYPE IDENT ASSIGN type_ref SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_TYPE) goto bt_type_alias_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_type_alias_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::ASSIGN) goto bt_type_alias_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap4 = rule_type_ref();
            if (cap4.is_null()) goto bt_type_alias_0;
            if (peek_token().kind != TK::SEMI) goto bt_type_alias_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::TYPE_ALIAS), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::TYPE, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_type_alias_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_enum_def() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_ENUM IDENT LBRACE variant_list? RBRACE => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_ENUM) goto bt_enum_def_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_enum_def_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::LBRACE) goto bt_enum_def_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap4 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap4_s = rule_variant_list();
                    if (cap4_s.is_null()) goto opt_fail_0;
                    cap4 = cap4_s;
                    goto opt_done_0;
                }
                opt_fail_0: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_0: ;
            }
            if (peek_token().kind != TK::RBRACE) goto bt_enum_def_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::ENUM), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::ITEMS, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_enum_def_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_variant_list() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: variant_def (...)* COMMA? => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            auto rcap_0 = doc_.make_array(4).get();
            [[maybe_unused]] AnyVal cap1 = rule_variant_def();
            if (cap1.is_null()) goto bt_variant_list_0;
            rcap_0.push_back(cap1).get();
            auto arr_1 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_1 = AnyVal{};
                        {
                            size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                            // Group alt 1
                            {
                                grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                {
                                    if (peek_token().kind != TK::COMMA) goto grp_fail_2_0;
                                    Token tok_rep_item_1_gi0_s0_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_1_gi0_s0 = doc_.make_string(tok_rep_item_1_gi0_s0_.text).get().to_anyval();
                                    [[maybe_unused]] AnyVal rep_item_1_gi0_s1 = rule_variant_def();
                                    if (rep_item_1_gi0_s1.is_null()) goto grp_fail_2_0;
                                    rcap_0.push_back(rep_item_1_gi0_s1).get();
                                    rep_item_1 = AnyVal{};  // multi-item group alt (no action)
                                    goto grp_done_2;
                                }
                                grp_fail_2_0: ;
                                pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                            }
                            goto rep_fail_1;  // no group alternative matched
                            grp_done_2: ;
                        }
                        if (!rep_item_1.is_null()) arr_1.push_back(rep_item_1).get();
                        continue;
                    }
                    rep_fail_1: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap2 = arr_1.to_anyval();
            [[maybe_unused]] AnyVal cap3 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    if (peek_token().kind != TK::COMMA) goto opt_fail_3;
                    Token tok_cap3_s_ = next_token();
                    [[maybe_unused]] AnyVal cap3_s = doc_.make_string(tok_cap3_s_.text).get().to_anyval();
                    cap3 = cap3_s;
                    goto opt_done_3;
                }
                opt_fail_3: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_3: ;
            }
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 3).get();
            node->put(logos_ast::ITEMS, rcap_0.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_variant_list_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_variant_def() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT ASSIGN INTEGER => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_variant_def_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::ASSIGN) goto bt_variant_def_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::INTEGER) goto bt_variant_def_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::VALUE, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::VARIANT_DEF), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_variant_def_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: IDENT => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_variant_def_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::VARIANT_DEF), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_variant_def_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_struct_def() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_STRUCT IDENT LBRACE field_def* method_def* RBRACE => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_STRUCT) goto bt_struct_def_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_struct_def_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::LBRACE) goto bt_struct_def_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto arr_0 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_0 = rule_field_def();
                        if (rep_item_0.is_null()) goto rep_fail_0;
                        if (!rep_item_0.is_null()) arr_0.push_back(rep_item_0).get();
                        continue;
                    }
                    rep_fail_0: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap4 = arr_0.to_anyval();
            auto arr_1 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_1 = rule_method_def();
                        if (rep_item_1.is_null()) goto rep_fail_1;
                        if (!rep_item_1.is_null()) arr_1.push_back(rep_item_1).get();
                        continue;
                    }
                    rep_fail_1: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap5 = arr_1.to_anyval();
            if (peek_token().kind != TK::RBRACE) goto bt_struct_def_0;
            Token tok_cap6_ = next_token();
            [[maybe_unused]] AnyVal cap6 = doc_.make_string(tok_cap6_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::STRUCT), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::FIELDS, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::ITEMS, cap5, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_struct_def_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_field_def() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT COLON type_ref SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_field_def_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::COLON) goto bt_field_def_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_type_ref();
            if (cap3.is_null()) goto bt_field_def_0;
            if (peek_token().kind != TK::SEMI) goto bt_field_def_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FIELD_DEF), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::TYPE, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_field_def_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_method_def() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: pub_fn_def
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_pub_fn_def();
            if (cap1.is_null()) goto bt_method_def_0;
            return cap1;
        }
        [[maybe_unused]] bt_method_def_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: fn_def
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_fn_def();
            if (cap1.is_null()) goto bt_method_def_1;
            return cap1;
        }
        [[maybe_unused]] bt_method_def_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_extern_fn_def() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_EXTERN KW_FN IDENT LPAREN param_list? RPAREN ARROW type_ref SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_EXTERN) goto bt_extern_fn_def_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_FN) goto bt_extern_fn_def_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_extern_fn_def_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::LPAREN) goto bt_extern_fn_def_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap5 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap5_s = rule_param_list();
                    if (cap5_s.is_null()) goto opt_fail_0;
                    cap5 = cap5_s;
                    goto opt_done_0;
                }
                opt_fail_0: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_0: ;
            }
            if (peek_token().kind != TK::RPAREN) goto bt_extern_fn_def_0;
            Token tok_cap6_ = next_token();
            [[maybe_unused]] AnyVal cap6 = doc_.make_string(tok_cap6_.text).get().to_anyval();
            if (peek_token().kind != TK::ARROW) goto bt_extern_fn_def_0;
            Token tok_cap7_ = next_token();
            [[maybe_unused]] AnyVal cap7 = doc_.make_string(tok_cap7_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap8 = rule_type_ref();
            if (cap8.is_null()) goto bt_extern_fn_def_0;
            if (peek_token().kind != TK::SEMI) goto bt_extern_fn_def_0;
            Token tok_cap9_ = next_token();
            [[maybe_unused]] AnyVal cap9 = doc_.make_string(tok_cap9_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::RET_TYPE, cap8, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::PARAMS, cap5, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::EXTERN_FN), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_extern_fn_def_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: KW_EXTERN KW_FN IDENT LPAREN param_list? RPAREN SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_EXTERN) goto bt_extern_fn_def_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_FN) goto bt_extern_fn_def_1;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_extern_fn_def_1;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::LPAREN) goto bt_extern_fn_def_1;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap5 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap5_s = rule_param_list();
                    if (cap5_s.is_null()) goto opt_fail_1;
                    cap5 = cap5_s;
                    goto opt_done_1;
                }
                opt_fail_1: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_1: ;
            }
            if (peek_token().kind != TK::RPAREN) goto bt_extern_fn_def_1;
            Token tok_cap6_ = next_token();
            [[maybe_unused]] AnyVal cap6 = doc_.make_string(tok_cap6_.text).get().to_anyval();
            if (peek_token().kind != TK::SEMI) goto bt_extern_fn_def_1;
            Token tok_cap7_ = next_token();
            [[maybe_unused]] AnyVal cap7 = doc_.make_string(tok_cap7_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::PARAMS, cap5, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::EXTERN_FN), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_extern_fn_def_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_pub_fn_def() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_PUB KW_FN IDENT LPAREN param_list? RPAREN ARROW type_ref block => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_PUB) goto bt_pub_fn_def_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_FN) goto bt_pub_fn_def_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_pub_fn_def_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::LPAREN) goto bt_pub_fn_def_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap5 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap5_s = rule_param_list();
                    if (cap5_s.is_null()) goto opt_fail_0;
                    cap5 = cap5_s;
                    goto opt_done_0;
                }
                opt_fail_0: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_0: ;
            }
            if (peek_token().kind != TK::RPAREN) goto bt_pub_fn_def_0;
            Token tok_cap6_ = next_token();
            [[maybe_unused]] AnyVal cap6 = doc_.make_string(tok_cap6_.text).get().to_anyval();
            if (peek_token().kind != TK::ARROW) goto bt_pub_fn_def_0;
            Token tok_cap7_ = next_token();
            [[maybe_unused]] AnyVal cap7 = doc_.make_string(tok_cap7_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap8 = rule_type_ref();
            if (cap8.is_null()) goto bt_pub_fn_def_0;
            [[maybe_unused]] AnyVal cap9 = rule_block();
            if (cap9.is_null()) goto bt_pub_fn_def_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 7).get();
            node->put(logos_ast::RET_TYPE, cap8, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::PARAMS, cap5, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FN), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap9, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_pub_fn_def_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: KW_PUB KW_FN IDENT LPAREN param_list? RPAREN block => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_PUB) goto bt_pub_fn_def_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_FN) goto bt_pub_fn_def_1;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_pub_fn_def_1;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::LPAREN) goto bt_pub_fn_def_1;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap5 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap5_s = rule_param_list();
                    if (cap5_s.is_null()) goto opt_fail_1;
                    cap5 = cap5_s;
                    goto opt_done_1;
                }
                opt_fail_1: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_1: ;
            }
            if (peek_token().kind != TK::RPAREN) goto bt_pub_fn_def_1;
            Token tok_cap6_ = next_token();
            [[maybe_unused]] AnyVal cap6 = doc_.make_string(tok_cap6_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap7 = rule_block();
            if (cap7.is_null()) goto bt_pub_fn_def_1;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::PARAMS, cap5, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FN), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap7, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_pub_fn_def_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_fn_def() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_FN IDENT LPAREN param_list? RPAREN ARROW type_ref block => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_FN) goto bt_fn_def_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_fn_def_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::LPAREN) goto bt_fn_def_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap4 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap4_s = rule_param_list();
                    if (cap4_s.is_null()) goto opt_fail_0;
                    cap4 = cap4_s;
                    goto opt_done_0;
                }
                opt_fail_0: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_0: ;
            }
            if (peek_token().kind != TK::RPAREN) goto bt_fn_def_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            if (peek_token().kind != TK::ARROW) goto bt_fn_def_0;
            Token tok_cap6_ = next_token();
            [[maybe_unused]] AnyVal cap6 = doc_.make_string(tok_cap6_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap7 = rule_type_ref();
            if (cap7.is_null()) goto bt_fn_def_0;
            [[maybe_unused]] AnyVal cap8 = rule_block();
            if (cap8.is_null()) goto bt_fn_def_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 7).get();
            node->put(logos_ast::RET_TYPE, cap7, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::PARAMS, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FN), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap8, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_fn_def_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: KW_FN IDENT LPAREN param_list? RPAREN block => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_FN) goto bt_fn_def_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_fn_def_1;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::LPAREN) goto bt_fn_def_1;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap4 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap4_s = rule_param_list();
                    if (cap4_s.is_null()) goto opt_fail_1;
                    cap4 = cap4_s;
                    goto opt_done_1;
                }
                opt_fail_1: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_1: ;
            }
            if (peek_token().kind != TK::RPAREN) goto bt_fn_def_1;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap6 = rule_block();
            if (cap6.is_null()) goto bt_fn_def_1;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::PARAMS, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FN), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap6, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_fn_def_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_param_list() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: param (...)* => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            auto rcap_0 = doc_.make_array(4).get();
            [[maybe_unused]] AnyVal cap1 = rule_param();
            if (cap1.is_null()) goto bt_param_list_0;
            rcap_0.push_back(cap1).get();
            auto arr_1 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_1 = AnyVal{};
                        {
                            size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                            // Group alt 1
                            {
                                grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                {
                                    if (peek_token().kind != TK::COMMA) goto grp_fail_2_0;
                                    Token tok_rep_item_1_gi0_s0_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_1_gi0_s0 = doc_.make_string(tok_rep_item_1_gi0_s0_.text).get().to_anyval();
                                    [[maybe_unused]] AnyVal rep_item_1_gi0_s1 = rule_param();
                                    if (rep_item_1_gi0_s1.is_null()) goto grp_fail_2_0;
                                    rcap_0.push_back(rep_item_1_gi0_s1).get();
                                    rep_item_1 = AnyVal{};  // multi-item group alt (no action)
                                    goto grp_done_2;
                                }
                                grp_fail_2_0: ;
                                pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                            }
                            goto rep_fail_1;  // no group alternative matched
                            grp_done_2: ;
                        }
                        if (!rep_item_1.is_null()) arr_1.push_back(rep_item_1).get();
                        continue;
                    }
                    rep_fail_1: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap2 = arr_1.to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 3).get();
            node->put(logos_ast::ITEMS, rcap_0.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_param_list_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_param() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT COLON type_ref => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_param_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::COLON) goto bt_param_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_type_ref();
            if (cap3.is_null()) goto bt_param_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::PARAM), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::TYPE, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_param_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_type_ref() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: ptr_type
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_ptr_type();
            if (cap1.is_null()) goto bt_type_ref_0;
            return cap1;
        }
        [[maybe_unused]] bt_type_ref_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: arr_type
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_arr_type();
            if (cap1.is_null()) goto bt_type_ref_1;
            return cap1;
        }
        [[maybe_unused]] bt_type_ref_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 3: simple_type
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_simple_type();
            if (cap1.is_null()) goto bt_type_ref_2;
            return cap1;
        }
        [[maybe_unused]] bt_type_ref_2:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_ptr_type() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: STAR KW_CONST simple_type => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::STAR) goto bt_ptr_type_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_CONST) goto bt_ptr_type_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_simple_type();
            if (cap3.is_null()) goto bt_ptr_type_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::POINTEE, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::PTR_TYPE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::MUTPTR, AnyVal::from_value(uint8_t(0)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_ptr_type_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: STAR KW_MUT simple_type => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::STAR) goto bt_ptr_type_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_MUT) goto bt_ptr_type_1;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_simple_type();
            if (cap3.is_null()) goto bt_ptr_type_1;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::POINTEE, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::PTR_TYPE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::MUTPTR, AnyVal::from_value(uint8_t(1)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_ptr_type_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_arr_type() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: LBRACKET simple_type SEMI INTEGER RBRACKET => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::LBRACKET) goto bt_arr_type_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_simple_type();
            if (cap2.is_null()) goto bt_arr_type_0;
            if (peek_token().kind != TK::SEMI) goto bt_arr_type_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::INTEGER) goto bt_arr_type_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            if (peek_token().kind != TK::RBRACKET) goto bt_arr_type_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::ARR_TYPE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SIZE, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::TYPE, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_arr_type_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_simple_type() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_simple_type_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::TYPE_REF), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_simple_type_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_block() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: LBRACE stmt* RBRACE => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            auto rcap_0 = doc_.make_array(4).get();
            if (peek_token().kind != TK::LBRACE) goto bt_block_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto arr_1 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_1 = rule_stmt();
                        if (rep_item_1.is_null()) goto rep_fail_1;
                        rcap_0.push_back(rep_item_1).get();
                        if (!rep_item_1.is_null()) arr_1.push_back(rep_item_1).get();
                        continue;
                    }
                    rep_fail_1: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap2 = arr_1.to_anyval();
            if (peek_token().kind != TK::RBRACE) goto bt_block_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::BLOCK), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::ITEMS, rcap_0.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_block_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: let_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_let_stmt();
            if (cap1.is_null()) goto bt_stmt_0;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: for_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_for_stmt();
            if (cap1.is_null()) goto bt_stmt_1;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 3: while_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_while_stmt();
            if (cap1.is_null()) goto bt_stmt_2;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_2:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 4: loop_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_loop_stmt();
            if (cap1.is_null()) goto bt_stmt_3;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_3:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 5: return_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_return_stmt();
            if (cap1.is_null()) goto bt_stmt_4;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_4:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 6: break_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_break_stmt();
            if (cap1.is_null()) goto bt_stmt_5;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_5:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 7: continue_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_continue_stmt();
            if (cap1.is_null()) goto bt_stmt_6;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_6:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 8: if_expr
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_if_expr();
            if (cap1.is_null()) goto bt_stmt_7;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_7:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 9: match_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_match_stmt();
            if (cap1.is_null()) goto bt_stmt_8;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_8:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 10: field_write_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_field_write_stmt();
            if (cap1.is_null()) goto bt_stmt_9;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_9:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 11: index_write_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_index_write_stmt();
            if (cap1.is_null()) goto bt_stmt_10;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_10:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 12: assign_stmt
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_assign_stmt();
            if (cap1.is_null()) goto bt_stmt_11;
            return cap1;
        }
        [[maybe_unused]] bt_stmt_11:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 13: expr SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_expr();
            if (cap1.is_null()) goto bt_stmt_12;
            if (peek_token().kind != TK::SEMI) goto bt_stmt_12;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::EXPR_STMT), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_stmt_12:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_for_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_FOR IDENT KW_IN expr DOTDOTEQ expr block => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_FOR) goto bt_for_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_for_stmt_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_IN) goto bt_for_stmt_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap4 = rule_expr();
            if (cap4.is_null()) goto bt_for_stmt_0;
            if (peek_token().kind != TK::DOTDOTEQ) goto bt_for_stmt_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap6 = rule_expr();
            if (cap6.is_null()) goto bt_for_stmt_0;
            [[maybe_unused]] AnyVal cap7 = rule_block();
            if (cap7.is_null()) goto bt_for_stmt_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 8).get();
            node->put(logos_ast::INCLUSIVE, AnyVal::from_value(uint8_t(1)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FOR), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::RHS, cap6, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap7, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::LHS, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_for_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: KW_FOR IDENT KW_IN expr DOTDOT expr block => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_FOR) goto bt_for_stmt_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_for_stmt_1;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_IN) goto bt_for_stmt_1;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap4 = rule_expr();
            if (cap4.is_null()) goto bt_for_stmt_1;
            if (peek_token().kind != TK::DOTDOT) goto bt_for_stmt_1;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap6 = rule_expr();
            if (cap6.is_null()) goto bt_for_stmt_1;
            [[maybe_unused]] AnyVal cap7 = rule_block();
            if (cap7.is_null()) goto bt_for_stmt_1;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 8).get();
            node->put(logos_ast::INCLUSIVE, AnyVal::from_value(uint8_t(0)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FOR), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::RHS, cap6, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap7, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::LHS, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_for_stmt_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_loop_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_LOOP block => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_LOOP) goto bt_loop_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_block();
            if (cap2.is_null()) goto bt_loop_stmt_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::LOOP), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_loop_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_break_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_BREAK SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_BREAK) goto bt_break_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::SEMI) goto bt_break_stmt_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 3).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::BREAK), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_break_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_continue_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_CONTINUE SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_CONTINUE) goto bt_continue_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::SEMI) goto bt_continue_stmt_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 3).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::CONTINUE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_continue_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_match_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_MATCH expr LBRACE match_arm* RBRACE => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            auto rcap_0 = doc_.make_array(4).get();
            if (peek_token().kind != TK::KW_MATCH) goto bt_match_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_expr();
            if (cap2.is_null()) goto bt_match_stmt_0;
            rcap_0.push_back(cap2).get();
            if (peek_token().kind != TK::LBRACE) goto bt_match_stmt_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto arr_1 = doc_.make_array(4).get();
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_1 = rule_match_arm();
                        if (rep_item_1.is_null()) goto rep_fail_1;
                        rcap_0.push_back(rep_item_1).get();
                        if (!rep_item_1.is_null()) arr_1.push_back(rep_item_1).get();
                        continue;
                    }
                    rep_fail_1: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap4 = arr_1.to_anyval();
            if (peek_token().kind != TK::RBRACE) goto bt_match_stmt_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::VALUE, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::MATCH), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::ITEMS, rcap_0.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_match_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_match_arm() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: pattern FATARROW block COMMA? => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_pattern();
            if (cap1.is_null()) goto bt_match_arm_0;
            if (peek_token().kind != TK::FATARROW) goto bt_match_arm_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_block();
            if (cap3.is_null()) goto bt_match_arm_0;
            [[maybe_unused]] AnyVal cap4 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    if (peek_token().kind != TK::COMMA) goto opt_fail_0;
                    Token tok_cap4_s_ = next_token();
                    [[maybe_unused]] AnyVal cap4_s = doc_.make_string(tok_cap4_s_.text).get().to_anyval();
                    cap4 = cap4_s;
                    goto opt_done_0;
                }
                opt_fail_0: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_0: ;
            }
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::MATCH_ARM), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::LHS, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_match_arm_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: pattern FATARROW stmt => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_pattern();
            if (cap1.is_null()) goto bt_match_arm_1;
            if (peek_token().kind != TK::FATARROW) goto bt_match_arm_1;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_stmt();
            if (cap3.is_null()) goto bt_match_arm_1;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::MATCH_ARM), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::LHS, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_match_arm_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_pattern() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT COLONCOLON IDENT => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_pattern_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::COLONCOLON) goto bt_pattern_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_pattern_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::PAT_VARIANT), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::FIELD, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_pattern_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: INTEGER => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::INTEGER) goto bt_pattern_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::PAT_INT), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_pattern_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 3: KW_TRUE => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_TRUE) goto bt_pattern_2;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, AnyVal::from_value(uint8_t(1)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::PAT_BOOL), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_pattern_2:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 4: KW_FALSE => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_FALSE) goto bt_pattern_3;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, AnyVal::from_value(uint8_t(0)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::PAT_BOOL), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_pattern_3:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 5: IDENT => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_pattern_4;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::PAT_WILD), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_pattern_4:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_index_write_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT LBRACKET expr RBRACKET ASSIGN expr SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_index_write_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::LBRACKET) goto bt_index_write_stmt_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_expr();
            if (cap3.is_null()) goto bt_index_write_stmt_0;
            if (peek_token().kind != TK::RBRACKET) goto bt_index_write_stmt_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            if (peek_token().kind != TK::ASSIGN) goto bt_index_write_stmt_0;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap6 = rule_expr();
            if (cap6.is_null()) goto bt_index_write_stmt_0;
            if (peek_token().kind != TK::SEMI) goto bt_index_write_stmt_0;
            Token tok_cap7_ = next_token();
            [[maybe_unused]] AnyVal cap7 = doc_.make_string(tok_cap7_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::VALUE, cap6, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::INDEX_WRITE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::LHS, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_index_write_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_let_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_LET KW_MUT IDENT COLON type_ref ASSIGN expr SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_LET) goto bt_let_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_MUT) goto bt_let_stmt_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_let_stmt_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::COLON) goto bt_let_stmt_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap5 = rule_type_ref();
            if (cap5.is_null()) goto bt_let_stmt_0;
            if (peek_token().kind != TK::ASSIGN) goto bt_let_stmt_0;
            Token tok_cap6_ = next_token();
            [[maybe_unused]] AnyVal cap6 = doc_.make_string(tok_cap6_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap7 = rule_expr();
            if (cap7.is_null()) goto bt_let_stmt_0;
            if (peek_token().kind != TK::SEMI) goto bt_let_stmt_0;
            Token tok_cap8_ = next_token();
            [[maybe_unused]] AnyVal cap8 = doc_.make_string(tok_cap8_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 7).get();
            node->put(logos_ast::IS_MUT, AnyVal::from_value(uint8_t(1)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::VALUE, cap7, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::LET), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::TYPE, cap5, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_let_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: KW_LET KW_MUT IDENT ASSIGN expr SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_LET) goto bt_let_stmt_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::KW_MUT) goto bt_let_stmt_1;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_let_stmt_1;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::ASSIGN) goto bt_let_stmt_1;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap5 = rule_expr();
            if (cap5.is_null()) goto bt_let_stmt_1;
            if (peek_token().kind != TK::SEMI) goto bt_let_stmt_1;
            Token tok_cap6_ = next_token();
            [[maybe_unused]] AnyVal cap6 = doc_.make_string(tok_cap6_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::IS_MUT, AnyVal::from_value(uint8_t(1)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::VALUE, cap5, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::LET), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_let_stmt_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 3: KW_LET IDENT COLON type_ref ASSIGN expr SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_LET) goto bt_let_stmt_2;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_let_stmt_2;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::COLON) goto bt_let_stmt_2;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap4 = rule_type_ref();
            if (cap4.is_null()) goto bt_let_stmt_2;
            if (peek_token().kind != TK::ASSIGN) goto bt_let_stmt_2;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap6 = rule_expr();
            if (cap6.is_null()) goto bt_let_stmt_2;
            if (peek_token().kind != TK::SEMI) goto bt_let_stmt_2;
            Token tok_cap7_ = next_token();
            [[maybe_unused]] AnyVal cap7 = doc_.make_string(tok_cap7_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::VALUE, cap6, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::LET), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::TYPE, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_let_stmt_2:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 4: KW_LET IDENT ASSIGN expr SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_LET) goto bt_let_stmt_3;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_let_stmt_3;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::ASSIGN) goto bt_let_stmt_3;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap4 = rule_expr();
            if (cap4.is_null()) goto bt_let_stmt_3;
            if (peek_token().kind != TK::SEMI) goto bt_let_stmt_3;
            Token tok_cap5_ = next_token();
            [[maybe_unused]] AnyVal cap5 = doc_.make_string(tok_cap5_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::VALUE, cap4, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::LET), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_let_stmt_3:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_while_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_WHILE expr block => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_WHILE) goto bt_while_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_expr();
            if (cap2.is_null()) goto bt_while_stmt_0;
            [[maybe_unused]] AnyVal cap3 = rule_block();
            if (cap3.is_null()) goto bt_while_stmt_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::WHILE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::COND, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::BODY, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_while_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_return_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_RETURN expr? SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_RETURN) goto bt_return_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap2_s = rule_expr();
                    if (cap2_s.is_null()) goto opt_fail_0;
                    cap2 = cap2_s;
                    goto opt_done_0;
                }
                opt_fail_0: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_0: ;
            }
            if (peek_token().kind != TK::SEMI) goto bt_return_stmt_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::RETURN), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_return_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_assign_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT ASSIGN expr SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_assign_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::ASSIGN) goto bt_assign_stmt_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_expr();
            if (cap3.is_null()) goto bt_assign_stmt_0;
            if (peek_token().kind != TK::SEMI) goto bt_assign_stmt_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::VALUE, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::ASSIGN), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_assign_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_field_write_stmt() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT DOT IDENT ASSIGN expr SEMI => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_field_write_stmt_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::DOT) goto bt_field_write_stmt_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_field_write_stmt_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            if (peek_token().kind != TK::ASSIGN) goto bt_field_write_stmt_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap5 = rule_expr();
            if (cap5.is_null()) goto bt_field_write_stmt_0;
            if (peek_token().kind != TK::SEMI) goto bt_field_write_stmt_0;
            Token tok_cap6_ = next_token();
            [[maybe_unused]] AnyVal cap6 = doc_.make_string(tok_cap6_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::RECEIVER, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::VALUE, cap5, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FIELD_WRITE), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::FIELD, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_field_write_stmt_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_if_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: KW_IF expr block KW_ELSE (...) => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_IF) goto bt_if_expr_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_expr();
            if (cap2.is_null()) goto bt_if_expr_0;
            [[maybe_unused]] AnyVal cap3 = rule_block();
            if (cap3.is_null()) goto bt_if_expr_0;
            if (peek_token().kind != TK::KW_ELSE) goto bt_if_expr_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap5 = AnyVal{};
            {
                size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                // Group alt 1
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        [[maybe_unused]] AnyVal cap5_gi0_s0 = rule_if_expr();
                        if (cap5_gi0_s0.is_null()) goto grp_fail_0_0;
                        cap5 = cap5_gi0_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_0: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 2
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        [[maybe_unused]] AnyVal cap5_gi1_s0 = rule_block();
                        if (cap5_gi1_s0.is_null()) goto grp_fail_0_1;
                        cap5 = cap5_gi1_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_1: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                goto bt_if_expr_0;  // no group alternative matched
                grp_done_0: ;
            }
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::ELSE, cap5, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::IF), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::COND, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::THEN, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_if_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: KW_IF expr block => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_IF) goto bt_if_expr_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_expr();
            if (cap2.is_null()) goto bt_if_expr_1;
            [[maybe_unused]] AnyVal cap3 = rule_block();
            if (cap3.is_null()) goto bt_if_expr_1;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::IF), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::COND, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::THEN, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_if_expr_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: log_expr
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_log_expr();
            if (cap1.is_null()) goto bt_expr_0;
            return cap1;
        }
        [[maybe_unused]] bt_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_log_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: cmp_expr (...) cmp_expr => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_cmp_expr();
            if (cap1.is_null()) goto bt_log_expr_0;
            [[maybe_unused]] AnyVal cap2 = AnyVal{};
            {
                size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                // Group alt 1
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::AND) goto grp_fail_0_0;
                        Token tok_cap2_gi0_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi0_s0 = doc_.make_string(tok_cap2_gi0_s0_.text).get().to_anyval();
                        cap2 = cap2_gi0_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_0: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 2
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::OR) goto grp_fail_0_1;
                        Token tok_cap2_gi1_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi1_s0 = doc_.make_string(tok_cap2_gi1_s0_.text).get().to_anyval();
                        cap2 = cap2_gi1_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_1: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                goto bt_log_expr_0;  // no group alternative matched
                grp_done_0: ;
            }
            [[maybe_unused]] AnyVal cap3 = rule_cmp_expr();
            if (cap3.is_null()) goto bt_log_expr_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::OP, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::BINOP), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::RHS, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::LHS, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_log_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: cmp_expr
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_cmp_expr();
            if (cap1.is_null()) goto bt_log_expr_1;
            return cap1;
        }
        [[maybe_unused]] bt_log_expr_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_cmp_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: add_expr (...) add_expr => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_add_expr();
            if (cap1.is_null()) goto bt_cmp_expr_0;
            [[maybe_unused]] AnyVal cap2 = AnyVal{};
            {
                size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                // Group alt 1
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::EQ) goto grp_fail_0_0;
                        Token tok_cap2_gi0_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi0_s0 = doc_.make_string(tok_cap2_gi0_s0_.text).get().to_anyval();
                        cap2 = cap2_gi0_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_0: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 2
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::NE) goto grp_fail_0_1;
                        Token tok_cap2_gi1_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi1_s0 = doc_.make_string(tok_cap2_gi1_s0_.text).get().to_anyval();
                        cap2 = cap2_gi1_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_1: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 3
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::LE) goto grp_fail_0_2;
                        Token tok_cap2_gi2_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi2_s0 = doc_.make_string(tok_cap2_gi2_s0_.text).get().to_anyval();
                        cap2 = cap2_gi2_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_2: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 4
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::GE) goto grp_fail_0_3;
                        Token tok_cap2_gi3_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi3_s0 = doc_.make_string(tok_cap2_gi3_s0_.text).get().to_anyval();
                        cap2 = cap2_gi3_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_3: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 5
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::LT) goto grp_fail_0_4;
                        Token tok_cap2_gi4_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi4_s0 = doc_.make_string(tok_cap2_gi4_s0_.text).get().to_anyval();
                        cap2 = cap2_gi4_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_4: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 6
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::GT) goto grp_fail_0_5;
                        Token tok_cap2_gi5_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi5_s0 = doc_.make_string(tok_cap2_gi5_s0_.text).get().to_anyval();
                        cap2 = cap2_gi5_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_5: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                goto bt_cmp_expr_0;  // no group alternative matched
                grp_done_0: ;
            }
            [[maybe_unused]] AnyVal cap3 = rule_add_expr();
            if (cap3.is_null()) goto bt_cmp_expr_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::OP, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::BINOP), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::RHS, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::LHS, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_cmp_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: add_expr
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_add_expr();
            if (cap1.is_null()) goto bt_cmp_expr_1;
            return cap1;
        }
        [[maybe_unused]] bt_cmp_expr_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_add_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: mul_expr (...) mul_expr => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_mul_expr();
            if (cap1.is_null()) goto bt_add_expr_0;
            [[maybe_unused]] AnyVal cap2 = AnyVal{};
            {
                size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                // Group alt 1
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::PLUS) goto grp_fail_0_0;
                        Token tok_cap2_gi0_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi0_s0 = doc_.make_string(tok_cap2_gi0_s0_.text).get().to_anyval();
                        cap2 = cap2_gi0_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_0: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 2
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::MINUS) goto grp_fail_0_1;
                        Token tok_cap2_gi1_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi1_s0 = doc_.make_string(tok_cap2_gi1_s0_.text).get().to_anyval();
                        cap2 = cap2_gi1_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_1: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                goto bt_add_expr_0;  // no group alternative matched
                grp_done_0: ;
            }
            [[maybe_unused]] AnyVal cap3 = rule_mul_expr();
            if (cap3.is_null()) goto bt_add_expr_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::OP, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::BINOP), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::RHS, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::LHS, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_add_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: mul_expr
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_mul_expr();
            if (cap1.is_null()) goto bt_add_expr_1;
            return cap1;
        }
        [[maybe_unused]] bt_add_expr_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_mul_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: unary_expr (...) unary_expr => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_unary_expr();
            if (cap1.is_null()) goto bt_mul_expr_0;
            [[maybe_unused]] AnyVal cap2 = AnyVal{};
            {
                size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                // Group alt 1
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::STAR) goto grp_fail_0_0;
                        Token tok_cap2_gi0_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi0_s0 = doc_.make_string(tok_cap2_gi0_s0_.text).get().to_anyval();
                        cap2 = cap2_gi0_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_0: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 2
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::SLASH) goto grp_fail_0_1;
                        Token tok_cap2_gi1_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi1_s0 = doc_.make_string(tok_cap2_gi1_s0_.text).get().to_anyval();
                        cap2 = cap2_gi1_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_1: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                // Group alt 3
                {
                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                    {
                        if (peek_token().kind != TK::PERCENT) goto grp_fail_0_2;
                        Token tok_cap2_gi2_s0_ = next_token();
                        [[maybe_unused]] AnyVal cap2_gi2_s0 = doc_.make_string(tok_cap2_gi2_s0_.text).get().to_anyval();
                        cap2 = cap2_gi2_s0;
                        goto grp_done_0;
                    }
                    grp_fail_0_2: ;
                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                }
                goto bt_mul_expr_0;  // no group alternative matched
                grp_done_0: ;
            }
            [[maybe_unused]] AnyVal cap3 = rule_unary_expr();
            if (cap3.is_null()) goto bt_mul_expr_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
            node->put(logos_ast::OP, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::BINOP), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::RHS, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::LHS, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_mul_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: unary_expr
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_unary_expr();
            if (cap1.is_null()) goto bt_mul_expr_1;
            return cap1;
        }
        [[maybe_unused]] bt_mul_expr_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_unary_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: STAR unary_expr => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::STAR) goto bt_unary_expr_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_unary_expr();
            if (cap2.is_null()) goto bt_unary_expr_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::DEREF), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_unary_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: MINUS unary_expr => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::MINUS) goto bt_unary_expr_1;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_unary_expr();
            if (cap2.is_null()) goto bt_unary_expr_1;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::OP, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::VALUE, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::UNARY), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_unary_expr_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 3: BANG unary_expr => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::BANG) goto bt_unary_expr_2;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_unary_expr();
            if (cap2.is_null()) goto bt_unary_expr_2;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::OP, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::VALUE, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::UNARY), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_unary_expr_2:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 4: AMP unary_expr => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::AMP) goto bt_unary_expr_3;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_unary_expr();
            if (cap2.is_null()) goto bt_unary_expr_3;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::OP, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::VALUE, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::UNARY), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_unary_expr_3:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 5: cast_expr
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_cast_expr();
            if (cap1.is_null()) goto bt_unary_expr_4;
            return cap1;
        }
        [[maybe_unused]] bt_unary_expr_4:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_cast_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: atom KW_AS type_ref => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_atom();
            if (cap1.is_null()) goto bt_cast_expr_0;
            if (peek_token().kind != TK::KW_AS) goto bt_cast_expr_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_type_ref();
            if (cap3.is_null()) goto bt_cast_expr_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::VALUE, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::CAST), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::TYPE, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_cast_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: atom
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_atom();
            if (cap1.is_null()) goto bt_cast_expr_1;
            return cap1;
        }
        [[maybe_unused]] bt_cast_expr_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_atom() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: primary_expr (...)*
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_primary_expr();
            if (cap1.is_null()) goto bt_atom_0;
            AnyVal fold_acc_0 = cap1;
            {
                while (true) {
                    size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                    size_t rep_doc_ = doc_.arena_checkpoint();
                    {
                        [[maybe_unused]] AnyVal rep_item_0 = AnyVal{};
                        {
                            size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                            // Group alt 1
                            {
                                grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                {
                                auto grp_rcap_1_0 = doc_.make_array(4).get();
                                    if (peek_token().kind != TK::DOT) goto grp_fail_1_0;
                                    Token tok_rep_item_0_gi0_s0_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_0_gi0_s0 = doc_.make_string(tok_rep_item_0_gi0_s0_.text).get().to_anyval();
                                    if (peek_token().kind != TK::IDENT) goto grp_fail_1_0;
                                    Token tok_rep_item_0_gi0_s1_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_0_gi0_s1 = doc_.make_string(tok_rep_item_0_gi0_s1_.text).get().to_anyval();
                                    if (peek_token().kind != TK::LPAREN) goto grp_fail_1_0;
                                    Token tok_rep_item_0_gi0_s2_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_0_gi0_s2 = doc_.make_string(tok_rep_item_0_gi0_s2_.text).get().to_anyval();
                                    [[maybe_unused]] AnyVal rep_item_0_gi0_s3 = AnyVal{};
                                    {
                                        size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                                        size_t opt_doc_ = doc_.arena_checkpoint();
                                        {
                                            [[maybe_unused]] AnyVal rep_item_0_gi0_s3_s = AnyVal{};
                                            {
                                                size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                                                // Group alt 1
                                                {
                                                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                                    {
                                                        [[maybe_unused]] AnyVal rep_item_0_gi0_s3_s_gi0_s0 = rule_expr();
                                                        if (rep_item_0_gi0_s3_s_gi0_s0.is_null()) goto grp_fail_3_0;
                                                        grp_rcap_1_0.push_back(rep_item_0_gi0_s3_s_gi0_s0).get();
                                                        auto arr_4 = doc_.make_array(4).get();
                                                        {
                                                            while (true) {
                                                                size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                                                                size_t rep_doc_ = doc_.arena_checkpoint();
                                                                {
                                                                    [[maybe_unused]] AnyVal rep_item_4 = AnyVal{};
                                                                    {
                                                                        size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                                                                        // Group alt 1
                                                                        {
                                                                            grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                                                            {
                                                                                if (peek_token().kind != TK::COMMA) goto grp_fail_5_0;
                                                                                Token tok_rep_item_4_gi0_s0_ = next_token();
                                                                                [[maybe_unused]] AnyVal rep_item_4_gi0_s0 = doc_.make_string(tok_rep_item_4_gi0_s0_.text).get().to_anyval();
                                                                                [[maybe_unused]] AnyVal rep_item_4_gi0_s1 = rule_expr();
                                                                                if (rep_item_4_gi0_s1.is_null()) goto grp_fail_5_0;
                                                                                grp_rcap_1_0.push_back(rep_item_4_gi0_s1).get();
                                                                                rep_item_4 = AnyVal{};  // multi-item group alt (no action)
                                                                                goto grp_done_5;
                                                                            }
                                                                            grp_fail_5_0: ;
                                                                            pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                                                                        }
                                                                        goto rep_fail_4;  // no group alternative matched
                                                                        grp_done_5: ;
                                                                    }
                                                                    if (!rep_item_4.is_null()) arr_4.push_back(rep_item_4).get();
                                                                    continue;
                                                                }
                                                                rep_fail_4: ;
                                                                pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                                                                doc_.arena_rollback(rep_doc_);
                                                                break;
                                                            }
                                                        }
                                                        [[maybe_unused]] AnyVal rep_item_0_gi0_s3_s_gi0_s1 = arr_4.to_anyval();
                                                        rep_item_0_gi0_s3_s = AnyVal{};  // multi-item group alt (no action)
                                                        goto grp_done_3;
                                                    }
                                                    grp_fail_3_0: ;
                                                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                                                }
                                                goto opt_fail_2;  // no group alternative matched
                                                grp_done_3: ;
                                            }
                                            rep_item_0_gi0_s3 = rep_item_0_gi0_s3_s;
                                            goto opt_done_2;
                                        }
                                        opt_fail_2: ;
                                        pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                                        doc_.arena_rollback(opt_doc_);
                                        opt_done_2: ;
                                    }
                                    if (peek_token().kind != TK::RPAREN) goto grp_fail_1_0;
                                    Token tok_rep_item_0_gi0_s4_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_0_gi0_s4 = doc_.make_string(tok_rep_item_0_gi0_s4_.text).get().to_anyval();
                                    auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 6).get();
                                    node->put(logos_ast::RECEIVER, fold_acc_0, logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::METHOD_CALL), logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::ARGS, grp_rcap_1_0.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::NAME, rep_item_0_gi0_s1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    {
                                        AnyVal result_;
                                        result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                                        rep_item_0 = result_;
                                    }
                                    goto grp_done_1;
                                }
                                grp_fail_1_0: ;
                                pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                            }
                            // Group alt 2
                            {
                                grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                {
                                    if (peek_token().kind != TK::DOT) goto grp_fail_1_1;
                                    Token tok_rep_item_0_gi1_s0_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_0_gi1_s0 = doc_.make_string(tok_rep_item_0_gi1_s0_.text).get().to_anyval();
                                    if (peek_token().kind != TK::IDENT) goto grp_fail_1_1;
                                    Token tok_rep_item_0_gi1_s1_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_0_gi1_s1 = doc_.make_string(tok_rep_item_0_gi1_s1_.text).get().to_anyval();
                                    auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
                                    node->put(logos_ast::RECEIVER, fold_acc_0, logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FIELD_READ), logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::FIELD, rep_item_0_gi1_s1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    {
                                        AnyVal result_;
                                        result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                                        rep_item_0 = result_;
                                    }
                                    goto grp_done_1;
                                }
                                grp_fail_1_1: ;
                                pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                            }
                            // Group alt 3
                            {
                                grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                {
                                    if (peek_token().kind != TK::LBRACKET) goto grp_fail_1_2;
                                    Token tok_rep_item_0_gi2_s0_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_0_gi2_s0 = doc_.make_string(tok_rep_item_0_gi2_s0_.text).get().to_anyval();
                                    [[maybe_unused]] AnyVal rep_item_0_gi2_s1 = rule_expr();
                                    if (rep_item_0_gi2_s1.is_null()) goto grp_fail_1_2;
                                    if (peek_token().kind != TK::RBRACKET) goto grp_fail_1_2;
                                    Token tok_rep_item_0_gi2_s2_ = next_token();
                                    [[maybe_unused]] AnyVal rep_item_0_gi2_s2 = doc_.make_string(tok_rep_item_0_gi2_s2_.text).get().to_anyval();
                                    auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
                                    node->put(logos_ast::RECEIVER, fold_acc_0, logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::VALUE, rep_item_0_gi2_s1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::INDEX_READ), logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
                                    {
                                        AnyVal result_;
                                        result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                                        rep_item_0 = result_;
                                    }
                                    goto grp_done_1;
                                }
                                grp_fail_1_2: ;
                                pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                            }
                            goto rep_fail_0;  // no group alternative matched
                            grp_done_1: ;
                        }
                        fold_acc_0 = rep_item_0;
                        continue;
                    }
                    rep_fail_0: ;
                    pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                    doc_.arena_rollback(rep_doc_);
                    break;
                }
            }
            [[maybe_unused]] AnyVal cap2 = fold_acc_0;
            return cap2;
        }
        [[maybe_unused]] bt_atom_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_primary_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: struct_lit
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_struct_lit();
            if (cap1.is_null()) goto bt_primary_expr_0;
            return cap1;
        }
        [[maybe_unused]] bt_primary_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 2: arr_lit
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_arr_lit();
            if (cap1.is_null()) goto bt_primary_expr_1;
            return cap1;
        }
        [[maybe_unused]] bt_primary_expr_1:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 3: paren_expr
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_paren_expr();
            if (cap1.is_null()) goto bt_primary_expr_2;
            return cap1;
        }
        [[maybe_unused]] bt_primary_expr_2:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 4: enum_lit
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_enum_lit();
            if (cap1.is_null()) goto bt_primary_expr_3;
            return cap1;
        }
        [[maybe_unused]] bt_primary_expr_3:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 5: call_expr
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            [[maybe_unused]] AnyVal cap1 = rule_call_expr();
            if (cap1.is_null()) goto bt_primary_expr_4;
            return cap1;
        }
        [[maybe_unused]] bt_primary_expr_4:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 6: STRING => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::STRING) goto bt_primary_expr_5;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::LIT_STR), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_primary_expr_5:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 7: KW_TRUE => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_TRUE) goto bt_primary_expr_6;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, AnyVal::from_value(uint8_t(1)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::LIT_BOOL), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_primary_expr_6:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 8: KW_FALSE => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::KW_FALSE) goto bt_primary_expr_7;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, AnyVal::from_value(uint8_t(0)), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::LIT_BOOL), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_primary_expr_7:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 9: INTEGER => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::INTEGER) goto bt_primary_expr_8;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::LIT_INT), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_primary_expr_8:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    // Alternative 10: IDENT => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_primary_expr_9;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::VAR_REF), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_primary_expr_9:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_enum_lit() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT COLONCOLON IDENT => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_enum_lit_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::COLONCOLON) goto bt_enum_lit_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            if (peek_token().kind != TK::IDENT) goto bt_enum_lit_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::ENUM_LIT), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::FIELD, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_enum_lit_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_struct_lit() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT LBRACE (...)? RBRACE => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            auto rcap_0 = doc_.make_array(4).get();
            if (peek_token().kind != TK::IDENT) goto bt_struct_lit_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::LBRACE) goto bt_struct_lit_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap3_s = AnyVal{};
                    {
                        size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                        // Group alt 1
                        {
                            grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                            {
                                [[maybe_unused]] AnyVal cap3_s_gi0_s0 = rule_field_init();
                                if (cap3_s_gi0_s0.is_null()) goto grp_fail_2_0;
                                rcap_0.push_back(cap3_s_gi0_s0).get();
                                auto arr_3 = doc_.make_array(4).get();
                                {
                                    while (true) {
                                        size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                                        size_t rep_doc_ = doc_.arena_checkpoint();
                                        {
                                            [[maybe_unused]] AnyVal rep_item_3 = AnyVal{};
                                            {
                                                size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                                                // Group alt 1
                                                {
                                                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                                    {
                                                        if (peek_token().kind != TK::COMMA) goto grp_fail_4_0;
                                                        Token tok_rep_item_3_gi0_s0_ = next_token();
                                                        [[maybe_unused]] AnyVal rep_item_3_gi0_s0 = doc_.make_string(tok_rep_item_3_gi0_s0_.text).get().to_anyval();
                                                        [[maybe_unused]] AnyVal rep_item_3_gi0_s1 = rule_field_init();
                                                        if (rep_item_3_gi0_s1.is_null()) goto grp_fail_4_0;
                                                        rcap_0.push_back(rep_item_3_gi0_s1).get();
                                                        rep_item_3 = AnyVal{};  // multi-item group alt (no action)
                                                        goto grp_done_4;
                                                    }
                                                    grp_fail_4_0: ;
                                                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                                                }
                                                goto rep_fail_3;  // no group alternative matched
                                                grp_done_4: ;
                                            }
                                            if (!rep_item_3.is_null()) arr_3.push_back(rep_item_3).get();
                                            continue;
                                        }
                                        rep_fail_3: ;
                                        pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                                        doc_.arena_rollback(rep_doc_);
                                        break;
                                    }
                                }
                                [[maybe_unused]] AnyVal cap3_s_gi0_s1 = arr_3.to_anyval();
                                cap3_s = AnyVal{};  // multi-item group alt (no action)
                                goto grp_done_2;
                            }
                            grp_fail_2_0: ;
                            pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                        }
                        goto opt_fail_1;  // no group alternative matched
                        grp_done_2: ;
                    }
                    cap3 = cap3_s;
                    goto opt_done_1;
                }
                opt_fail_1: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_1: ;
            }
            if (peek_token().kind != TK::RBRACE) goto bt_struct_lit_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::STRUCT_LIT), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::ITEMS, rcap_0.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_struct_lit_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_field_init() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT COLON expr => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::IDENT) goto bt_field_init_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::COLON) goto bt_field_init_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = rule_expr();
            if (cap3.is_null()) goto bt_field_init_0;
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::VALUE, cap3, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::FIELD_INIT), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::NAME, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_field_init_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_arr_lit() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: LBRACKET (...)? RBRACKET => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            auto rcap_0 = doc_.make_array(4).get();
            if (peek_token().kind != TK::LBRACKET) goto bt_arr_lit_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap2_s = AnyVal{};
                    {
                        size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                        // Group alt 1
                        {
                            grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                            {
                                [[maybe_unused]] AnyVal cap2_s_gi0_s0 = rule_expr();
                                if (cap2_s_gi0_s0.is_null()) goto grp_fail_2_0;
                                rcap_0.push_back(cap2_s_gi0_s0).get();
                                auto arr_3 = doc_.make_array(4).get();
                                {
                                    while (true) {
                                        size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                                        size_t rep_doc_ = doc_.arena_checkpoint();
                                        {
                                            [[maybe_unused]] AnyVal rep_item_3 = AnyVal{};
                                            {
                                                size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                                                // Group alt 1
                                                {
                                                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                                    {
                                                        if (peek_token().kind != TK::COMMA) goto grp_fail_4_0;
                                                        Token tok_rep_item_3_gi0_s0_ = next_token();
                                                        [[maybe_unused]] AnyVal rep_item_3_gi0_s0 = doc_.make_string(tok_rep_item_3_gi0_s0_.text).get().to_anyval();
                                                        [[maybe_unused]] AnyVal rep_item_3_gi0_s1 = rule_expr();
                                                        if (rep_item_3_gi0_s1.is_null()) goto grp_fail_4_0;
                                                        rcap_0.push_back(rep_item_3_gi0_s1).get();
                                                        rep_item_3 = AnyVal{};  // multi-item group alt (no action)
                                                        goto grp_done_4;
                                                    }
                                                    grp_fail_4_0: ;
                                                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                                                }
                                                goto rep_fail_3;  // no group alternative matched
                                                grp_done_4: ;
                                            }
                                            if (!rep_item_3.is_null()) arr_3.push_back(rep_item_3).get();
                                            continue;
                                        }
                                        rep_fail_3: ;
                                        pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                                        doc_.arena_rollback(rep_doc_);
                                        break;
                                    }
                                }
                                [[maybe_unused]] AnyVal cap2_s_gi0_s1 = arr_3.to_anyval();
                                cap2_s = AnyVal{};  // multi-item group alt (no action)
                                goto grp_done_2;
                            }
                            grp_fail_2_0: ;
                            pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                        }
                        goto opt_fail_1;  // no group alternative matched
                        grp_done_2: ;
                    }
                    cap2 = cap2_s;
                    goto opt_done_1;
                }
                opt_fail_1: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_1: ;
            }
            if (peek_token().kind != TK::RBRACKET) goto bt_arr_lit_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::ARR_LIT), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::ITEMS, rcap_0.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_arr_lit_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_paren_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: LPAREN expr RPAREN => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            if (peek_token().kind != TK::LPAREN) goto bt_paren_expr_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap2 = rule_expr();
            if (cap2.is_null()) goto bt_paren_expr_0;
            if (peek_token().kind != TK::RPAREN) goto bt_paren_expr_0;
            Token tok_cap3_ = next_token();
            [[maybe_unused]] AnyVal cap3 = doc_.make_string(tok_cap3_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 4).get();
            node->put(logos_ast::VALUE, cap2, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::PAREN_EXPR), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_paren_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

AnyVal LogosParser::rule_call_expr() {
    size_t saved_pos;
    bool   saved_la;
    size_t saved_doc_;

    // Alternative 1: IDENT LPAREN (...)? RPAREN => {...}
    {
        saved_pos  = pos_;
        saved_la   = have_la_;
        saved_doc_ = doc_.arena_checkpoint();
        Token    saved_tok_  = la_;
        uint32_t saved_line_ = line_;

        {
            [[maybe_unused]] uint32_t first_line_ = peek_token().line;
            auto rcap_0 = doc_.make_array(4).get();
            if (peek_token().kind != TK::IDENT) goto bt_call_expr_0;
            Token tok_cap1_ = next_token();
            [[maybe_unused]] AnyVal cap1 = doc_.make_string(tok_cap1_.text).get().to_anyval();
            if (peek_token().kind != TK::LPAREN) goto bt_call_expr_0;
            Token tok_cap2_ = next_token();
            [[maybe_unused]] AnyVal cap2 = doc_.make_string(tok_cap2_.text).get().to_anyval();
            [[maybe_unused]] AnyVal cap3 = AnyVal{};
            {
                size_t opt_pos_ = pos_; bool opt_la_ = have_la_; Token opt_tok_ = la_; uint32_t opt_line_ = line_;
                size_t opt_doc_ = doc_.arena_checkpoint();
                {
                    [[maybe_unused]] AnyVal cap3_s = AnyVal{};
                    {
                        size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                        // Group alt 1
                        {
                            grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                            {
                                [[maybe_unused]] AnyVal cap3_s_gi0_s0 = rule_expr();
                                if (cap3_s_gi0_s0.is_null()) goto grp_fail_2_0;
                                rcap_0.push_back(cap3_s_gi0_s0).get();
                                auto arr_3 = doc_.make_array(4).get();
                                {
                                    while (true) {
                                        size_t rep_pos_ = pos_; bool rep_la_ = have_la_; Token rep_tok_ = la_; uint32_t rep_line_ = line_;
                                        size_t rep_doc_ = doc_.arena_checkpoint();
                                        {
                                            [[maybe_unused]] AnyVal rep_item_3 = AnyVal{};
                                            {
                                                size_t grp_pos_; bool grp_la_; Token grp_tok_; size_t grp_doc_; uint32_t grp_line_;
                                                // Group alt 1
                                                {
                                                    grp_pos_ = pos_; grp_la_ = have_la_; grp_tok_ = la_; grp_doc_ = doc_.arena_checkpoint(); grp_line_ = line_;
                                                    {
                                                        if (peek_token().kind != TK::COMMA) goto grp_fail_4_0;
                                                        Token tok_rep_item_3_gi0_s0_ = next_token();
                                                        [[maybe_unused]] AnyVal rep_item_3_gi0_s0 = doc_.make_string(tok_rep_item_3_gi0_s0_.text).get().to_anyval();
                                                        [[maybe_unused]] AnyVal rep_item_3_gi0_s1 = rule_expr();
                                                        if (rep_item_3_gi0_s1.is_null()) goto grp_fail_4_0;
                                                        rcap_0.push_back(rep_item_3_gi0_s1).get();
                                                        rep_item_3 = AnyVal{};  // multi-item group alt (no action)
                                                        goto grp_done_4;
                                                    }
                                                    grp_fail_4_0: ;
                                                    pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                                                }
                                                goto rep_fail_3;  // no group alternative matched
                                                grp_done_4: ;
                                            }
                                            if (!rep_item_3.is_null()) arr_3.push_back(rep_item_3).get();
                                            continue;
                                        }
                                        rep_fail_3: ;
                                        pos_ = rep_pos_; have_la_ = rep_la_; la_ = rep_tok_; line_ = rep_line_;
                                        doc_.arena_rollback(rep_doc_);
                                        break;
                                    }
                                }
                                [[maybe_unused]] AnyVal cap3_s_gi0_s1 = arr_3.to_anyval();
                                cap3_s = AnyVal{};  // multi-item group alt (no action)
                                goto grp_done_2;
                            }
                            grp_fail_2_0: ;
                            pos_ = grp_pos_; have_la_ = grp_la_; la_ = grp_tok_; doc_.arena_rollback(grp_doc_); line_ = grp_line_;
                        }
                        goto opt_fail_1;  // no group alternative matched
                        grp_done_2: ;
                    }
                    cap3 = cap3_s;
                    goto opt_done_1;
                }
                opt_fail_1: ;
                pos_ = opt_pos_; have_la_ = opt_la_; la_ = opt_tok_; line_ = opt_line_;
                doc_.arena_rollback(opt_doc_);
                opt_done_1: ;
            }
            if (peek_token().kind != TK::RPAREN) goto bt_call_expr_0;
            Token tok_cap4_ = next_token();
            [[maybe_unused]] AnyVal cap4 = doc_.make_string(tok_cap4_.text).get().to_anyval();
            auto* node = logos::hermes::HermesCtrAccess::raw_tiny_map(doc_, 5).get();
            node->put(logos_ast::CALLEE, cap1, logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::CODE, AnyVal::from_value(logos_ast::CALL), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::ARGS, rcap_0.to_anyval(), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            node->put(logos_ast::SRC_LINE, AnyVal::from_value(first_line_), logos::hermes::HermesCtrAccess::arena(doc_)).get();
            {
                AnyVal result_;
                result_.set_pointer(node, logos::hermes::HermesCtrAccess::base(doc_));
                return result_;
            }
        }
        [[maybe_unused]] bt_call_expr_0:
        pos_      = saved_pos;
        have_la_  = saved_la;
        la_       = saved_tok_;
        line_     = saved_line_;
        doc_.arena_rollback(saved_doc_);
    }

    return AnyVal{}; // no alternative matched
}

} // namespace logos::compiler

